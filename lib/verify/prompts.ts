/**
 * Phase 61b — 검증 프롬프트 (순수 함수, import 0)
 *
 * ⚠️ 이 파일에 import를 두지 말 것 — `npm run test:verify`가 tsc로 단독 컴파일한다.
 *
 * 원본은 이 파일이다. 시트(gas-project-audition) pmt는 이후 여기서 손복사한다(운용 규칙).
 * 시트 STEP1·2(`prompts/pmt.csv`)와 STEP3(`QualityVerification.gs`)에서 **계승한 것**:
 *   보수 판정 문구("확신 못 하면 check", "내가 못 풀었다고 결함이 아니다") ·
 *   fail 확정 전 재검토 1회 · 후보 상한 8 · `$...$` LaTeX 규약 · 축약 인용 · 순수 JSON 출력.
 * **바꾼 것**: 출력 스키마. 시트는 사람이 읽는 문자열(error_report)이고
 *   Mathory는 블록 앵커·태그가 달린 findings 배열이다.
 */

/* ═══════════════════════════════════════════════════════ */
/* 치환                                                     */
/* ═══════════════════════════════════════════════════════ */

/**
 * `{key}` 치환.
 *
 * ⚠️ **반드시 함수형 콜백을 쓸 것.** `String.replace`에 치환값을 *문자열로* 넘기면
 *    값 안의 `$$`·`$&`·`` $` ``가 치환 패턴으로 해석되어 LaTeX가 손상된다.
 *    콜백 반환값은 그 해석을 받지 않는다.
 *    (시트 STEP3가 같은 이유로 함수형을 쓴다 — `QualityVerification.gs:271-273`의 `★` 주석.
 *     반면 STEP1은 아직 `.replace('{problem}', stem)`(`Itemverification.gs:302-303`)이다 —
 *     고쳐지지 않은 자리이니 그쪽을 베끼지 말 것.)
 */
export function fillTemplate(tpl: string, vars: Record<string, string>): string {
  return String(tpl ?? '').replace(/\{(\w+)\}/g, (m, k: string) => (k in vars ? vars[k] : m));
}

/* ═══════════════════════════════════════════════════════ */
/* 블록 라벨링                                               */
/* ═══════════════════════════════════════════════════════ */

export interface LabeledBlock {
  blockKey: string;
  type: string;
  text: string;
  /** Phase 61f — image 블록의 Storage URL. 서버(`route.ts`)가 실물을 첨부한다.
   *  ⚠ `normalizeBlocks`(route.ts)는 받은 객체를 재구성하므로 **거기서도 이 필드를 실어야** 한다. */
  imageUrl?: string;
}

/**
 * 블록마다 `[블록 n]` 라벨을 붙여 한 덩어리로.
 * 라벨은 모델이 위치를 가리키게 하는 **힌트일 뿐**이다 — 실제 앵커는
 * `anchorByQuote`가 인용 실재성으로 확정한다(모델의 자기 신고를 믿지 않는다).
 */
export function labelBlocks(blocks: LabeledBlock[]): string {
  return blocks
    .map((b, i) => `[블록 ${i + 1}]${b.type && b.type !== 'text' ? ` (${b.type})` : ''}\n${b.text}`)
    .join('\n\n');
}

/** 대상 블록 텍스트 총량 (사전 차단·상한 판정용) */
export function totalChars(blocks: LabeledBlock[]): number {
  return blocks.reduce((n, b) => n + (b.text ? b.text.length : 0), 0);
}

/* ═══════════════════════════════════════════════════════ */
/* 답안 형식 안내 ({format})                                 */
/* ═══════════════════════════════════════════════════════ */

/**
 * 시트 `getFormatGuide`(K열 answer_type 기반, `Itemverification.gs:522-528`)의 4갈래를
 * Mathory 재료로 재현한다 — Mathory에는 answer_type 열이 없으므로 블록 구성과 answer로 정한다.
 */
export function deriveAnswerFormat(a: {
  hasChoices: boolean;
  hasGanaOrRoman: boolean;
  answer: string;
}): string {
  if (a.hasChoices && a.hasGanaOrRoman) return "옳은 선택지 조합 (예: 'ㄱ' 또는 'ㄱ, ㄴ' 등)";
  if (a.hasChoices) return '선택지 번호 하나 (① ~ ⑤)';
  if (/^\d{1,3}$/.test(String(a.answer ?? '').trim())) return '1~999 사이의 자연수';
  return '표준 수치 형식';
}

/* ═══════════════════════════════════════════════════════ */
/* 공통 규약 (세 프롬프트가 공유)                             */
/* ═══════════════════════════════════════════════════════ */

const COMMON_RULES = `### 공통 규약
- 수식은 반드시 \`$...$\` KaTeX 호환 LaTeX로 감쌉니다.
- **순수 JSON만 출력합니다.** 마크다운 펜스·설명·머리말을 붙이지 않습니다.
- 인용(\`quote\`)은 **원문에서 끊기지 않고 이어지는 부분 문자열**을 그대로 씁니다.
  - 식별 가능한 **최소 길이**로 짧게 자릅니다. 긴 수식 전체를 복붙하지 않습니다.
  - **중간을 생략하거나 \`…\`으로 잇지 마십시오.** 원문에 없는 문자열은 위치를 찾을 수 없어 버려집니다.
  - 표기를 고치거나 다듬지 말고 **글자 그대로** 옮깁니다.
  - ⚠️ 원문에 있는 \`$\` 구분자를 **빼지 마십시오.** 수식이 \`$...$\`로 감싸여 있으면 그 \`$\`까지
    포함해 옮깁니다. 구분자를 떨어뜨리면 화면에서 수식이 아니라 LaTeX 소스로 보입니다.
- 각 지적에는 그 내용이 있는 \`[블록 n]\`의 번호 n을 \`block\` 필드로 함께 적습니다.
- 그림이 이미지로 첨부되어 있으면(본문의 \`[그림 k]\` = 첨부된 k번째 이미지) **그림을 직접 보고
  판단합니다.** \`[그림 k — 첨부되지 않음]\`으로 표시된 그림을 보아야만 판단할 수 있으면
  판정하지 말고 \`skip\`을 택하고 이유를 적습니다. 보이지 않는 것을 추측해 지적하지 마십시오.`;

/**
 * 1차 전용 — **재현율(recall)** 담당.
 *
 * ⚠️ 여기에 보수 판정 문구를 넣지 말 것. 이 시스템은 **비대칭** 교차검증이다 —
 *    1차는 넓게 올리고 2차가 좁힌다. 1차에도 "확신 없으면 적지 말라"를 주면 비대칭이
 *    무너져 **1차가 대부분 빈 배열을 뱉고 2차는 아무것도 기각하지 않는 상태**가 된다
 *    (실측 2026-08-22 베이스라인 20건: 14건이 후보 0 · 2차 기각 0건 · 누락 67% · 과검출 0%).
 */
const RECALL_RULES = `### 후보 생성 원칙 (당신의 역할)
- 당신은 **후보를 올리는 사람**이고, 확정 판정은 다음 단계의 심판이 합니다.
  그러니 **의심되면 올리십시오.** 확신이 없다는 이유로 넘어가지 마십시오.
- **문턱을 낮추십시오**: "확실히 결함"이 아니라 "**결함일 수도 있겠다**" 싶으면 올립니다.
  확실한 것만 올리면 이 단계는 존재할 이유가 없습니다. 걸러 내는 일은 다음 단계가 합니다.
  ⚠️ 다만 이것은 **개수 할당량이 아닙니다.** 채울 후보가 없으면 빈 배열이 정답입니다.
  숫자를 맞추려고 결함이 아닌 것을 넣지 마십시오.
- 넘긴 후보가 오탐으로 밝혀지는 것은 **정상이고 의도된 것**입니다. 놓치는 쪽이 훨씬 나쁩니다.
- 각 후보는 **결함 주장**입니다. \`quote\`에 의심되는 원문 한 대목을 그대로 옮기고,
  \`reason\`에는 *무엇이 왜 잘못되었는지*를 한국어로 적습니다.
  (진행 메모나 "이상 없음" 서술은 결함 주장이 아니므로 \`reason\`이 될 수 없습니다.)
- 대상을 **한 줄씩 따라 읽으며** 각 줄이 앞줄에서 실제로 따라 나오는지 확인하십시오.
  건너뛴 채 결론만 맞는지 보면 논리 비약을 놓칩니다.
- 후보는 **최대 8개**. 의심이 강한 것부터 적습니다.`;

/**
 * 2차 전용 — **정밀도(precision)** 담당. 보수 판정의 자리는 여기 하나뿐이다.
 *
 * ⚠️ "보수적"이 "대부분 기각"을 뜻하지는 않는다. 이 리포트는 **사람이 최종 판단**하는
 *    자료다 — 오탐은 몇 초에 걸러내지만 놓친 결함은 그대로 출판된다. 비용이 비대칭이므로
 *    판정도 비대칭이어야 한다: `valid`의 기준만 높게 두고, `invalid`는 **아님을 확인했을 때만**.
 *    (실측 2026-08-22: 과검출 0% · 누락 70% — 조일 이유가 없고 느슨하게 갈 여유만 있었다)
 */
const CONSERVATIVE_RULES = `### 판정 기준
- **"valid"(결함 확정)의 기준은 높습니다.** 확신할 수 없으면 확정하지 마십시오.
- **당신이 풀지 못했다는 사실만으로 결함이라고 하지 마십시오.**
- 다른 표기 관습·다른 접근일 뿐인 것, 대수적으로 동치인 변형은 오류가 아닙니다.
- "valid"로 적기 **직전에 1회 재검토**하십시오: "내가 문제를 잘못 읽은 것은 아닌가?",
  "풀이자의 다른 표기 관습일 뿐 아닌가?"

### 비용의 비대칭 (판정 성향을 정하는 근거)
이 리포트는 **사람이 최종 판단하는 자료**입니다.
- 오탐 하나 → 사람이 몇 초 만에 걸러냅니다. **작은 비용입니다.**
- 놓친 결함 하나 → 그대로 출판됩니다. **큰 비용입니다.**
따라서 **의심스러운 것을 남기는 쪽으로 기울이십시오.**
걸러 낼 확신이 없으면 "invalid"가 아니라 "uncertain"입니다.
받은 후보를 대부분 기각했다면, 기준이 아니라 당신의 성향을 의심하십시오.`;

/* ═══════════════════════════════════════════════════════ */
/* 1차 — 문제 검증 (시트 STEP1 계승 + 후보 생성)              */
/* ═══════════════════════════════════════════════════════ */

export const PROMPT_PROBLEM_FIRST = {
  system: `당신은 대한민국 수능 수학 문제를 풀어 정답을 도출하고, 문제 자체의 결함 후보를 찾는 '엄밀한 수학자'입니다.

### [1] 원칙
- 정합성: 수능 수학의 정답은 논리적으로 유일해야 합니다. 답이 하나로 확정되지 않으면 먼저 숨은 조건(자연수, 양수, 연속성 등)을 재검토하십시오.
- 가정 배제: 문제에 명시되지 않은 조건을 임의로 가정하지 않습니다.

### [2] 추론 절차
1. 조건 기호화: 발문 조건과 변수 범위를 정리.
2. 합답형(ㄱ/ㄴ/ㄷ): 각 보기를 독립적으로 판정.
3. 도형/함수: 수치가 존재 조건을 만족하는지 확인.
4. 검산: 최종 답이 모든 조건과 모순이 없는지 확인.

${RECALL_RULES}

### [3] derived_answer 규칙
1. 수식·설명 없이 최종 결과값만 작성.
2. 합답형: 옳은 보기를 쉼표로 나열 (예: ㄱ, ㄷ). LaTeX 사용 금지.
3. 수치/수식: 유리수는 기약분수, 무리수는 유리화. 이 경우에만 $...$로 감싼 LaTeX.
4. 답을 확정하지 못했으면 빈 문자열("").

### [4] 결함 후보 (candidates)
문제 **자체**의 결함만 봅니다. 풀이는 보지 않습니다. 태그는 아래 넷 중 하나:
- "조건결함": 조건이 서로 상충하거나 필요한 조건이 빠졌다.
- "답없음": 조건을 만족하는 답이 존재하지 않거나 둘 이상이다.
- "선택지오류": 선택지에 정답이 없거나 둘 이상 옳거나 중복이다.
- "표기": 같은 문자가 다른 의미로, 또는 다른 문자가 같은 의미로 쓰였다.
결함이 없으면 빈 배열을 반환합니다.

${COMMON_RULES}

### [5] 출력 스키마
{"skip": false, "skip_reason": "", "derived_answer": "string",
 "candidates": [{"tag": "조건결함", "quote": "원문 그대로", "reason": "무엇이 왜 문제인지 한두 문장", "suggestion": "수정 제안(없으면 빈 문자열)", "block": 1}]}`,

  user: `다음 수학 문제를 풀어 답을 구하고, 문제 자체의 결함 후보를 찾아라.

[답안 형식]
{format}

[문제]
{problem}`,
};

/* ═══════════════════════════════════════════════════════ */
/* 1차 — 풀이 검증 (시트 STEP2 + STEP3 gemini_quality_verify 통합) */
/* ═══════════════════════════════════════════════════════ */

/**
 * ⚠️ 풀이 1차는 **두 패스로 나눈다** (시트 구조 계승 · 실측 근거).
 *
 * 시트는 계산·표기(STEP2, `Itemverification.gs`)와 논리(STEP3, `QualityVerification.gs`)를
 * **분리된 패스**로 돌린다. 한 프롬프트에 태그 7개를 다 넣었더니 눈에 띄는 표기·계산이 먼저
 * 소모되고 논리가 묻혔다 — 실측(2026-08-22): 시트가 '논리'로 지적한 6건 중 1건만 검출,
 * '표기' 3건 중 2건 검출.
 *
 * 두 호출은 서로 독립이므로 **반드시 병렬로** 보낼 것. 직렬로 보내면 300초 예산이 무너진다.
 */
const SOLUTION_PREAMBLE = `당신은 수능 수학 풀이의 감사자입니다. 결함 **후보**를 찾아 넘기는 것이 임무이고, 최종 확정은 다른 판정자가 합니다.

### [0] 전제
- [문제]는 항상 올바르다고 가정합니다. 문제를 의심하지 않습니다.
- 오직 [풀이]만 판정합니다.`;

const SOLUTION_SCHEMA = (sampleTag: string) => `### 출력 스키마
{"skip": false, "skip_reason": "",
 "candidates": [{"tag": "${sampleTag}", "quote": "원문 그대로", "reason": "무엇이 왜 문제인지 한두 문장", "suggestion": "수정 제안(없으면 빈 문자열)", "block": 2}]}`;

/** 패스 A — 계산·표기 (시트 STEP2 계승) */
export const PROMPT_SOLUTION_FIRST_CALC = {
  system: `${SOLUTION_PREAMBLE}

### [1] 이번 검토의 범위 — **계산과 표기만**
논리 전개·추론의 타당성은 **다른 검토자가 따로 봅니다. 여기서는 보지 마십시오.**
등호·부등호가 있는 식, 명시적 대입·치환·변형의 결과에 집중하십시오.

태그는 아래 셋 중 하나:
- "계산오류": 앞줄의 결과 또는 문제 조건과 충돌하는 산술 오류. 대수적으로 동치이면 오류가 아님.
- "표기오류": 같은 문자가 서로 다른 의미로, 또는 서로 다른 문자가 같은 의미로 사용됨.
  기호 오기(첨자·괄호·로그 누락 등)도 여기.
- "수식비일관": 앞뒤 식이 서로 어긋남(같은 대상을 다르게 씀).
결함이 없으면 빈 배열을 반환합니다.

${RECALL_RULES}

${COMMON_RULES}

${SOLUTION_SCHEMA('계산오류')}`,

  user: `[문제]
{problem}

[풀이]
{solution}

위 [문제]를 참으로 가정하고, [풀이]의 **계산·표기** 결함 후보를 찾아라.
논리 전개는 이번 검토 대상이 아니다.`,
};

/** 패스 B — 논리 (시트 STEP3 계승) */
export const PROMPT_SOLUTION_FIRST_LOGIC = {
  system: `${SOLUTION_PREAMBLE}

### [1] 이번 검토의 범위 — **논리 전개만**
산술 실수·기호 오기는 **다른 검토자가 따로 봅니다. 여기서는 보지 마십시오.**
숫자가 맞는지가 아니라 **각 줄이 앞줄에서 정말 따라 나오는지**를 보십시오.

태그는 아래 넷 중 하나:
- "논리비약": 결론으로 가는 중간 단계가 빠져 그 줄이 앞줄에서 따라오지 않음.
- "논리오류": 추론 자체가 성립하지 않음(역·이 혼동, 필요조건을 충분조건으로 씀,
  특수한 경우에서 일반을 단정함 등).
- "경우누락": 경우를 나누었으나 빠진 경우가 있음. 부호·구간·정의역의 분기를 특히 살필 것.
- "문제풀이불일치": 풀이가 문제의 조건·물음과 다른 것을 다룸.
결함이 없으면 빈 배열을 반환합니다.

**특히 흔한 유형** — 조건이 주는 범위보다 좁게 단정하는 것.
예: "$\\pi<\\theta<2\\pi$ 이므로 $\\sin\\theta<0$" 은 제4사분면을 빠뜨린 단정입니다.
범위·부호·근의 개수를 단정하는 문장을 만나면 **그 단정이 조건 전체에서 참인지** 확인하십시오.

${RECALL_RULES}

${COMMON_RULES}

${SOLUTION_SCHEMA('논리비약')}`,

  user: `[문제]
{problem}

[풀이]
{solution}

위 [문제]를 참으로 가정하고, [풀이]의 **논리 전개** 결함 후보를 찾아라.
산술 실수·기호 오기는 이번 검토 대상이 아니다.`,
};

/** 풀이 1차 패스 목록. **병렬로** 호출하고 결과를 합친다. */
export const SOLUTION_FIRST_PASSES = [PROMPT_SOLUTION_FIRST_CALC, PROMPT_SOLUTION_FIRST_LOGIC];

/* ═══════════════════════════════════════════════════════ */
/* 2차 — 엄격 판정 (시트 claude_quality_judge 계승)           */
/* ═══════════════════════════════════════════════════════ */

export const PROMPT_JUDGE = {
  system: `당신은 **사람이 검토할 목록을 추려 주는** 심판입니다. 문지기가 아닙니다.

앞선 검토자들이 의심스러운 지점을 폭넓게 올렸습니다. 그중 일부는 오탐입니다.
당신의 임무는 후보를 **하나씩** 원문과 대조해 셋으로 가르는 것입니다:
확정할 것 · **아님을 확인해 버릴 것** · 사람에게 넘길 것.

⚠️ 세 번째가 가장 큽니다. 확신을 갖고 버릴 수 있는 것은 생각보다 적습니다.

### [1] 판정 어휘
- "valid": 명백한 결함이다. 재검토 후에도 확실하다.
- "invalid": 결함이 아니다. 오탐이다.
- "uncertain": 결함일 수도 있으나 확신할 수 없다.

${CONSERVATIVE_RULES}

### [2] 판정 원칙
- 세 값의 쓰임이 다릅니다. **뭉뚱그리지 마십시오.**
  - "invalid" = 결함이 **아님을 확인**했다. (오탐이라고 판단할 근거가 있을 때)
  - "uncertain" = 결함인지 아닌지 **가리지 못했다**. 판단이 서지 않으면 여기입니다.
  - "valid" = 결함임을 **확신**한다.
  ⚠️ 확신이 없다고 해서 "invalid"로 보내지 마십시오. 그러면 사람이 볼 기회 자체가 사라집니다.
  기각은 "아니라고 확인한 것"이지 "모르겠는 것"이 아닙니다.
- 후보의 \`reason\`을 그대로 믿지 마십시오. 원문을 직접 보고 스스로 판단하십시오.
- ⚠️ **\`reason\`이 부실하다는 이유로 기각하지 마십시오.** 앞 검토자가 지점(\`quote\`)은
  정확히 짚고 설명만 엉성하게 쓰는 일이 있습니다. 판단의 대상은 설명이 아니라
  **\`quote\`가 가리키는 원문 그 자리**입니다. 그 자리를 직접 보고 결함 여부를 정하십시오.
- 인용(\`quote\`)을 원문에서 찾지 못했다면 그 사실을 \`note\`에 적고 **"uncertain"**으로 두십시오.
  표기 차이일 뿐일 수 있고, 인용의 실재 여부는 시스템이 따로 대조합니다.
- 그림이 이미지로 첨부되어 있으면(본문의 \`[그림 k]\` = 첨부된 k번째 이미지) 그림을 직접 보고
  판단합니다. **첨부되지 않은** 그림을 보아야만 판단할 수 있는 후보는 "uncertain"으로 두고
  그 이유를 \`note\`에 적습니다.
- ⚠️ 앞 단계는 **최종 결함 수의 두 배쯤을 일부러 올립니다.** 오탐이 섞여 있는 것이
  설계된 결과이고, 그것을 가려내는 것이 당신의 일입니다. 다만 **가려낸다는 것이
  '없애는 것'은 아닙니다** — 확실히 아닌 것만 "invalid"로 지우고, 나머지는 "uncertain"으로
  남겨 사람이 보게 하십시오. 후보마다 원문을 직접 보고 개별로 판단하십시오.
- 앞 단계는 **계산·표기**와 **논리**를 각각 따로 검토합니다. 서로 다른 시각에서 올라온
  후보이므로, 한쪽 유형이 많다고 해서 다른 쪽을 가볍게 보지 마십시오.

### [3] note 규칙
- 판정 근거를 **한두 문장**으로. "valid"면 무엇이 어떻게 틀렸는지, 기각이면 왜 결함이 아닌지.
- 수식은 $...$ LaTeX.

${COMMON_RULES}

### [4] 출력 스키마
받은 후보 **전부**에 대해, 받은 \`id\`를 그대로 써서 판정을 냅니다.
{"judgments": [{"id": "c1", "ruling": "valid", "note": "판정 근거 한두 문장", "suggestion": "수정 제안(없으면 빈 문자열)"}]}`,

  user: `[문제]
{problem}

[검토 대상]
{solution}

[제기된 후보]
{candidates}

각 후보를 엄격히 판정하라.`,
};

/** 2차 user 프롬프트의 {candidates} 자리에 넣을 가독 텍스트 (시트 `formatCandidatesForJudge_` 등가) */
export function formatCandidatesForJudge(
  cands: { id: string; tag: string; quote: string; reason: string }[],
): string {
  return cands
    .map((c) => `${c.id} [${c.tag}]\n지점: ${c.quote}\n주장: ${c.reason}`)
    .join('\n\n');
}
