# 개선묶음 M9 — 각종 기능 개선·버그 수정 v5 실행판 (구현 완료 2026-09-19 · §9 구현 기록 · 덕수 실물 검수 대기)

> 계보: 덕수 스케치 `M9 각종 기능 개선과 버그 수정.md`(2026-09-19, 8항 + 이미지 3) → v1 web(HEAD `46e5603` 실측) → v2 착수판(P1~P16 확정 · M1 확정) → v3 CLI 교차검토판(정정 부록 B · D′ 21건 · Q1~Q19) → **v4 착수판**(이 문서 — **Q1~Q19 전부 권장안 확정(덕수 2026-09-19 "모두 권장안대로")** · 덕수 추가 사항 `M9 추가 사항 (블록정리 기능 조정).md`를 **I항(D26~D30)**으로 편입 · M2 중 Firestore는 **공개 읽기 REST로 직접 실측**해 닫음(§1-C11) · P5 단가 표 확정(§4-3)). 다음: 착수(§8) → 실행판(§9 구현 기록).
> **v5 web 재검증(2026-09-19)**: v4를 원격 `main`에 다시 대조했다 — 원격 HEAD는 여전히 **`46e5603`**(CLI 미push)이라 행 번호 기준이 같다. **부록 B의 정정 55건을 표본 28건으로 재확인해 전부 성립**(A-4·A-5·B-1·C-1~C-4·D-1·E-1·F-1·G-2~G-4·H-1~H-3·H-6·H-11 · `'ㄱ'.normalize('NFKC')` = U+1100 · `thoughtsTokenCount` 0건 · `ai_models` `allow read: if true` · dnd-kit 6.3.1·`@codemirror/view` 6.39.14 잠금 · I1~I5는 `tidyBlocks`를 직접 실행해 `## $\mathrm{STEP}$ $2$ …`·`$\mathrm{GUIDE}$ …`·`\section*` 잔존·`\frac` 무변경·문제 탭 `15.` 무변경을 재현). v4의 D′·Q1~Q23 사양은 **그대로 유효**하다. 고친 것은 **정정 2**(C-1 §4-3 [Q8]의 "착수 시 확인"을 닫음 · C-2 `blockTidy.ts` 행 번호 드리프트)와 **보완 7**이며 전부 **부록 C**에 있다 — M3′는 정적으로 닫혔다(`clipboardInputFilter` 실재). 이 문서가 CLI 착수의 최종본이다.
> **v4에서 무엇이 바뀌었나**: ① §7-2 Q1~Q19가 **확정**이다 — `[Q#]` 표시는 이제 잠긴 사양이다 ② **I항 신설** — 문제번호 제거(문제 탭) · 번분수 `\frac`→`\dfrac` · `STEP n`·`GUIDE` 수식화 제외 · `\section*{…}` 벗기기 · `GUIDE` 행을 제목 블록으로. 현행 실측(§1-I)에서 `STEP 2`가 `$\mathrm{STEP}$ $2$`로, `GUIDE`가 `$\mathrm{GUIDE}$`로 **이미 파괴되고 있음**을 확인했다 ③ I항의 해석이 갈리는 4곳은 **Q20~Q23 권장안을 가적용**했다(§7-3 — 이의가 있으면 착수 전에 알려 달라) ④ 덕수 입력(Vercel env · OCR 이미지)은 **v4 갱신에서 모두 반영**했다 — Vercel에 검증 override가 하나도 없어 **운영 1차 Gemini 비용이 지금 $0으로 찍히고 있음**이 확정됐다(§1-C12).
> **CLI에게**: §2 원 결정(D1~D25)은 §2-9의 D′가 덮는다 — **D′가 있는 항목은 D′만 볼 것**. I항은 §2 I(D26~D30) · §4-9가 사양이다. 덕수 입력(M2·M2′·M4)은 전부 닫혔다 — 착수 차단 요소 없음.
> 스케치 이미지: 계획서 안의 이미지 1(ㄱ,ㄴ 상자 편집·미리보기 대조)과 이미지 2·3(해설 원문의 `⋯⋯ ㉠`·`㉡`)은 별도 첨부본으로 확인했다. 이미지 1의 실측치는 §1-G에 있다.

---

## 0. 요약

| 항목 | 내용 |
|---|---|
| 취지 | 폴더뷰·패널·비용·모바일·수식 분할·빈 탭·상자 마커·인용 번호 — 실사용에서 드러난 8항을 한 묶음으로 닫는다. 화면 계열(A·B·D)·데이터 무결성(F)·렌더 파이프라인(E·G)·AI 운영(C·H)이 섞여 있어 **커밋은 항목 단위로 독립**시킨다(§8) |
| 범위 | 스케치 8항 = **A~H 8묶음**(§1). 수정 파일 약 **26**(신설 3: `lib/panelStore.ts`·`lib/aiPricing.ts`·`scripts/ocrProbe.mjs` · 테스트 보강 6) |
| 무접촉 | Firestore 규칙 **0**(비용 집계는 클라 memo 유지 — `discussion_sessions` update가 `['name','updatedAt']`로 잠겨 있어 세션 문서에 합계를 쓰려면 규칙이 바뀐다, §1-C9) · Storage 규칙 0 · raw_text 저장 형식 0(G의 정규화는 **비가시 문자 제거뿐**, 표기 변경 없음) · 검증 프롬프트 본문은 **H2 태그 1종 추가**만 · 폰 셸의 세로 보기 0 |
| 스케치를 뒤집은 판정 | **E** "행 분리 과정에서 기호를 꺼내는 기능의 결함" → 분리(`mathSplit`)는 기호를 건드리지 않고 **정확히** 행을 나눈다. 기호를 꺼내는 것은 **정돈(broom)의 M7 ⇒ 규칙**이고, 기호가 윗행 끝에 보이는 것은 **렌더의 빈 줄 주입 규칙(`MATH_ONLY_LINE_RE`)이 `⇔ $…$` 행을 "수식만 있는 행"으로 안 보아 세 행이 한 문단으로 합쳐진 뒤 줄바꿈이 기호 뒤에서 일어난** 결과다(§1-E). 처방은 정규식 1줄. **F** "블록 1개면 못 지우게 한 설정의 결함" → 그 가드는 탭 단위로 **정상**이다(§1-F1). 0개가 되는 진짜 경로는 **저장의 delete-all→re-add가 중간에 실패**하는 것과, **로드 시 빈 탭을 그대로 받아들이는 것**, 그리고 **빈 탭에는 `+` 버튼이 없는 것**(§1-F7~F9) → 저장을 탭 단위 원자 배치로, 로드 시 1블록 시드, 빈 탭 자리표시자. **G** ㄱ. 내어쓰기 → 렌더 규칙은 세 행에 **대칭**이고(실행 확인), 첫 행만 어긋나려면 첫 행의 **행머리에 정규식이 못 보는 글자**(폭 0 문자·호환 자모 아닌 초성 자모 등)가 있어야 한다. 그 경우 첫 행은 marker span을 못 받을 뿐 아니라 **재인용(ref-marker)으로 오분류**된다(§1-G5, 실행 확인). 원인 글자를 덕수가 30초 절차로 확정한 뒤(§8-0 M1) 정규화·정규식·편집기 가시화 3중으로 막는다 |
| 이미 되어 있는 것 | **B** agent의 `fetch`는 패널이 언마운트돼도 **끊기지 않고** 응답은 `addComment`로 저장된다(`CommentPanel.tsx:258-261` 주석이 그 설계다). 날아가는 것은 **진행 표시·선택 세션·모델 칩·입력 중 초안·스크롤·폭·열림 상태**다(§1-B6) → 패널을 AppShell로 끌어올리지 않고 **그 상태만 모듈 스토어로** 올린다. **D** "맨 위 도달 시만 복귀"는 리듀서에 **`reveal-at-top` 모드로 이미 구현·테스트**돼 있고 호출부가 기본값 `reveal-on-up`을 쓰고 있을 뿐이다(§1-D3) — 단 M8 Q1에서 덕수가 (a)를 **확정**한 결정을 뒤집는 것이라 판정 항목으로 올린다(P9). **C** 카드 아래 비용과 헤더 합계는 **둘 다 이미 두 흐름 모두에 뜬다**(같은 코드 경로) → 카드 아래 것을 지우면 끝. 진짜 구멍은 **1차 Gemini 단가 기본값 0**과 **교정·자동완성의 비용 미집계**다(§1-C2·C4) |
| 남은 판정 | **없음** — P1~P16(v2) · Q1~Q19(v3) 전부 권장안 확정(덕수 2026-09-19) · Q20~Q23(v4, 추가 사항 해석)은 권장안 가적용. 덕수 입력도 **모두 도착**(Vercel env → §1-C12 · OCR 이미지 → `docs/phaseSketch/m9-ocr/`). 선택 사항 1건: `~/Documents/crops`에서 이 페이지의 원본 크롭 파일명 |


### 0-1. v3 교차검토 요약 (항목별)

| 항목 | v2 판정 | v3 결론 |
|---|---|---|
| **A** 클릭 | 선택/더블클릭 | 방향 유지. **구멍 넷**: ① 선택 바가 `selectable` 없이 뜬다 → D2대로 공유 뷰에서 선택하면 **남의 문항에 '폴더 변경·휴지통' 바**가 뜬다 ② 단건 클릭마다 바가 우측 컨트롤(보기 전환·일괄 검증·휴지통 비우기)을 **대체**한다 ③ ⋮·체크박스를 빠르게 두 번 누르면 **문항에 진입**한다(dblclick 전파 — Phase 45a 함정) ④ window Enter가 포커스된 버튼의 네이티브 click과 **겹친다**(휴지통 확인 + 진입). `handleCheck`는 ListView 내부 함수이고 `toggleSelect`·`isEditableTarget`는 존재하지 않는다 |
| **B** 패널 | 상태 스토어 | 방향 유지. `patchPanel(Partial)`은 병렬 모델의 **함수형 갱신을 유실**한다 → `updatePanel(fn)`. 초안을 알림형 스토어에 두면 **키 입력마다 패널 전체 리렌더**(61c 규약 위반) → passive 쓰기. 캐시 복원은 `setLoading(true)` 무조건 호출로 **무력화**된다. 키에 uid가 없어 계정 전환 시 **남의 agent 메시지가 잠깐 보인다** |
| **C** 비용 | 표 + 표시 통일 | **v2의 "입출력 모두 정확"(C5)은 틀렸다** — Gemini `thoughtsTokenCount`를 읽는 곳이 0건이라 사고 토큰(출력 단가로 청구)이 빠진다. 1차 검증(`thinkingLevel HIGH` × 최대 3패스)이 가장 크게 과소. xAI도 과소 가능. 교정 비용 표시 위치 정정 |
| **D** 크롬 | reveal-at-top | 확인. D5 잠복 버그는 "300ms 지연"이 아니라 **다시 스크롤할 때까지 안 뜬다** → D20 필수. 기본값 이중화 정리 |
| **E** ⇒ 행 | 정규식 1줄 | **전부 실행으로 확인**(⌘⇧L은 proofread를 부르지 않는다 · 현행 빈 줄 0 → D21 빈 줄 2 · 산문 뒤 행 무변경). 변경 없음 |
| **F** 빈 탭 | 원자 저장·시드·자리표시자 | 방향 유지. 새 문항 한 배치는 **규칙이 거부**(에뮬레이터) · 탭 단위 원자화만으론 **교차 탭 중복**이 남는다(현행 잠복) · "저장 직전 시드"는 **로컬↔서버를 어긋나게** 한다 · 시드 id가 탭끼리 **충돌** |
| **G** ㄱ. | 정규화 3중 | 방향 유지, 설계 **대수술**: NFD 파괴 · 정규화 지점(로드 필수) · 시트 삽입 위치(stemHash·import 0 규약) · IND 소비가 **목록 들여쓰기를 먹음** · ZWJ 제거가 이모지 합자를 깸. 원인 글자의 유입 단서: `'ㄱ'.normalize('NFKC')` = **U+1100**(NFKC/NFKD 처리 흔적) |
| **H** 인용 | 후처리 + 태그 + 일괄 필터 | 방향 유지, 규칙 ⑤는 **행 단위로 재설계**(수식 안 리더·인라인 `\tag` 불가·display 다중 `\tag` 오류) · 필터는 순수 함수로(테스트 가능하게) · 휴리스틱 **강화판**(오살 3/5 → 0/5) · CLAUDE.md M7 D19 "autoFix 소비처는 blockTidy 하나"가 **틀렸다**(OCR `lib/ocr.ts:93` — 댓글 OCR 포함) |
| **I** 정돈 조정(추가) | — | 5요청 모두 현행에 **없다**(실행 확인). 규칙 둘은 `blockTidy`(heading 분할·문제번호), 셋은 `proofread` 결정적 세트(`\section*`·번분수·STEP/GUIDE 보호 — 편집창 정돈·시트 정돈·OCR 공통). `STEP 2`·`GUIDE`는 지금 **수식으로 파괴**된다 |
---

## 1. 현행 — 실측 (HEAD `46e5603`)

### A. 폴더뷰 문항 클릭

| # | 현행 |
|---|---|
| A1 | 진입은 라우터가 아니라 **콜백**이다: `FolderView.tsx:80-81 onView/onEdit` props → `AppShell.tsx:375 handleViewProblem = (p) => setView({ type:'problem', problemId })`. URL·히스토리 변화 없음(`ViewState :49-58`, `useState :154`). ⚠ `onEdit`은 `FolderView.tsx:126`에서 구조분해만 되고 **본문에서 안 쓴다**(죽은 prop) |
| A2 | 클릭 사이트는 **둘뿐**: 카드 `FolderView.tsx:795 onClick={() => onView(problem)}` · 행 `ListView.tsx:459 onClick={() => onView(p)}`. 둘 다 같은 div에 dnd-kit `{...attributes} {...pointerListeners}`(`:793-794` · `:455-456`). 하위 폴더 행·칩(`ListView.tsx:396` · `FolderView.tsx:685`)은 **폴더 이동**이라 이번 대상이 아니다 |
| A3 | 전파를 끊어 진입을 막는 자식: 체크박스 셀 `ListView.tsx:476-477`(+ `handleCheck :482`) · 행 ⋮ `:503-505` · perm select `:329` · 카드 ⋮ `FolderView.tsx:859-860`(`openCardMenu :420 stopPropagation`) |
| A4 | **선택 상태는 이미 있다** — `FolderView.tsx:299 selectedIds: Set<string>`, 게이트 `:298 selectable = !!dragUid`(내 소유·비공유 뷰 `:258`). `ListView`로 내려간다(`:757-758` → `ListView.tsx:111-112`, `:171`). **리스트 보기 전용**: 선택 바 `FolderView.tsx:601-618`(`viewMode === 'list' && selectedIds.size > 0`) · 체크박스 열 `ListView.tsx:473`. **카드에는 선택 배선이 없다**. 범위 선택은 **체크박스 Shift 클릭만**(`handleCheck :219-239`, 앵커 `lastCheckRef :169`) — 행 본문 Shift/⌘ 없음. 해제: 폴더 변경 `:301` · 사라진 id 정리 `:303-311` · **Escape `:313-318`** · 보기 전환 `:145-148` · `clearSelection :320` |
| A5 | 선택의 소비처: `runBatchMove :326` · `runBatchTrash :339` · `runBatchRestore :347` · `runBatchDelete :348` · Finder 의미론 드래그(`ListView.tsx:440-448` — 선택에 든 행을 끌면 **선택 전체**를 끈다). 일괄/정밀 검증 버튼(`:623-635`)은 `selectedIds`와 무관(`BatchVerifyDialog` 자체 체크박스) |
| A6 | hover 스타일: 카드·행 모두 CSS 변수 `--card-surface`(`globals.css:941-942`), zebra `.is-alt :945`, `@media (hover: hover)` 안에서 `.problem-card:hover { --card-surface: var(--block-bg-active); box-shadow: 0 4px 14px … }` · `.folder-row:hover { …; 0 2px 8px … }`(`:950-951`). 토큰 `--block-bg-active #E8DFCE`(`:109`). **`:939-940` 규약: `#E8DFCE`보다 어둡게 내리지 말 것**(Phase 58·59·59a 명암비 계산의 전제). `:943-944` 주석이 캐스케이드 순서를 **`alt → hover → (S5) selected`**로 이미 적어 두었다 — 자리는 설계돼 있고 `.is-selected` 규칙은 **아직 없다**(grep 0) |
| A7 | 인라인 transition은 둘 다 있다(카드 `FolderView.tsx:810` · 행 `ListView.tsx:469`). 카드 커서는 `grab`(`:807`), 행은 `pointer`(`ListView.tsx:467`, "클릭 우선") |
| A8 | **더블클릭 0건**(레포의 `onDoubleClick`은 열 리사이즈 `ListView.tsx:644`·세션 이름 `CommentPanel.tsx:1365/1436`·블록 접기 바 `EditorView.tsx:828/847/881`뿐). **Enter 0건** — dnd-kit `attributes`가 `role=button tabIndex=0`을 주지만 키보드 활성자는 D30으로 **일부러 제거**(`FolderView.tsx:789` · `ListView.tsx:451`) → 포커스된 행에서 Enter는 아무 일도 안 한다. "지금 열려 있는 문항" 표시도 없다(`activeProblemId`·`aria-current` grep 0) |
| A9 | DnD: `DndContext`는 앱 루트 하나(`AppShell.tsx:790-796`, CLAUDE.md 금지 규약), 센서 `PointerSensor { distance: 8 }`(`:513-516`) → 제자리 클릭은 드래그를 시작하지 않아 `click`·`dblclick`이 정상 발화한다. 카드 `Draggable disabled={!dragUid \|\| authorUid !== dragUid}`(`FolderView.tsx:783`) |
| A10 | 폰 경로는 완전히 별개(`PhoneList.tsx:27/31/55/64`) — `FolderView`·`ListView`를 import하지 않는다. 화면 hover는 `@media (hover: hover)`로 이미 터치 안전 |

### B. ProblemView ↔ EditorView 우측 패널

| # | 현행 |
|---|---|
| B1 | 두 뷰는 `<main>` 안의 **형제 조건 렌더**(`AppShell.tsx:948 view.type === 'problem'` · `:963 'editor'`) → 전환 시 서브트리 통째로 언마운트. 게다가 `ProblemView key={problemId:problemViewNonce}`(`:950`)와 `handleEditorBack`의 nonce++(`:752-760`)로 **편집에서 돌아올 때 ProblemView를 일부러 재마운트**한다 |
| B2 | `CommentPanel`은 **두 뷰가 각각** 마운트한다: `ProblemView.tsx:1270-1287` · `EditorView.tsx:4147-4167`. `chrome` 미지정(둘 다 `'drawer'`, `CommentPanel.tsx:173`). 차이: EditorView만 `onInsertGraphBlock`·`onInsertToEditor`·`onBeforeAskSend`(`:4159-4162`), ProblemView는 `onJumpToBlock`을 `isVerifyOwner`로 게이트(`:1283`), `activeTabId`는 ProblemView가 `tabs[0]`(`:1275`)·EditorView가 활성 탭(`:4152`). 폰은 `PhoneApp.tsx:368 chrome='sheet'`로 **세 번째 소비처** |
| B3 | 열림 상태 `panelMode`는 **각 뷰의 로컬 state**(`ProblemView.tsx:216` · `EditorView.tsx:1134`, 기본 `null`) → ProblemView에서 agent를 열어 둔 채 편집에 들어가면 **닫힌 채** 시작한다 = 스케치의 불편. 폭도 로컬(`useDrawerResize` — ProblemView `:218-220`·`:224-226`, EditorView `:1062-1068`), `hooks/useDrawerResize.ts:13-14` "폭은 세션 내 상태로만 · localStorage 금지"(덕수 확정) |
| B4 | **버전 드로어(스케치의 "cvs")는 EditorView에만 있다**(`EditorView.tsx:47` import · `:3455-3469` 마운트, ProblemView grep 0). 항상 마운트하고 `display: open ? 'flex' : 'none'`(`VersionDrawer.tsx:303`)로 숨기는 — CommentPanel과 반대인 — 패턴이다 |
| B5 | ProblemView의 **우측 단**(스케치의 "우측 메뉴 패널", 지속 대상 아님): `rightOpen :240`, `rightColShown = rightOpen && !panelMode :244` — 패널과 **공존 불가**(CLAUDE.md, `ProblemView.tsx:241-243` 주석: 패널 min 360이 단 max 360을 덮는다). 내용 `:1086-1255`(메뉴·문제정보·CopyrightPanel) |
| B6 | **CommentPanel 내부 state — 언마운트에 죽는 것**(전부 `useState`/`useRef`, 스토어·컨텍스트 0): `activeSessionId :202`(재마운트 시 `:347-355`가 **가장 오래된** normal 세션을 자동 선택 — `listSessions`가 `createdAt asc`, `discussion-sessions.ts:106`) · `selectedModelIds :203` · **`pendingAI :204`**(진행 표시 + `retryContext` — 사라지면 실패한 호출을 재시도할 수 없고 진행 중인 호출은 **표시가 없다**) · 세션 생성/개명 `:205-208` · `replyingTo :212` · `editingId :213` · `freshAiCommentId :217` · `inputHeight :199` · 입력 초안(`CommentEditor.tsx:54 useState(initialValue)`, localStorage 없음) · 스크롤(`messagesScrollRef :210`, 마운트 시 `:406-413`이 맨 아래로 고정). 1회 가드 `ensureDoneRef :309`·`flagsLoadedRef :322`·`initialSelectionDoneRef :347`가 재무장돼 초기 로드 4회 왕복(`:245-256`)과 `loading` 깜빡임이 다시 난다 |
| B7 | **죽지 않는 것(핵심)**: `invokeOneAI`는 `await fetch('/api/discuss')`(`CommentPanel.tsx:682-698`) — **`AbortController` 0건**, 스트리밍 아님. 언마운트돼도 fetch는 계속되고 응답은 같은 클로저가 `addComment({… aiUsage})`(`:702-715`)로 **저장**한다. `setFreshAiCommentId`·`setPendingAI`(`:717-719`)만 no-op. `:258-261` 주석이 이 설계를 명시하고, 재마운트된 패널은 `watchAllComments`(`:264-282`) 실시간 구독으로 그 답을 받는다. 정밀 검증도 같다(`runVerify :880-909` → `onRunVerify` → `verifyFlow.ts:124 fetch`, `setVerification`은 `verifyFlow` 자신이 한다 `:196-202`). **즉 "agent의 작업이 날아간다"는 실제로는 "작업은 계속되는데 보이지 않고, 돌아와도 세션·칩·초안이 초기화된다"이다** |
| B8 | 패널을 AppShell로 끌어올릴 때의 비용: 뷰 내부 클로저 콜백 5종(`onInsertToEditor`·`onInsertGraphBlock`·`onBeforeAskSend`·`onJumpToBlock`·`onRunVerify`, `EditorView.tsx:4159-4163`·`ProblemView.tsx:1282-1283`)의 재배선 · 절대배치 기준(뷰 루트, `VersionDrawer.tsx:283-303`·CLAUDE.md "고정 모달 가둠" 규약)과 `paddingRight` 밀기 산식(`EditorView.tsx:3504·3689·3852` · `ProblemView.tsx:256-258`) 변경 · 폰 소비처 · nonce 재마운트. AppShell에 올라가 있는 상태는 항법·데이터뿐(`:154 view`·`:155 nonce`·`:104-109` 사이드바 폭), 패널 관련 0 |
| B9 | ⚠ **v3 신설 — 스토어 이관 시 걸리는 현행 구조**: ① `setPendingAI`·`setSelectedModelIds`는 전부 **함수형 갱신자**(`CommentPanel.tsx:719·724·842·883·899·901·933·944`)이고 여러 모델이 `Promise.allSettled`로 병렬로 돈다 ② 초기 로드 effect가 `setLoading(true)`를 **무조건** 부른다(`:245`) — 스크롤 effect(deps `loading`, `:413`)가 다시 돌아 맨 아래로 고정 ③ 재마운트 시 다시 받는 것이 댓글·세션 외에도 `profiles`·`aiModels`·`myProfile`(이름이 잠깐 '?'로) ④ `PendingAI`·`DiscussRequestContext`는 `CommentPanel.tsx:130·148`의 **비공개** interface ⑤ `useDrawerResize`에 `initialWidth`·`onCommit` **둘 다 없다**(단 `defaultWidth`가 곧 `useState` 초기값) ⑥ 댓글 패널 폭 규격은 두 뷰가 같다(min 360 · max 0.9vw · default 420) → 하나를 공유해도 clamp 충돌 없음. 버전 드로어 폭(`EditorView.tsx:1066`)은 별개 |

### C. AI 토큰 비용 산출

| # | 현행 |
|---|---|
| C1 | **입력·출력 둘 다** 계산한다 — 산식 사본 2: `discuss/route.ts:423-434 calcCost` · `verify/route.ts:637-639 cost`(`round4 :641`). 캐시 읽기/쓰기·thinking·툴 시간 차원은 없다(`discuss/route.ts:585-586` 주석이 인정: "code_interpreter 컨테이너 시간은 별도 청구되지만 입출력 토큰만"). 저장 모양은 하나 — `AIUsage { inputTokens, outputTokens, costUsd }`(`types/problem.ts:149-153`) |
| C2 | **단가표는 코드에 없다.** 토론·문답 검증(ask)·일괄 검증 후속 대화의 단가는 **Firestore `ai_models/{modelId}` 문서**의 `inputCostPerMillion`/`outputCostPerMillion`(`lib/ai-models.ts:30-33`)이고 콘솔에서 손으로 관리한다(`:5` 주석 · `firestore.rules:462` 앱은 읽기 전용 · 시드 스크립트 0). ⚠ **조용한 0 함정**: `typeof === 'number'`가 아니면(콘솔에 문자열로 넣으면) **0으로 떨어져 $0으로 찍힌다** — Phase 66a E1/E2가 `maxTokens`로 같은 사고를 겪었다(`Phase66a … v6 착수판.md:472`). `enabled`가 문자열 `"true"`면 모델이 꺼지고 `order`가 없으면 `orderBy`에서 사라진다(`:38`) |
| C3 | **정밀 검증은 env 고정 모델이라 `ai_models`를 못 쓰고 라우트 상수**(`verify/route.ts:161-165`): `claudeCostIn: 5, claudeCostOut: 25`(리터럴 — `VERIFY_CLAUDE_MODEL`을 바꿔도 Opus 5 단가가 남는다) · **`geminiCostIn/Out = Number(process.env.VERIFY_GEMINI_COST_IN/OUT \|\| 0)`** → env가 없으면 **1차 Gemini(2~3패스)는 $0**. Phase 61b(`:302` "0이면 미집계")·61d(`:157` "사실상 Claude 판정분만")·61f(`:37` "`$0.00000`은 env 단가가 0이라서") 문서가 이를 기록했고 `BatchVerifyDialog.tsx:276·305` 라벨이 "비용(2차 Claude 기준)"이라 적는다. 합산: 1차 `:255-257 usdFirst` · 2차 `:470-478 usdJudge`(판정 2콜) · 서버는 stateless라 `carriedUsd: 0`(`:225`) → 클라 `verifyFlow.ts:169-173`가 1차+2차를 더한다 · 일괄 `batchVerify.ts:155-162 addUsage` |
| C4 | **비용을 아예 안 세는 호출 3**: `/api/proofread`(Claude Haiku `claude-haiku-4-5`, `proofread/route.ts:17`, `messages.create :115-133` 뒤 `usage` 미접근) · `/api/ai-complete`(`lib/ai-provider.ts:605-614` — `AI_MODEL \|\| 'gemini-2.5-flash'` 또는 Claude; `ai-complete/route.ts:71`이 `inputTokens/outputTokens`를 버린다) · `/api/ocr`(Mathpix, 건당 과금 — 토큰 개념 없음) |
| C5 | provider별 usage 읽기(`lib/ai-provider.ts`): Gemini `usageMetadata.promptTokenCount/candidatesTokenCount`(`:144-145`) · OpenAI-compat `prompt_tokens/completion_tokens`(`:194-195`) · Responses `input_tokens/output_tokens`(`:312-313`) · Claude는 `pause_turn` 툴 턴을 **누적**(선언 `:423-424` · 누적 `:443-444`, 최대 4요청). 전부 `?? 0`이라 usage가 빠지면 조용히 $0. **스트리밍은 어디에도 없다**(`:441` 단언 주석 · 2026-09-11 사고 주석 `:369-372`). ⚠ **v3 정정 — "정확"이 아니다**: 공급자마다 사고(reasoning) 토큰을 세는 방식이 다르다(→ C10) |
| C6 | 저장은 **한 필드 한 컬렉션**: `problems/{id}/tab_comments/{cid}.aiUsage`(`lib/comments.ts:112`, 생산자 = 토론 `CommentPanel.tsx:705-713` · 검증 `verifyFlow.ts:177-189`(`authorUid:'ai:verify'`, 일괄도 같은 함수)). `Problem.verification`엔 비용 없음(`verifyFlow.ts:196-202`). `discussion_sessions`에도 없고 `firestore.rules:351-353`이 update를 `hasOnly(['name','updatedAt'])`로 잠근다 |
| C7 | **표시 4곳**: ⓐ 패널 **헤더 1행 우측** `CommentPanel.tsx:1053-1066` — `이 세션: $X.XXXX`, 게이트 `isAISession && sessionCostUsd > 0`(`sessionCostUsd :378-380` = `visibleComments`의 `aiUsage.costUsd` 합) ⓑ **메시지 카드 아래 액션 행** `:2026-2040` — `$X.XXXXX`(5자리), 게이트 `info.isAI && comment.aiUsage`, `title`에 **입력/출력 토큰 수**(UI에서 토큰 수가 보이는 **유일한** 자리) ⓒ 일괄 대화상자 진행 행 `BatchVerifyDialog.tsx:276` ⓓ 요약 행 `:305`. `VerifyReportCard.tsx`는 비용을 그리지 않는다 |
| C8 | **두 흐름은 이미 같은 코드 경로다**: 일괄 검증 세션은 `createSession`(`discussion-sessions.ts:75-77`, `aiEnabled:true`)으로 만든 보통 세션(`BATCH_SESSION_NAME '일괄 검증'`, `batchVerify.ts:28`)이고, 검증 리포트는 `modelId:'verify'`의 AI 댓글이라 `getDisplayInfo :418-422`가 `isAI:true`로 본다 → ⓐ·ⓑ 모두 두 흐름에 뜬다. 스케치가 본 비대칭은 "어느 패널이 열려 있었나"의 차이지 코드 분기가 아니다 |
| C9 | 단가 참고값이 적힌 곳은 문서뿐 — `Phase37 AI토론기능 신설.md:158-166`(Gemini 3.1 Pro $2/$12 · 3.5 Flash ~$1.5/$9 · DeepSeek V4 Pro $0.435/$0.87 · GPT-5.4 $2.5/$15 · Grok 4.3 $1.25/$2.5, **작성 당시 값 — 현재 Firestore와 일치 보장 없음**) · `:642` "Gemini 3.1 Pro 200K 초과 시 단가 2배" → 코드에 **구간 단가 없음**(토론 컨텍스트는 15,000자 cap이라 실질 영향 0). Remote Config·Cloud Functions·`.env*` 파일은 레포에 없다(`functions/` 없음, `firebase.json`은 hosting/rules) |
| C10 | ⚠ **v3 신설 — 사고 토큰 누락(과소 청구 표시)**: ① **Gemini**: `thoughtsTokenCount`를 읽는 곳이 레포 전체 **0건**(grep). `candidatesTokenCount`에는 사고 토큰이 없는데 청구는 출력 단가다. 설치된 SDK 0.24 타입(`generative-ai.d.ts:1384-1393`)에 필드가 없을 뿐 응답 원본엔 온다. 최대 피해처 = 검증 1차(`geminiThinkingLevel:'HIGH'`, `verify/route.ts:247-257`, 문제 1패스·풀이 3패스 전부 합산은 됨) · 토론 Gemini(3.x 기본 사고) · ai-complete(`gemini-2.5-flash`). `toolUsePromptTokenCount`도 미합산 ② **Claude**: `output_tokens`에 thinking 포함, `cache_control` 사용 0건 → 정확 ③ **OpenAI Chat·Responses·DeepSeek**: completion/output에 reasoning 포함 → 정확(캐시 할인분만 약간 과다) ④ **xAI(Grok)**: 문서 예시상 `completion_tokens`에 `reasoning_tokens`가 빠진다(total = prompt + completion + reasoning) → **과소 가능, 실측 필요**. 결론: P5 단가를 채워도 1차 검증 비용은 크게 낮게 찍힌다 — D15′ |
| C11 | ⚠ **v4 신설 — Firestore `ai_models` 실측(REST, 2026-09-19)**: 7문서 전부 단가·`maxTokens`·`order`가 **숫자형**(integer/double), `enabled`는 boolean → **C2의 "문자열 → $0" 함정은 현재 없다**. 문서(provider · apiModelName · enabled · order · maxTokens · 단가 in/out): `gemini-3.1-pro` google `gemini-3.1-pro-preview` ✓ 1 16384 **2/12** · `gemini-3.5-flash` google `gemini-3.5-flash` ✗ 2 8192 1.5/9 · `claude-ask` anthropic `claude-opus-5` ✓ 3 16000 5/25 · `gpt-5.4` openai `gpt-5.4-2026-03-05` ✓ 4 8192 2.5/15 · `grok-4.3` xai `grok-4.3` ✗ 5 8192 1.25/2.5 · `claude-opus-5` anthropic `claude-opus-5` ✓ 7 16000 5/25 · `claude-reporter` anthropic **`claude-opus-4-8`** ✗ 7 8192 **15/75**. ⚠ `claude-reporter`의 15/75는 옛 Opus 4 단가다(Opus 4.8은 5/25) — 꺼져 있어 영향 0, 켜기 전에 콘솔 수정. **DeepSeek 문서는 없다**(`DEEPSEEK_API_KEY`는 로컬에 있음). 로컬 `.env.local`(값은 비밀 아닌 두 변수만 확인): `AI_PROVIDER=gemini` · `AI_MODEL=gemini-2.5-pro` → 로컬 ai-complete는 **`gemini-2.5-pro`**(v3 키 목록에 없던 모델) · `VERIFY_*`·`CLAUDE_PROOFREAD_MODEL` **없음** → 로컬 1차 Gemini 비용은 지금 $0 |
| C12 | ⚠ **v4 신설 — Vercel env 실측(덕수 스크린샷 2026-09-19, 정렬 "Last Updated")**: `VERIFY_GEMINI_MODEL` · `VERIFY_CLAUDE_MODEL` · `VERIFY_GEMINI_COST_IN/OUT` · `VERIFY_JUDGE_CODE_EXEC` · `CLAUDE_PROOFREAD_MODEL` **전부 없음** → 운영의 검증 모델은 기본값 `gemini-3.1-pro-preview`·`claude-opus-5`, 2차 code_execution 꺼짐, 교정은 `claude-haiku-4-5`, 그리고 **운영의 1차 Gemini 비용은 지금 $0으로 찍히고 있다**(C3 확정). `AI_PROVIDER=gemini` · `AI_MODEL=gemini-2.5-pro`(All Environments) → 운영 ai-complete도 **`gemini-2.5-pro`**(표 키 추가됨). `ANTHROPIC_MODEL=claude-haiku-4-5-20251001`은 **코드에서 읽는 곳이 0건**(grep `app`·`lib`·`components`·`scripts`) — 죽은 변수라 비용 계산과 무관(정리는 §6). 결론: override가 없으므로 검증·교정·자동완성의 단가는 **§4-3 표가 곧 운영값**이다 |

### D. 모바일 가로 보기 상단바 복귀

| # | 현행 |
|---|---|
| D1 | 순수 리듀서 `lib/chromeAutoHide.ts`(97행, import 0, `test:chrome` 11건) + 공급자 `PhoneShell.tsx`(`onScroll :109-119`, `active = chromeAutoHide && landscape :69`, 크롬 래퍼 `marginTop: hidden ? -chromeH : 0 :137`) + 유일 소비처 `PhoneReader.tsx:131`(공개 `/p`·`/shared`·로그인 리더 공통). 세로 보기는 자동 숨김 **없음**(M8 §8-4 후속) |
| D2 | 리듀서 순서(`nextChromeState :51-86`): ① 러버밴드 `y<0 \|\| y>max` 무시(`:59`) → ② 무시 창 `now < ignoreUntil`이면 위치만 추적(`:62`) → **③ `y <= 0`이면 항상 show**(`:65-68`, 모드 무관) → 히스테리시스 누적 `:70-73` → hide 분기 `:75-80`(`acc ≥ 24 && y > chromeH && canHide`) → **show 분기 `:81-83`: `mode === 'reveal-on-up' && acc <= -24`** — 위로 24px 누적되면 즉시 복귀 = 스케치의 불편 |
| D3 | **`RevealMode = 'reveal-on-up' \| 'reveal-at-top'`(`:26-27`)이 이미 있고**, `reveal-at-top`이면 `:81`의 show 분기가 꺼져 ③(맨 위)만 남는다. 테스트도 있다: `tests/chromeAutoHide.test.mjs:87-92`("reveal-at-top — 위로 스크롤만으로는 show 되지 않는다") · `:71-76`("맨 위는 모드 무관 show"). 호출부 `PhoneShell.tsx:113`이 **3번째 인자를 안 넘겨** 기본값 `'reveal-on-up'`(`:54`)이 적용된다 |
| D4 | **현행은 덕수 확정이다** — M8 실행판 `:114 Q1`("권장 (a) 위로 24px 즉시 · (b)면 리듀서 모드 값만 바뀐다, 코드 차이 2줄") · `:211` 판정 (a) 확정(근거: "긴 풀이 중간에서 탭 전환 가능") · `:118` 알고 두는 손실 ② · `:184` T8 실기 검증 · `:206` "검증 완료. 모두 정상"(2026-09-12). 이번 스케치는 그 Q1을 **번복**한다 |
| D5 | 잠복: ②(무시 창)가 ③(맨 위)보다 **앞**이라, hide 직후 300ms 안에 맨 위에 닿으면 `y===0` 샘플이 삼켜진다. ⚠ **v3 정정 — "~300ms 늦게 뜨는 정도"가 아니다**: 그 뒤 scroll 이벤트가 더 없으면 크롬은 **다시 스크롤할 때까지 계속 숨는다**(안드로이드는 맨 위에서 당겨도 이벤트가 없고, iOS는 한 번 더 당겨야 뜬다). `reveal-at-top`에서는 맨 위가 유일한 복귀 경로라 D20이 **필수**다 |

### E. 여러 행 displaystyle 수식 분리 — ⇒·⇔ 위치

| # | 현행 |
|---|---|
| E1 | **분리는 정확하다**(실행 확인): `lib/mathSplit.ts:105-123 splitDisplayMathBody` — `\\` 분할 정규식 `/\\\\(?:\[[^\]]*\])?/`(`:109`)는 뒤 공백·`\Leftrightarrow`를 삼키지 않는다. `$$\begin{aligned} f(x) &= 2x+1 \\ \Leftrightarrow f'(x) &= 2 \\ \Rightarrow x = 3 \end{aligned}$$` → rows `["f(x) = 2x+1", "\Leftrightarrow f'(x) = 2", "\Rightarrow x = 3"]`. `mathSplit.ts`·`tests/mathSplit.test.mjs`(14건)에 `Rightarrow` 토큰 **0건** — 분리기는 기호를 모른다 |
| E2 | 기호를 수식 밖으로 꺼내는 것은 **정돈(broom)의 M7 규칙**: `lib/proofread.ts:774-796` Step 3, `ARROW_HEAD :782` · `ARROW_TEXT :783`(`⇒ → ⇔`), **인라인 `$…$`만** 대상(`:784`), `$\Leftrightarrow f'(x)=2$` → `⇔ $f'(x)=2$`(`:791-793`). 소비처는 `lib/blockTidy.ts:151-155` R3(= 편집창 broom `EditorView.tsx:1518-1558` · 시트 가져오기 정돈 토글) 하나(CLAUDE.md) |
| E3 | 소스에는 빈 줄을 두지 않는다(덕수 규약 — `EditorView.tsx:1701-1706` 주석 · `:1733 result.rows.join('\n')` · `mathSplit.ts:10-17`). 행이 눈에 따로 보이는 이유는 **렌더 시 `insertMarkerLineBreaks`가 "수식만 있는 행이 연달아 올 때" 사이에 빈 줄을 넣기 때문**이다 — `lib/locale.ts:120 splitMathRun = MATH_ONLY_LINE_RE.test(line) && MATH_ONLY_LINE_RE.test(prevLine)` · `EditorPreview.tsx:118`(같은 상수를 import, `:11`) · `remark-breaks` 없음(CLAUDE.md 불변식) |
| E4 | **원인**: `MATH_ONLY_LINE_RE = /^⟦MATH_\d+⟧(?:[ \t]*\\tag\{\d+\})?[ \t]*$/`(`lib/locale.ts:72`)가 **`^`에 고정**이라 `⇔ ⟦MATH_1⟧`을 "수식만 있는 행"으로 보지 않는다. 실행 확인: `$f(x)=2x+1$\n$\Leftrightarrow f'(x)=2$\n$\Rightarrow x=3$` → 빈 줄 2개 삽입(정상), `$f(x)=2x+1$\n⇔ $f'(x)=2$\n⇒ $x=3$` → **빈 줄 0**. 세 행이 **한 CommonMark 문단**이 되고, 각 `$…$`는 `\displaystyle` 주입(`preprocess.ts:168-175` · `EditorPreview.tsx:230-235`)으로 넓고 높은 인라인 상자라 브라우저의 줄바꿈 기회는 **화살표 앞뒤 공백뿐** → `MATH0 ⇔` 까지 첫 줄, `MATH1 ⇒` 둘째 줄… = **"기호가 윗행 끝에 있다"**. 소스 순서는 정확하고, 문단 합침의 줄바꿈 부산물이다 |
| E5 | 시나리오 재구성: ⌘⇧L(정상, 세 행) → 정돈(⇒ 규칙으로 텍스트화) → 렌더에서 합쳐짐. 스케치의 "꺼낸 기호 뒤에서 행을 구별" 짐작은 화면상 결과를 정확히 묘사한 것이지만 기전은 위와 같다 |

### F. 블록 0개 탭

| # | 현행 |
|---|---|
| F1 | 삭제 가드는 **정상·탭 단위**: `handleDeleteBlock` 첫 줄 `if (currentBlocks.length <= 1) return;`(`EditorView.tsx:1892`, `currentBlocks = allBlocks[activeTab] :1399`) · UI `canDelete={currentBlocks.length > 1}`(`:3921`). 다중 삭제 없음(블록을 지우는 filter는 `:1913` 하나) |
| F2 | 0개를 **만들 수 없는** 경로(전부 확인): DnD는 `setCurrentBlocks` 안에서만(`handleDragEnd :2341-2382`, `SortableContext`는 활성 탭만 `:3905`) · 탭 추가는 1블록 시드(`handleAddTab :2846-2858`) · 정돈은 조각이 다 비면 원 블록 복원(`blockTidy.ts:126`, 빈 탭 early return `EditorView.tsx:1520`) · 시트 가져오기는 빈 문자열도 text 1블록(`sheetImport.ts:359`, `:333-334` "이 갈래를 없애지 말 것") · `toPersistedBlock`은 필드 매핑뿐(`normalize.ts:29-54`) |
| F7 | ⚠ **진짜 경로 1 — 저장 중간 실패**: `handleSave`(`:2942-3089`) 탭마다 `for oldId of origIds { try { await deleteBlock } catch {} }`(`:2977-2983`, **에러 삼킴**) → `for blocks { await saveTabBlock }`(`:2987-2990`, 직렬·삼키지 않음). 삭제 뒤 `saveTabBlock`이 던지면(네트워크·권한·쿼터) Firestore의 그 탭은 **블록 0개**, 바깥 `catch`는 status·`saveError`만(`:3081-3084`), 롤백·재시도 없음. `clearDraft`는 성공 경로에서만(`:3072`)이라 **같은 브라우저**의 드래프트 복구 배너(`:1356-1374`)로만 되살릴 수 있다 |
| F8 | ⚠ **진짜 경로 2 — 로드가 빈 탭을 그대로 받는다**: `getProblemWithBlocks`는 doc이 없는 탭에 `[]`(`lib/firestore.ts:245-258`, 읽기 실패도 `[]` + `tabLoadErrors`) → `blocksMap[tab.id] = toLocal(blocks)`(`EditorView.tsx:1348-1353`), **최소 1블록 가드 0**(`length === 0` grep 0). 다음 저장은 `blocks=[]`·`origIds=[]`로 0쓰기·0삭제 → 빈 상태를 **조용히 영속**하고 드래프트도 지운다(`:3072`) = "되돌릴 수 없다"는 느낌의 정체 |
| F9 | ⚠ **진짜 경로 3 — 빈 탭엔 `+`가 없다**: `BlockBottomToolbar`(`+` 메뉴 `BlockBottomToolbar.tsx:144`)는 블록 카드 안, 그것도 `isActive && !block.collapsed`일 때만(`EditorView.tsx:1011-1019`). `handleAddBlock` 호출처는 `:3954` 하나, 단축키 없음. 블록 목록은 `currentBlocks.map`뿐이고 **빈 상태 자리표시자 없음**(`:3906-3970`, 뒤에 100vh 스페이서 `:3978`). 유일한 탈출구는 블록 붙여넣기(`:2396-2429`, D16 빈 탭 예외 — 접힘 모드 + 메모리 클립보드 필요) |
| F10 | 진짜 경로 4·5(드묾): 새 문항 생성 `AppShell.tsx:340-353`(`createProblem` → `saveQuestionBlock` → `saveSolutionBlock` 3연속, 실패 시 `catch :356-359`가 alert만 — `createProblem`은 블록을 안 만든다 `firestore.ts:30-43`) · `duplicateProblem`(`firestore.ts:531-575`)이 읽기 실패로 `[]`가 된 탭을 그대로 복사(`tabLoadErrors` 검사 없음 — `lib/version/adapter.ts:27-31 collectCurrentContent`는 던진다) |
| F11 | 전파만 하는 것: 버전 복원 `applyVersionContent :3346-3373`·`adapter.ts:47-59`(최소 없음) · undo/redo(`useBlockHistory.ts:28-57`) — 0개 상태를 재생할 뿐 만들지 않는다. 빈 탭으로 전환하면 `activeBlockId` 보정 effect가 `blocks.length > 0` 게이트(`:2744`)라 다른 탭의 id를 가리킨 채 남는다 |
| F12 | 레포에 `writeBatch` 전례가 있다 — `moveProblemsToFolder`(`lib/firestore.ts:214-232`, 500 청크 루프). `saveBlock`은 `addDoc`(`:369-378`), `deleteBlock`은 `deleteDoc`(`:404-409`) — 블록 n개 저장 = **왕복 2n회 직렬** |
| F13 | ⚠ **v3 신설 — 현행 잠복: 교차 탭 중복**. 탭 k에서 저장이 실패하면 앞 탭들은 새 id로 커밋됐는데 `origBlockIds`는 옛 id 그대로다 → 다음 저장에서 옛 id 삭제는 삼켜지고(없는 문서) 재추가만 일어나 **앞 탭 블록이 두 벌**이 된다. `refreshed`에 `tabLoadErrors`가 있으면 그 탭 `origIds`가 `[]`가 되어 같은 중복이 난다. 탭 단위 원자화(v2 D23-1)만으로는 막히지 않는다 → D23-1′. 또 `handleSave`는 `updateProblem`(`:2960`) → 삭제된 탭 정리 → 탭 루프 순이라 **문항 메타와 블록도 원자적이지 않다** |
| F14 | ⚠ **v3 신설 — 규칙 실측(에뮬레이터)**: 문항 doc + 블록 2개를 **한 batch**로 쓰면 `permission-denied`(`firestore.rules:103 parentOwner()`의 `get()`이 배치 **이전** 상태를 읽어 부모가 없다 — `:144 allow write`). 기존 문항에 대한 **250 delete + 250 set 배치는 통과**, 없는 doc delete도 통과. `tabLoadErrors` 타입은 `Record<string, string>`(`types/problem.ts:336` · `firestore.ts:244`) — v2 스니펫의 `?.[tab.id]`·`Object.keys` 용법은 맞다 |

### G. ㄱ,ㄴ 상자 첫 항목 내어쓰기

| # | 현행 |
|---|---|
| G1 | 타입 `roman`(`types/problem.ts:238`), 라벨 `'ㄱ, ㄴ 상자'`(`EditorView.tsx:121`), 프리셋 `'ㄱ. \nㄴ. \nㄷ. '`(`:159`), `BORDERED_TYPES`(`:193` + 사본 4: `TabBody.tsx:31`·`FolderView.tsx:43`·`ProblemTabContent.tsx:24`·`PrintableContent.tsx:45`). 렌더 5곳 전부 **테두리 div 하나 + `<EditorPreview>` 하나**(예: `EditorView.tsx:4074-4084` · `TabBody.tsx:170-178`) — 항목별 래퍼 없음(CLAUDE.md "블록을 두 `<EditorPreview>`로 쪼개지 말 것") |
| G2 | `(a)`→`ㄱ.` 변환은 **없다**(Phase 60 후속 삭제, `lib/locale.ts:8-10·39-43`) → 저장 raw는 **리터럴 `ㄱ.`**이다(아니면 `(a)`가 그대로 보인다). 변환 정규식: `GIYEOK_LITERAL_RE = /^([ㄱㄴㄷㄹㅁㅂㅅㅇㅈㅊ])\.[ \t]*/`(`:59`) · `GANA_LITERAL_RE :56` · `CIRCLED_NUM_LINE_RE :64` — 셋 다 **`^` 바로 뒤 마커, 선행 공백 불허**. 반면 행 분리용 `MARKER_LINE_RE :44-49`는 `IND = '[^\\S\\n\\r]*'`(`:35`)로 **선행 공백을 허용**한다(`:33-34` HWP·웹 붙여넣기 대비) |
| G3 | 파이프라인(`preprocessLocale :336-364`): 수식 보호 → `insertMarkerLineBreaks :108-127`(마커 행 앞 빈 줄 → 독립 `<p>`) → `convertGiyeokLiteral :182-185`(`/gm`으로 **모든 행** 동일 처리) → `convertRefMarkers :291-306`(본문 중간 `ㄱ.` 재인용 → `ref-marker`; 정의부 span은 `PROTECTED_ELEMENT_RE :267-268`로 보호). EditorPreview 사본 `:96-180`은 같은 상수를 import(`:11`). `remark-breaks` 없음, `p` 오버라이드 없음(`EditorPreview.tsx:319-389`) |
| G4 | CSS(`globals.css:547-567`): `.marker-giyeok { display:inline-block; min-width:2em }` · `.preview-content p:has(.marker-giyeok) { padding-left:2em; text-indent:-2em }`. `:542-543` 계약: **`min-width` = 내어쓰기 폭**. 인쇄는 `PrintStyles.css:142-144`가 `p:has(> .marker-giyeok:first-child)`·2.5em(Phase 60 D4/Q2가 알고 둔 차이) |
| G5 | **실행 확인(locale.ts를 tsc로 빌드해 돌렸다)**: 프리셋 3행 입력 → 세 행 모두 `<span class="marker-giyeok">ㄱ.</span>…` + 사이 빈 줄 = **대칭**(`tests/locale.test.mjs:94-95`가 고정). 첫 행 앞에 글자를 하나 붙이면 — `\u2060`(word joiner)·`\u00A0`(NBSP): `GIYEOK_LITERAL_RE` 불일치 → span 없음 → **`convertRefMarkers`가 `ㄱ.`을 재인용으로 보아 `<span class="ref-marker" data-reftype="giyeok">`로 감싼다**(hover 말풍선·`cursor:help`가 생긴다) / `\u1100`(초성 자모 \u1100, U+3131 ㄱ과 **모양 동일**): 불일치, 리터럴 그대로. 어느 쪽이든 그 행은 `p:has(.marker-giyeok)`에 안 걸려 **2em 라벨 상자·내어쓰기를 잃는다** |
| G6 | **이미지 1 실측**(첨부): 미리보기에서 `ㄱ.`·`ㄴ.`·`ㄷ.`의 **왼쪽 끝 x는 같고**, 라벨과 본문 사이 간격이 ㄱ 행만 좁다(ㄱ→`t` ≈ 15px, ㄴ→`t`·ㄷ→`곡선` ≈ 40px). = ㄱ 행에만 **2em `min-width` 라벨 상자가 없다** → G5의 "첫 행만 변환을 못 받은 상태"와 정확히 일치. 편집창(좌)에서는 세 행의 `ㄱ.`·`ㄴ.`·`ㄷ.`이 같은 x에서 시작하고 보이는 선행 공백이 없다 → 원인 글자는 **폭 0(ZWJ/ZWNJ/word joiner 등 — CM6 기본 `highlightSpecialChars`가 표시하는 `\u200B`·`\uFEFF`는 빨간 점으로 보였을 것)** 또는 **동형 다른 코드포인트**(`\u1100` U+1100 / `．` U+FF0E / `․` U+2024). 블록 **첫 글자** 자리라는 점이 유입 경로(시트 가져오기 셀 선두·OCR 출력 선두·웹 붙여넣기)를 가리킨다. 레포에 비가시 문자 정규화는 **0건**(`u200b`·`uFEFF`·`u2060`·NFC 관련 grep — `sheetImport.ts:321·472·475`·`figure/route.ts:171`의 파일명 NFC뿐) |
| G7 | ⚠ **v3 신설 — 원인 글자의 유입 단서와 파급**: ① `'ㄱ'.normalize('NFKC')` = **U+1100**(`ㄴ`→U+1102, `①`→`1`) — 유입원(OCR 후처리·HWP 변환·외부 도구)에 **NFKC/NFKD 처리**가 있었을 공산이 크다. 같은 원천이면 `①`도 `1`로 손상됐을 수 있다(실행판에 기록할 것) ② **NFD 한글과 충돌**: `'가'.normalize('NFD')` = `1100 1161`. CLAUDE.md(61e-2차)대로 Drive 파일명의 절반이 NFD다 — U+1100을 "전 위치" 매핑하면 NFD 음절이 `ㄱ`+`ᅡ`로 쪼개지고 NFC로도 **복원되지 않는다**(실행 확인: `해설_fig1.jpg` → `ㅎᅢㅅᅥᆯ_fig1.jpg`) ③ M7 D11: 저장은 로컬 블록을 서버본으로 **교체하지 않고**, 미리보기는 로컬 `raw_text`를 그린다(`EditorView.tsx:4080`) → 저장 시점에만 정규화하면 **같은 세션에서는 저장 뒤에도 ㄱ 행이 깨져 있다** ④ `lib/sheetImport.ts`는 import 0 규약(`:4`)이고 `stemHash`는 E 정규화 직후 값(`:536`) → 정규화를 `rowToDraft`·`normalizeText`에 넣으면 **중복 키가 바뀌어 두 벌 저장** ⑤ `tests/chatExtract.test.mjs:38-40`·`proofread.ts:77` 마스킹은 locale 정규식의 **사본**이라 locale만 고치면 조용히 이격된다 |

### H. 인용 인덱스 번호 (`⋯⋯ ㉠`)

| # | 현행 |
|---|---|
| H1 | OCR 요청(`app/api/ocr/route.ts:41-47`): `{ src, formats:['text'], math_inline_delimiters:['$','$'], math_display_delimiters:['$$','$$'], rm_spaces:true }`. 그 외 옵션 **0**(`include_line_data`·`idiomatic_eqn_arrays`·`rm_fonts`·`alphabets_allowed` 등 레포 grep 0). `alphabets_allowed`는 **일부러** 안 보낸다(`:29-32` "인식률 자체가 급락") — 언어 편향은 Mathpix 대시보드 Preferred Language. 응답은 `data.text`·`confidence`만(`:58-68`), `line_data` 미사용. 후처리 `lib/ocr.ts:74-95 normalizeAndFix`(구분자 정규화 → `autoFixDeterministicIssues`) — `⋯`·`㉠`·`\tag` 처리 0 |
| H2 | 앱의 정본 표기는 **`\tag{n}`**이고 렌더는 `(n)`이다: 텍스트 행 끝 `\tag{n}` → `<span class="tag-marker">(n)</span>`(`locale.ts:313-320`, `globals.css:592-598 float:right`) · 수식 안 → `\tag*{(n)}`(`EditorPreview.tsx:195`, `.mtable` 앵커 `globals.css:604-621`). 재인용 `\ref{n}`·본문의 `(n)` → `ref-marker`(`locale.ts:308-311`·`REF_TAGNUM_RE :238`) + `RefTooltip.tsx`. **`㉠㉡`는 파이프라인 지원 0** — `CIRCLED_CONSONANTS`(`locale.ts:19-21`)는 소비처 0인 죽은 export, `㉠`는 팔레트 버튼(`UnifiedToolbar.tsx:163`)뿐 → 타자한 `⋯⋯㉠`는 관성 리터럴(span·툴팁·`\tag` 연동 없음), 검증 모델에겐 정의 없는 기호 |
| H3 | 검증 finding 구조: `VerifyFinding { tag, verdict:'fail'\|'check', blockKey, quote, reason, suggestion?, quoteFound }`(`types/problem.ts:68-77`, **라우트에 사본** `verify/route.ts:90-111`) · 1차 `RawFinding`(`parse.ts:26-38`) · 태그 어휘 `SOLUTION_TAGS :207-212`(계산오류·표기오류·논리비약·논리오류·수식비일관·경우누락·문제풀이불일치·근거없는가정·충분성미확인) · `normalizeTag :225-256`(fallback `논리오류`) |
| H4 | 합성은 **서버**: `route.ts:501-524` findings 조립(`invalid` 탈락 `:506`, `fail`은 `valid && anchor.found` `:510-512`) → 군더더기 격상 `:526-556` → **`:570 synthesizeVerdict(findings)`**(`parse.ts:519-524`). 리포트는 AI 댓글 본문의 ```` ```mathory-verify ```` 펜스 JSON(`VerifyReportCard.tsx:21·50-59`, 머리행에 `지적 n건 · 군더더기 g건`), `problems/{id}.verification`은 `{verdict, verifiedAt, contentHash, stale?, reportCommentId?}`만(`types/problem.ts:52-63`). 카드 `VerifyReportCard.tsx:100-114`(개수 = `findings.length`), 배지 `VerifyBadge.tsx:40-58`(verdict·stale만), 정렬 `lib/listColumns.ts verifyRank` → **finding을 거르면 verdict·개수·배지·정렬이 함께 바뀌어야** 한다 |
| H5 | **일괄과 단건을 구분하는 플래그가 없다**: `batchVerify.ts:221-232`가 단건(`EditorView.tsx:3151`·`ProblemView.tsx:353`·`BatchVerifyDialog.tsx:177`)과 같은 `runVerifyFlow` 인자로 호출. `verifyFlow.ts:105-205`·`VerifyRequestBody`(`route.ts:113-128`)에 mode/batch 필드 0. 기존 필터는 `sanitizeFindings`(`parse.ts:280-320`, 빈 값·태그 정규화·상한)·`mergeCandidates :324-344`(quote 중복 제거)뿐 — **태그·본문으로 거르는 단계 0** |
| H6 | 프롬프트에 `\tag`·`\ref`·`㉠` 설명은 **0건**. ⚠ **v3 정정 — 관례 설명의 전례는 있다**: `COMMON_RULES :109-110`(M7 D20 "C1·C1a는 … 정의되지 않은 기호가 아닙니다")이 프롬프트 5종 모두에 들어가고 `verify.test.mjs:288`이 그 문장을 고정한다(v2가 "quote 규칙"으로 분류한 `:105`는 실제로 `:110`이고 이 관례 문장이다). 블록은 raw_text 그대로 전달(`verifyFlow.ts:33-50`) → 모델은 `\tag{1}`·`(1)`·`㉠`의 관례를 모른 채 "무엇을 가리키는지 알 수 없다"를 `표기오류`/`수식비일관`/`논리비약`/fallback으로 낸다 = **거를 구조적 범주가 지금은 없다** |
| H7 | ⚠ **v3 신설 — autoFix 소비처**: CLAUDE.md M7 D19와 `blockTidy.ts:6`은 "`autoFixDeterministicIssues`의 소비처는 blockTidy 하나"라고 적지만 **틀렸다** — `lib/ocr.ts:93`도 부른다(Phase 28부터, `proofread.ts:538` 주석이 인정). OCR 경로는 `EditorView.tsx:2505`(편집창)와 **`CommentEditor.tsx:118`(댓글 OCR)** 둘이다 → 규칙 ⑤는 댓글 입력에도 적용된다. S8에서 CLAUDE.md·blockTidy 헤더 정정 |
| H8 | ⚠ **v3 신설 — KaTeX 제약(0.16.28 실측)**: 인라인 `$…\tag{1}$`은 "\tag works only in display equations" **오류**, display 하나에 `\tag` 2개는 "Multiple \tag" **오류**. Mathpix는 `\cdots`를 **수식 안에** 내므로(`$…\cdots\cdots$ ㉠` · `$…\cdots\cdots ㉠$` · `$… \cdots \text { ㉠ }$`) 텍스트 전용 정규식은 흔한 입력을 전부 놓친다 |

### I. 블록 정돈 조정 (추가 사항 · 덕수 2026-09-19)

| # | 현행 (실행 확인 — `lib/blockTidy.ts`를 tsc로 빌드해 `tidyBlocks` 실행) |
|---|---|
| I1 | **문제번호 제거 없음(문제 탭)**: R2 머리 정리는 `opts.tab !== 'question'`에서만 돈다(`blockTidy.ts:136`) — `NUMBER_HEAD_RE = /^\d{1,2}\.[ \t]*\n?/`(`:52`)는 풀이·추가 탭 첫 블록 전용. 실행: question `15. 함수 $f(x)$에 대하여` → **무변경** |
| I2 | **번분수 규칙 없음**: `\frac`→`\dfrac` 변환은 레포 어디에도 없다(grep — `proofread`·`ocr`·`sheetImport` 0건. `dfrac`은 프롬프트 `discuss/route.ts:107`·`ai-complete/route.ts:33`, 툴바, `katexMacros.ts:21`뿐). 인라인은 `\displaystyle` 주입으로 **바깥** 분수가 display 크기지만, KaTeX는 분수 인자 안의 `\frac`을 한 단계 작은 style로 그린다 → "안의 분수만 작다"가 덕수 관찰의 기전. 실행: `$\frac{\frac{1}{2}}{3}$` → 무변경. `\frac`·`\dfrac` 둘 다 팬텀 매크로(`katexMacros.ts:20-21`)라 변환 뒤에도 M3 세로 여백 규약이 유지된다 |
| I3 | **`STEP` 수식화 파괴**: R1 ⑤ `STEP_RE = /^STEP[ \t]?\d+/`(`:48`)가 행을 `## STEP…` heading으로 분할하지만(현행), R3 `autoFixDeterministicIssues`가 heading에도 돈다. 실행: `STEP 2 극값 구하기` → **`## $\mathrm{STEP}$ $2$ 극값 구하기`** · `STEP1. 조건 정리` → `## STEP1. 조건 정리`(붙여쓰기는 `numRe` lookbehind `(?<![A-Za-z0-9.])`로 우연히 산다) · 본문 `STEP1 에서 A와 B` → 행 전체가 heading으로 분할(현행 R1 ⑤의 오탐 — 알고 둘 것) |
| I4 | **`\section*{…}` 무처리**: 실행: `\section*{GUIDE}` → 그대로 text 블록에 남는다(`collectControlSeqRanges`가 인자까지 보호해 수식화도 안 된다). 미리보기에선 리터럴로 보인다 |
| I5 | **`GUIDE` 수식화 파괴 · heading 규칙 없음**: 실행: `GUIDE 함수의 증가 감소를 조사한다.` → **`$\mathrm{GUIDE}$ 함수의…`**(`autoWrapBareLetters`가 영문 대문자 단어를 `\mathrm`으로) · 분할 없음 |
| I6 | 적용 경로: `proofread`(autoFix)에 둔 규칙은 편집창 정돈 · 시트 가져오기 정돈 · **OCR(편집창·댓글)** 모두에 적용되고(§1-H7), `blockTidy`(R0~R2)에만 둔 규칙은 정돈 두 곳에만 적용된다. heading 분할(STEP·GUIDE)은 블록 경계를 만드는 일이라 구조상 tidy 전용이다 |

---

## 2. 결정 (D1~D25) — 스케치 대비 판정 · **전부 확정(2026-09-19)**

### A. 폴더뷰 클릭 — **수용**, `e.detail`로 타이머 없이

| # | 결정 | 근거 |
|---|---|---|
| **D1** →D1′ | 카드·행 공통 규칙: **`click`(`e.detail === 1`) = 선택**(그 한 건으로 `selectedIds` 교체) · **`dblclick` = `onView`** · 이미 선택된 한 건을 `e.detail === 1`로 다시 클릭 = **해제**. "약간의 시간이 지난 뒤"는 브라우저의 더블클릭 판정 창(`detail`이 2가 되는 창)이 그대로 담당한다 — 창 안의 두 번째 클릭은 `detail === 2`라 선택 분기를 타지 않고 `dblclick`이 진입시킨다. **타이머·setTimeout 없음** | 타이머 방식은 첫 클릭의 선택 표시가 지연되거나(대기) 더블클릭 때 선택→해제가 깜빡인다. `detail`은 마우스 경로에서 정확하고 폰은 별도 경로(§1-A10)라 무관. Escape 해제(`:313-318`)는 그대로 |
| **D2** →D2′ | 선택의 게이트를 **뷰 무관**으로 넓힌다: `selectedIds`는 공유·수신 뷰에서도 클릭으로 채워진다(시각 선택). **선택 바(`:601-618`)·체크박스 열·Finder 드래그·일괄 동작은 종전 게이트 `selectable`(`dragUid`) 그대로** | 게이트를 그대로 두면 공유 뷰에서 첫 클릭이 아무 일도 안 해 "죽은 클릭"이 된다. 동작은 소유 뷰에서만 — 선택과 동작의 게이트를 분리한다 |
| **D3** | **카드 보기에도 선택 배선**: `selectedIds`·`onSelect`를 카드에 내려 `.is-selected` 클래스. 카드 보기의 선택은 **시각 + 더블클릭 진입용**이며 선택 바는 종전대로 리스트 전용(`viewMode === 'list'` 게이트 유지) | 스케치의 카드 규정("포인터가 나가도 진해진 톤·음영 유지") 그대로. 카드 일괄 동작은 범위 밖(§6) |
| **D4** | 스타일 = **hover와 같은 면·음영을 고정** + 선택 링: `.problem-card.is-selected, .folder-row.is-selected { --card-surface: var(--block-bg-active); box-shadow: <각자의 hover 값>, var(--drop-ring) }` — `globals.css:951` 뒤, `@media (hover: hover)` **밖**(선택은 입력 장치와 무관), 순서 `alt → hover → selected`(`:943-944` 주석 그대로). 배경은 `#E8DFCE`를 넘지 않는다(`:939-940` 규약) | 스케치 "살짝 진해지고 음영"은 hover 값과 같다. hover와 **완전히** 같으면 선택 항목 옆의 hover 항목과 구별이 안 되므로 기존 `--drop-ring`(M6 레드 틴트, `dnd.tsx:88`) 한 겹을 얹는다 — 새 토큰 0 |
| **D5** →D5′ | 행 본문 **Shift 클릭 = 범위**(체크박스의 `handleCheck(id, shift)`·`lastCheckRef`를 그대로 재사용) · **⌘/Ctrl 클릭 = 토글**. 카드도 같은 규칙(소유 뷰에서만 다중 — D2) | 클릭이 선택의 1차 수단이 되면 체크박스만 범위를 지원하는 것이 어색하다. `handleCheck`가 이미 있어 비용이 작다 |
| **D6** →D6′ | **Enter = 선택 1건 열기**(FolderView 창 keydown, Escape 옆 `:313-318`). dnd-kit 키보드 활성자 제거(D30)는 유지 | 클릭이 진입이 아니게 되면 키보드 진입 경로가 하나는 있어야 한다 |
| **D7** | 커서: 카드 `grab` → **`default`**, 행 `pointer` → **`default`**(⋮·체크박스 등 버튼은 각자 `pointer`) | `pointer`는 "클릭하면 간다"의 신호인데 이제 클릭은 선택이다. 드래그 가능성은 M6 이후 링·틴트로 표시한다 |

### B. 우측 패널 지속 — **패널 호이스팅 기각, 상태 스토어로**

| # | 결정 | 근거 |
|---|---|---|
| **D8** →D8′ | `CommentPanel`은 **두 뷰가 각자 마운트하는 구조를 유지**하고, 사용자가 "잃는다"고 느끼는 상태를 **모듈 싱글턴 스토어 `lib/panelStore.ts`**(import 0, `useSyncExternalStore`, `problemId` 키)에 둔다: `mode`(열림 종류) · `width` · `sessionId` · `modelIds` · **`pending`**(진행/실패 표시 + retryContext) · `draft`(입력 초안) · `inputHeight` · `scrollTop` · `versionOpen`(D10) · `cache { comments, sessions }`(D11). `invokeOneAI`·`runVerify`의 클로저는 **컴포넌트 state가 아니라 스토어**에 `pending`을 쓴다 → 언마운트 뒤에도 완료·실패가 기록되고 재마운트된 패널이 그것을 본다 | §1-B7: 네트워크 호출과 결과 저장은 **이미** 살아남는다. 호이스팅(§1-B8)은 콜백 5종 재배선·절대배치 기준·밀기 산식·폰 소비처·nonce까지 건드리는 큰 수술인데 얻는 것은 스크롤 위치와 재조회 생략뿐이고, 그 둘은 스토어로도 대부분 회수된다(D11). 스케치의 "agent의 작업이 이어져야"는 D8로 충족된다 — 작업은 계속되고 **표시가 이어진다** |
| **D9** →D9′ | 지속 범위 = **agent · 댓글 · (EditorView 한정) 버전 드로어 열림 상태**. ProblemView 우측 단(`rightOpen`)은 **로컬 그대로**(스케치 "해당 없음"). 패널이 열린 채 ProblemView로 오면 `rightColShown`(`:244`)이 단을 숨기는 기존 규칙이 그대로 적용된다 | |
| **D10** | "cvs" = 버전 드로어: ProblemView에 없으므로(§1-B4) 지속할 대상이 없다. **`versionOpen`을 스토어에 기록만** 해 EditorView → ProblemView → EditorView 재진입 시 열림을 복원한다. ProblemView에 버전 드로어를 **추가하지 않는다** | 재진입 복원은 스토어에 필드 하나라 공짜. ProblemView 버전 드로어는 별도 Phase(읽기 전용 타임라인·복원 권한 등 설계가 따로 필요) |
| **D11** →D11′ | **재조회 깜빡임 제거**: 마지막 `comments`·`sessions` 배열을 스토어 `cache`에 두고 재마운트 초기값으로 쓴다(`loading`은 cache가 있으면 `false`로 시작). `watchAllComments`·`refreshSessions`는 종전대로 다시 돌아 갱신한다(stale-while-revalidate). `scrollTop`은 언마운트 cleanup에서 저장, 첫 렌더 뒤 복원(값이 있으면 `:406-413`의 "맨 아래 고정"을 그 1회만 건너뛴다) | 4회 왕복 자체는 남지만(실시간 구독이 어차피 다시 붙는다) 화면은 비지 않는다 |
| **D12** →D12′ | 폭: `useDrawerResize`에 `initialWidth?` 옵션을 더해 스토어 값을 초기값으로, 드래그 종료 시 스토어에 쓴다. **localStorage는 여전히 쓰지 않는다**(`useDrawerResize.ts:13-14` 덕수 확정은 "새로고침·재진입은 기본값" — 뷰 전환은 재진입이 아니라 **같은 세션 안의 이동**이라 규약과 충돌하지 않는다) | 뷰를 오갈 때 폭이 튀면 "다른 패널"로 보인다(Phase 62 D13이 폭 통일을 둔 이유와 같다) |
| **D13** | 스토어 수명 = 앱 세션. 문항별 엔트리는 지우지 않는다(수십 개 × 수 KB). `cache`만 **문항 20개 LRU** | 메모리 상한 |

### C. 토큰 비용 — **표시 통일 + 단가 구멍 메우기**

| # | 결정 | 근거 |
|---|---|---|
| **D14** →D14′ | **카드 아래 비용(`CommentPanel.tsx:2026-2040`) 삭제.** 헤더 배지(`:1053-1066`)를 두 흐름의 단일 표시로: 게이트를 `sessionCostUsd > 0` → **`isAISession && sessionAiCount > 0`**(AI 메시지가 1건이라도 있으면 `$0.0000`이라도 표시), `title`에 **세션 합계 입력/출력 토큰**(새 memo `sessionTokens`, `:378-380` 옆). 정밀도 4자리 유지 | 스케치 그대로. `> 0` 게이트를 그대로 두면 단가 0인 1차 전용 종료(skip·후보 0)에서 배지가 **사라져** 카드 아래 `$0.00000`보다 못해진다. 토큰 수 tooltip은 UI에서 유일한 노출(§1-C7 ⓑ)이라 배지로 옮긴다 |
| **D15** →D15′ | **단가의 단일 원천 `lib/aiPricing.ts`**(import 0): `PRICES: Record<apiModelName, { in, out }>` + `priceFor(apiModelName)`. 소비처 ① `verify/route.ts:161-165`의 리터럴·env 기본값 → `priceFor(env.claudeModel)`·`priceFor(env.geminiModel)`(env `VERIFY_*_COST_*`는 **override로 유지**) ② `proofread`·`ai-complete`(D16) ③ **`ai-models.ts:30-33`의 fallback**: 문서 값이 number가 아니거나 0이면 `priceFor(apiModelName)`로 대체하고 `console.warn('[ai_models] 단가 누락/문자열 → 코드 표 사용', modelId)`. 표의 **값은 덕수가 채운다(P5)** — 이 문서는 키 목록만 확정한다: `claude-opus-5` · `claude-haiku-4-5` · `gemini-3.1-pro-preview` · `gemini-2.5-flash` · `gemini-3.5-flash` · `gpt-5.4-2026-03-05` · `deepseek-v4-pro` · `grok-4.3` | 지금은 (a) Firestore 문서 타입 오류 → $0 (b) env 미설정 → 1차 $0 (c) 모델 교체 → 옛 리터럴 — 세 갈래가 전부 **조용히** 틀린다. 코드 표는 낡을 수 있지만 **0으로 떨어지지는 않고** warn이 남는다. Firestore 문서는 여전히 1순위(덕수가 콘솔에서 고치는 워크플로 유지) |
| **D16** →D16′ | 교정(`/api/proofread`)·AI 자동완성(`/api/ai-complete`)에 **usage 읽기 + `costUsd` 계산 + 응답에 `usage` 동봉**. 영구 저장은 **하지 않는다**(둘 다 댓글이 없다). 표시: 교정은 `ProofreadResultBox` 머리에 `교정 $0.00xx`, 자동완성은 status 토스트 1줄 | "모든 모델의 비용이 빠짐없이"의 최소 충족. 사용자별 월간 집계(새 컬렉션 + 규칙)는 별도 Phase(§6) |
| **D17** | `discussion_sessions`에 합계 필드를 **쓰지 않는다**(규칙 `hasOnly(['name','updatedAt'])` `firestore.rules:351-353`). 합계는 클라 memo(`:378-380`) 유지 | 규칙 무접촉 |
| **D18** | 덕수 점검 목록(스크린샷 요청 — §8-0 M2): ① Firestore `ai_models` 각 문서의 `inputCostPerMillion`·`outputCostPerMillion`·`maxTokens`·`order`가 **number 타입**인지, `enabled`가 boolean인지, `apiModelName`이 D15 표의 키와 같은지 ② Vercel env(Production·Preview 둘 다): `VERIFY_GEMINI_COST_IN/OUT`(현재 없으면 1차 $0 확정) · `VERIFY_GEMINI_MODEL` · `VERIFY_CLAUDE_MODEL` · `CLAUDE_PROOFREAD_MODEL` · `AI_PROVIDER` · `AI_MODEL` | §1-C2·C3 |

### D. 가로 보기 상단바 — **`reveal-at-top`로 전환(M8 Q1 번복, P9)**

| # | 결정 | 근거 |
|---|---|---|
| **D19** →D19′ | `PhoneShell.tsx:113`에 `'reveal-at-top'`을 넘긴다. 모드는 `PhoneShell` prop `chromeReveal?: RevealMode`(기본 `'reveal-at-top'`)로 두어 `PhoneReader.tsx:131` 호출부에서 보이게 한다. 리듀서 무변경. 테스트 `:58-63`(reveal-on-up)은 모드 명시 케이스로 유지 | 코드 2줄(M8 `:114`의 예고 그대로). **알고 두는 손실**: 접힌 상태에서 탭을 바꾸려면 **맨 위까지** 올려야 한다(M8 `:118` ②가 강화됨) — 덕수 실사용 요청이므로 수용 |
| **D20** | 리듀서 순서 보정: **③ 맨 위(`y <= 0`) 판정을 ② 무시 창보다 앞으로** 옮긴다(`chromeAutoHide.ts:62`↔`:65-68`). 테스트 1건 추가: "hide 직후 300ms 안에 `y=0` → show" | §1-D5. `reveal-at-top`에서 맨 위가 유일한 복귀 경로라 삼켜지면 안 된다. 맨 위 show도 `ignoreUntil`을 새로 잡으므로 진동 없음 |

### E. ⇒·⇔ 행 — **정규식 1줄(렌더), 분리기·정돈 무접촉**

| # | 결정 | 근거 |
|---|---|---|
| **D21** | `lib/locale.ts:72`: `MATH_ONLY_LINE_RE = /^(?:[⇒⇔→↔∴∵][ \t]+)?⟦MATH_\d+⟧(?:[ \t]*\\tag\{\d+\})?[ \t]*$/` — **닫힌 집합**(정돈 ⇒ 규칙이 내는 `⇒ → ⇔` + 같은 부류의 `↔ ∴ ∵`), 구분 공백 **필수**. `EditorPreview.tsx:118`은 같은 상수를 import하므로 코드 0. 테스트 `tests/locale.test.mjs`(`:158-185` 옆): `$x=1$\n⇔ $y=2$\n⇒ $z=3$` → 빈 줄 2 · `따라서\n⇒ $x=1$` → 무변경(산문 뒤 수식 행은 이어 붙는다 — `:171-179` 계약 유지) | §1-E4. "아무 접두나 허용"으로 넓히면 `:171-179`("산문 뒤 수식 행은 이어 붙는다")가 깨진다. 소스에 빈 줄을 넣는 안(분리기·정돈이 `\n\n` 출력)은 덕수 규약(`EditorView.tsx:1701-1706`) 위반이라 기각 |
| **D22** | 스케치의 "분리 과정의 결함" 서술은 실행판에서 **기전 정정**으로 기록한다(분리 정확 · 정돈이 텍스트화 · 렌더가 합침). `mathSplit.ts`·`proofread.ts` ⇒ 규칙은 무접촉 | 근본 원인이 렌더 규칙 하나이므로 그것만 고친다 |

### F. 빈 탭 — **3중 구조 처방(원자 저장 · 로드 시드 · 빈 상태 UI)**

| # | 결정 | 근거 |
|---|---|---|
| **D23-1** →D23-1′ | **저장을 탭 단위 원자 배치로**: `lib/firestore.ts`에 `replaceTabBlocks(problemId, tabId, oldIds, persisted[]): Promise<string[]>` — `writeBatch`에 `delete(old…)` + `set(doc(collection(...)), data)`(id는 `doc()`으로 미리 생성)를 담아 **한 번 commit**(500 초과 시 실패로 던진다 — 블록 250개 넘는 탭은 없다; 넘으면 `moveProblemsToFolder :214-232`식 청크가 아니라 **에러**로 두어 반쪽 상태를 만들지 않는다). `handleSave :2976-2990`의 두 루프를 이 한 호출로. 새 id 배열을 반환하므로 `origBlockIds`는 여기서 갱신(§M7 D11의 `refreshed` 기반 갱신과 결과 동일) | §1-F7. 실패하면 **아무것도 안 바뀐다**(0개도, 중복도 없다). 부수 효과: 왕복 2n → 1(저장 체감 속도) |
| **D23-2** →D23-2′ | **로드 시 1블록 시드**: `EditorView.tsx:1348-1352`에서 `blocks.length === 0`이고 그 탭이 `data.tabLoadErrors`에 **없으면** `[seedBlock()]`(`handleAddTab :2848-2857`과 같은 `{ id:'new-…', block_key: nanoid(), type:'text', raw_text:'' }`)로 채우고 `setDirty`는 건드리지 않는다(다음 저장이 자연히 싣는다). `tabLoadErrors` 탭은 **제외**(서버에 데이터가 있는데 못 읽은 것 — 덮어쓰면 안 된다; 이미 `collectCurrentContent`가 그 탭을 던지는 규약) · 저장 직전에도 같은 가드(`blocks.length === 0`이면 시드) — 벨트+멜빵 | §1-F8. 이미 깨진 탭이 **다음 편집 진입에서 스스로 낫는다** |
| **D23-3** | **빈 탭 자리표시자**: `:3906 currentBlocks.map` 앞에 `currentBlocks.length === 0`이면 점선 상자 "이 탭에 블록이 없습니다 · [+ 블록 추가]" → `handleAddBlock('text')`(활성 블록이 없으면 `prev.length`에 붙는다 `:1932-1937`). 접힘 모드·붙여넣기 예외(`:2396-2429`)는 그대로 | §1-F9. D23-2 뒤에는 도달하기 어렵지만(로드 에러 탭·복원·undo) 남는 경로의 탈출구 |
| **D23-4** →D23-4′ | 드문 경로: `AppShell.tsx:340-353` 새 문항 생성을 `createProblemWithBlocks`(문항 doc + 문제·풀이 블록을 **한 배치**)로 · `duplicateProblem`은 `original.tabLoadErrors`가 비어 있지 않으면 **던진다**(`firestore.ts:531-535`, `collectCurrentContent`와 같은 규약) | §1-F10 |

### G. ㄱ. 내어쓰기 — **원인 글자 확정(M1) + 3중 처방**

| # | 결정 | 근거 |
|---|---|---|
| **D24-1** →D24-1′ | **정규화 `stripInvisibles(text)`**(`lib/blocks/normalize.ts`에 export, `toPersistedBlock :31`의 trim 앞): `\u200B-\u200D`·`\u2060`·`\uFEFF`·`\u00AD`·`\u034F` **제거**(전 위치), 행머리의 `\u00A0`·`\u3000`·`\u2000-\u200A` → 제거, **초성 자모 U+1100–U+1112(`\u1100-\u1112`) → 호환 자모 `ㄱ…ㅎ`(전 위치 — M1로 확정된 원인)**, 자모 뒤 `．`·`․` → `.`. 같은 함수를 **유입 3경로**에도: OCR `normalizeAndFix`(`lib/ocr.ts:74`) 첫 단계 · 시트 가져오기 `rowToDraft` 텍스트 · 붙여넣기(CM `EditorState.transactionFilter`가 아니라 **저장·정돈 시**로 한정 — 편집 중 실시간 치환은 IME와 간섭). 정돈(R4 trim 옆)에도 | 원인 글자가 무엇이든(M1 결과 무관) 저장본에 남지 않게 한다. raw_text의 **표기**는 바뀌지 않는다 — 보이지 않는 글자만 |
| **D24-2** →D24-2′ | **정규식 관용**: `GANA_LITERAL_RE`·`GIYEOK_LITERAL_RE`·`CIRCLED_NUM_LINE_RE`(`locale.ts:56·59·64`)에 `MARKER_LINE_RE`와 같은 `IND` 선행 허용(**소비** — 인쇄의 `p:has(> .marker-giyeok:first-child)`가 앞 텍스트 노드를 못 견딘다). `convertRefMarkers`의 오분류(§1-G5)도 이것으로 사라진다. 회귀 표면: `tests/locale.test.mjs:29-42·94-95·144·152` · `tests/chatExtract.test.mjs:95·347-348` · `proofread.ts` 마스킹(`:77`, `grep -a`) | 폭 0 글자는 `\s`에 없어 정규식만으론 못 잡는다(D24-1 필요), 공백류는 정규식이 잡는다 — 둘이 짝 |
| **D24-3** →D24-3′ | **편집기 가시화**: `MarkdownEditor.tsx:886 basicSetup` 옆에 `highlightSpecialChars({ addSpecialChars: /[\u00A0\u2060\u200C\u200D\u3000\u1100-\u115F．․]/ })` — CM6는 `specialChars` facet을 합치고 플러그인은 싱글턴이라 중복 무해(**CLI 실측 항목 M3**). 폭 0·동형 글자가 빨간 점/치환 문자로 보인다 | 다음 유입을 사람이 즉시 본다. 지금은 편집창에서 **보이지 않아** 원인 추적이 불가능했다(이미지 1) |
| **D24-4** | 인쇄 2.5em/화면 2em 차이(Phase 60 D4/Q2)·화면 선택자 자식형 통일은 **손대지 않는다** | 이번 결함과 무관, 별도 판정 사안 |

### H. 인용 인덱스 — **H1 연구 절차 + 결정적 후처리 · H2 태그 기반 서버 필터(일괄만)**

| # | 결정 | 근거 |
|---|---|---|
| **D25-1 (H1)** | **연구는 프로브로**: `scripts/ocrProbe.mjs`(`verifyProbe.mjs`와 같은 raw fetch 방식, 로컬 `MATHPIX_APP_ID/KEY`) — 첨부 이미지 2·3을 입력으로 옵션 조합을 돌려 `text`와 `line_data`를 파일로 남긴다. 조합: 현행 · `rm_spaces:false` · `include_line_data:true` · `idiomatic_eqn_arrays:true` · `alphabets_allowed:{ko:true}`(레포가 "급락"으로 기록 — **대조군**으로만). 판정 기준: `㉠`/`㉡`가 (a) `text`에 나오나 (b) `line_data`에라도 나오나 (c) 엉뚱한 글자로 나오나(예: `\neg`·`7`·`①`). ⚠ **이 문서는 어느 옵션이 되는지 단정하지 않는다** — Mathpix 옵션의 효과는 실측 전엔 추정이다 | 스케치 "연구한다"의 실체. 결과는 v2/실행판에 기록 |
| **D25-2 (H1)** →D25-2′ | 연구 결과와 **무관하게** 결정적 후처리 규칙 ⑤를 `proofread.ts` 결정적 세트에 추가(정돈·OCR 후처리가 공유): **행 끝의 `⋯`-run(`\cdots`·`⋯`·`……`·`…`·`\dots` 1개 이상, 공백 허용) + `㉠…㉭`** → `\tag{n}`(㉠=1 … 순서 `JAMO_ORDER :523`와 같은 자모 순), 수식 `$$…$$` 안 끝이면 `\tag{n}`을 수식 안에(KaTeX가 `\tag*{(n)}`로 그린다 — §1-H2), 텍스트 행 끝이면 ` \tag{n}`. **본문 중간의 `㉠`** → `(n)`(`REF_TAGNUM_RE`가 재인용으로 잡는 형태). `\cdots`-run 없이 행 끝에 `㉠`만 있어도 `\tag{n}` | 앱 정본이 `\tag{n}`·`(n)`이라(§1-H2) OCR이 `㉠`를 **어떤 글자로든** 살려만 주면 그 다음은 코드가 맡을 수 있다. `(ㄱ)→(1)` 규칙(M1 D12′)과 같은 부류 |
| **D25-3 (H2)** →D25-3′ | **구조적 범주 신설**: `SOLUTION_TAGS`(`parse.ts:207-212`)에 `인용번호미해결` 추가 + `normalizeTag` 힌트(**`kind==='problem'` 분기 `:239-246` 아래**, 61g 경고 — 위에 두면 문제 검증에 샌다) + 1차 프롬프트(`prompts.ts` 풀이 유형 목록)에 한 줄: "식 번호·인용 기호(`\tag{n}`·`(n)`·`㉠`)가 무엇을 가리키는지 찾을 수 없으면 태그 `인용번호미해결`". **탐지 자체는 유지**(스케치). 단건 정밀 검증은 태그만 바뀌고 카드에 그대로 뜬다 | 스케치 "탐지를 안 하도록 프롬프트를 짜는 것은 바람직하지 않다" 존중. 본문 정규식(`reason`에 "가리키"·"알 수 없")으로 거르는 안은 표현이 바뀌면 새고 다른 지적을 오살한다 — **보조**로만 둔다(D25-4) |
| **D25-4 (H2)** →D25-4′ | **일괄 플래그 + 서버 필터**: `VerifyRequestBody`(`route.ts:113-128`)에 `batch?: true`, `runVerifyFlow` 인자 `batch?`를 `base`(`verifyFlow.ts:141-152`)로 두 phase에 전달, 설정처는 `batchVerify.ts:221-232` **하나**. 서버 `route.ts:570` **직전**에서 `batch`이면 `tag === '인용번호미해결'` **또는** 보조 휴리스틱(`quote`/`reason`이 `/\\tag\{\|\(\d{1,2}\)\|[㉠-㉭]/` **그리고** `/가리키\|지칭\|정의되\|알 수 없\|찾을 수 없/` 둘 다)에 걸리는 finding을 제거하고, 제거 수를 `notes[]`(`:558-568`)에 `인용 번호 관련 지적 n건은 일괄 검증에서 제외(M9)`로 남긴다. `synthesizeVerdict`·카드 개수·머리행·`verification.verdict`·배지·정렬이 **자동으로** 일치한다(§1-H4) | 클라에서 거르면 verdict 재계산·개수 문자열 2곳을 따로 맞춰야 한다. `report.garbage`는 별개 축이라 무접촉. 추후 OCR이 좋아지면 **`batch` 필터 블록 하나만 지우면** 된다(스케치의 "추후 제거" — 위치가 한 곳) |
| **D25-5** | `COMMON_RULES`에 `\tag`/`\ref` 관례 설명을 넣어 **원천에서 없애는 안은 채택하지 않는다**(단건 검증에서 인용 번호 검토가 반드시 남아야 한다는 스케치 조건). 단, 프롬프트에 "`\tag{n}`은 식 번호, `(n)`·`\ref{n}`은 그 재인용"이라는 **관례 설명 1문장**은 단건에서도 오탐을 줄이므로 **P16**으로 덕수에게 묻는다 | 관례를 모르는 채 "정의를 못 찾겠다"는 것과, 관례를 알고도 "위에 (2)가 없다"는 것은 다르다 — 후자가 덕수가 원하는 검토다 |

---
### I. 블록 정돈 조정 — **추가 사항 편입(덕수 2026-09-19)**

| # | 결정 | 근거 |
|---|---|---|
| **D26** | **문제번호 제거(문제 탭)**: R2를 탭별로 가른다 — question 탭은 **첫 블록 첫 행** 앞머리 `^\d{1,2}\.(?:[ \t]+\|[ \t]*$)` 제거(번호만 있는 행이면 그 행 삭제)[Q23]. 정답 행 삭제(`ANSWER_LINE_RE`)는 question 탭에 적용하지 않는다. 소수(`1.5배`)는 마침표 뒤 공백 조건으로 비껴간다 | §1-I1 |
| **D27** | **번분수 → `\dfrac`**: `proofread` 결정적 세트 새 단계 `nestFracToDfrac`(Step 1 앞 — 수식 영역 확정 뒤). 수식 영역마다 `\frac`·`\dfrac`의 두 인자를 `lib/latexScan.ts` `readGroup`(균형 스캔 — M1 W2)으로 읽어 **인자 안에 분수가 있는 묶음**을 찾고, 그 묶음의 `\frac`을 **바깥·안 모두** `\dfrac`으로(덕수 문면 "모두")[Q20]. 지수·첨자(`^{…}`·`_{…}`) 안의 분수는 **제외**(`e^{\frac{1}{2}}`를 키우지 않는다) · `\tfrac`·`\cfrac` 무접촉(작성자 의도) · 인자가 중괄호 없는 `\frac12`형이나 스캔 실패는 **무변환** | §1-I2 · `\displaystyle` 원칙과 같은 방향(대가: 줄간 증가 — 수용된 원칙) |
| **D28** | **`STEP n`·`GUIDE` 수식화 제외**: 공용 헬퍼 `collectHeadWordRanges(text)` — `(?<![A-Za-z])STEP[ \t]?\d+\.?`(번호·마침표 포함) · `(?<![A-Za-z])GUIDE(?![A-Za-z])` — 를 `autoWrapBareNumbers`·`autoWrapBareLetters` **둘 다**의 보호 목록에(61e `collectControlSeqRanges`와 같은 방식 · 사본 금지). 범위는 행머리뿐 아니라 **본문 어디서나**(`STEP 2에서 구한 값` 같은 참조)[Q22] | §1-I3·I5 |
| **D29** | **`\section*{…}` 벗기기**: `proofread` 새 함수 `unwrapSectionCommands` — `\section`·`\subsection`·`\subsubsection`(별표 유무 모두)[Q21] + 균형 스캔 인자 → **내용만** 남긴다. 두 곳에서 부른다: ① `autoFixDeterministicIssues` **맨 앞**(Step 00 — OCR 경로) ② `blockTidy` **R0**(R1 분할 **앞** — 그래야 `\section*{STEP1}` → `STEP1` → heading 분할이 한 번의 정돈으로 끝난다). 멱등 | §1-I4·I6 |
| **D30** | **`GUIDE` 행 → 제목 블록**: R1에 `GUIDE_RE = /^GUIDE(?![A-Za-z])/` 추가 — `STEP`과 **같은 형식** `## GUIDE …`(M7 P13 전례: 표지 보존)으로 그 행을 heading 블록으로 분할. question 탭 포함 전 탭(STEP 규칙과 같다). heading은 요약 보기에 자동으로 남는다(Phase 59 — 풀이의 뼈대로 의도에 맞다) | §1-I5 |

### 2-9. v3 개정 결정 (D′) — 원 D를 덮는다

두 종류가 섞여 있다. **[정정]**은 v2 사양이 코드·규칙·실행 결과와 맞지 않아 고친 것(판정 불요 — 원 결정의 취지를 그대로 구현하는 방법의 수정)이고, **[→Q]**는 원 확정(P)을 바꾸거나 새 선택지가 생긴 것이라 §7-2에서 덕수 판정을 받는다. **(v4: §7-2 Q1~Q19 전부 권장안 확정 — [→Q] 항목도 표기된 권장안이 곧 사양이다)**

| # | 개정 | 근거(§1·부록 B) |
|---|---|---|
| **D1′** [정정] | 클릭 핸들러는 **ListView 안**에 둔다(행 범위·앵커가 ListView의 `sorted`·`lastCheckRef`에 있다). FolderView는 콜백만 받는다. ListView props를 가른다: `selectedIds`(표시용 — 뷰 무관) · `onSelectionChange(updater, origin)`(뷰 무관) · `selectable`(체크박스 열·그리드 트랙·Finder 드래그·바 — 종전 게이트). **일반 클릭·⌘ 클릭도 `lastCheckRef`를 세운다**(안 그러면 클릭 뒤 Shift가 옛 앵커를 쓴다). 행 본문 Shift = **범위 추가**(Finder 방식) — `handleCheck`의 "대상이 이미 선택돼 있으면 범위 해제"를 재사용하지 않는다. 추가: 행·카드 `onMouseDown`에서 `e.detail > 1 \|\| e.shiftKey`면 `preventDefault`(더블클릭·Shift 글자 선택 방지 — `user-select` 없음, dnd-kit은 pointerdown이라 무영향) · **dblclick 차단 4곳**(체크박스 셀 `ListView:476` · 행 ⋮ `:503` · perm 셀 `:329` · 카드 ⋮ `FolderView:859`에 `onDoubleClick={e => e.stopPropagation()}` — Phase 45a 규약) · 드래그 뒤 click은 dnd-kit 6.3.1이 캡처 단계에서 삼키므로(core.esm.js :1478·1506) 추가 처리 불요 | 부록 B A-1~A-6 |
| **D2′** [정정] | 선택 바 조건에 **`selectable &&`를 명시**(`FolderView.tsx:601`은 지금 `viewMode==='list' && size>0`뿐 — D2대로 공유 뷰에서 선택하면 남의 문항에 '폴더 변경·휴지통' 바가 뜬다). 바의 노출 규칙 자체는 **Q1** | §0-1 A |
| **D5′** [→Q4] | 카드의 Shift·⌘ 다중 선택 **삭제**(카드 Draggable data는 단건 `FolderView:783`, 바는 리스트 전용 → 다중 선택의 소비처가 없다). Shift·⌘는 리스트 전용 | Q4 |
| **D6′** [정정] | Enter 가드: `!e.isComposing && !e.repeat && (activeElement === body \|\| activeElement.closest('.folder-row, .problem-card'))` — 포커스된 **버튼**(confirmDialog 확인 · 바 '해제' · ⋮ · FolderPickerDialog)에서 Enter를 누르면 네이티브 click과 진입이 겹친다(휴지통 확인 + 진입). 헬퍼는 신설(`isEditableTarget`는 없다 — EditorView에는 인라인 사본 `:2768-2772`·`:2785-2788`뿐) | 부록 B A-3 |
| **D8′** [정정] | 스토어 API: `updatePanel(key, fn)`(**함수형**, 동기 적용 — 병렬 모델 갱신 유실 방지) + **passive 쓰기**(`draft`·`scrollTop`·`cache` — 알림 없음, 키 입력마다 패널 리렌더 금지: 61c 규약). 키 = **`${uid}:${problemId}`**(계정 전환 시 남의 캐시 노출 차단). `PendingAI`·`DiscussRequestContext`를 `panelStore.ts`로 옮겨 export, `ProblemComment`·`DiscussionSession`은 `import type`(런타임 import 0 유지). **제외 명시**: `replyingTo`(복원하면 D17 — 답글 분기가 return해 AI 미호출) · `editingId` · `freshAiCommentId` · 세션 생성·개명 입력 · AskListPopover 상태(`mathory.ask.v1`로 따로 산다). 초안은 모드별 분리 여부 **Q7** | §1-B9 |
| **D9′** [→Q5] | `mode` 복원 범위 — 같은 문항의 ProblemView↔EditorView 전환 사이로 한정할지 **Q5** | Q5 |
| **D11′** [정정] | 캐시 복원: `loading` 초기값 = `!cache` · 초기 로드 effect의 `setLoading(true)`를 **`if (!cache)`로 감싼다**(`:245`) · `cache`에 **`profiles`** 포함 · `lib/ai-models.ts`에 동기 게터 `peekEnabledModels()`(모듈 캐시 반환) 추가 · `scrollTop`은 unmount cleanup이 아니라 **`onScroll`에서 passive 기록**(cleanup 시점엔 ref가 풀려 있다), 복원은 첫 layout effect에서 1회 | §1-B9 ②③ |
| **D12′** [정정] | `useDrawerResize`에 **`onCommit?`**(pointerup)만 추가. 초기값은 기존 `defaultWidth`에 스토어 값을 넘긴다. **마운트 시 clamp**(창이 줄었을 때 — effect 안에서, `max()`는 SSR에서 못 부른다). 폭 저장 단위 **Q6**. 버전 드로어 폭은 스토어에 넣지 않는다(로컬 유지) | §1-B9 ⑤⑥ |
| **D14′** [→Q9] | 헤더 배지 tooltip에 입력/출력 합계 + **모델별 합계**(66a 모델 비교 실험이 카드별 비용을 잃는 대가 보전) — Q9 | 부록 B C-6 |
| **D15′** [정정 + →Q8] | ① **usage 정규화**(`ai-provider.ts`): Gemini `out = candidatesTokenCount + (thoughtsTokenCount ?? 0)`, `in = promptTokenCount + (toolUsePromptTokenCount ?? 0)` · OpenAI-compat 계열 `out = completion + max(0, total − prompt − completion)`(xAI처럼 reasoning이 completion 밖에 있으면 total이 그만큼 크다 — OpenAI·DeepSeek는 0이 더해질 뿐) ② ai_models 단가가 **숫자 문자열**이면 `Number()`로 살리고 warn(표로 떨어뜨리지 않는다), 숫자로 못 읽을 때만 표 fallback, warn은 modelId별 1회(Set) ③ 검증 Claude 단가 = env override → 표 → **warn + 0**(표에 없는 모델로 바꾸면 지금의 5/25보다 후퇴해 0 — 틀린 5/25보다 "모른다"가 낫다) ④ `BatchVerifyDialog.tsx:276·305`의 "(2차 Claude 기준)" 라벨을 "비용"으로 ⑤ `scripts/verifyProbe.mjs:590`의 5/25 리터럴(사본 3번째)에 "aiPricing과 동기" 주석. ①의 포함 여부는 **Q8** | §1-C10 |
| **D16′** [정정] | 교정·자동완성 비용 표시는 **`setStatus` 한 줄**(요청 1회 = 1줄). `ProofreadResultBox`는 블록마다 하나라(`EditorView.tsx:1488-1512`) 머리에 달면 N번 중복된다. ai-complete는 성공 시 표시가 없던 곳(`:1463`은 오류 전용)이라 한 줄 추가. ai-complete는 모델명을 몰라 단가를 못 찾는다 → `getAIProvider`가 `{ provider, model }`을 반환하게 | 부록 B C-7 |
| **D19′** [정정] | 리듀서 기본값(`chromeAutoHide.ts:54`)을 **`'reveal-at-top'`으로 바꾸고**, `PhoneReader.tsx:131`에서 `chromeReveal="reveal-at-top"`을 **명시**(PhoneShell prop은 기본값 없이 통과만 — 기본값이 두 곳에 갈리지 않게). 테스트 `:58-63`은 지금 모드를 명시하지 않으므로 `'reveal-on-up'`을 **명시**하도록 고친다. `:26` 주석 "(Q1 권장)" 갱신 | 부록 B D-2·D-3 |
| **D21** | **변경 없음** — 실행으로 전부 확인(부록 B E-1). 낡은 주석 `EditorView.tsx:1701`("행 사이는 빈 줄이다" — `:1703`과 모순)만 S0에서 고친다 | |
| **D23-1′** [→Q10] | 저장 원자 단위를 **문항 전체 1배치**로 올린다: `updateProblem` 1 + 삭제된 탭의 옛 블록 + 탭별 옛 블록 삭제 + 새 블록 set ≤ 500이면 한 번에 commit(문항당 약 249블록까지). 초과할 때만 탭 단위로 내리되 **탭 commit 직후마다 그 탭의 `origBlockIds`를 반환 id로 갱신**(§1-F13 중복 차단). `origBlockIds`의 진실은 `refreshed`가 아니라 **반환 id**. 블록 문서 조립은 `saveTabBlock`에서 공용 함수로 뽑아 배치도 같은 필드를 쓴다 | §1-F13·F14 |
| **D23-2′** [정정] | "저장 직전 시드"(벨트+멜빵)는 **폐기** — 서버만 1블록이 되고 로컬은 0으로 남아(M7 D11) 다음 저장이 그것을 지우고, 검증 해시·스냅샷이 서버와 갈린다. 대신 시드를 두 곳에: ① 로드 루프(`setAllBlocks`와 같은 배치라 `skipDirtyRef`가 소비 — dirty 안 찍힘, 실측) ② `applyVersionContent`(`:3346` — undo·redo·복원·드래프트가 모두 모이는 곳, 이미 `setDirty(true)`). 시드 id는 `new-${tab.id}-${nanoid()}` — `new-${Date.now()}`를 루프에서 쓰면 **여러 탭이 같은 id**를 받는다 | 부록 B F-3 |
| **D23-4′** [→Q11] | 새 문항 생성의 한 배치안(`createProblemWithBlocks`)은 **규칙이 거부**한다(§1-F14). 대안 = `createProblem` 뒤 블록 2개만 batch로, 실패 시 `deleteProblem` 보상(SheetImportModal D7과 같은 방식) — **규칙 0 유지**. `duplicateProblem` 가드는 그대로(타입 확인됨) | Q11 |
| **D24-1′** [정정 + →Q12] | ① 초성 매핑은 **단독 초성만**: 먼저 결합 자모 연쇄 `[U+1100–115F][U+1160–11A7][U+11A8–11FF]?`를 NFC로 조합하고, 남은 `[U+1100–1112](?![U+1160–11A7])`만 호환 자모로(실행 확인: `\includegraphics{해설…}` NFD 보존 · `ᄀ.`(U+1100 + `.`) → `3131 2e`). **전역 NFC 금지**(U+2126→03A9 · 호환 한자 U+F900→8C48 같은 부수 변경) ② **ZWJ(U+200D)는 제거 대상에서 뺀다**(이모지 합자 — M5 이후 본문 이모지는 OS 글꼴) ③ 행머리 공백류는 **비ASCII만**(NBSP·U+3000·U+2000–200A·U+202F) — ASCII 들여쓰기는 목록·코드블록 의미가 있다 ④ 적용 대상은 **TEXT_BASED 타입만**(svg·ggb raw_text 제외) ⑤ 함수는 `lib/blocks/normalize.ts`(nanoid ESM·preprocess를 import)가 아니라 **import 0 신설 `lib/invisibles.ts`**(ocr·blockTidy·테스트 하니스가 가볍게) ⑥ 적용 지점 = **Q12** | §1-G7 |
| **D24-2′** [정정] | 마커 3정규식은 **ASCII 들여쓰기를 캡처해 보존하고 비ASCII 공백만 소비**(`^([ \t]*)[비ASCII 공백]*` → `$1<span…>`). v2의 "IND 전체 소비"는 `- 항목\n  ㄱ. 내용`(목록 연속 문단)을 **목록 밖 최상위 문단**으로 만든다(실행 확인, `1. 첫\n   ① 가`도 같다). v2 근거 "인쇄 `:first-child`가 앞 텍스트 노드를 못 견딘다"는 **틀렸다**(`:first-child`는 요소만 센다) — 실제 이유는 NBSP가 라벨 앞 폭을 차지하는 것과 `convertRefMarkers` 오분류다. **사본 2곳 동반 수정**: `tests/chatExtract.test.mjs:38-40` · `proofread.ts:77` 마스킹 | §1-G7 ⑤ |
| **D24-3′** [정정] | 가시화 범위를 좁힌다: `addSpecialChars: /[\u200C\u2060\uFF0E\u2024]\|[\u1100-\u1112](?![\u1160-\u11A7])/` — v2의 `\u1100-\u115F` 전체는 **붙여넣은 NFD 한글의 모든 초성을 빨간 점으로** 바꾼다. NBSP·U+3000은 행머리 외에는 정규화 대상이 아니라 넣으면 **영구 점**이 된다 → 제외. M3는 정적 확정(`@codemirror/view` 6.39.14 단일 설치 · `specialCharConfig`는 `combineConfig` · 플러그인 `_plugin` 싱글턴 `index.js:9682-9709` — 두 번 등록 무해). 기본 Specials에 U+200B·FEFF·AD는 이미 있다 | 부록 B G-6 |
| **D25-2′** [정정 + →Q13·Q16·Q17] | 규칙 ⑤를 **행 단위 처리로 재설계**(§4-8): 행 끝 라벨(`㉠…㉭` 또는 `\text{㉠}`, 뒤에 닫는 `$` 허용) → 그 앞 리더를 **수식 안팎 가리지 않고** 걷기(`\cdots\|\ldots\|\dots\|⋯\|…\|···\|\.{3,}`) → 인라인 수식 안이었으면 `\tag`를 **수식 밖 행 끝**으로 · display 안이면 닫는 `$$` 직전 최상위(라벨 2개 이상인 display는 **변환 안 함**) · 리더를 걷고 남은 빈 수식(`$ $`) 삭제 · **전 행 루프**(v2 정규식은 `g` 없는 `/m`이라 첫 행만) · 리더 없는 행 끝 `㉠`는 **직전이 수식일 때만**. 위치는 Step 3 뒤가 아니라 **Step 0-2(`convertJamoRefs`) 직후, 같은 `skipJamoRefs` 게이트 아래**(choices·roman의 `㉠`가 `① (1)`이 되는 것 방지 · `\tag{…}`·`\(\d+\)`는 `autoWrapBareNumbers` 보호 목록 `:273·277`에 있어 순서 무관) · 번호는 **고정 매핑 ㉠=1**(정의는 문제 탭, 인용은 풀이 탭 — blockTidy가 블록마다 부르므로 오프셋 방식 불가) · 변환 건수를 `count`에 가산 · 중간 재인용 `(n)`은 앞이 한글 음절이면 `REF_TAGNUM_RE` lookbehind에 걸려 ref-marker가 안 된다(`식㉠에서`) — 알고 둘 한계 | §1-H8 |
| **D25-3′** [정정 + →Q15] | 태그 추가의 동반 수정: 논리 1차의 "일곱 중 하나" 두 곳(`prompts.ts:346`·`:424-425`) → "여덟" · 심각도 서열(`:404-405`) 맨 뒤 · 판정자 `[2']`(`:559-`)에 기준 1줄("정의부(`\tag{n}`·행 끝 `⋯ ㉠`)가 [문제]·[풀이] 어디에도 없으면 valid, 있으면 invalid") · `normalizeTag` 힌트는 **`:240` 뒤(풀이 힌트 구역 241-253)**에 `has('인용','unresolved','refer')`(v2의 "`:246` 아래"는 `근거없는가정` 힌트 줄이다). 계산 패스 목록에도 넣을지 **Q15**. P16 관례 문장은 `COMMON_RULES :109-110` 옆, `verify.test.mjs:288`과 같은 방식으로 고정 | 부록 B H-3~H-5 |
| **D25-4′** [정정 + →Q14] | ① 필터는 라우트 인라인이 아니라 **순수 함수 `dropCitationFindings(findings) → { kept, dropped }`를 `lib/verify/parse.ts`에**(`test:verify`는 route.ts를 컴파일하지 않는다 — v2의 "필터 테스트 3건"은 v2 설계로 불가능) ② 자리: `runJudge(a)` 안에는 `body`가 없다 → `:220` 호출에 `batch: body.batch === true`를 넘긴다 · `findings`는 `const`(`:503`)라 재대입 불가 → `kept`를 `synthesizeVerdict`와 `report`에 넘긴다 · note는 `:567`과 `:569` 사이 ③ 휴리스틱 **강화판**: reason에서 `$…$`를 지운 뒤, 인용 토큰 뒤 24자 안에 "미해결" 서술이 있어야 한다 — `(?:\\(?:tag\|ref)\{\d+\}\|(?<![0-9A-Za-z가-힣_\\'])\(\d{1,2}\)\|[㉠-㉭])[^.\n]{0,24}?(?:무엇을\s*)?(?:가리키\|지칭\|정의(?:부)?(?:가\|이)?\s*(?:되지 않\|없)\|찾을 수 없\|어디에도 없)` (v2 AND 정규식은 실제 결함 5건 중 3건 오살 — `f'(1)=0만으로는 … 알 수 없다` · `g(2)가 … 정의되는지` · `P(2)의 값이 … 알 수 없다`; 강화판은 같은 5건 오살 0 · 적중 2) ④ 휴리스틱에서 **제외**: 태그 `정답불일치` · reason이 `[군더더기 검토에서 격상]`으로 시작하는 것(태그가 `인용번호미해결`이면 거른다) ⑤ `report.garbage`는 무접촉 ⑥ `base`에는 `...(args.batch ? { batch: true } : {})` — **단건 요청 바디 바이트 불변** ⑦ note는 카드 머리에 보인다(`VerifyReportCard.tsx:57·108`). 문제 검증에도 적용할지 **Q14** | 부록 B H-6~H-8 |


## 3. 수정 파일

| 파일 | A | B | C | D | E | F | G | H |
|---|---|---|---|---|---|---|---|---|
| `components/problem/FolderView.tsx` · `ListView.tsx` · `app/globals.css`(`.is-selected`) | ● | | | | | | | |
| `lib/panelStore.ts` **신설** · `components/comment/CommentPanel.tsx` · `CommentEditor.tsx`(draft prop) · `hooks/useDrawerResize.ts`(`initialWidth`) · `ProblemView.tsx` · `EditorView.tsx`(panelMode·version 열림) | | ● | | | | | | |
| `lib/aiPricing.ts` **신설** · `lib/ai-models.ts` · `app/api/verify/route.ts` · `app/api/proofread/route.ts` · `app/api/ai-complete/route.ts` · `components/editor/ProofreadResultBox.tsx` · `CommentPanel.tsx`(배지·카드) | | | ● | | | | | |
| `components/layout/PhoneShell.tsx` · `components/phone/PhoneReader.tsx` · `lib/chromeAutoHide.ts`(순서) · `tests/chromeAutoHide.test.mjs` | | | | ● | | | | |
| `lib/locale.ts`(`MATH_ONLY_LINE_RE`) · `tests/locale.test.mjs` | | | | | ● | | ● | |
| `lib/firestore.ts`(`replaceTabBlocks`·`createProblemWithBlocks`·`duplicateProblem`) · `EditorView.tsx`(로드 시드·저장·빈 상태) · `AppShell.tsx` | | | | | | ● | | |
| `lib/blocks/normalize.ts`(`stripInvisibles`) · `lib/ocr.ts` · `lib/sheetImport.ts` · `lib/blockTidy.ts` · `lib/locale.ts`(마커 3정규식) · `components/editor/MarkdownEditor.tsx`(specialChars) · `tests/normalize.test.mjs` **신설** | | | | | | | ● | |
| `scripts/ocrProbe.mjs` **신설** · `lib/proofread.ts`(규칙 ⑤) · `tests/proofread.test.mjs` · `lib/verify/parse.ts` · `lib/verify/prompts.ts` · `app/api/verify/route.ts`(batch 필터) · `lib/verifyFlow.ts` · `lib/batchVerify.ts` · `tests/verify.test.mjs` | | | | | | | | ● |
| `package.json`(`test:normalize`·`probe:ocr`) · `CLAUDE.md` · `docs/roadmap.md` · `docs/phasedocs/개선묶음 M8 …`(Q1 번복 각주) | | | | | | | | |


**v3 추가·변경**: `lib/invisibles.ts` **신설**(← `normalize.ts`에 두지 않는다) · `tests/invisibles.test.mjs` **신설**(← `normalize.test`) · `lib/ai-provider.ts`(usage 정규화·`getAIProvider` 반환형) · `lib/ai-models.ts`(`peekEnabledModels`) · `components/problem/BatchVerifyDialog.tsx`(라벨) · `scripts/verifyProbe.mjs`(주석) · `components/import/SheetImportModal.tsx`(정규화 위치) · `tests/chatExtract.test.mjs`(사본 정규식) · `lib/verify/parse.ts`(`dropCitationFindings`) · `lib/blockTidy.ts`(헤더 정정) · `.gitignore`(`.probe/`) · `test:rules` 하니스 1건 · `lib/firestore.ts`는 `createProblemWithBlocks` 대신 `commitProblemSave`·`blockDocData`·`saveInitialBlocks` · `ProofreadResultBox.tsx`는 **무접촉**(D16′).
---

## 4. 구현 사양 (v4 — §2-9·§2 I 반영, `[Q#]`는 확정된 판정 · Q20~Q23은 가적용)

### 4-1. 폴더뷰 클릭 (A)

```tsx
// ListView 내부 — 앵커·범위는 ListView의 sorted(prefs.sort 순서)와 lastCheckRef(:169)가 소유
const onRowClick = (e: React.MouseEvent, p: Problem) => {
  if (e.detail !== 1) return;                                   // 2번째 클릭은 dblclick 몫(D1)
  if (lastPointerType.current === 'touch'                        // [Q3] 터치: 이미 단독 선택된 항목 재탭 = 진입
      && selectedIds.size === 1 && selectedIds.has(p.id)) return onView(p);
  if (e.shiftKey && selectable) return rangeAdd(p.id);           // 앵커~대상 범위를 "추가"(Finder) — handleCheck 재사용 금지
  if ((e.metaKey || e.ctrlKey) && selectable) {                  // 토글
    onSelectionChange(prev => toggled(prev, p.id), 'click'); lastCheckRef.current = p.id; return;
  }
  lastCheckRef.current = p.id;                                   // 일반 클릭도 앵커를 세운다(D1′)
  onSelectionChange(prev => prev.size === 1 && prev.has(p.id) ? new Set() : new Set([p.id]), 'click');
};
<div onPointerDown={e => { lastPointerType.current = e.pointerType; listeners.onPointerDown?.(e); }}
     onMouseDown={e => { if (e.detail > 1 || e.shiftKey) e.preventDefault(); }}   // 글자 선택 방지
     onClick={e => onRowClick(e, p)}
     onDoubleClick={() => onView(p)} … />
```
- ⚠ dnd-kit의 `pointerListeners`를 덮어쓰지 말고 **합성**할 것(위 `onPointerDown`).
- ListView props: `selectedIds`(표시용, **뷰 무관**) · `onSelectionChange(updater, origin: 'click' | 'checkbox')`(뷰 무관) · `selectable`(종전 게이트 — 체크박스 열·그리드 트랙·Finder 드래그·바). 체크박스 `handleCheck :220-238`은 `origin 'checkbox'`로 기존 동작 그대로.
- 카드(FolderView `:795`): **단일 선택만**[Q4] — `detail===1` 토글 · 터치 재탭 진입[Q3] · `onMouseDown` 글자 선택 방지 · `onDoubleClick` 진입.
- **dblclick 차단 4곳**: `ListView:476`(체크박스 셀) · `:503`(행 ⋮) · `:329`(perm 셀) · `FolderView:859`(카드 ⋮) — `onDoubleClick={e => e.stopPropagation()}`.
- 선택 바(`FolderView:601`): `selectable && viewMode === 'list' && (selectedIds.size >= 2 || selectionOrigin === 'checkbox')`[Q1]. `selectionOrigin`은 FolderView state(마지막 `onSelectionChange`의 origin).
- 빈 곳 클릭 해제[Q2]: 목록·그리드 스크롤 컨테이너 `onClick={e => { if (e.target === e.currentTarget) clearSelection(); }}`.
- CSS: `globals.css:952`의 `}` **뒤**(`@media (hover: hover)` 밖). `--drop-ring`은 box-shadow 전체 값(`globals.css:152` `0 0 0 2px rgba(…)`)이라 콤마 결합이 유효하다:
```css
.problem-card.is-selected { --card-surface: var(--block-bg-active); box-shadow: 0 4px 14px rgba(0,0,0,.08), var(--drop-ring); }
.folder-row.is-selected   { --card-surface: var(--block-bg-active); box-shadow: 0 2px 8px  rgba(0,0,0,.06), var(--drop-ring); }
```
  카드·행에 인라인 `boxShadow` 없음, 배경은 `var(--card-surface…)`라 클래스가 먹는다(실측). 드롭 타깃 링(`ListView:402`)과 모양이 같다 — 드래그 중 의미가 섞이는 것은 수용.
- Enter(D6′): FolderView keydown(`:313-318` 옆)에 `if (e.key === 'Enter' && isListNavTarget(e) && selectedIds.size === 1) onView(…)`; `isListNavTarget = !e.isComposing && !e.repeat && (document.activeElement === document.body || !!document.activeElement?.closest('.folder-row, .problem-card'))`.
- 커서(D7): 카드 `canDrag ? 'grab' : 'pointer'`(`:807`) → `'default'` · 행 `:467` → `'default'`.
- 알고 둘 것: Escape는 DialogHost(document)와 FolderView(window)가 동시에 받아 대화상자 취소가 선택까지 푼다(기존 동작, 빈도 증가).

### 4-2. 패널 스토어 (B)

```ts
// lib/panelStore.ts — 런타임 import 0 (import type만: types/problem.ts)
export interface PendingAI { … }               // CommentPanel.tsx:130에서 이관
export interface DiscussRequestContext { … }   // :148에서 이관
export interface PanelEntry {                  // 알림 대상(구독자 리렌더)
  mode: 'comments' | 'agent' | null; sessionId: string; modelIds: string[];
  pending: PendingAI[]; inputHeight: number | null; versionOpen: boolean;
}
interface PanelPassive {                       // 알림 없음 — 키 입력·스크롤마다 리렌더 금지(61c)
  draft: { comments: string; agent: string };  // [Q7] 모드별 분리
  scrollTop: number | null;
  cache: { comments: ProblemComment[]; sessions: DiscussionSession[]; profiles: Record<string, unknown> } | null;
}
export const panelKey = (uid: string, problemId: string) => `${uid}:${problemId}`;
export function readPanel(key: string): PanelEntry;                                // 없으면 동결된 DEFAULT(동일 객체)
export function updatePanel(key: string, fn: (s: PanelEntry) => Partial<PanelEntry>): void;  // 동기 적용 · 항상 새 객체 · emit
export function readPassive(key: string): PanelPassive;
export function writePassive(key: string, p: Partial<PanelPassive>): void;         // emit 없음
export function usePanel(key: string): PanelEntry;   // useSyncExternalStore(subscribe, () => readPanel(key), () => DEFAULT)
export function getPanelWidth(): number | null; export function setPanelWidth(w: number): void;  // [Q6] 전역 1값
export function clearPanelMode(key: string): void;   // [Q5]
// cache LRU 20(D13)
```
- `CommentPanel`: `setPendingAI(u)` → `updatePanel(key, s => ({ pending: u(s.pending) }))` (호출처 `:719·724·842·883·899·901·933·944` 전부 함수형 유지). `selectedModelIds`·`activeSessionId`·`inputHeight`도 같은 래퍼. `initialSelectionDoneRef :347-355`: 스토어 `sessionId`가 `normalSessions`에 있으면 자동 선택 건너뜀.
- 캐시(D11′): `const cached = readPassive(key).cache` → `loading` 초기값 `!cached` · `:245` `if (!cached) setLoading(true)` · `comments`·`sessions`·`profiles` 초기값에 사용 · 갱신될 때마다 `writePassive({ cache })` · AI 모델 목록은 `peekEnabledModels()`(신설, `lib/ai-models.ts` 모듈 캐시 동기 반환)로 초기화.
- 스크롤: `messagesScrollRef`의 `onScroll` → `writePassive({ scrollTop })`. 첫 `useLayoutEffect`에서 값이 있으면 복원하고 `:406-413`의 맨 아래 고정을 **그 1회만** 건너뛴다.
- `CommentEditor`: `initialValue` + `onDraftChange?`(passive 쓰기, 전송 성공 시 `''`) — 키 = 현재 모드.
- **복원 제외**: `replyingTo`·`editingId`·`freshAiCommentId`·세션 생성/개명 입력·AskListPopover.
- `ProblemView.tsx:216`·`EditorView.tsx:1134`: `panelMode` 초기값 = `readPanel(key).mode`, `setPanelMode` 래퍼가 `updatePanel`. EditorView 버전 드로어 `open`도 `versionOpen`으로(폭 `:1066`은 로컬 유지).
- [Q5] `AppShell`: `setView`로 **같은 problemId의 problem↔editor가 아닌** 화면으로 나갈 때 직전 키에 `clearPanelMode`(uid는 AppShell의 user). 같은 문항을 폴더에서 한참 뒤 다시 열면 패널은 닫힌 채 시작.
- 폭(D12′): `useDrawerResize`에 `onCommit?: (w) => void`(pointerup) 추가 · 두 뷰가 `defaultWidth: getPanelWidth() ?? 420`, `onCommit: setPanelWidth` · 마운트 effect에서 현재 창 기준 clamp. localStorage 무사용(규약 유지).
- 폰: `PhoneApp.tsx:368`도 같은 CommentPanel → 캐시·세션 공유는 무해, `mode`는 폰 경로에서 읽지 않는다.

### 4-3. 단가·표시 (C)

```ts
// lib/aiPricing.ts (import 0) — USD / 1M tokens
export const PRICES: Record<string, { in: number; out: number }> = {
  // Anthropic 공시(2026-06-24 기준)
  'claude-opus-5':      { in: 5,    out: 25 },   // verify/route.ts:163-165 리터럴 이관
  'claude-opus-4-8':    { in: 5,    out: 25 },   // ai_models claude-reporter(현재 15/75로 잘못 기입 — §1-C11)
  'claude-haiku-4-5':   { in: 1,    out: 5 },    // proofread 기본
  // Firestore ai_models 실측값(§1-C11) — 운영 중인 값과 같게 둔다(표는 fallback이고 1순위는 문서)
  'gemini-3.1-pro-preview': { in: 2,    out: 12 },   // ≤200K 구간(>200K 구간 단가는 §6 범위 밖)
  'gemini-3.5-flash':       { in: 1.5,  out: 9 },
  'gpt-5.4-2026-03-05':     { in: 2.5,  out: 15 },
  'grok-4.3':               { in: 1.25, out: 2.5 },
  // Google 공시(작성자 지식 기준 — 착수 시 ai.google.dev/pricing 대조): ai-complete 기본값·로컬 AI_MODEL
  'gemini-2.5-flash':       { in: 0.30, out: 2.50 },
  'gemini-2.5-pro':         { in: 1.25, out: 10 },   // ≤200K
};
export function priceFor(apiModelName: string): { in: number; out: number } | null;
export function calcCostUsd(inTok: number, outTok: number, p: { in: number; out: number }): number;  // discuss:423-434 · verify:637-639 통합
```
- [Q8] usage 정규화(`lib/ai-provider.ts`): Gemini(`:144-145`) `const um = res.usageMetadata as any; in = (um.promptTokenCount ?? 0) + (um.toolUsePromptTokenCount ?? 0); out = (um.candidatesTokenCount ?? 0) + (um.thoughtsTokenCount ?? 0)` — 검증 1차도 `getVerifyProviders`(`verify/route.ts:30`) → 같은 `GeminiProvider`를 거치므로(부록 C-1) 이 한 곳으로 전부 덮인다 · OpenAI-compat(`:194-195`) `out = completion + Math.max(0, (total ?? 0) - prompt - completion)` · Responses(`:312-313`) 동일 원칙. 저장 필드 `outputTokens`의 의미는 "사고 포함"으로 통일(과거 저장분 소급 없음 — §5-3).
- `verify/route.ts:163-165`: `claude = envOverride('VERIFY_CLAUDE_COST') ?? priceFor(env.claudeModel)`, `gemini = envOverride('VERIFY_GEMINI_COST') ?? priceFor(env.geminiModel)`; 둘 다 없으면 `console.warn('[aiPricing] 단가 없음', model)` + 0.
- `ai-models.ts:30-33`: `num(v) = typeof v === 'number' ? v : (typeof v === 'string' && v.trim() !== '' && isFinite(+v) ? (warnOnce(id), +v) : null)` → `num(...) ?? priceFor(apiModelName)?.in ?? 0`(대체 시 warnOnce).
- `CommentPanel.tsx`: `:2026-2037` 삭제 · `:1053` 게이트 `isAISession && sessionAiCount > 0` · `sessionTokens` memo(입·출력 합 + [Q9] `modelId`별 `{in, out, usd}`) · `title` 여러 줄.
- proofread: `messages.create` 결과 `usage` → `costUsd`(Haiku 표) → 응답 `usage` · EditorView는 요청당 `setStatus('교정 $0.00xx')` 1줄(D16′). ai-complete: `getAIProvider()` → `{ provider, model }` 반환으로 바꾸고 응답에 `usage` · 성공 시 `setStatus` 1줄.
- `BatchVerifyDialog.tsx:276·305` 라벨 "비용(2차 Claude 기준)" → "비용". `scripts/verifyProbe.mjs:590` 주석 "lib/aiPricing.ts와 동기".

### 4-4. 가로 보기 (D)
- `chromeAutoHide.ts:54` 기본 모드 `'reveal-at-top'` · `:26` 주석 갱신 · `PhoneShell`에 `chromeReveal?: RevealMode`(기본값 없이 전달) · `PhoneReader.tsx:131` `chromeReveal="reveal-at-top"` 명시 · 테스트 `:58-63`에 `'reveal-on-up'` 명시.
- D20 순서(`nextChromeState`):
```ts
if (y < 0 || y > max) return { state: s, action: null };                          // ① 러버밴드
if (y <= 0) {                                                                      // ③ 맨 위 — 무시 창보다 앞
  if (s.hidden) return { state: { hidden: false, lastY: 0, acc: 0, ignoreUntil: now + IGNORE_MS }, action: 'show' };
  return { state: { ...s, lastY: 0, acc: 0 }, action: null };
}
if (now < s.ignoreUntil) return { state: { ...s, lastY: y, acc: 0 }, action: null };  // ② 무시 창
// …히스테리시스·hide·(reveal-on-up일 때만) show
```
  진동 없음: show는 hidden일 때만, 숨김이 y=0을 만들 수 없다(hide는 `y > chromeH`에서만, `canHide`가 새 max ≥ 40 보장). 기존 11건 무회귀, 추가 1건("hide 직후 300ms 안 y=0 → show").

### 4-5. 렌더 정규식 (E) — D21 그대로. 인쇄 경로는 `lib/preprocess.ts:186-202`(`preprocessLocale` 호출 `:191`)가 같은 상수라 자동. S0에 `EditorView.tsx:1701` 낡은 주석 정정 동반.

### 4-6. 원자 저장·시드·빈 상태 (F)

```ts
// lib/firestore.ts
export function blockDocData(b: PersistedBlockData): Record<string, unknown>;   // saveTabBlock의 문서 조립을 추출(타임스탬프 등 동일)
export async function commitProblemSave(
  problemId: string, meta: Record<string, unknown>,                             // updateProblem과 같은 필드 + updated_at
  tabs: { tabId: string; oldIds: string[]; blocks: PersistedBlockData[] }[],
  removedTabs: { tabId: string; oldIds: string[] }[],
  onTabCommitted: (tabId: string, newIds: string[]) => void,                     // 탭 단위 폴백에서 커밋 직후 호출
): Promise<Record<string, string[]>> {
  const ops = 1 + Σ removedTabs.oldIds + Σ (tabs.oldIds + tabs.blocks);
  if (ops <= 500) { /* 한 writeBatch: update(problem) + 전부 → commit 1회 */ }
  else { /* update(problem) 먼저 → 탭마다 writeBatch(delete old + set new) → onTabCommitted */ }
}
```
- `handleSave :2960-2990`: `updateProblem` + 삭제된 탭 정리 + 탭 루프를 `commitProblemSave` 한 호출로. `origBlockIds`는 **반환 id**(폴백이면 `onTabCommitted`)로 갱신 — `refreshed` 기반 갱신 제거. 같은 id를 지웠다 다시 쓰는 일은 없다(새 doc은 항상 새 id).
- ⚠ 실측 필요(M5): **문항 doc update + 블록 delete/set을 한 배치**로 보낼 때의 규칙 평가 — 에뮬레이터로 확인된 것은 "기존 문항에 대한 블록 배치"뿐이다. 문항 update가 `parentOwner()`의 `get()`(배치 이전 상태를 읽음)에 영향을 주지 않을 것으로 보지만 `npm run test:rules` 하니스에 1건 추가해 확정.
- 로드 시드(D23-2′): `:1348-1352` 루프 `if (!blocks.length && !data.tabLoadErrors?.[tab.id]) blocksMap[tab.id] = [seedLocalBlock(tab.id)]` · `applyVersionContent :3346`에서도 빈 탭이면 시드 · `seedLocalBlock = (tabId) => ({ id: \`new-${tabId}-${nanoid()}\`, block_key: nanoid(), type: 'text', raw_text: '' })`. 저장 직전 시드 없음.
- 빈 상태(D23-3): `:3906` 앞 `{currentBlocks.length === 0 && <EmptyTabPlaceholder onAdd={() => handleAddBlock('text')} />}` — 점선 `1px dashed var(--border-content)`, `--text-muted`, 버튼은 Row 2 스타일.
- 새 문항(D23-4′·[Q11]): `AppShell.tsx:340-353` = `createProblem` → `saveInitialBlocks(problemId, q, s)`(블록 2개 batch) → 실패 시 `deleteProblem(newId)` best-effort + 오류 표시(`:354-358` catch).
- `duplicateProblem :531-535`: `if (Object.keys(original.tabLoadErrors ?? {}).length) throw …`(타입 `Record<string,string>` 확인).

### 4-7. 비가시 문자 (G)

```ts
// lib/invisibles.ts (신설, import 0)
const ZW_RE = /[\u200B\u200C\u2060\uFEFF\u00AD\u034F]/g;                          // 폭 0 — 전 위치 제거 (ZWJ \u200D 제외: 이모지 합자)
const LEAD_WS_RE = /^([ \t]*)[\u00A0\u3000\u2000-\u200A\u202F]+/gm;                // 행머리 비ASCII 공백만 제거, ASCII 들여쓰기 보존
const NFD_SYL_RE = /[\u1100-\u115F][\u1160-\u11A7][\u11A8-\u11FF]?/g;              // 결합 자모 연쇄 → NFC 음절(NFD 한글 보존)
const LONE_CHOSEONG_RE = /[\u1100-\u1112](?![\u1160-\u11A7])/g;                  // 단독 초성 → 호환 자모 (M1)
const CHOSEONG_TO_COMPAT: Record<string, string> = { /* 19종 표: U+1100→U+3131 ㄱ, U+1101→ㄲ, U+1102→ㄴ, U+1103→ㄷ, …, U+1112→ㅎ */ };
const JAMO_DOT_RE = /([\u3131-\u314E])[\uFF0E\u2024]/g;                             // 자모 뒤 전각·리더 마침표 → .
export function stripInvisibles(s: string): string {
  return s.replace(ZW_RE, '').replace(LEAD_WS_RE, '$1')
    .replace(NFD_SYL_RE, (m) => m.normalize('NFC'))
    .replace(LONE_CHOSEONG_RE, (c) => CHOSEONG_TO_COMPAT[c] ?? c)
    .replace(JAMO_DOT_RE, '$1.');
}
```
- 전역 `normalize('NFC')` 금지(부수 변경). 대상은 **TEXT_BASED 타입만**.
- 적용 지점[Q12 권장]: ① **로드** `EditorView` `toLocal`(비dirty — 다음 dirty 저장이 서버본을 고친다; 같은 세션 미리보기가 즉시 정상) ② `toPersistedBlock`(`normalize.ts:31`, 안전망) ③ 정돈 `blockTidy`(R4 앞) ④ OCR `normalizeAndFix`(`lib/ocr.ts:79`) 첫 단계 ⑤ 붙여넣기: `EditorView.clipboardInputFilter`가 있으면 그것(M3′ 확인), 없으면 `transactionFilter`를 `tr.isUserEvent('input.paste')`로 한정(IME `input.type.compose`와 무간섭) ⑥ 시트: `SheetImportModal.tsx:379` `tidyDrafts` **앞**, text 블록만(stemHash `sheetImport.ts:536` 확정 뒤 · `convertJamoRefs`가 호환 자모를 보도록). `lib/sheetImport.ts`(import 0)는 무접촉.
- 마커 정규식(D24-2′): `locale.ts:56·59·64`를 `^([ \t]*)[비ASCII 공백]*(마커)…` 꼴로, 치환은 `$1<span…>`. 사본 `tests/chatExtract.test.mjs:38-40` · `proofread.ts:77`(`grep -a`) 동반.
- 가시화(D24-3′): `MarkdownEditor.tsx:886` 옆 `highlightSpecialChars({ addSpecialChars: /[\u200C\u2060\uFF0E\u2024]|[\u1100-\u1112](?![\u1160-\u11A7])/ })`.

### 4-8. 인용 번호 (H)
- `scripts/ocrProbe.mjs`: `.env.local` 로더는 `verifyProbe.mjs:41-52` 복제 · 출력 `--out <dir>`(기본 scratch) 또는 `.probe/` + `.gitignore` 추가 · 입력 이미지 경로는 덕수 공급(§8-0 M4) · 조합: 현행 · `rm_spaces:false` · `include_line_data:true` · `idiomatic_eqn_arrays:true`(㉠와 무관할 공산) · `alphabets_allowed` 대조군은 `{ko:true}`만으로는 **무동작**일 공산(기본값이 전부 true) → 다른 문자계를 false로 둔 조합으로. `package.json` `probe:ocr`.
- 규칙 ⑤(`proofread.ts`, Step 0-2 `convertJamoRefs` 직후 · 같은 `skipJamoRefs` 게이트, 보호 범위도 같게 — 코드펜스·인라인 코드·HTML):
  1. 행마다 끝 라벨 탐지: `[㉠-㉭]` 또는 `\text{ ?[㉠-㉭] ?}`, 뒤에 닫는 `$`/`$$` 허용 · [Q13] 행 끝 `⋯ (ㄱ)`/`⋯ (1)`(convertJamoRefs 뒤라 `(1)` 형태)도 라벨로
  2. 라벨 앞 리더를 수식 안팎 무관하게 역방향으로 걷기: `\cdots` `\ldots` `\dots` `⋯` `…` `···` `\.{3,}` 와 공백 · 리더 없는 행 끝 라벨은 **직전 토큰이 수식일 때만**
  3. `scanMathRegions`(proofread에 이미 import)로 라벨·리더의 위치를 판정: 인라인 수식 안 → 리더·라벨 제거 후 **수식 밖 행 끝**에 ` \tag{n}` · display 안 → 닫는 `$$` 직전 최상위에 `\tag{n}`(그 display에 라벨 2개 이상이면 **변환 안 함**) · 텍스트 → 행 끝 ` \tag{n}`(공백 1칸) · 남은 빈 수식(`$ $`) 삭제
  4. 본문 중간 `[㉠-㉭]` → `(n)` · 번호는 고정 매핑(`'㉠㉡…㉭'.indexOf + 1`)
  5. [Q16] 충돌: 블록 묶음(정돈은 탭 단위 `tidyBlocks`, OCR은 텍스트 단위)에 이미 있는 `\tag{k}`·`\ref{k}` 번호와 새 번호가 겹치면 그 묶음은 규칙 ⑤를 건너뛰고 통계에 경고
  6. 변환 건수 `count` 가산
  - 테스트(≥10): 텍스트 `⋯⋯ ㉠` · 인라인 안 리더 `$…\cdots\cdots$ ㉠` · 인라인 안 라벨 `$…\cdots\cdots ㉠$` · `\text { ㉠ }` · `\ldots`·`···` · display 끝 · display 라벨 2개 무변환 · 중간 재인용 · 리더 없는 `$x$ ㉠` / 산문 끝 `㉠` 무변환 · 번호 충돌 건너뜀 · choices/roman 무접촉(게이트) · 여러 행.
- 태그(D25-3′): `parse.ts:207-212` `'인용번호미해결'` · `normalizeTag` 힌트 `:240` 뒤 · `prompts.ts` 논리 1차 목록 + "일곱→여덟"(`:346`·`:424-425`) + 서열 맨 뒤(`:404-405`) + [Q15] 계산 패스 1줄 + 판정자 `[2']` 기준 1줄 · P16 관례 문장(`COMMON_RULES :109-110` 옆, 테스트는 `verify.test.mjs:288` 방식).
- 필터(D25-4′):
```ts
// lib/verify/parse.ts (import 0)
const CITE_UNRESOLVED_RE = /(?:\\(?:tag|ref)\{\d+\}|(?<![0-9A-Za-z가-힣_\\'])\(\d{1,2}\)|[㉠-㉭])[^.\n]{0,24}?(?:무엇을\s*)?(?:가리키|지칭|정의(?:부)?(?:가|이)?\s*(?:되지 않|없)|찾을 수 없|어디에도 없)/;
export function dropCitationFindings(findings: VerifyFinding[]): { kept: VerifyFinding[]; dropped: number } {
  const isCite = (f: VerifyFinding) =>
    f.tag === '인용번호미해결' ||
    (f.tag !== '정답불일치' && !f.reason.startsWith('[군더더기 검토에서 격상]') &&
     CITE_UNRESOLVED_RE.test(f.reason.replace(/\$[^$]*\$/g, ' ')));
  const kept = findings.filter((f) => !isCite(f));
  return { kept, dropped: findings.length - kept.length };
}
```
  라우트: `:220` `runJudge({ …, batch: body.batch === true })` → `runJudge` 안에서 findings(`:503-523`)·격상(`:526-556`) 뒤 `const { kept, dropped } = a.batch && a.kind === 'solution' /*[Q14]*/ ? dropCitationFindings(findings) : { kept: findings, dropped: 0 }` → `synthesizeVerdict(kept)`·`report.findings = kept` · note는 `:567`과 `:569` 사이. `verifyFlow.ts` base(`:141-152`) `...(args.batch ? { batch: true } : {})` · `batchVerify.ts:221-232` `batch: true`.
  테스트(`test:verify`): 태그 일치 제거 · 강화 휴리스틱 적중 2 · 오살 0(v2 반례 3건 `f'(1)=0만으로는…알 수 없다` 등) · 격상·정답불일치 보존 · 전부 제거 시 verdict `ok`.
### 4-9. 블록 정돈 조정 (I)

```ts
// lib/proofread.ts — 결정적 세트(import는 기존 mathRegions·latexScan만)
export function unwrapSectionCommands(text: string): { fixed: string; count: number };
//   /\\(?:sub){0,2}section\*?(?![A-Za-z])/ 위치에서 readGroup으로 {…}를 읽어 내용으로 치환(선택 [opt] 인자도 흡수). 수식·코드펜스·인라인 코드 안은 무접촉
function collectHeadWordRanges(text: string): Array<[number, number]>;
//   /(?<![A-Za-z])STEP[ \t]?\d+\.?/g · /(?<![A-Za-z])GUIDE(?![A-Za-z])/g
//   → autoWrapBareNumbers(:256)·autoWrapBareLetters(:322)의 protectedRanges에 push(collectControlSeqRanges 옆)
export function nestFracToDfrac(text: string): { fixed: string; count: number };
//   scanMathRegions로 수식 영역 → 영역 안에서 \frac|\dfrac 위치마다 readGroup×2
//   인자 안(스크립트 그룹 ^{…}·_{…} 제외)에 \frac|\dfrac가 하나라도 있으면 "묶음" — 묶음 안의 \frac 전부 → \dfrac
//   스크립트 그룹 판정: 분수 토큰 직전(공백 무시)이 ^ 또는 _ 인 그룹 안이면 제외. \tfrac·\cfrac 무접촉
// autoFixDeterministicIssues 순서: Step 00 unwrapSectionCommands → Step 0 … → Step 0b → **Step 0c nestFracToDfrac** → Step 1 …
```
- `lib/blockTidy.ts`: **R0**(신설, R1 앞) — SPLITTABLE 블록에 `unwrapSectionCommands`. `autoFix` 옵션과 **무관하게 항상** 수행(R1 분할과 한 몸 — `\section*{STEP1}`이 벗겨져야 분할된다) · R1 heading 규칙에 `GUIDE_RE` · **R2-Q**(question 탭): `idx === 0` 블록의 첫 행 `/^\d{1,2}\.(?:[ \t]+|[ \t]*(?:\n|$))/` 제거, `stats.removed++`. 헤더 주석의 규칙 목록·`:6` 소비처 문장(§1-H7) 갱신.
- 순서: R0(section 벗기기) → R1(분할: display·case·subcase·[참고]·STEP·**GUIDE**) → R2(question: 문제번호 / 그 외: 번호·정답 행) → R3(autoFix — Step 00이 R0과 같은 함수라 멱등) → R4.
- 테스트 — `test:tidy`(+6): question `15. 함수…` → `함수…` · question `1.5배가…` 무변경 · question `15.\n본문` → `본문` · `\section*{STEP1}\n본문` → heading `## STEP1` + text · `GUIDE 함수의…` → heading `## GUIDE 함수의…`(수식화 없음) · `STEP 2 극값` → `## STEP 2 극값`(수식화 없음). `test:proofread`(+9): 번분수 `$\frac{\frac{1}{2}}{3}$` → `$\dfrac{\dfrac{1}{2}}{3}$` · 단순 분수 무변경 · `e^{\frac{1}{2}}` 무변경 · 번분수 안 지수 분수 `\frac{e^{\frac12}}{2}`는 스크립트 안 제외 · `\tfrac` 무접촉 · `\section*{GUIDE}` → `GUIDE` · `\subsection*{…$x$…}` 내용 보존 · 본문 `STEP 2에서 구한 A` → `STEP 2`는 보존, `A`만 `$\mathrm{A}$` · 멱등(2회 적용 동일).

---

## 5. 검증

### 5-1. 자동
`test:locale`(E 2건 · G 마커 비ASCII 공백 소비 3건 + **목록 연속 들여쓰기 보존 2건**) · `test:invisibles`(신설, G ≥ 8건: 폭 0 · ZWJ 보존 · 행머리 NBSP · ASCII 들여쓰기 보존 · 단독 초성 · **NFD 음절 보존**(`해설` NFD) · `\includegraphics{NFD 파일명}` 보존 · 자모 뒤 전각 마침표) · `test:proofread`(H 규칙 ⑤ ≥ 10건, §4-8) · `test:verify`(H 필터 5건 + 태그 정규화 1건 + P16 문장 고정 1건) · `test:chrome`(D 순서 1건 · 기존 `:58-63` 모드 명시) · `test:rules`(F 문항 update + 블록 배치 1건 — M5) · `test:tidy`·`test:sheet`·`test:mathsplit`(16건)·`test:extract`(사본 정규식 동기 후) 회귀 · **I**: `test:tidy` +6 · `test:proofread` +9(§4-9) · `npm run build`(dev 종료 후).

### 5-2. 실물
| # | 시나리오 | 기대 |
|---|---|---|
| A-1 | 리스트: 행 클릭 → 잠시 뒤 다시 클릭 → 더블클릭 | 선택(면·음영·링) → 해제 → 진입. 더블클릭 때 제목 글자가 **파랗게 선택되지 않는다** |
| A-2 | 카드: 클릭 후 포인터를 카드 밖으로 · 드래그(8px) | 선택 표시 유지 · 드래그 뒤 선택 토글 없음 |
| A-3 | 공유 뷰(수신) 클릭·더블클릭 | 선택 표시만 · **선택 바 없음** · 더블클릭 진입 |
| A-4 | 리스트 Shift·⌘ 클릭 · 체크박스 1개 체크 · Enter · Escape · 빈 곳 클릭 | 범위 추가·토글(클릭한 행이 앵커) · [Q1] 단건 클릭엔 바 없음/체크박스·2건 이상엔 바 · 진입 · 해제 · 해제[Q2] |
| A-5 | ⋮·체크박스·perm 셀을 빠르게 두 번 | **진입하지 않는다** |
| A-6 | 1건 선택 → 바 '휴지통' → 확인 버튼에 포커스된 채 Enter | 휴지통 이동만(진입 없음) |
| A-7 | iPad(데스크톱 셸) 탭 → 같은 항목 재탭 | 선택 → 진입[Q3] |
| B-1 | ProblemView agent에 질문 전송 직후 편집 진입 → 답 도착 | 편집창 패널이 **열린 채** 같은 세션·같은 칩·진행 표시 → 답이 패널에 뜬다 |
| B-2 | 두 모델 동시 전송 직후 뷰 전환 | 진행 표시 2개 모두 유지·완료(유실 없음) |
| B-3 | 편집창에서 입력 중(전송 전) ProblemView로 복귀 | 초안·스크롤 위치·폭 유지 · 로딩 깜빡임·AI 이름 '?' 없음 · 입력 중 패널 리렌더 없음(React DevTools) |
| B-4 | 버전 드로어 연 채 ProblemView → 편집 재진입 | 드로어 열림 복원 |
| B-5 | ProblemView 우측 단 열고 편집 → 복귀 · 폴더로 나갔다가 같은 문항 재진입 | 단은 닫힘(로컬) · 패널은 닫힌 채 시작[Q5] |
| B-6 | 로그아웃 → 다른 계정 로그인 → 같은 문항 | 이전 계정의 캐시·초안이 **보이지 않는다** |
| C-1 | 일괄 검증 세션 · 단건 정밀 검증 세션 | 카드 아래 `$` 없음 · 헤더 `이 세션: $x.xxxx`(tooltip에 입·출력 토큰 + 모델별[Q9]) 두 흐름 동일 · 1차 전용 종료도 `$0.0000` 표시 |
| C-2 | 정밀 검증 1회 후 서버 로그 | 1차 Gemini 출력 토큰이 사고 포함 값(이전보다 크게)[Q8] · 1차 비용 0 아님 |
| C-3 | `ai_models` 단가를 `"2.5"`(문자열)로 → 토론 | warn 1회 + 2.5로 계산(표로 떨어지지 않음) |
| C-4 | 교정 1회(블록 5개) · AI 자동완성 1회 | status 한 줄씩(블록 수만큼 중복 없음) |
| D-1 | 폰 가로: 아래로 접힘 → 위로 24px | 그대로 접힘 → 맨 위 도달 시 두 바 복귀 · 세로 무변화 |
| D-2 | 접힌 직후 0.3초 안에 맨 위로 튕김(관성 멈춤) | 복귀(이벤트가 더 없어도) |
| E-1 | aligned 3행(⇔·⇒ 행머리) ⌘⇧L → 정돈 | 세 행이 각자 줄, 화살표는 **자기 행 머리** — 미리보기·ProblemView·인쇄 동일 |
| F-1 | dev 네트워크 오프라인 → 저장 | 실패 토스트 · Firestore 블록·메타 **무변화** |
| F-2 | (강제) 두 번째 탭 저장 실패 주입 → 재저장 | 어느 탭에도 **중복 블록 없음** |
| F-3 | 기존 0블록 탭 문항 편집 진입 | 빈 text 블록 1개 · dirty 아님 · 첫 편집 저장 시 서버에 1개 |
| F-4 | 빈 탭이 생기는 undo/복원 | 시드 1블록(`applyVersionContent`) · 여러 탭 시드 id 서로 다름 |
| F-5 | 새 문항 생성 중 블록 저장 실패 주입 | 문항 doc이 남지 않는다(보상 삭제) |
| G-1 | 해당 문항(M1) 편집 진입 | **로드 즉시** 미리보기 ㄱ 행 내어쓰기 정상 · ref-marker 오분류 없음 · 저장 후 서버 `3131` · 인쇄 동일 |
| G-2 | NBSP 선행 `ㄱ.` 붙여넣기 · NFD 한글 문장 붙여넣기 · `- 항목\n  ㄱ. 내용` | 마커 정상 · NFD 음절 **깨지지 않고 빨간 점 없음** · 목록 연속 유지 |
| G-3 | 시트 가져오기(이미 가져온 행 포함) | 중복 판정 종전과 동일(stemHash 불변) · 그림 NFD 파일명 정상 |
| H-1 | `probe:ocr` 이미지 2·3 | 조합별 `text`·`line_data` 파일 · 판정표 기록 |
| H-2 | 정돈: `$= \frac{2}{(t+1)^2} \cdots\cdots$ ㉠` / 다음 줄 `㉠에서` | `$= \frac{2}{(t+1)^2}$ \tag{1}` · `(1)에서` · KaTeX 오류 없음 · RefTooltip 동작 |
| H-3 | 인용 번호 지적이 있는 문항: 단건 · 일괄 | 단건 카드에 `[인용번호미해결]` · 일괄 카드엔 없음 + 머리 note 1줄 · verdict/배지 일치 |
| H-4 | 댓글 OCR로 `⋯⋯ ㉠` 포함 이미지 | 규칙 ⑤ 적용 결과가 말풍선에 `(1)`로 보인다[Q17] |
| I-1 | 편집창 정돈: 문제 탭 `15. 함수 …` | `함수 …`(번호 제거) · 풀이 탭 규칙은 종전 |
| I-2 | 정돈: `$\dfrac{1}{1+\frac{1}{x}}$` · 인라인·display 각각 | 안쪽도 바깥과 같은 크기 · 지수 안 분수는 그대로 · 인쇄 동일 |
| I-3 | 정돈: `\section*{GUIDE}` / 다음 줄 설명 · `\section*{STEP1}` | 각각 제목 블록 `GUIDE`·`STEP1`로 분할 · 수식 없음 · 요약 보기에 제목으로 남는다 |
| I-4 | 정돈: 본문 `STEP 2에서 구한 값` | `STEP 2` 수식화 없음 |
| I-5 | OCR 붙여넣기(Mathpix `\section*{…}` 포함) | 벗겨진 내용 · 수식화 없음(분할은 정돈에서) |

### 5-3. 알고 두는 손실
- D1: 첫 클릭이 선택이라 **진입에 클릭 2번**(스케치 취지). 진입 뒤 복귀 시 선택 없음. Escape는 대화상자 취소와 선택 해제를 함께 한다(기존).
- D8: 패널 DOM은 재마운트된다 — cache로 가리지만 GeoGebra 등 무거운 메시지의 재렌더 비용은 남는다.
- D14: 카드 단위 비용은 사라진다(모델별 합계는 헤더 tooltip[Q9]).
- D15′: 과거에 저장된 `aiUsage`는 사고 토큰 미포함 그대로(소급 없음) — 세션 합계에 옛 값과 새 값이 섞인다.
- D19: 접힌 상태에서 탭·← 뒤로·댓글·agent·⋯ 버튼(`PhoneReader:131-149`) 모두 **맨 위까지 올려야** 닿는다(M8 손실 ② 강화).
- D23-1′: 문항 전체 500 ops 초과(약 249블록) 시 탭 단위로 내려 교차 탭 원자성만 잃는다(중복은 없음).
- D24-1′: 정규화가 저장본을 바꾼 문항은 다음 저장에서 검증 해시가 달라져 **stale이 한 번 켜진다**(해당 문항만).
- D25-2′: 한글 음절 바로 뒤 재인용(`식㉠에서` → `식(1)에서`)은 ref-marker가 되지 않는다(`REF_TAGNUM_RE` lookbehind).
- D25-4: 일괄 검증은 인용 번호 지적을 잃는다(스케치 의도) — note에 건수만.

---

## 6. 범위 밖
카드 보기 일괄 동작(선택 바·체크박스) · 돌아온 문항 강조(`activeProblemId`) · ProblemView 버전 드로어 신설 · 패널 AppShell 호이스팅 · 사용자별 월간 비용 집계(새 컬렉션·규칙) · Gemini 200K 초과 구간 단가 · Mathpix 건당 비용 집계 · 세로 보기 크롬 숨김 · 인쇄 2.5em/화면 2em 통일 · `COMMON_RULES` 인용 관례 설명(P16에서 판정) · `mathIndex.ts`·`locale.ts` `$$` 스캐너 · `FolderView.tsx:126` 죽은 `onEdit` prop 정리(S1에 1줄로 얹어도 됨) · `CIRCLED_CONSONANTS` 죽은 export 정리. · **(v3 추가)** 과거 `aiUsage` 소급 재계산 · Mathpix `line_data` 병합(Q18) · 카드 다중 선택(Q4) · 인라인 수식 안 `\tag` 지원 · Vercel의 미사용 env `ANTHROPIC_MODEL` 정리(§1-C12)

---

## 7. 덕수 판정 — **P1~P16 전부 권장안으로 확정 (2026-09-19, "모두 권장안대로")**

표는 v1을 그대로 두어 무엇이 확정됐는지 기록한다. "권장" 열이 곧 확정값이고 "대안" 열은 **채택되지 않은 것**이다 — CLI가 대안 쪽으로 흘러가지 않도록 남긴다.

| # | 결정 | **권장** | 대안 |
|---|---|---|---|
| **P1** | (A) 재클릭 해제의 "시간" 판정 | **`e.detail`(브라우저 더블클릭 창)**, 타이머 없음 — D1 | 400ms setTimeout(선택 표시 지연) |
| **P2** | (A) 공유·수신 뷰의 클릭 | **선택 표시 허용, 동작 바는 소유 뷰만** — D2 | 종전 게이트 유지(공유 뷰 첫 클릭 무반응) |
| **P3** | (A) 선택 스타일 | **hover 면·음영 + `--drop-ring` 한 겹** — D4 | hover와 완전 동일(구별 불가) / 새 토큰 |
| **P4** | (A) Shift·⌘ 클릭 · Enter · 커서 default | **포함** — D5~D7 | 클릭·더블클릭만 |
| **P5** | (C) `lib/aiPricing.ts` 단가 **값** | **덕수가 공급자 공시가로 기입**(키 8종, §4-3) — Claude Opus 5 `5/25`는 현행 리터럴 이관 | — (코드 판정 아님 · 입력 필요) |
| **P6** | (C) `ai_models` 문서 값이 잘못됐을 때 | **코드 표 fallback + warn** — D15 ③ | 0 유지(현행) / 에러로 호출 거부 |
| **P7** | (C) 교정·자동완성 비용 | **응답에 usage 동봉 + 화면 표시, 저장 없음** — D16 | 집계 안 함 / 새 컬렉션 영구 저장(규칙 변경) |
| **P8** | (C) 헤더 배지 게이트 | **AI 메시지 1건 이상이면 `$0.0000`도 표시** — D14 | `> 0` 유지(1차 전용 종료 시 배지 소멸) |
| **P9** | (D) **M8 Q1 번복(확정)** — 가로 크롬 복귀 조건 | **(b) 맨 위 도달 시만(`reveal-at-top`)** — D19, 손실 ② 수용 · M8 실행판 Q1에 번복 각주(2026-09-19) | (a) 유지 / 절충: 위로 **연속 200px** 이상일 때 복귀(리듀서 상수 1개 — 새 모드 없이 `HYSTERESIS_UP=200`) |
| **P10** | (B) 지속 방식 | **상태 스토어(두 마운트 유지)** — D8 | AppShell 호이스팅(큰 수술, §1-B8) |
| **P11** | (B) 지속 항목에 폭·스크롤·초안·cache 포함 | **전부 포함** — D11·D12 | 열림·세션·칩·진행만 |
| **P12** | (B) cvs = 버전 드로어 | **EditorView 재진입 복원만(기록)** — D10 | ProblemView에 드로어 신설(별도 Phase) |
| **P13** | (F) 저장 원자화 방식 | **`writeBatch` 탭 단위, 500 초과 시 에러** — D23-1 | 순서만 바꾸기(새로 쓰고 지우기 — 실패 시 중복) / 청크 배치(반쪽 상태 가능) |
| **P14** | (G) 비가시 문자 제거 범위 | **폭 0 문자 전 위치 + 행머리 공백류 + 동형 자모·마침표** — D24-1 | 행머리만 |
| **P15** | (H) 일괄 필터의 보조 휴리스틱 | **태그 + 본문 휴리스틱(AND 조건)** — D25-4 | 태그만(모델이 태그를 빠뜨리면 샌다) |
| **P16** | (H) 프롬프트에 `\tag`/`\ref` 관례 **설명 1문장**(탐지 정책 아님) | **추가** — D25-5(단건 오탐도 준다) | 미추가(스케치 문면 엄수) |

⚠ **M1(G 원인 글자 확정)**은 판정이 아니라 **덕수 절차**다(§8-0). 결과와 무관하게 D24 처방은 같지만, 실행판에 원인 글자를 기록해야 "근본적 해결"이 성립한다.

⚠ **v3에서 영향받는 확정(P)**: P5(Claude 2종 기입 — 나머지 6종 대기) · P7(표시 위치만 `setStatus`로 정정, 취지 불변) · P13(→ Q10) · P14(범위의 기술적 정정 — D24-1′ ①~④, 취지 불변). 나머지 P는 그대로다.

### 7-2. v3 판정 — **Q1~Q19 확정 (덕수 2026-09-19, "모두 권장안대로")**

| # | 항목 | **권장** | 대안 | 근거 |
|---|---|---|---|---|
| **Q1** | (A) 선택 바 노출 | **`selectable` + (선택 ≥ 2 또는 체크박스로 만든 선택)**일 때만. 단건 클릭 선택은 바 없이 우측 컨트롤(보기 전환·일괄 검증·휴지통 비우기) 유지 — 단건 조작은 ⋮ 메뉴 | ⓑ 바와 보기 전환 토글을 함께 표시(바 레이아웃 변경) ⓒ v2대로 클릭마다 바(보기 전환이 사라져 카드 보기로 가려면 먼저 해제해야 하는 회귀) | §0-1 A ② |
| **Q2** | (A) 빈 곳 클릭 해제 | **포함**(스크롤 컨테이너 `target === currentTarget`) | Escape만 | Finder 관례 · 선택이 기본 동작이 되면 해제 경로가 더 필요 |
| **Q3** | (A) 터치(iPad — 데스크톱 셸이라 FolderView를 쓴다) | **이미 단독 선택된 항목을 다시 탭하면 진입**(`pointerType === 'touch'`). 재클릭 해제(D1)는 마우스 전용 | 더블탭에 맡김(iPad Safari는 더블탭 확대와 겹친다) | §1-A10은 폰만 덮는다 |
| **Q4** | (A) 카드 다중 선택 | **단일 선택만**(Shift·⌘는 리스트 전용) | v2대로 카드도 다중(소비처 없음 — 드래그·바 모두 단건/리스트 전용) | D5′ |
| **Q5** | (B) 패널 `mode` 복원 범위 | **같은 문항의 ProblemView↔EditorView 전환 사이에서만** 유지, 문항 밖으로 나가면 닫힘 | 앱 세션 내내 문항별 기억(폴더에서 한참 뒤 열어도 자동으로 열림) | 스케치는 "전환 시 지속" |
| **Q6** | (B) 패널 폭 저장 단위 | **전역 1값**(문항 무관) | 문항별 | Phase 62 폭 통일 취지 · 두 뷰 규격 동일 |
| **Q7** | (B) 입력 초안 | **모드별 분리**(`comments` / `agent`) | 공용 1개(agent용 초안이 **공개 대상인 댓글 스트림**에 남아 잘못 전송될 수 있다) | D8′ |
| **Q8** | (C) Gemini 사고 토큰·xAI reasoning을 출력에 합산 | **포함(S4)** — 안 하면 P5 단가를 채워도 1차 검증 비용이 크게 낮게 나온다 | 제외(현행 — 청구액과 표시가 계속 어긋남) | §1-C10 |
| **Q9** | (C) 헤더 tooltip에 모델별 합계 | **포함** | 입·출력 합계만(66a 모델 비교 실험에서 모델별 비용이 사라진다) | D14′ |
| **Q10** | (F) 저장 원자 단위 — **P13 개정** | **문항 전체 1배치(≤ 500 ops, 약 249블록)**, 초과 시 탭 단위 + 탭별 `origBlockIds` 즉시 갱신 | v2 P13(탭 단위만 — 교차 탭 중복 잠복이 남는다) | §1-F13 |
| **Q11** | (F) 새 문항 생성 — **D23-4 개정** | **`createProblem` → 블록 2개 batch → 실패 시 `deleteProblem` 보상**(규칙 0) | ⓑ 로드 시드(D23-2′)에만 맡기고 생성 경로는 그대로 ⓒ 규칙에 `getAfter` 도입(규칙 변경 — 기각 권장) | §1-F14 |
| **Q12** | (G) 정규화 지점 | **로드(`toLocal`) + 저장 + 정돈 + OCR + 붙여넣기 이벤트 한정 + 시트(Modal의 tidy 앞)**. 대가: 해당 문항은 다음 저장에서 검증 stale이 한 번 켜진다 | 저장·정돈·OCR만(v2 — 같은 세션에서 ㄱ 행이 계속 깨져 보이고, G-1 기대가 성립하지 않는다) | §1-G7 ③ |
| **Q13** | (H) 행 끝 `⋯ (ㄱ)`·`⋯ (1)`도 `\tag`로 | **예** — 지금은 `convertJamoRefs`가 `(1)` 재인용으로 만들어 **정의부가 참조로 둔갑**한다 | ㉠ 계열만 | D25-2′ |
| **Q14** | (H) 일괄 필터를 문제 검증에도 적용 | **풀이 검증만**(스케치 문면 "해설 일괄 검증") | 문제·풀이 모두(강화 휴리스틱이라 오살 위험은 낮다) | 스케치 |
| **Q15** | (H) 계산 패스에도 `인용번호미해결` 1줄 | **예** — 계산 패스는 인용 문제를 `표기오류`로 내서 태그 필터를 비껴간다 | 논리 패스만(휴리스틱에 맡김) | D25-3′ |
| **Q16** | (H) 번호 충돌(기존 `\tag{k}`와 ㉠→k) | **그 묶음(탭·OCR 텍스트)은 규칙 ⑤를 건너뛰고 통계에 경고** | 오프셋 부여(블록 단위 호출이라 탭 간 일관성 불가) · 강행 | D25-2′ |
| **Q17** | (H) 댓글 OCR(`CommentEditor.tsx:118`)에도 규칙 ⑤ | **그대로 적용**(같은 `normalizeAndFix` — 정본 표기) · 실물 검수 H-4 | 댓글 경로만 제외(옵션 인자 추가) | §1-H7 |
| **Q18** | (H) M4 결과가 "`line_data`에만 ㉠가 산다"일 때 라우트 병합 | **M9 범위 밖**(결과표만 남기고 별도 판정) — M9는 프로브 + 후처리까지 | M9에 포함(OCR 라우트 신규 작업) | D25-1 |
| **Q19** | (C) P5 단가 값 | **확정(v4)** — §4-3 표: Claude 3종은 Anthropic 공시가, Gemini 3.x·GPT·Grok은 **Firestore 운영값**(§1-C11), Gemini 2.5 2종은 Google 공시가(착수 시 대조). `deepseek-v4-pro`는 문서가 없어 표에서 뺐다(추가하면 Firestore가 1순위, 표 누락은 warn + 0) | — | §1-C11 |

### 7-3. v4 추가 판정 — **Q20~Q23 (추가 사항 I의 해석, 권장안 가적용)**

추가 사항의 문면이 두 가지로 읽히는 곳이다. **권장안으로 사양(§2 I·§4-9)을 이미 적었다** — 이의가 있으면 착수 전에 알려 주면 그 줄만 바꾼다.

| # | 항목 | **권장(가적용)** | 대안 | 근거 |
|---|---|---|---|---|
| **Q20** | 번분수 변환 범위 | **분수가 중첩된 묶음의 바깥·안 모두 `\dfrac`, 지수·첨자 안 분수는 제외** | ⓑ 안쪽만 ⓒ 모든 `\frac`(단순 분수·지수 안 분수까지 — `e^{\frac12}`가 커진다) | 문면 "안의 분수도 밖의 분수와 마찬가지로 모두" |
| **Q21** | `\section` 계열 범위 | **`\section`·`\subsection`·`\subsubsection`(별표 유무 모두)** | `\section*`만 | Mathpix는 제목 위계에 따라 셋을 섞어 낸다 |
| **Q22** | `STEP n`·`GUIDE` 수식화 제외 위치 | **본문 어디서나**(단어 경계) | 행머리만(본문의 `STEP 2에서`는 `$\mathrm{STEP}$ $2$`로 계속 깨진다) | 참조 표현도 같은 약속된 낱말 |
| **Q23** | 문제번호 뒤 공백 | **공백 1개 이상 또는 번호 단독 행**(`15.` 뒤 줄바꿈) | 정확히 1개(OCR이 공백 2개를 내면 남는다) | 문면 "공백1개" — OCR 출력 편차 |

---

## 8. 착수 (CLI)

### 8-0. 실측으로만 닫히는 것

| # | 항목 | 절차 | 갈래 |
|---|---|---|---|
| **M1** | **G 원인 글자** — **확정(덕수 실측 2026-09-19)** | 블록 3 첫 행 `ᄀ. $a=0$이면,` → **`1100 2e 20 24`**: `ㄱ`이 호환 자모 U+3131이 아니라 **초성 자모 U+1100**. 선택지 블록 `① ㄱ`은 `3131`(유입 경로가 다르다). ⚠ **v3 보강**: `'ㄱ'.normalize('NFKC')` = U+1100이다 — 유입원에 NFKC/NFKD 처리가 있었을 공산이 크고, 같은 원천이면 `①`→`1` 손상도 있을 수 있다(실행판에 기록). ㄴ.·ㄷ. 행은 S1 뒤 세 행이 `3131·3134·3137`인지 확인 | **D24-1′**(단독 초성만 매핑 + NFD 음절은 NFC 조합). v2의 "U+1100–1112 전 위치 매핑"은 NFD 한글을 파괴하므로 폐기 |
| **M2** | **C Firebase·env 점검** | **Firestore — 닫힘(§1-C11)**: `ai_models`는 공개 읽기(`firestore.rules` `allow read: if true`)라 REST로 직접 조회했다 → 스크린샷 불필요 | 수정 권장 1건: `claude-reporter`(꺼짐) 단가 15/75 → 5/25 |
| **M2′** | **Vercel env — 닫힘(덕수 스크린샷 2026-09-19, §1-C12)** | `VERIFY_*`·`CLAUDE_PROOFREAD_MODEL` 없음 · `AI_PROVIDER=gemini` · `AI_MODEL=gemini-2.5-pro` · `ANTHROPIC_MODEL`은 미사용 변수 | override 없음 → §4-3 표가 운영 단가. S4 선행 조건 해소 |
| M3 | CM6 `highlightSpecialChars` 2회 등록 | — | **닫힘(정적 확정)** — D24-3′ |
| **M3′** | 붙여넣기 필터 API | `@codemirror/view` 6.39.14에 `EditorView.clipboardInputFilter`가 있는지 확인 | 있으면 그것 · 없으면 `transactionFilter` + `isUserEvent('input.paste')` |
| M4 | Mathpix 옵션(H1) | `probe:ocr` 결과표. **입력 파일 준비 완료(2026-09-19)** — 덕수가 첨부한 이미지와 해상도가 일치하는 바탕화면 스크린샷을 복사했다: `docs/phaseSketch/m9-ocr/ocr-line-1.png`(648×62, `= 2/(t+1)^2 ……… ㉠`) · `ocr-line-2.png`(700×60, `⇒ f'(t) = … ……… ㉡`) · `ocr-page.png`(2022×1598, 그림 포함 페이지) · `giyeok-box.png`(2858×1262, §1-G 이미지 1 — 프로브 입력 아님). ⚠ 셋 다 **화면 캡처**다 — Mathpix가 실제로 받은 원본은 `~/Documents/crops/`(`X18·X20(260918)_해설_…_c1.png` 등 해설 크롭 + PDF)에 있을 공산이 크다. 같은 문항의 크롭을 찾으면 **그 파일도 함께** 프로브에 넣는다(캡처와 원본의 결과가 갈리면 원본이 기준). 첨부본 관찰: 리더는 기준선 점 9개 안팎(`\cdots` 3연 또는 `\ldots` 계열로 나올 공산), 라벨은 행 오른쪽 끝 원문자 ㉠·㉡, **둘째 행은 `⇒`로 시작**(E·H 결합 — `⇒ $f'(t)=\dfrac{1}{2}\times\dfrac{d\theta}{dt}$ \tag{2}`가 목표형, D21의 `\tag` 꼬리 허용으로 행 분리까지 맞물린다. 번분수는 아니므로 D27 대상 아님) | `text`에 `㉠`가 살면 옵션 채택 · `line_data`에만 살면 Q18(범위 밖) · 어디에도 없으면 규칙 ⑤만 |
| **M5** | 문항 update + 블록 배치의 규칙 평가 | `npm run test:rules` 하니스(에뮬레이터)에 1건 추가 | 통과 → Q10 그대로 · 거부 → `updateProblem` 먼저 + 블록 전체 1배치 |
| **M6** | xAI usage 형식 | `grok-4.3` 토론 1회의 원 응답 `usage` 기록 | `total > prompt + completion`이면 D15′ ① 식이 reasoning을 더한다 |

### 8-1. 선행 읽기
v2 목록 + 추가: `ListView.tsx:160-240·395-410`(앵커·드롭 링) · `FolderView.tsx:595-640·850-865` · `node_modules/@dnd-kit/core/dist/core.esm.js:1470-1510` · `CommentPanel.tsx:125-155·240-260·400-415`(interface·로드·스크롤) · `lib/ai-models.ts` 모듈 캐시 · `BatchVerifyDialog.tsx:270-310` · `ai-provider.ts:130-150·600-620`(`getAIProvider`) · `firestore.rules:95-150` · `EditorView.tsx:1340-1360·1480-1515·2955-3075·3340-3375` · `SheetImportModal.tsx:370-390` · `sheetImport.ts:530-540` · `tests/chatExtract.test.mjs:30-45` · `proofread.ts:60-90·260-290·520-560`(`grep -a`) · `prompts.ts:100-115·340-350·400-430·555-570` · `route.ts:215-225·495-570`.

### 8-2. 커밋 순서 (항목 독립, push는 덕수)

| S | 내용 | 커밋 | 완료 기준 |
|---|---|---|---|
| S0 | **E** `MATH_ONLY_LINE_RE` + 테스트 2 + `:1701` 주석 | `render: 화살표 접두 수식 행도 행 분리` | 5-2 E-1 · `test:locale` |
| S1 | **G** `lib/invisibles.ts` + `test:invisibles` + 마커 3정규식(ASCII 보존) + 사본 2곳 + 가시화 + 적용 지점(Q12) | `text: 비가시·초성 자모 정규화 · 마커 선행 비ASCII 공백 관용 · 편집기 가시화` | 5-2 G-1~G-3 · `test:locale`·`test:extract`·`test:proofread`·`test:sheet` |
| S1b | **I** `unwrapSectionCommands`·`collectHeadWordRanges`·`nestFracToDfrac`(proofread) · R0·`GUIDE_RE`·R2-Q(blockTidy) · 테스트 +15 | `tidy: 문제번호·번분수·STEP/GUIDE·\section* 정돈` | 5-2 I-1~I-5 · `test:tidy`·`test:proofread`·`test:sheet`(시트 정돈 회귀) |
| S2 | **F** `commitProblemSave`·`blockDocData` · 로드/복원 시드 · 빈 상태 · 새 문항 보상 · `duplicateProblem` 가드 · `test:rules` 1건(M5) | `save: 문항 단위 원자 저장·빈 탭 시드·자리표시자` | 5-2 F-1~F-5 · 버전 diff block_key 유지 |
| S3 | **D** 기본 모드 · prop · 리듀서 순서 · 테스트 | `phone: 가로 크롬 맨 위 복귀(M8 Q1 번복)` | 5-2 D-1·D-2 · `test:chrome` · 실기(가로) |
| S4 | **C** · usage 정규화 · `aiPricing.ts` · verify·ai-models·proofread·ai-complete · 배지·라벨 | `cost: 사고 토큰 합산·단가 단일 원천·세션 합계 배지 통일` | 5-2 C-1~C-4 · `probe:verify` 1회 |
| S5 | **A** 클릭/더블클릭·선택 배선·바·CSS·Enter·커서·dblclick 차단 | `folder: 클릭 선택·더블클릭 진입` | 5-2 A-1~A-7 · DnD 회귀(8px) |
| S6 | **B** `panelStore.ts` · CommentPanel/Editor · `useDrawerResize.onCommit` · 두 뷰 · AppShell(Q5) · `peekEnabledModels` | `panel: 뷰 전환 시 agent·댓글·버전 상태 지속` | 5-2 B-1~B-6 · 폰 sheet 회귀 |
| S7 | **H** `ocrProbe.mjs`(M4) · 규칙 ⑤ · 태그·프롬프트 · `dropCitationFindings` · batch 플래그 · 테스트 | `verify: 인용 번호 태그·일괄 필터 · proofread: ㉠→\tag 후처리` | 5-2 H-1~H-4 · `test:verify`·`test:proofread` |
| S8 | CLAUDE.md(§8-3 + **M7 D19 정정**) · `blockTidy.ts:6` 헤더 정정 · roadmap · M8 실행판 Q1 각주 · 실행판 등록 | `docs: M9` | — |

S0·S1은 파급이 작고 독립이라 먼저. S2는 저장 경로라 단독 커밋. S6은 가장 큰 변경이라 뒤에. S4는 입력 대기라 순서를 밀어도 된다.

### 8-3. CLAUDE.md에 남길 규약(초안 — v3 개정)
- **폴더뷰 클릭은 선택, 더블클릭이 진입(M9 D1)** — 재클릭 해제는 `e.detail`로, 타이머 금지. 선택 표시는 뷰 무관, 선택 바·일괄 동작은 `selectable`(바는 Q1 조건). 행 클릭 핸들러는 ListView 소유(앵커·범위가 `sorted`에 있다). 클릭 가능한 자식(⋮·체크박스·perm)은 `onDoubleClick` 전파도 막을 것. `.is-selected`는 `alt → hover → selected` 순서의 마지막, 미디어 쿼리 밖.
- **패널 상태의 원천은 `lib/panelStore.ts`(D8′)** — 갱신은 `updatePanel(fn)`(함수형), 초안·스크롤·캐시는 passive(알림 없음), 키는 `uid:problemId`. `replyingTo`는 복원하지 말 것(D17). 패널 호이스팅은 하지 않는다.
- **usage의 출력 토큰은 사고 포함이다(D15′)** — Gemini는 `thoughtsTokenCount`를 더한다. 단가 우선순위: 토론 계열 = `ai_models` 문서(숫자 문자열은 살림) → `lib/aiPricing.ts`(fallback + warn) · 정밀 검증 = env override → 표 → warn + 0. 라우트에 단가 리터럴을 다시 적지 말 것. 세션 합계는 클라 memo(규칙 `hasOnly`).
- **가로 크롬은 `reveal-at-top`(D19′, M8 Q1 번복 2026-09-19)** — 리듀서 기본값이 곧 사용값. 맨 위 판정은 무시 창보다 앞이다(D20).
- **`MATH_ONLY_LINE_RE`는 `⇒ ⇔ → ↔ ∴ ∵` 접두를 허용한다(D21)** — 집합을 열지 말 것.
- **블록 저장은 `commitProblemSave` — 문항 전체 1배치, 초과 시 탭 단위 + 탭별 `origBlockIds` 즉시 갱신(D23-1′)** — delete/add 루프로 되돌리지 말 것. `origBlockIds`의 진실은 반환 id. 로드·`applyVersionContent`에서 빈 탭은 시드하되 `tabLoadErrors` 탭은 제외, 저장 직전 시드 금지(D23-2′). 문항 doc과 그 하위 블록을 **한 배치로 생성하지 말 것**(규칙 `parentOwner()`의 `get()`이 배치 이전 상태를 본다).
- **비가시·초성 자모 정규화는 `lib/invisibles.ts`(import 0) 하나(D24-1′)** — 초성 매핑은 **단독 초성만**(NFD 한글 보존), 전역 NFC 금지, ZWJ 보존, ASCII 들여쓰기 보존. 로드·저장·정돈·OCR·붙여넣기·시트(tidy 앞)에서 호출. 시트는 stemHash 뒤에서만. 마커 정규식은 비ASCII 공백만 소비(D24-2′) — 사본(`chatExtract.test.mjs`·`proofread.ts:77`) 동기.
- **`autoFixDeterministicIssues` 소비처는 둘이다: blockTidy · `lib/ocr.ts`(편집창·댓글 OCR)** — M7 D19 문장 정정.
- **인용 번호 지적은 태그 `인용번호미해결`(D25-3′)이고 일괄 검증(`batch`)의 풀이 검증에서만 `dropCitationFindings`가 거른다(D25-4′)** — 유일한 필터 자리, OCR 개선 뒤 제거 대상. 규칙 ⑤는 인라인 수식에 `\tag`를 넣지 않는다(KaTeX 오류).
- **블록 정돈은 R0(`\section` 계열 벗기기) → R1(분할 — `STEP`·`GUIDE` 행은 `## …` 제목) → R2(문제 탭: 첫 행 문제번호 / 그 밖: 번호·정답 행) → R3(결정적 세트) → R4 순이다(M9 D26~D30)** — M7의 "R2는 question이 아닌 탭 전부"를 개정(문제 탭은 문제번호만). `STEP n`·`GUIDE`는 `collectHeadWordRanges` 하나로 두 수식화 함수에서 보호. 번분수는 묶음의 `\frac`을 전부 `\dfrac`으로, 지수·첨자 안은 제외

### 8-4. 하지 말 것
FolderView/Sidebar에 `DndContext` 재생성 · hover 배경을 `#E8DFCE`보다 어둡게 · `useDrawerResize`에 localStorage · `discussion_sessions`에 비용 필드 · 폰 세로 보기에 자동 숨김 · `[data-phone]` 판별 · 소스에 빈 줄 삽입(mathSplit/tidy) · `mathSplit.ts` 수정 · `p:has` 인쇄 선택자 변경 · `\s*` 행머리 정규식 · `splitFigures`/`rowToDraft` 갈래 삭제 · `normalizeTag` 힌트를 problem 분기 위에 · 필터를 클라(`verifyFlow`)에 · dev 켠 채 `npm run build` · **(v3 추가)** 초성 자모 전 위치 매핑·전역 NFC · `lib/sheetImport.ts`에 정규화 삽입(import 0 · stemHash) · 문항 doc + 블록 생성 한 배치 · 패널 초안을 알림형 스토어에 · `patchPanel`류 비함수형 갱신 · 인라인 `$…$` 안 `\tag` · dnd-kit `pointerListeners`를 덮어쓰는 `onPointerDown` · 도구 파라미터에 `\u` 이스케이프를 직접 적기(Write·Bash가 실제 문자로 바꾼다 — memory `feedback_write_tool_nul_escape`). · **(v4 추가)** `STEP`·`GUIDE` 보호를 한 수식화 함수에만 · 지수·첨자 안 분수를 `\dfrac`으로 · `\section` 벗기기를 R1 뒤에

---

## 부록 A. 스케치 ↔ 실측 대조 요약

| 스케치 | 실측 | 처방 |
|---|---|---|
| 클릭 1회 진입 → 선택/더블클릭 | 클릭 사이트 2곳, 선택 상태는 리스트에 이미 존재, 더블클릭·Enter 0 | D1~D7 |
| 뷰 전환 시 패널 꺼져 agent 작업 소실 | 작업(fetch·저장)은 **살아남음**, 표시·세션·칩·초안·폭·열림이 소실 | D8~D13 |
| 입력/출력 모두 산출? | 예(사본 2). 스트리밍 없음 | D15 통합 |
| 모델당 단가 제대로? | 코드 표 없음 · Firestore 문서(타입 오류 시 0) · 1차 Gemini env 기본 0 · Claude 리터럴 | D15·D18·P5 |
| 모든 모델 빠짐없이 합산? | 교정·자동완성 미집계 | D16 |
| 카드 아래 비용 → 헤더 합산 | 둘 다 이미 두 흐름에 뜸 | D14(카드 삭제·게이트) |
| 역방향 스크롤 시작에 상단바 | `reveal-on-up`(M8 Q1 확정) · `reveal-at-top` 구현·테스트 존재 | D19·D20·P9 |
| 분리 과정에서 기호 위치 결함 | 분리 정확 · 정돈이 텍스트화 · 렌더 정규식이 행을 합침 | D21 |
| 블록 1개 삭제 방지 설정의 결함? | 가드 정상 · 저장 중간 실패·로드 무가드·빈 탭 `+` 없음 | D23 |
| ㄱ. 내어쓰기 첫 항목만 | 렌더 대칭 · 첫 행 행머리의 비가시/동형 글자 → span 미부여 + ref-marker 오분류 | M1 + D24 |
| `⋯⋯ ㉠` OCR 미인식 | 옵션 5개 · `㉠` 파이프라인 지원 0 · 정본 `\tag{n}` | D25-1·D25-2 |
| 일괄 검증만 인용 지적 제외 | 구분 플래그·구조 범주·필터 단계 전부 0 | D25-3·D25-4 |


## 부록 B. CLI 정정·보완표 (v3 — 로컬 HEAD `46e5603` 전수 대조)

인용 정정(행 번호·이름)은 **구분 = 인용**, 사실·설계 오류는 **구분 = 사실/설계**. 여기 없는 v2 인용은 일치를 확인했다.

| # | 구분 | v2 서술 | 실측 | 반영 |
|---|---|---|---|---|
| A-1 | 인용 | §1-A4 `handleCheck :219-239`·`lastCheckRef :169`(FolderView로 읽힘) | 둘 다 **ListView.tsx** — `handleCheck :220-238`(주석 :219) · `lastCheckRef :169` | D1′ |
| A-2 | 사실 | §4-1 `handleCheck`를 FolderView 헬퍼로 · `toggleSelect` 호출 | `handleCheck`는 ListView 내부이고 범위는 ListView `sorted` 기준(FolderView `folderProblems`와 순서 다름) · `toggleSelect` 없음 · `handleCheck`는 대상이 선택돼 있으면 범위를 **해제** | D1′ |
| A-3 | 인용·사실 | §4-1 `isEditableTarget`(EditorView :2646-2652 계열) | 함수 없음. :2646-2652는 블록 자동 스크롤 effect. 입력 가드는 인라인 사본 `:2768-2772`·`:2785-2788` | D6′ |
| A-4 | 인용 | §1-A7 카드 커서 `grab`(:807) | `canDrag ? 'grab' : 'pointer'` — 비소유 카드는 이미 pointer | §4-1 |
| A-5 | 인용 | D4 `--drop-ring`(dnd.tsx:88) | 토큰 정의 `globals.css:152`(box-shadow 전체 값) · dnd.tsx:88은 JS 래퍼 `DROP_RING` | §4-1 |
| A-6 | 설계 | 선택 바 `:601-618` 조건 | `viewMode==='list' && size>0` — `selectable` 없음 → 공유 뷰에서 남의 문항에 바 · 바가 우측 컨트롤을 대체 | D2′·Q1 |
| A-7 | 설계 | (없음) | dblclick 전파: 체크박스 셀 `:476-477`·행 ⋮ `:503`·perm `:329`·카드 ⋮ `FolderView:859`이 click·pointerdown만 막는다 → 빠른 두 번 누름 = 진입 | D1′ |
| A-8 | 설계 | (없음) | 행·카드 `user-select` 없음 → 더블클릭 단어 선택 · Shift 클릭 텍스트 범위 | D1′ |
| A-9 | 확인 | (없음) | 드래그 뒤 click은 dnd-kit 6.3.1이 캡처 단계에서 50ms 삼킴(`core.esm.js:1478·1506`) — 선택 토글 오발 없음 | — |
| D-1 | 인용 | §1-D1 `chromeAutoHide.ts` 97행 · §1-D4 M8 `:206` | 96행 · M8 "모두 정상"은 `:236` | — |
| D-2 | 사실 | D19 "테스트 `:58-63`은 모드 명시 케이스로 유지" | 지금 `:58-63`은 모드를 **명시하지 않고** 기본값에 기댄다 | D19′ |
| D-3 | 설계 | D19 PhoneShell prop 기본 `'reveal-at-top'` | 리듀서 기본(`:54` `'reveal-on-up'`)과 갈려 기본값 이중화 | D19′ |
| D-4 | 사실 | §1-D5 "~300ms 늦게 뜨는 정도" | 이벤트가 더 없으면 **다시 스크롤할 때까지** 안 뜬다 | §1-D5 정정 |
| B-1 | 인용 | §1-C8 `createSession`(`discussion-sessions.ts:75-77`) | `createNormalSession :67-82`(`aiEnabled :77`) · 호출 `batchVerify.ts:116-118` | — |
| B-2 | 사실 | D12 `useDrawerResize`에 `initialWidth` 추가 | `initialWidth`·`onCommit` 둘 다 없음 · `defaultWidth`가 곧 초기값 → `onCommit`만 추가 + 마운트 clamp | D12′ |
| B-3 | 설계 | §4-2 `patchPanel(Partial)` | 갱신자 8곳이 함수형 · 병렬 모델 → 유실 | D8′ |
| B-4 | 설계 | D11 "cache가 있으면 loading false로 시작" | `:245`가 `setLoading(true)`를 무조건 호출 → 스크롤 복원이 덮인다 · `profiles`·`aiModels` 재로드 누락 | D11′ |
| B-5 | 설계 | §4-2 스토어 키 `problemId` · "import 0" | 계정 전환 시 캐시 노출 · `PendingAI`·`DiscussRequestContext`는 CommentPanel 비공개 interface | D8′ |
| B-6 | 설계 | D8 `draft` 단일 · 알림형 | 키 입력마다 패널 리렌더 · 댓글(공개)/agent 초안 혼용 | D8′·Q7 |
| C-1 | 인용 | §1-C1 `AIUsage :149-153` · "산식 사본 2" | `:148-152` · 사본 3(`scripts/verifyProbe.mjs:590` 5/25 리터럴) | D15′ ⑤ |
| C-2 | 인용 | §1-C3 "1차 Gemini 2~3패스" · 상수 `:161-165` | 문제 1패스 · 풀이 3패스(61h) · 상수 `:163-165`(161-162는 주석) | — |
| C-3 | 인용 | §1-C5 Claude 누적 `:423-424·443-444` | 423-424는 선언, 누적은 443-444 | §1-C5 |
| C-4 | 사실 | §1-C5 "정확" | Gemini `thoughtsTokenCount` 미합산(0건) · xAI 과소 가능 | §1-C10·D15′·Q8 |
| C-5 | 설계 | D15 ③ "number 아니면 표로" | 숫자 문자열은 `Number()`로 살리는 편이 정확 · warn은 modelId별 1회 · Claude 표 누락 시 5/25 → 0 후퇴(명시) | D15′ |
| C-6 | 설계 | D14 카드별 비용 삭제 | 66a 모델 비교 실험의 모델별 비용이 사라진다 | Q9 |
| C-7 | 설계 | D16 `ProofreadResultBox` 머리 | 블록마다 하나(`EditorView.tsx:1488-1512`) → N번 중복 · ai-complete 성공 표시 자리 없음(`:1463` 오류 전용) · 모델명 미상 | D16′ |
| C-8 | 누락 | §3 C 파일 목록 | `BatchVerifyDialog.tsx:276·305` "(2차 Claude 기준)" 라벨 · `lib/ai-provider.ts`(usage·`getAIProvider`) | D15′ ④·D16′ |
| E-1 | 확인 | §1-E 전체 | ⌘⇧L(`handleSplitMathLines :1713-1780`)은 proofread·tidy를 부르지 않는다 · aligned 3행 → tidy → `"$f(x) = 2x+1$\n⇔ $f'(x) = 2$\n⇒ $x = 3$"` · 현행 빈 줄 0 / D21 빈 줄 2 · `따라서\n⇒ $x=1$` 무변경 · 소비처 2곳 모두 상수 import · Step 3 출력은 항상 `${arrow} $…$`(공백 1) | D21 유지 |
| E-2 | 인용 | §1-E1 `mathSplit.test.mjs` 14건 · §1-E3 `:1701-1706` · §4-5 `preprocess.ts:190-201` | 16건 · `:1701`은 낡은 주석(`:1703`과 모순) → `:1703-1706`만 인용 · 함수 `:186-202`, 호출 `:191` | S0 주석 정정 |
| F-1 | 인용 | §1-F10 catch `:356-359` · §1-F12 `moveProblemsToFolder :214-232` | `:354-358` · 본체 `:217-231` | — |
| F-2 | 사실 | §1-F7 탭 내부 실패만 | 교차 탭 중복 잠복 · 메타·블록 비원자 | §1-F13·D23-1′ |
| F-3 | 설계 | D23-2 저장 직전 시드 · 시드 id `new-…` | 로컬↔서버 어긋남(M7 D11) · 루프 안 `Date.now()` id 충돌 | D23-2′ |
| F-4 | 설계 | D23-4 `createProblemWithBlocks` 한 배치 | 에뮬레이터 `permission-denied`(`parentOwner()` `get()` = 배치 이전 상태) | D23-4′·Q11 |
| F-5 | 확인 | D23-1 탭 배치 | 250 delete + 250 set 기존 문항 통과 · 없는 doc delete 통과 · `tabLoadErrors: Record<string,string>` | — |
| G-1 | 인용 | D24-1 `lib/ocr.ts:74` | `normalizeAndFix :79`(주석 70-78), autoFix 호출 `:93` | §4-7 |
| G-2 | 인용·사실 | D24-2 회귀 표면 `chatExtract.test.mjs:95·347-348` | 그 테스트는 import가 아니라 **자체 사본 정규식**(`:38-40`) — 안 깨지고 조용히 이격 · `proofread.ts:77`도 사본 | D24-2′ |
| G-3 | 사실 | D24-2 근거 "인쇄 `:first-child`가 앞 텍스트 노드를 못 견딘다" | `:first-child`는 요소만 센다 — 근거 무효(처방 이유는 NBSP 폭·ref-marker 오분류) | D24-2′ |
| G-4 | 설계 | D24-1·§4-7 초성 전 위치 매핑 | NFD 한글 파괴(실행: `해설_fig1.jpg` 비가역 손상) | D24-1′ |
| G-5 | 설계 | D24-1 적용 지점(저장·정돈·OCR·시트 `rowToDraft`) · `normalize.ts`에 둠 | 같은 세션에서 미해결(로드 필요) · 시트는 import 0·stemHash 충돌 · normalize.ts는 nanoid·preprocess import | D24-1′·Q12 |
| G-6 | 설계 | D24-3 `ᄀ-ᅟ` 전체 가시화 · M3 미확정 | NFD 초성 전부 점 표시 · M3 정적 확정(`@codemirror/view` 6.39.14 · `combineConfig` · 싱글턴) | D24-3′ |
| G-7 | 설계 | D24-2 IND 전체 소비 | 목록 연속 들여쓰기(`- 항목\n  ㄱ. 내용`)가 최상위 문단으로 · 30개 locale 테스트는 통과(표본이 없었다) | D24-2′ |
| G-8 | 설계 | ZW_RE에 ZWJ 포함 | 이모지 합자 파괴 | D24-1′ ② |
| H-1 | 인용 | §1-H1 `normalizeAndFix :74-95` | 함수 `:79-95`, autoFix `:93` | — |
| H-2 | 인용 | `normalizeTag :225-256` · problem 분기 `:239-246` · 힌트 "`:246` 아래" | `:225-254` · problem 분기 `:234-240` · `:246`은 `근거없는가정` 힌트 → 삽입은 `:240` 뒤 | D25-3′ |
| H-3 | 인용 | `route.ts:501-524`·`:506`·notes `:558-568`·`:570` | `:503-523` · invalid 탈락 `:507` · notes `:562-567` · `:570`은 `return NextResponse.json({`(`:569`) 안 → 필터는 `:567`–`:569` 사이, 실제로는 `runJudge` 안 | D25-4′ |
| H-4 | 인용 | 단건 호출처 `EditorView :3151`·`ProblemView :353`·`BatchVerifyDialog :177` | `:3145`·`:345` · BatchVerifyDialog `:173`은 `runBatchVerify` 호출(단건 호출처는 둘뿐) | — |
| H-5 | 인용·사실 | §1-H6 prompts "인용" `:105`는 quote 규칙 · 관례 설명 0건 | `:110`이고 M7 D20 관례 문장(C1 기호) — 전례 있음, `verify.test.mjs:288`이 고정 | §1-H6·D25-3′ |
| H-6 | 사실 | §4-8 필터 스니펫 | `runJudge(a)` 안에 `body` 없음 · `findings`는 `const`(`:503`) — 컴파일 불가 | D25-4′ ② |
| H-7 | 사실 | §5-1 "`test:verify` 필터 3건" | `test:verify`는 route.ts를 컴파일하지 않는다 → 순수 함수로 빼야 테스트 가능 | D25-4′ ① |
| H-8 | 설계 | D25-4 휴리스틱(AND) | 실제 결함 5건 중 3건 오살(`\(\d\)`가 함수값과 겹침 · '알 수 없'·'정의되'가 흔한 말) | D25-4′ ③ |
| H-9 | 설계 | §4-8 `LEADER_RE` | 수식 안 `\cdots`·`\text{㉠}`·`\ldots`·`···` 전부 불일치 · 첫 행만(`g` 없음) · ` \tag` 치환 시 공백 2칸 · 인라인 `\tag`는 KaTeX 오류 | D25-2′ |
| H-10 | 설계 | D25-2 위치 "Step 3 뒤" | choices·roman의 `㉠` → `① (1)` · `⋯ (ㄱ)` 정의부가 `convertJamoRefs`로 재인용화 | D25-2′·Q13 |
| H-11 | 사실 | CLAUDE.md M7 D19 · `blockTidy.ts:6` "autoFix 소비처 = blockTidy 하나" | `lib/ocr.ts:93`도 소비(편집창 `EditorView:2505` · 댓글 `CommentEditor:118`) — 계획서 §1-H1이 맞고 CLAUDE.md가 틀렸다 | §1-H7·S8 |
| H-12 | 사실 | D25-1 `alphabets_allowed:{ko:true}` 대조군 | 기본값이 전부 true라 무동작일 공산(확신 중간) · `idiomatic_eqn_arrays`는 ㉠와 무관할 공산 · `.probe/`는 gitignore에 없음 · 이미지 경로 미기재 | §4-8·M4 |

## 9. 구현 기록 (2026-09-19 · CLI · 로컬 HEAD `46e5603` 위 9커밋 · push 대기 · **덕수 실물 검수 대기**)

### 9-1. 커밋

| S | 커밋 | 내용 |
|---|---|---|
| S0 | `287d656` | E — `MATH_ONLY_LINE_RE` 화살표 접두(`⇒ ⇔ → ↔ ∴ ∵`, 공백 필수) · 테스트 2 · `EditorView.tsx:1701` 낡은 주석 |
| S1 | `dfda589` | G — `lib/invisibles.ts`(신설) · 적용 6곳(로드 `toLocal` · `toPersistedBlock` · blockTidy 선두 · OCR · `clipboardInputFilter` · 시트 Modal) · 마커 3정규식(비ASCII 공백 소비 · ASCII 들여쓰기 캡처 보존) + 사본 2곳 · 가시화 · `blockTidy.ts:6` 소비처 문장 정정 |
| S1b | `acaf8aa` | I — `unwrapSectionCommands` · `collectHeadWordRanges` · `nestFracToDfrac`(proofread) · R0 · `GUIDE_RE` · R2-Q(blockTidy) |
| S2 | `fb6b79c` | F — `commitProblemSave`(writeBatch 1회, 초과·페이로드 거부 시 탭 단위 + 탭별 id 즉시 갱신) · 로드·`applyVersionContent` 시드 · 자리표시자 · 새 문항 보상 삭제 · `duplicateProblem` 가드 · `test:rules` 66·67 |
| S3 | `46f69ce` | D — 리듀서 기본 `reveal-at-top` · 맨 위 판정을 무시 창 앞으로 · PhoneReader 명시 |
| S4 | `22065b3` | C — usage 정규화(Gemini 사고 토큰 · total 차분) · `lib/aiPricing.ts` · verify env→표→warn+0 · ai_models 숫자 문자열 살림·표 대체 · 교정·자동완성 usage · 헤더 배지(모델별 tooltip) · 카드 비용 삭제 · 일괄 라벨 |
| S5 | `cbed530` | A — 클릭 선택·더블클릭 진입 · Shift 범위 추가·⌘ 토글(리스트) · 카드 단일 선택 · 터치 재탭 진입 · 선택 바 조건 · Enter · 빈 곳 해제 · dblclick 차단 4곳 · `.is-selected` |
| S6 | `3d83e09` | B — `lib/panelStore.ts`(신설) · CommentPanel·CommentEditor·useDrawerResize(`onCommit`·마운트 clamp)·두 뷰·AppShell(Q5) · `peekEnabledModels` |
| S7 | `1409d91` | H — 규칙 ⑤ · 태그 `인용번호미해결`(프롬프트·판정자·normalizeTag) · P16 관례 문장 · `dropCitationFindings` + batch 플래그 · **Mathpix `include_equation_tags` 채택** + `normalizeMathpixEquationTags` · `scripts/ocrProbe.mjs`(`probe:ocr`) · `.probe/` gitignore |
| S8 | (이 커밋) | 문서 — 이 실행판 · CLAUDE.md · roadmap · M8 실행판 Q1 각주 |

검증: 로직 테스트 21종 **539건** 전부 통과(신설 `test:invisibles` 8 · `test:ocr` 3, 증가분 locale +4 · verify +5 · tidy +4 · proofread +11 · chrome +2) · `test:rules` **69건**(+2, 에뮬레이터) · `tsc --noEmit` 통과 · **`npm run build` 통과**(dev 꺼진 상태).

### 9-2. 계획과 달라진 것(R)

| # | 내용 | 근거 |
|---|---|---|
| **R1** | 부록 C-2의 "로컬 미커밋 편집" 가설은 **틀렸다** — `git status` 깨끗, 로컬 = HEAD. v4가 `blockTidy.ts` 행 번호를 4줄 잘못 적은 인용 오류였다(부록 C-2 행 정정) | 실측 |
| **R2** | `lib/panelStore.ts`는 import 0이 아니라 **React(`useSyncExternalStore`) 하나를 import**한다 — 순수 로직 테스트 하니스를 두지 않으므로 훅을 파일 안에 두는 편이 단순하다. 메인 입력창은 `key={mode}`로 모드별 초안을 되살린다(Q7 — 모드 전환 시 입력창이 재마운트되는 것이 새 동작) | §4-2 |
| **R3** | **M4 결과로 OCR 요청을 바꿨다**(계획의 M4 갈래 "text에 ㉠가 살면 옵션 채택"). 프로브 실측(아래 9-3): 기본 요청은 `⋯⋯ ㉠`를 `equation_number` 줄로 **인식하고도 text에서 제외**한다(line_data `......... (ㄱ)` · included:false). `include_equation_tags: true`가 그것을 수식 안 `\tag{ㄱ}`로 실어 준다. 부작용(display가 `equation*`·`align*`로 감싸짐)은 `normalizeMathpixEquationTags`가 되돌린다(`\tag{ㄱ}`→`\tag{1}` · `equation*` 벗기기 · `align*`→`aligned`). KaTeX는 네 형태 모두 그린다(실측). ⚠ **모든 OCR 결과의 다행 수식이 `array` 대신 `aligned`로 온다**(idiomatic_eqn_arrays 동반) — 정돈 분할·⌘⇧L은 aligned를 이미 안다 | 9-3 · Q18은 line_data 병합이 범위 밖이라는 판정이었고, 이 옵션은 병합 없이 text로 받는 길이다 |
| **R4** | 규칙 ⑤의 번호 충돌(Q16)은 정돈이 **탭 전체의 기존 `\tag` 번호**를 `reservedTagNumbers`로 넘겨 판정하고, 건너뛴 블록 수를 정돈 status에 붙인다(`… · 식 번호 충돌로 ㉠→\tag 변환 건너뜀 n블록`) | §4-8 ⑤ |
| **R5** | 규칙 ⑤ 수식 안 리더는 **토큰 2개 이상**일 때만 걷는다(`$a_1+\cdots$ ㉠`의 정당한 `\cdots` 보호). 행에 리더와 라벨뿐이면(붙일 식이 없다) 무변환 | 테스트 |
| **R6** | `commitProblemSave`는 삭제된 탭의 블록 id를 **서버에서 읽는다**(로컬 origIds에 없을 수 있다) — 옛 `deleteAllTabBlocks`와 같은 결과 | §4-6 |
| **R7** | M5(문항 update + 블록 배치의 규칙 평가)는 에뮬레이터로 **통과 확정**(test:rules 66) — 폴백 갈래 불필요 | 8-0 M5 |

### 9-3. M4 프로브 결과표 (`node scripts/ocrProbe.mjs`, 2026-09-19, 입력 = `docs/phaseSketch/m9-ocr/` 화면 캡처 3장)

| 조합 | ocr-line-1(`⋯⋯ ㉠`) | ocr-line-2(`⇒ … ⋯⋯ ㉡`) | ocr-page |
|---|---|---|---|
| current(현행) | `$$=\frac{2}{(t+1)^{2}}$$` — 리더·㉠ **없음** | `$$\Rightarrow f^{\prime}(t)=…$$` — 없음 | 없음 |
| keepSpaces | 없음 | 없음 | 없음 |
| lineData | text엔 없음 · **line_data에 `equation_number` 줄 `......... (ㄱ)`(conf 0.34, included:false)** | `......... (ㄴ)`(0.80, 제외) | `$\cdots \cdots \cdots \cdots$ (ㄱ)`(0.70) · `……… (ㄴ)`(0.63), 둘 다 제외 |
| eqnArrays | 없음 | 없음 | 없음(`array`→`aligned`만) |
| **eqnTags**(`include_equation_tags`) | **`\begin{equation*} … \tag{ㄱ} \end{equation*}`** | **`… \tag{ㄴ}`** | **`\tag{ㄱ}` · align* 행 `\tag{ㄴ}`** |
| koEnOnly(대조) | ✖ Content not found | ✖ Content not found | ✖ HTTP 504 |

⚠ 입력이 화면 캡처다 — Mathpix 원본 크롭(`~/Documents/crops/`)으로 다시 돌려 보면 인식률이 다를 수 있다(선택 과제). ⚠ Mathpix는 원문자 ㉠을 `(ㄱ)`로 읽는다 — 원문자 그대로(`㉠`)는 한 번도 나오지 않았다.

### 9-4. 덕수 실물 검수 항목

§5-2 표(A-1~A-7 · B-1~B-6 · C-1~C-4 · D-1~D-2 · E-1 · F-1~F-5 · G-1~G-3 · H-1~H-4 · I-1~I-5)가 그대로 검수 목록이다. 특히 볼 것:
- **G-1** 원인 문항(M1) 편집 진입 **즉시** ㄱ 행 내어쓰기 정상 — 로드 정규화(Q12)
- **B-1·B-2** agent 전송 직후 편집 진입 → 답 도착 · 두 모델 동시
- **A-1·A-4** 클릭·재클릭·더블클릭 · 단건 클릭에 선택 바가 뜨지 않음(Q1)
- **H-2(실OCR)** 해설 이미지 OCR 삽입 → `… \tag{1}` · 다행 수식이 aligned로 오는 것 확인(R3)
- **C-1** 헤더 배지(모델별 tooltip) · **C-2** 1차 Gemini 비용이 0이 아님(운영 env에 override 없음 — 표가 운영 단가)
- **D-1** 폰 가로 — 위로 밀어도 안 뜨고 맨 위에서만 복귀(실기기)


## 부록 C. v4 → v5 web 재검증 정정·보완표 (원격 HEAD `46e5603`)

| # | 구분 | v4 서술 | 실측 | 반영 |
|---|---|---|---|---|
| C-1 | **정정(닫힘)** | §4-3 [Q8] "verify 라우트의 Gemini 호출이 같은 provider를 거치는지 착수 시 확인하고, 직접 호출이면 같은 식을 적용" | `verify/route.ts:30 import { getVerifyProviders } from lib/ai-provider` — 1차 Gemini도 **`GeminiProvider`(`:111 usageMetadata` · `:144-145`) 하나**를 거친다. `usageMetadata`·`promptTokenCount`를 읽는 곳은 레포 전체에서 `ai-provider.ts`뿐(grep) → D15′ ① 한 곳 수정으로 토론·검증 1차·ai-complete가 전부 덮인다 | §4-3 [Q8] 문장 삭제 · S4 |
| C-2 | **정정(인용)** | §1-I1·I3 `blockTidy.ts` `NUMBER_HEAD_RE :52` · `STEP_RE :48` · `opts.tab !== 'question' :136` | 실제는 **`:47` · `:44` · `:132`**. ⚠ **CLI 확인(2026-09-19): 미커밋 편집은 없다** — `git status` 깨끗, 로컬 파일 = `HEAD:lib/blockTidy.ts`(같은 행 번호). v4 작성 때 행 번호를 4줄 잘못 적은 **인용 오류**였다 | §1-I |
| C-3 | **보완(닫힘)** | §8-0 M3′ "`EditorView.clipboardInputFilter`가 있는지 확인" | `@codemirror/view` 6.39.14 `index.d.ts:1224 static clipboardInputFilter: Facet<(text, state) => string>` **실재** → [Q12]의 붙여넣기 정규화는 이 facet 하나(`transactionFilter` 분기 불필요). 붙여넣은 텍스트에만 돌고 IME·타자에는 안 닿는다 | M3′ 닫힘 · §4-7 |
| C-4 | 보완 | D24-1′ ③ 행머리 공백류 목록(NBSP·U+3000·U+2000–200A·U+202F) | **한글 채움 문자**를 더한다: U+3164(ㅤ, HANGUL FILLER — HWP·웹에서 공백처럼 쓰이는 대표 유입원) · U+115F·U+1160(초성·중성 채움, NFD 자모 연쇄 끝에 남는 것). 셋 다 `\s`가 아니라 정규식·`trim()` 어느 쪽도 못 잡고, 편집기에서도 안 보인다 → ZW_RE(전 위치 제거)에 넣되 D24-1′ ①의 NFD 조합 **뒤**에 돌린다(채움 자모가 조합 재료가 되는 경우를 먼저 소진). D24-3′ 가시화 집합에도 U+3164 추가 | §4-7 |
| C-5 | 보완 | D15′ ① OpenAI-compat `out = completion + max(0, total − prompt − completion)` | 식은 맞다. 근거 보강: DeepSeek는 `completion_tokens`에 reasoning을 **포함**한다고 문서화(`reasoning_tokens`는 details 필드) → 더해지는 값 0 · xAI는 `completion_tokens_details.reasoning_tokens`가 completion 밖 → total 차분이 곧 reasoning(M6가 확정). `total_tokens`가 없는 응답이면 `?? 0`이라 차분이 음수 → `max(0, …)`가 막는다. 캐시 히트(`prompt_tokens_details.cached_tokens`)는 여전히 만액 계산 — §5-3에 한 줄 | §4-3 |
| C-6 | 보완 | §4-3 표의 Google 공시 두 값(`gemini-2.5-flash` 0.30/2.50 · `gemini-2.5-pro` 1.25/10) · Haiku 4.5 1/5 | web 작성자 지식과 **일치**(2.5 Pro는 ≤200K 구간). Opus 4.8/5 = 5/25는 코드 주석(`verify/route.ts:162`, 2026-06 공시)과 Firestore 운영값이 같아 교차 확인됨. 착수 시 공시 페이지 1회 대조는 그대로 | — |
| C-7 | 보완 | D23-1′ / Q10 문항 전체 1배치 | Firestore 배치 상한은 500 **ops** 외에 **커밋 페이로드 10 MiB**가 있다 — 블록은 텍스트라 249블록 × 수 KB로 여유가 크지만, `replaceProblemBlocks`의 상한 검사에 `ops > 500 → 탭 단위` 갈래만 있고 바이트 갈래는 없다 → 커밋이 `invalid-argument`(payload too large)로 실패하면 **아무것도 안 바뀐다**(원자성 유지)는 점을 §5-3에 명시하고, 그때의 처방(탭 단위 fallback 재시도 1회)을 S2에 1줄 | §4-6 · §5-3 |
| C-8 | 보완 | §4-1 [Q3] `lastPointerType.current = e.pointerType`를 `onPointerDown`에서 기록 | **맞는 설계** — `click`의 `pointerType`은 Chrome·Firefox에선 PointerEvent라 있지만 iPad Safari의 `click`은 MouseEvent라 없다(브라우저별 갈림). v4가 이미 pointerdown 기록 방식이므로 변경 0. 단 `onMouseDown`의 `preventDefault`(detail>1·Shift)는 **포커스 이동도 막는다** — 단일 클릭(detail 1·Shift 없음)에는 안 걸리므로 D6′의 `activeElement.closest('.folder-row')` 가드는 성립한다. 실물 A-4에 "Shift 클릭 뒤 Enter"를 1건 추가 | 5-2 A-4 |
| C-9 | 보완 | D1′ "드래그 뒤 click은 dnd-kit 6.3.1이 캡처 단계에서 삼킨다" | `package-lock.json:240-241`이 **6.3.1로 잠겨** 있어 성립(`^6.3.1` caret이지만 lock이 기준). `npm update`로 6.4+가 되면 이 전제를 재확인 — CLAUDE.md 규약 1줄 | §8-3 |

### 부록 C-표본. 재확인한 인용(전부 일치)
`FolderView.tsx:807 canDrag ? 'grab' : 'pointer'` · `:601 viewMode==='list' && selectedIds.size > 0`(selectable 없음) · `globals.css:152 --drop-ring` · `chromeAutoHide.ts` 96행 · M8 실행판 `:236` · `discussion-sessions.ts:67-82 createNormalSession`(`:77 aiEnabled:true`) · `types/problem.ts:148 AIUsage` · `verifyProbe.mjs:590` 5/25 리터럴 · `verify/route.ts:163-165` 상수 · `ai-provider.ts:144-145·194-195·312-313·443-444` · `firestore.rules:462-464` · `parse.ts:225·234·246` · `route.ts:409 runJudge`·`:503 const findings`·`:565·567 notes.push`·`:570` · `ocr.ts:79·93` · `CommentEditor.tsx:118 normalizeAndFix` · `chatExtract.test.mjs:38-40` 사본 정규식 · CLAUDE.md `:294` 45a 규약 · `katexMacros.ts:20-21` · `runVerifyFlow` 호출 3곳(`ProblemView:345`·`EditorView:3145`·`batchVerify:221`) · `providerParams.ts:88 thinkingConfig`.
