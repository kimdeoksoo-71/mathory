# 개선묶음 M6 — 디자인·기능 조정 구현 계획서 v2 (CLI 실측 교차검토 · 정정 6 · 보완 9 · 실측 3)

> 계보: 덕수 메모 "M6 기능 및 디자인 조정"(2026-09-09) + 추가 요청 4건 → **v1 web**(레포 실측 · 덕수 확정 N1~N10 반영) → **v2 CLI**(이 문서 — v1의 인용을 실제 코드와 대조하고, 실측으로만 닫히는 항목 3건을 닫았다). 다음: 덕수 판정 1건(D7) → 착수(§8).
> 기준: `kimdeoksoo-71/mathory` **HEAD `f90cdb9`** + Phase 65 후속 3커밋(`29f60c4`·`8da7775`·`de8b83f` — EditorView·MarkdownEditor만 건드려 M6 대상 파일과 겹치지 않는다) · `@phosphor-icons/core` **2.1.1** · Pretendard **1.3.9**(앱은 `app/layout.tsx:15`에서 jsDelivr CDN variable dynamic subset을 받는다 — 로컬 설치 없음).
> **v1을 인용하기 전에 부록 C(정정·보완 표)를 먼저 볼 것.** v1의 §1 실측 인용은 행 번호 ±5 안에서 전부 맞았고, 틀린 것은 **수치 1건·규약 위반 1건·누락 4건**이다.

---

## 0. 요약

| 항목 | 내용 |
|---|---|
| 취지 | M4·M5로 아이콘 체계를 Phosphor로 통일한 뒤 남은 **잔여 도안**(저장·댓글·정렬 화살표·토글·파비콘)과 **화면 구조 잔손질**(리스트 칼럼 · 문제/풀이 카드 구분 · 스테퍼 위치 · 사이드바 헤더 3종)을 한 묶음으로 닫는다. 신기능 0 |
| 범위 | 메모 11항 + 추가 4항 = 15항, 라벨 변경 1항은 N2로 철회 → **실행 14항**, A~K 11묶음(§1). 수정 파일 **21**(신설 2 · 테스트 2) + 생성 산출 2 + 문서. ICONS **+7 / −4**(56 → **59**) |
| 무접촉 | 서버 0 · Firestore 규칙 0 · 스키마 0 · 프롬프트 0 · raw_text 0 · exportMd 0 · 전처리 0 · 렌더 5사이트 0(카드 크롬만). 로직 테스트 기존분 2개 갱신, 신규 케이스 1(prefs 마이그레이션) |
| v2에서 닫힌 것 | ① **파비콘 path 실물 검증**(Pretendard 600 'M' 잉크 폭 272.5 vs path 272.6 = 0.999) ② 토글 글리프 24px 실측 22.5×13.5(현행 트랙 22×13) ③ 정렬 화살표 12/14 시트 — **14 권장** |
| v2에서 고친 것 | D5 네이티브 `title` 병기 → **금지**(이중 툴팁) · `chatText` 삭제가 컨택트시트 3곳을 깨뜨림 · `tests/folderIcon` :25-28도 갱신 대상 · CLAUDE.md M5 "활성 행만 bold" 절 개정 · 명암비 사다리 수치 정정 · HoverTip API 확정(훅) |
| 남은 판정 | **없음** — D7은 덕수 판정 **12**(2026-09-09), 확인 1·2 OK. D1~D25 전부 닫힘 → 착수 |

---

## 1. 현행 — 실측 (HEAD `f90cdb9`, 행 번호는 v2 시점)

### A. 폴더뷰 리스트 — 칼럼 · 정렬 화살표

| # | 요청 | 현행 |
|---|---|---|
| A1 | 칼럼 순서 | `lib/listColumns.ts:23-30` `LIST_COLUMNS` = title(fixed) → **blockchain → verify_problem → verify_solution → agent → comments** → updated(fixed). `OPTIONAL_COLUMN_IDS`(:33)가 나열 순서 = 기본 표시 순서. **저장된 prefs가 있으면 그 순서가 이긴다** — `sanitizePrefs`(:71) `r.v !== 1 → 기본값`(:75), `orderIn`(:81) 우선 + 새 id 뒤에 붙임(:85), 반환 `v: 1`(:103). 키 `mathory.listPrefs.<scopeKey>`(`hooks/useListPrefs.ts:14`), 훅은 `sanitizePrefs`만 부른다(:17·:19·:21) |
| A2 | 제목명 | `'검증(문제)'`·`'검증(풀이)'`(:26-27). `DEFAULT_TABS`(`types/problem.ts:269-272`) = 문제·풀이. **N2로 라벨 불변** |
| A3 | 헤더 아이콘화 | `ListHeader`(`ListView.tsx:487`) sortable 버튼 :540-550 — `{columnLabel(id)}` + `fontSize 9`의 ▲/▼(:548-549), 비sortable은 span(:552). 버튼 스타일 `11.5/600 · gap 3 · padding 0 · display flex`(:543-547, 고정 높이 없음). 셀 쪽은 이미 아이콘: comments `IconComment 14`(:257) · blockchain `BlockchainBadge 12`(:244) · agent 숫자만(:252). **유령 라벨 행**(:318-325)이 `{columnLabel(id)} ▲` 텍스트로 자동폭 트랙을 확보 → 헤더를 바꾸면 이 행도 같이. 칼럼 설정 팝오버(:605-690)의 ▲▼(:673·:678)는 **순서 이동** 버튼이라 범위 밖 |
| A4 | 정렬 화살표 | 헤더 :549 `▲/▼` 9px 문자. 카드 보기 `SortControls`(`FolderView.tsx:968-990`) `↑/↓` 문자(:987) |

앱의 말풍선 관행: 툴바 `IconButton`(`UnifiedToolbar.tsx:213-304`) — `TOOLTIP_DELAY_MS 600`(:213) · `position: fixed`(:286) · `fontSize 11`(:293) · `background rgba(33,33,33,.92)`(v1의 "진하기 92%") · `zIndex 9999` 리터럴(:299) · `role="tooltip"`(:284). **버튼에는 `aria-label={title}`만 있고 네이티브 `title` 속성은 없다** — 이중 툴팁을 피한 설계(→ D5 정정). `Z_TOOLTIP = Z_DIALOG − 100 = 10400`(`dialogStyles.ts:27`)이 이미 있다.

### B. 문제 카드 ↔ 풀이 카드

| # | 현행 |
|---|---|
| B1 | `TabBody.tsx:54 CARD_RADIUS = 6`(문제·풀이 공용, :306). peek 카드 `ProblemView.tsx:975 borderRadius: CARD_RADIUS`. 폴더뷰 카드 `FolderView.tsx:801 borderRadius: 12` |
| B2 | 셋 다 `0.5px solid var(--border-content)`(`TabBody:305` · `FolderView:800`). 토큰(`globals.css:107-114`): `--border-content #D2C8B8` · `--block-hairline #C2B7A2` · `--block-border-active #AC9C7E` · `--border-content-active #B89B78` |

명암비 **재계산**(`((c+0.055)/1.055)^2.4`, 카드면 `--bg-content #F4EFE7` / 활성 블록 `#E8DFCE`):

| 토큰 | vs 카드면 | vs 활성 블록 |
|---|---|---|
| `--border-content` #D2C8B8 | 1.45 | 1.25 |
| **`--block-hairline` #C2B7A2** | **1.73** | 1.50 |
| `--border-content-active` #B89B78 | 2.29 | 1.99 |
| `--block-border-active` #AC9C7E | **2.35** | 2.03 |

⚠ v1은 `--block-border-active`를 2.0으로 적어 사다리 순서를 "…active(2.0) → content-active(2.29)"로 뒤집어 놓았다. 카드면 기준 실제 순서는 위 표(2.03은 *활성 블록* 기준값). D10(`--block-hairline`)에는 영향 없다.

### C. 스테퍼 위치

`ProblemView.tsx` — `HEADER_H 57`(:67) · `OUTER_PAD 16`(:79) · `rightReserve`(:260-262: 댓글/agent = `width + 8`, 우측 단 = `width − 8`, 없으면 0) · 루트 `overflow:hidden; position:relative; background --bg-functional`(:678-679) · 제목행 `height HEADER_H · paddingRight rightReserve · background --bg-functional`(:691-694) · 본문 래퍼 `paddingRight rightReserve`(:846) · `viewControls`(:651 — SizeStepper 2개) → 우측 단 드로어 1행(:1044-1063: `minHeight DRAWER_ROW1_H · padding 0 16px · gap 12` · 닫기 26×26 · `flex:1` · `{viewControls}`). **'열기' 버튼은 `!panelMode && !rightColShown`일 때만 렌더**(:999) — `absolute; top 16; right 16 + rightReserve; zIndex 11; 26×26`(:1005-1007). 우측 단 카드 z 40(:1028) · 댓글/agent 50.

### D. 브라우저 탭 아이콘

`app/icon.svg` = 64×64 · `rx 15` · `#D97757` · `M17 45 V21 L32 39 L47 21 V45` stroke 7 round(v1 그대로). 인수문서 `docs/images/mathory-logo-handoff.html:22` `.icon span { font-size 72px; font-weight 600 }` = "앱 아이콘용 심볼" **SemiBold**. Pretendard는 **로컬에 없고** 앱이 CDN에서 받는다 — 그래서 헤드리스에서도 진짜 글리프를 그릴 수 있었다(§5-0).

### E. 요약 스위치

`EditorView.tsx:813 summaryToggle`(제외 = `isHeading || isCaseBlock` — **탭 무관**) · `:802 showSummaryOnlyBar` · `SortableEditorBlock`(:674)은 탭 id를 모른다. 열람뷰는 이미 풀이 계열만: `TabBody:88 scoped = isToneScoped(tab.id)`(`lib/keyTone.ts:20` = `tabId !== 'question'`) → `:279`·`:321`. 공개 뷰어도 `ProblemTabContent:36 scoped`. `ToggleSwitch`(`components/ui/ToggleSwitch.tsx`) = 트랙 22×13 r7(:49) · ON `--border-content-active` / OFF `--text-placeholder`(:53) · 손잡이 9(:59) · `role=switch`·`aria-checked`(:29-30). 소비처 **4곳**(`grep -rnw`): `OutlineToggle:27`(TabBody·ProblemTabContent가 씀) · `CommentPanel:983·985` · `EditorView:820`.

### F. 저장 아이콘

`Icons.tsx:130 IconSave`(Feather 구름 stroke · `checked`) — 헤더 :11 "유지 예외 3종". 소비처: `EditorView:3372 size 18 checked={!dirty}` · `ListView:99 size 13 --mathory-red-dark` · `VersionTimeline:9`(맵 값) · `:72`(폴백).

### G. 사이드바 헤더 3종

| 헤더 | 위치 | 구조 · 타이포 |
|---|---|---|
| My | `Sidebar.tsx:820-865` | [라벨 button flex:1 12.5/600/`--text-muted`/자간 .3/`padding 4px 0`] [+ 버튼 `visibility: myHeaderHovered`] [쉐브론 `IconChevron` rotate 90] |
| 공유 | `ShareTree.tsx:45-70` | [라벨 button — **동일 타이포**] [쉐브론 `IconChevron 14`] · 래퍼 `Sidebar:1003 padding '0 12px'`(My 섹션은 `'8px 12px'`) |
| 최근 문항 | `Sidebar.tsx:1017-1035` | 11.5/600/**uppercase**(:1023-1024) · 쉐브론 :1029 |

파생: `ShareTree:102` 받은 문항 아이콘 `scaleX(-1)`(좌우 반전) · `:124` 보낸 문항 원본. 활성 행 글자 700(`ShareTree:214·237`).

### H. 댓글 아이콘

`IconComment = phIcon(PH.chatText, 14)`(`Icons.tsx:101`) · Tip `IconCoachImportant = chat-centered-text`(:104, `CoachLabel:37 size 17`). `IconComment` 소비처 **7곳**(14×3 · 16×3 · 17×1). ⚠ `chatText` 키는 컴포넌트 밖에서 **컨택트시트가 3곳** 쓴다(`gen-phosphor-paths.mjs:172·216·288`) — v1 "다른 소비처 없음"은 컴포넌트에만 맞는 말.

### I. 활성 폴더 아이콘 bold

`lib/folderIcon.ts:14-15·26·29·32` · `FolderGlyph.tsx:21-24 INLINE_KEYS`(regular/bold 쌍) · `Sidebar:333 active={active}`(FolderGlyph 소비처 10곳 중 **유일**하게 `active`를 넘긴다) · `Sidebar:730-731` bold 자산 prefetch · `Icons.tsx:50-53 PhAsset weight` · `:67-68 phAssetUrl(name, weight)` · gen ICONS `:56·59·61` · `ASSET_WEIGHTS`(:305) · 시트 M5-1 절(:279-281) · `tests/folderIcon.test.mjs` **:25-28(`bold: false` 기대) · :31-35 · :40**.

### K. 원본인증 아이콘 톤

`BlockchainBadge.tsx:39 color: 'currentColor'` · 소비처 `ProblemView:802 size 16`(h1 `--text-primary`) · `FolderView:855 13` · `ListView:244 12`. 옆의 댓글·agent는 `--text-muted`.

### Phosphor 2.1.1 실재 확인
`arrow-up`·`arrow-down`·`cloud-arrow-up`·`cloud-check`·`chat-dots`·`toggle-left`·`toggle-right`(**fill 있음**)·`share-fat`·`clock`·`folder`·`lego-smiley`·`graph` 전부 regular/fill 존재, **단일 `<path>`**(생성기 `pathOf` 전제 통과). 현재 ICONS **56종**.

---

## 2. 결정 (D1~D25 · v2 정정 표시)

| # | 결정 | v2 비고 |
|---|---|---|
| D1 | optional 기본 순서 = **verify_problem → verify_solution → comments → agent → blockchain** | 그대로 |
| D2 (N1) | prefs **v:2** — v:1 입력은 `order`만 버리고 hidden·widths·sort 보존 | 그대로. `useListPrefs`는 무변경(sanitize가 흡수, :17·:19·:21 확인) |
| D3 (N2) | 라벨 변경 **철회** — `'검증(문제)'`·`'검증(풀이)'` 유지 | 테스트 `:121` 단언 그대로 |
| D4 | 헤더 아이콘: comments `IconComment` · agent `IconAgent` · blockchain `IconBlockchain`, **14px** | 그대로. 레지스트리 `header: 'text' \| 'icon'` |
| **D5′** (N3 · **v2 정정**) | 말풍선 = `IconButton` 툴팁을 **`useHoverTip` 훅**으로 추출. **네이티브 `title`은 병기하지 않는다** — `aria-label`만 | v1의 "title도 병기"는 **이중 툴팁**(600ms 커스텀 + ~1s 네이티브)을 만든다. `IconButton`이 `aria-label`만 쓰는 것이 그 이유(:261). 훅 형태를 고른 이유는 §4-5 |
| D6 | 정렬 화살표 = `arrow-up`/`arrow-down` regular → `IconSortAsc`·`IconSortDesc` | 그대로 |
| **D7** (덕수 확정 2026-09-09) | 화살표 **12** — "작아도 충분하다" | 권장은 14였으나 덕수 판정 12. M4 "최소 렌더 14px" 규약의 **† 예외**로 등록(Icons.tsx 헤더·CLAUDE.md † 목록 9번째). 헤더 행 높이 불변(13) |
| D8 | 카드 보기 `↑/↓` → `IconSortAsc/Desc` | 그대로 |
| D9 | 문제 카드 radius **12**(`CARD_RADIUS_QUESTION`) · 풀이 6 · peek 12 | 그대로. 판별 `!isToneScoped(tab.id)` — extra 탭은 풀이 계열(6) |
| D10 (N4) | 문제 카드 테두리 = `--border-card-problem` = `var(--block-hairline)` 0.5px, 3곳 동일 | 명암비 **1.45 → 1.73**(§1 B 재계산). 네 변 동시 변경(Phase 45a 코너 규약 준수) |
| D11 | 풀이 카드·리스트 행·U자 프레임은 `--border-content` 유지 | 그대로 |
| D12 | 스테퍼를 드로어 1행에서 빼 **제목행 우단 절대배치**: `right: 16 + 26 + 8`, `top 0; height 57`, `zIndex 10`, `rightReserve` 미가산 | **검증**: 루트가 `position:relative`(:679)라 절대배치 기준이 '열기' 버튼과 같다. 열기 버튼은 드로어가 열리면 **렌더되지 않으므로**(:999) 겹칠 일이 없다. 드로어 카드 inset 8 위 띠(0~8px)에는 스테퍼 본체가 없다(세로 중앙 28) |
| D13 | 스테퍼 래퍼 `background --bg-functional · paddingLeft 8` | 제목행 배경과 **같은 토큰**(:691) → 이음새 없음 |
| D14 | 드로어 1행은 닫기 버튼만, `DRAWER_ROW1_H` 유지 | y=57 규약 불변 |
| **D15** (N5 · **v2 실측 통과**) | 파비콘 = Pretendard **SemiBold 'M' path**, 배경 rx 15 · #D97757 유지 | **§5-0**: CDN 폰트로 600 'M'을 캡높이 260px에 맞춰 재보니 잉크 폭 272.5 vs path 272.6 — **비율 0.999**. 웹 추출값이 실물과 0.1px 안에서 일치. Bold path는 부록 A에 보존 |
| D16 | 문제 탭에서 '요약에 넣기' 스위치·요약 전용 바 **둘 다** 숨김 — `summaryEligible = isToneScoped(activeTab)` | 그대로. 저장된 `showInSummary`는 읽는 곳이 없어 데이터 무접촉 |
| D17 | `ToggleSwitch` → `toggle-left`(OFF regular) / `toggle-right`(ON **fill**) | 그대로 |
| **D18** (N6 · **v2 실측**) | 토글 **24px** | 24px 글리프 bbox **22.5×13.5**(현행 트랙 22×13과 0.5px 차) — 4곳 레이아웃 무이동 |
| D19 | ON `--border-content-active` · OFF **`--text-muted`** | 그대로(OFF `--text-placeholder`는 선 도안에서 1.35:1) |
| D20 | `IconSave` = `cloud-arrow-up`(미저장) / `cloud-check`(저장됨) · ListView 13→14 | `VersionTimeline`은 값 참조라 시그니처 유지로 무변경 ✓ |
| **D21′** (**v2 보완**) | `IconComment` = `chat-dots` · `chatText` 키 삭제 | ⚠ **컨택트시트 3곳(:172·:216·:288)을 `chatDots`로 함께 바꿀 것** — 안 바꾸면 `PH_.chatText`가 undefined라 `icons:sheet`가 깨진다(빌드는 안 깨지지만 시트 갱신이 조용히 죽는다) |
| D22 (N7) | `SidebarSectionHeader` 공용 — My `IconFolder` · 공유 `IconShare`(share-fat) · 최근 `IconRecent`(clock), 16px `--text-muted` | 그대로. 래퍼 여백 `'8px 12px'` 통일 |
| **D23′** (**v2 보완**) | 활성 폴더 bold 폐기 — `resolveFolderGlyph` `active`·`bold`·`weight` 제거, `PhAsset`/`phAssetUrl` weight 인자 제거, ICONS bold 3종·`ASSET_WEIGHTS`·시트 M5-1 절·prefetch 삭제 | ⚠ 테스트는 v1이 적은 `:31-35·:40`에 더해 **`:25-28`도**(`bold: false` 기대값이 스펙 타입에서 사라진다). `FolderGlyph` 소비처 10곳 중 `active`를 넘기는 곳은 `Sidebar:333` 하나뿐 → 나머지 9곳 무변경. ⚠ **CLAUDE.md "폴더 아이콘은 Phosphor 카탈로그다" 절의 "활성 행만 bold — 글자 700과 같은 조건" 문구 개정 필수** |
| D24 | 받은 문항 아이콘 `scaleX(-1)` → `rotate(180deg)` | §5-0 시트에서 "오른쪽 위→왼쪽 아래" 확인 |
| D25 (N9) | `BlockchainBadge` `color: var(--text-muted)` 고정 | 그대로 |

---

## 3. 수정 파일 (v2 갱신)

| 파일 | 묶음 | 변경 | v2 비고 |
|---|---|---|---|
| `scripts/gen-phosphor-paths.mjs` | A4·E2·F·H·I | ICONS +7/−4 · `ASSET_WEIGHTS=['regular']` · 시트 **M6 절 신설 · M5-1 절 삭제 · `chatText` 참조 3곳 → `chatDots`** | 마지막 항목이 v2 보완 |
| `components/ui/phosphorPaths.ts` · `lib/phosphor-index.json` | 생성 | `icons:gen` | 56 → **59** |
| `components/ui/Icons.tsx` | D4·D6·D20·D21·D23 | `IconSortAsc/Desc` 신설 · `IconSave` PhIcon화 · `IconComment → chatDots` · `PhAsset`·`phAssetUrl` weight 제거 · 헤더 "유지 예외 3종 → 2종" | |
| `lib/listColumns.ts` · `tests/listColumns.test.mjs` | D1·D2·D4 | 순서 · `header` · v:2 · 마이그레이션 · 테스트 v:2 + 신규 1 | 라벨 단언 불변 |
| `components/ui/HoverTip.tsx`(신설) · `components/editor/UnifiedToolbar.tsx` | D5′ | `useHoverTip` 추출 · `IconButton`이 소비(시각 불변) | 훅 |
| `components/problem/ListView.tsx` | D4·D5′·D6·D7·D20 | 헤더 아이콘+훅 · 유령 행 · 화살표 · `IconSave 14` | `aria-label`만 |
| `components/problem/FolderView.tsx` | D8·D10 | `SortControls` · 카드 border 토큰 | |
| `components/problem/TabBody.tsx` · `ProblemView.tsx` · `app/globals.css` | D9·D10·D12~D14 | radius 분기 · 토큰 · 스테퍼 이동 · 주석 3곳 | |
| `app/icon.svg` | D15 | path 교체 | 검증 통과 |
| `components/editor/EditorView.tsx` | D16 | `summaryEligible` | |
| `components/ui/ToggleSwitch.tsx` | D17~D19 | PhIcon 렌더 | |
| `components/layout/SidebarSectionHeader.tsx`(신설) · `Sidebar.tsx` · `ShareTree.tsx` | D22·D23·D24 | 헤더 3종 · prefetch 삭제 · rotate | |
| `lib/folderIcon.ts` · `tests/folderIcon.test.mjs` · `components/ui/FolderGlyph.tsx` | D23′ | bold 제거 · 테스트 :25-28·:31-35·:40 | :25-28이 v2 보완 |
| `components/ui/BlockchainBadge.tsx` | D25 | 색 고정 | |
| `docs/icons-contact-sheet.html` | 생성 | `icons:sheet` | |
| `CLAUDE.md` · `docs/roadmap.md` · `THIRD_PARTY_LICENSES.md` | 문서 | §4-9 | CLAUDE.md M5 절 문구 개정이 v2 보완 |

무접촉 확인(v2 재확인): `VersionTimeline`(값 참조) · `components/share/*`(N10) · `PhosphorIconPicker`(`<PhAsset name size/>`만 — weight 미사용이라 D23′ 무영향) · `FolderPickerDialog`·`FolderPathBar`·`SheetImportModal`·`ListView:351`·`FolderView:559·700`(FolderGlyph를 `active` 없이 씀) · `lib/verify/*` · `app/api/*` · `firestore.rules`.

---

## 4. 구현 사양 (v1 유지분은 요지만, v2 변경분은 전문)

### 4-1. 생성기
v1 §4-1 그대로 + **시트 참조 치환**:
```js
// :172  ['IconComment *', 'chatText']  →  ['IconComment *', 'chatDots']
// :216  ph(PH_.chatText, s) …          →  ph(PH_.chatDots, s) …   (M4 비교 행 — chat-centered-text 옆)
// :288  ph(PH_.chatText, s) …          →  ph(PH_.chatDots, s) …   (M5 Agent 행)
// :279-281 M5-1 bold 사다리 절 삭제 (folderBold·folderSimpleBold·folderOpenBold 참조 제거)
```
`copyAssets`는 버전 디렉터리 단위 청소라 로컬 `…/2.1.1/bold/`가 남는다 — `ASSET_WEIGHTS`에 없는 weight 디렉터리는 지우는 1줄 보강(v1 유지).

### 4-2. `Icons.tsx` — v1 §4-2 그대로.

### 4-3. `listColumns.ts` — v1 §4-3 그대로(`header` 필드 · v:2 · 마이그레이션 · `columnHeaderKind` — mode 칼럼 owner·perm은 `MODE_COLUMN_LABELS`(:36)에만 있으므로 text).

### 4-4. `ListView.tsx`
v1 §4-4 + **정정**:
```tsx
// 헤더 버튼 — title 속성 없음. aria-label만(IconButton :261 선례). 훅이 role=tooltip 말풍선을 공급한다.
const { bind, tip } = useHoverTip(columnLabel(id), { disabled: columnHeaderKind(id) === 'text' });
<button {...bind} aria-label={columnLabel(id)} onClick={…}>{<HeaderLabel id/>}{sortMark}</button>{tip}
```
유령 행: 아이콘 칼럼은 `<HeaderLabel/>` + 17px 스페이서(= 14 아이콘 + gap 3). 칼럼 설정 팝오버 체크박스 라벨은 텍스트 유지 + 아이콘 칼럼만 앞에 14px 아이콘 병기(v1 권고 채택).

### 4-5. `HoverTip.tsx` — **훅으로 확정**
```ts
export function useHoverTip(label: string, opts?: { disabled?: boolean; delay?: number }): {
  bind: { onMouseEnter; onMouseLeave; onFocus; onBlur; onMouseDown; ref };   // 앵커 버튼에 스프레드
  tip: ReactNode | null;                                                       // 앵커 **형제**로 렌더(fixed)
}
```
훅을 고른 이유: `IconButton`은 지금 `<>{button}{tip}</>` 구조라(:257-303) 훅으로 바꾸면 **JSX가 한 줄도 안 바뀌고** 상태·타이머 코드만 빠진다 — "시각 불변" 검증이 diff만으로 끝난다. render-prop은 `IconButton`의 `buttonRef` 병합 로직을 다시 짜야 한다. `zIndex: Z_TOOLTIP`(10400, 현행 9999 리터럴 대체). ⚠ **fixed 말풍선은 transform 조상 아래에서 좌표를 잃는다**(Phase 65 S11에서 CM 툴팁이 그랬다) — 툴바·리스트 헤더 어느 쪽도 transform 조상이 없음을 확인했고, 훅 주석에 이 제약을 적는다.

### 4-6. 카드 — v1 §4-6 그대로(`--border-card-problem: var(--block-hairline)` · `CARD_RADIUS_QUESTION = 12` · peek 12).

### 4-7. 스테퍼 — v1 §4-7 그대로. v2 확인: 열기 버튼이 `!panelMode && !rightColShown` 조건이라(:999) 스테퍼(`right 50`)와 열기 버튼(`right 16`)은 **드로어가 닫혀 있을 때만 나란히**, 열리면 버튼은 사라지고 스테퍼는 드로어 카드(z 40/50) 밑으로 들어간다. 주석 3곳(:646-650 · :1002-1004 D49 · :1040-1043) 갱신.

### 4-8. 요약·토글·저장·사이드바·배지 — v1 §4-8 그대로. `SidebarSectionHeader` 시그니처 `{ icon, label, open, onToggle, trailing?, style? }` — 최근 문항의 uppercase·11.5는 폐기하고 12.5/600으로 통일(N7의 "세 헤더 모두 같은 규격").

### 4-9. 문서·규약
- **CLAUDE.md** ① "리스트 칼럼 prefs는 v:2 — 기본 순서를 바꿀 때는 `sanitizePrefs` 마이그레이션과 같이" ② "'요약에 넣기'는 풀이 계열 탭 전용(`isToneScoped`)" ③ **"폴더 아이콘은 Phosphor 카탈로그다" 절의 "활성 행만 bold" 문구를 "M6에서 bold 폐기 — 활성 강조는 배경·글자 700만"으로 개정**, `ASSET_WEIGHTS` 언급(3,024개 → 1,512개) 정정 ④ "아이콘 체계" 절의 IconSave 유지 예외 삭제(2종으로) ⑤ 말풍선 규약 1줄: "hover 말풍선은 `useHoverTip` 하나 — 네이티브 `title`과 병기하지 말 것(이중 툴팁)".
- `docs/roadmap.md` M6 절 신설, M4 D6·M5 D3에 "M6에서 뒤집힘" 각주.
- `THIRD_PARTY_LICENSES.md` Pretendard 출처 1줄(OFL — 렌더 결과물 사용은 자유, 고지 의무 없음, 출처만 기록).

---

## 5. 검증

### 5-0. v2에서 이미 실측한 것 (헤드리스 Chrome)

| 항목 | 결과 |
|---|---|
| **파비콘 path vs 실물 글리프** | CDN `Pretendard Variable` 600 'M'을 canvas `measureText`로 캡높이 260px(=26/64×640)에 맞춤(font-size 367.7px) → 잉크 **272.5×260** vs 계획서 path **272.6×260**(비율 **0.999**). 웹 추출값 채택 |
| 토글 글리프 24px | `toggle-right` fill · `toggle-left` regular 모두 bbox **22.5×13.5** — 현행 CSS 트랙 22×13과 동치 |
| 정렬 화살표 12/14 | 11.5px/600 헤더 목업에서 헤더 행 높이 ▲9=13 · 12=13 · **14=14**. 시각 균형은 14 |
| `share-fat` rotate(180) vs scaleX(−1) | rotate가 "오른쪽 위 → 왼쪽 아래"(메모 의도), scaleX는 "왼쪽 위로 되오르는" 모양 |
| cloud-arrow-up/cloud-check 18·14 · chat-dots 14 vs Tip 17 | 구분 명확 |

### 5-1. 자동 검증 — v1 §5-1 + `icons:sheet`가 **에러 없이** 재생성되는지(chatText 잔존 검출).
### 5-2. 실물 점검 — v1 §5-2 그대로(8항).
### 5-3. 컨택트시트 M6 절 — 판정용은 **D7 12/14** 하나. 기록용: 토글 20/24/28 · 저장 2도안 · chat 2종 · 파비콘 3종 × 64/32/16 · 카드 테두리 A/B/C · 사이드바 헤더 아이콘 3종.
### 5-4. 알고 두는 손실 — v1 그대로(v:1 순서 커스텀 소실 · 우측 단 열림 중 스테퍼 접근 불가 · 활성 폴더 강조는 배경·700만).

---

## 6. 범위 밖 — v1 §6 그대로.

## 7. 덕수 판정 (v2 시점)

| # | 항목 | 권장 | 근거 |
|---|---|---|---|
| **Q-D7** | 정렬 화살표 12 vs 14 | 권장 14 → **덕수 판정 12**("작아도 충분하다") | † 예외 등록 |
| 확인 1 | D5′ 네이티브 `title` 병기 안 함 | **OK** | |
| 확인 2 | D15 path 그대로 채택 | **OK** | |

N1~N10은 v1 §7 그대로 유효.

## 8. 착수 체크리스트 — v1 §8 S1~S12 + 보강
- S1에 **시트 `chatText` 3곳 치환** 포함 → `icons:sheet`가 돌아야 S1 완료.
- S4 HoverTip은 **훅** — `IconButton` diff가 상태·타이머 제거뿐인지 눈으로 확인(JSX 불변).
- S10 테스트 갱신 범위 `:25-28` 포함.
- S12 문서에 CLAUDE.md ①~⑤.

---

## 부록 A. 파비콘 path — v1 그대로(SemiBold 채택 · Bold 보존). v2 실측으로 SemiBold path의 폭·높이가 실물과 0.999 일치함을 확인.
## 부록 B. 후보 아이콘 비교 — v1 그대로.

## 부록 C. v1 정정·보완 표 — **v1을 인용하기 전에 볼 것**

| # | v1 | 실측 | 영향 |
|---|---|---|---|
| C-1 (정정) | D5 "말풍선 + `title` 병기" | `IconButton`은 `aria-label`만 쓴다(:261). 병기하면 커스텀(600ms)과 네이티브(~1s) **두 툴팁**이 뜬다 | D5′ — `title` 금지 |
| C-2 (정정) | `--block-border-active` 2.0:1, 사다리 "active(2.0) → content-active(2.29)" | 카드면 기준 **2.35**(2.03은 활성 블록 기준) — 순서가 뒤집힘 | D10 무영향 · §1 B 표 |
| C-3 (누락) | "`chatText` 다른 소비처 없음" | 컨택트시트가 3곳(:172·:216·:288) — 키를 지우면 시트 생성이 깨진다 | D21′ · §4-1 |
| C-4 (누락) | `tests/folderIcon` 갱신 `:31-35·:40` | **`:25-28`**도 `bold: false`를 기대한다 | D23′ |
| C-5 (누락) | CLAUDE.md M5 절 "활성 행만 bold — 글자 700과 같은 조건" | 규약 문구가 코드와 정반대가 된다 | §4-9 ③ |
| C-6 (보완) | HoverTip API "CLI가 고른다" | **훅**으로 확정 — `IconButton` JSX 불변 | §4-5 |
| C-7 (보완) | D15 path "재현 절차" | Pretendard가 로컬에 없고 fontTools도 없어 재추출 불가 → **CDN 폰트로 잉크 상자 실측**하여 검증(0.999) | §5-0 |
| C-8 (보완) | D18 "글리프 실측 약 21×12" | 22.5×13.5 | 무영향(24 유지) |
| C-9 (보완) | D12 열기 버튼과의 겹침 미기술 | 버튼은 드로어 열림 시 **미렌더**(:999) — 겹침 불가 | §4-7 |
| C-10 (보완) | D7 판정 근거 없음 | 시트 실측 → 14 권장 | §7 |
| C-11 (보완) | 행 번호 | LIST_COLUMNS 23-30 · SortControls 968-990 · IconCoachImportant :104 · VersionTimeline 폴백 :72 · BlockchainBadge :39 등 | 인용 갱신 |
| C-12 (보완) | Pretendard "npm 정적 OTF" | 앱은 `layout.tsx:15` **jsDelivr CDN**(variable dynamic subset) — 로컬 설치·npm 의존 0 | §1 D |

---

## 9. 구현 기록 (2026-09-09 · CLI) — 실행판 확정

### 9-1. 커밋 (S1~S12, push는 덕수)
| 커밋 | 범위 |
|---|---|
| `11be4fe` S1 | 생성기 ICONS +7/−4(59종) · `ASSET_WEIGHTS = ['regular']`(1,512개, `bold/` 삭제) · 컨택트시트 M6-1~7 절 · `chatText` 3곳 → `chatDots` |
| `587a165` S2·S3·S4·S10 | Icons(IconSortAsc/Desc 12 · IconSave → PhIcon cloud · IconComment chat-dots · PhAsset regular 고정 · `phAssetUrl` 삭제) · listColumns v:2 + `header` + `columnHeaderKind` + 마이그레이션(테스트 +2) · `useHoverTip` 훅 + IconButton 소비(JSX 불변) · 폴더 bold 폐기(folderIcon·FolderGlyph·Sidebar prefetch/active·테스트) |
| `d253e2b` S5~S11 | ListView 헤더 아이콘·말풍선·화살표·유령 행·팝오버 병기·IconSave 14 · FolderView SortControls·카드 테두리 · globals `--border-card-problem` · TabBody `CARD_RADIUS_QUESTION` · ProblemView 스테퍼 절대배치·드로어 1행·peek 12 · EditorView `summaryEligible` · ToggleSwitch Phosphor 24 · `SidebarSectionHeader` 신설 + Sidebar·ShareTree · BlockchainBadge · `app/icon.svg` |
| S12 | CLAUDE.md ①~⑤ + 현재 Phase 절 · roadmap M6 절 + M4·M5 각주 · THIRD_PARTY_LICENSES Pretendard · 이 실행판 |

### 9-2. 계획 대비 그대로 간 것
D1~D25 전항 계획대로. `useHoverTip`은 §4-5의 훅 API 그대로이고 `IconButton` diff는 상태·타이머·`<span role="tooltip">` 제거 + `{tip}` 한 줄 — JSX 구조 불변(§4-5의 "diff만으로 시각 불변 검증" 성립). `VersionTimeline`은 무변경(IconSave 시그니처 유지).

### 9-3. 착수 중 확정한 세부(계획서에 없던 것)
- **유령 라벨 행 스페이서 = 12 + 3 = 15px**(§4-4의 "17px"은 화살표 14 기준이라 D7=12에 맞춰 조정). 헤더와 같은 `HeaderLabel` 노드로 폭을 잰다.
- **`HeaderSortButton`** 컴포넌트로 분리 — 훅은 컴포넌트 최상위에서만 부를 수 있어 `visible.map` 안에서 `useHoverTip`을 직접 호출할 수 없다(Rules of Hooks). 비정렬 칼럼(`owner`·`perm`)은 `<HeaderLabel/>`만.
- `useHoverTip`의 `bind`에 `onMouseDown`은 넣지 않았다(§4-5 시그니처에 있었으나 IconButton 원본에 없는 동작 — 클릭 시 숨김은 `onMouseLeave`·리렌더로 충분). `anchorRef`·`show`·`hide`를 함께 노출해 ref 병합 소비처(IconButton)와 단순 소비처(리스트 헤더) 둘 다 받는다.
- 접힘(collapsed) 사이드바의 My 항목은 `SidebarItem`을 중앙 정렬 div로 감쌌다(옛 헤더 div의 `justifyContent:center` 유지).
- `SidebarSectionHeader` 라벨 버튼 `padding '4px 2px'`(아이콘과 2px 숨) · chevron `transition var(--transition-fast, .15s)`.
- `SortControls` 화살표 버튼에 `display:inline-flex` 추가 — 글자에서 SVG로 바뀌며 세로 중앙이 필요.

### 9-4. 검증
- `npx tsc --noEmit` 무오류 · `npm run test:list` 17(+2) · `npm run test:foldericon` 9(케이스 재편) · 전체 로직 테스트는 §9-5.
- 잔여 참조 0: `phAssetUrl`·`folderBold`·`folderOpenBold`·`folderSimpleBold`·`chatText`(`grep -rnw`).
- `icons:gen` 59종 · `icons:check` OK · `icons:sheet` 재생성(에러 0 — `chatText` 잔존 검출 통과) · `icons:assets` 1,512개.
- 실물 점검(§5-2 8항)은 덕수 검수 몫.

### 9-5. 빌드·테스트 (2026-09-09, dev 종료 후)
- `npm run build`: `[icons:check] OK — 59종 · core@2.1.1` · `[icons:assets] OK — 1512개` · Compiled successfully · 정적 14/14.
- 로직 테스트 12종 **373건 전부 통과**(371 → 373: `test:list` 15 → 17). `test:rules`(에뮬레이터)는 이번 범위 밖(규칙 0).
- 남은 것: 덕수 실물 검수(§5-2 8항 — 리스트 헤더 말풍선·화살표 12 · 문제/풀이 카드 · 스테퍼·드로어 1행 · 파비콘 · 토글 4곳 · 사이드바 헤더 3종 · 받은 문항 화살표 방향) → push → Vercel 로그에서 `icons:assets 1512` 확인.

### 9-6. 덕수 검수 1차(2026-09-09, "전체적으로 매우 만족") — 추가 조정 6건 (`docs/phaseSketch/M6more.md`)
| # | 요청 | 반영 |
|---|---|---|
| 1 | 코칭 블록 로고 + Tip + 왼쪽 바를 빨강으로 통일 | `--coach-important: var(--mathory-red-dark)` 토큰 하나(셋이 전부 `--coach-accent`를 읽는다). 경우 dot과 같은 레드. 명암비 3.80/3.28 — 텍스트 4.5 미달을 알고 채택(식별은 아이콘+600) |
| 2 | 리스트 칼럼 정렬 — 제목 왼쪽, 나머지 가운데, 헤더 동일 | 본문 셀 래퍼에 `flex + justifyContent:center`(제목 제외 — block+ellipsis 보존) · 폴더 행 수정일 `textAlign:center` · ListHeader 칼럼 `justifyContent` 동일 기준 |
| 3 | 문제 카드 테두리 2배 · 풀이 카드 직각 | `CARD_BORDER_QUESTION = '1px solid var(--border-card-problem)'`(TabBody) · FolderView 카드 1px · `CARD_RADIUS = 0` |
| 4 | 제목행 아래 구분선이 스테퍼 아래서 사라짐 | **원인**: 스테퍼 래퍼가 `position:absolute; top:0; height:57` + 불투명 `--bg-functional`인데 제목행은 border-box 57이라 아래 선이 y=56~57 — 래퍼가 그 1px을 덮었다. **처방**: 래퍼 `height: HEADER_H − 1`(세로 중앙 28 = 내용 상자 중앙, 불변) |
| 5 | 요약 토글을 옛 디자인으로, 크기만 축소 | D17~D19 철회. 트랙+손잡이 복귀, 22×13(손잡이 9) → **18×11(손잡이 7)**. `toggleOff/On` ICONS 삭제 → **57종**, 시트 M6-2 절을 철회 기록으로 교체 |
| 6 | 줄바꿈 아이콘 80% | `LineWrapIcon` 20 → 16 |

검증: `tsc` 무오류 · `icons:gen` 57종 · `icons:check` OK · `icons:sheet` 재생성. 로직 테스트 무접촉(lib 변경 0).

### 9-7. 덕수 검수 2차(2026-09-09, "완벽하게 작동") — 문제 카드 테두리 원복 · 그림자
- 판정: 1px 테두리가 "시각적으로 부담스럽다". ① 테두리 굵기·색을 원래대로 ② 열람뷰 문제 카드에 은은한 그림자.
- 반영: TabBody·FolderView 문제 카드 테두리 → **0.5px `--border-content`**(M6 이전 값). `--border-card-problem` 토큰·`CARD_BORDER_QUESTION` 삭제(소비처 0).
  열람뷰 문제 카드(`!scoped`)에 `--card-shadow-problem`(`0 1px 2px .04` + `0 4px 14px .06` — 드로어 2겹 구조의 절반 강도). 풀이 카드는 `none`.
- 결과 규약: 문제/풀이 카드 차별화 = **모서리(12/0) + 그림자(有/無)**. 테두리는 같다. D10은 최종적으로 철회.

### 9-8. 검수 종결 (2026-09-09)
덕수: 2차 반영(테두리 원복 + 그림자) 후 "이제야 제대로 된 답을 찾았다". M6 전항 종결.
커밋 계보: `11be4fe` S1 → `587a165` S2·3·4·10 → `d253e2b` S5~S11 → `dde531f` S12 → `5e7b6ed` 검수 1차 6건 → `ae7bcf4` 검수 2차 → 종결 문서 1건.
남은 것: push(덕수) → Vercel 빌드 로그에서 `[icons:check] OK — 57종` · `[icons:assets] OK — 1512개` 확인 → 배포 후 Cmd+Shift+R.
⚠ 교훈(카드 차별화 왕복 3회): "테두리를 진하게"는 두 번 다 부담스럽다는 판정을 받았고 답은 **모서리+그림자**였다 —
  카드 사이의 위계는 선 굵기가 아니라 떠오름(그림자)으로 주는 편이 이 팔레트(아이보리·클레이)에 맞는다.

### 9-9. 덕수 검수 3차(2026-09-09) — 사이드바 아이콘 · 체크박스 · 색 팔레트 전수 정리
| # | 요청 | 반영 |
|---|---|---|
| 1 | '가져오기' 아이콘이 작아 보임 · Phosphor 통일 | 이미 Phosphor(`download-simple`)였고 **크기**가 14로 이웃(새 문제·검색 18)보다 작았다 → 사이드바 항목만 `size={18}` |
| 2 | 팝업 체크박스 파랑 → 빨강 | 시트 가져오기(3)·일괄 검증(1)이 inline accentColor 없이 기본 파랑. `globals.css`에 `input[type=checkbox|radio] { accent-color: var(--mathory-red) }` 전역 규칙 1개 · 기존 inline 7곳 삭제(ListView 3 · PdfDialog 1 · ShareSettingsPanel 3 — 후자는 accent-primary였다) |
| 3 | 팔레트 밖 색 전수 정리 | **유채색** 약 60곳/25파일: 구글 diff 팔레트(VersionDiff 초록·노랑·파랑) · 교정 박스(노랑·초록·종류별 6색) · lint 밑줄(#e53935·주황) · 검색/수식 하이라이트(노랑·주황) · DnD 링·찾기 버튼(인디고) · 심볼 설정/그룹/팔레트(인디고·파랑 틴트·#22c55e·#ef4444) · 오류 띠 5곳(#fdecea/#a4322a/#721c24) · 위험 버튼(#e53935 → `dialogStyles.DANGER` 토큰) · 폴백 hex(#2563eb·#eef2ff·#e8eaf6) · GeoGebra 토스트 · 심볼 설정 옛 액센트 #B8845C. **회색** 111곳/31파일: 문맥(글자/배경/테두리)별 토큰으로 `var(--token, #옛값)` 꼴 치환. 남긴 것: IconGoogle 브랜드색 · 인쇄 CSS · `app/firebase-test`·`editor-test`·admin 콘솔 상자 |
| 신규 토큰 | `--accent-success-bg #EDEFE3` · `--bg-warn`(=accent-soft) · `--highlight-match rgba(217,119,87,.22)` · `--highlight-match-active rgba(188,95,63,.42)` · `--drop-ring` · `--drop-tint` | 성공(올리브)은 팔레트 밖의 유일한 예외 — '완료'를 빨강과 갈라야 한다 |

검증: `tsc` 무오류. 잔여 유채색 grep 0(주석 제외). 회색 잔여는 주석·인쇄·dev 페이지뿐.
⚠ 실물 확인 포인트: 버전 diff 카드(추가=올리브 · 삭제=danger · 이동=중립) · 교정 박스 톤 · 편집창 검색 하이라이트가 레드 틴트로 바뀐 것 · DnD 드롭 링.
