# Phase 66b — 문답 검증 질문 카테고리(문제·풀이) · 문제 검증 질문 4종 구현 계획서 **v3 착수판**

> **구현 완료(2026-09-15) — 구현 기록은 §11.** 본문(§0~§10)은 착수 시점 그대로다.

작성일: 2026-09-15 · 작성: **CLI 실측 재검증** · 기준 커밋: **mathory `main f6513a0`**
계보: 질문 목록 v2 초안(web) → 66b v1(CLI) → v2(web 재검증) → **v3 = CLI 착수판**
짝 문서: `docs/phasedocs/Phase66a … v6 착수판.md`(§13 구현 기록 포함) · `docs/phaseSketch/ask-question-list-v2.md`
착수 시 CLAUDE.md 규칙 1에 따라 현재 파일을 다시 읽을 것.

> **v2 판정: 결정·사실 정정 0 · 표기 정정 2 · 보완 5(Z1~Z5).**
> v2의 F1~F11·S1~S6·D5′·D11~D13을 전건 실물 대조했고 전부 사실이다. D5′의 전제("시스템 프롬프트가
> 이미 `$...$`를 강제한다")도 `route.ts:62-68`에서 확인했다.
> v3가 더하는 것은 **v2가 적은 결정을 그대로 구현하면 조용히 깨지는 자리 셋**(Z1~Z3)과 관측 보완 둘(Z4·Z5)이다.
> 셋 다 덕수 판단이 필요한 갈림길이 아니라 결정의 구현 명세라 **착수판**으로 낸다. Q1~Q8은 v1·v2에서 전항 확정됐다.

---

## 0. 요약

질문 리스트를 **[문제] [풀이] 두 카테고리**로 나누고 문제 검증 질문 **P1~P4**를 더한다. 기존 G1~G3는 [풀이]로 간다.

**초안을 그대로 넣으면 조용히 틀어지는 넷(v1) + 결정을 그대로 구현하면 깨지는 셋(v3)**

| # | 그대로 두면 | 처방 |
|---|---|---|
| F1 | 새 질문이 덕수 목록에 **안 생긴다**(씨앗은 빈 목록에만) | D4 보충 + `askSeedKeys` 기록 |
| F2 | 문제 질문이 **풀이까지 지적하라는 안내**와 함께 간다 | D2 target별 범위 |
| F3 | P1의 "풀이는 보지 말고"가 **지켜질 수 없다** | D3 `withTabs` |
| F4 | 말풍선에 `…`·`$\displaystyle ...$`가 보인다 | D5′ 꼬리 줄 삭제 |
| **Z1** | **질문을 보낼 때마다 탭 기억이 지워진다** | 저장 키를 병합해서 쓴다 |
| **Z2** | 풀이 질문에서 탭 전송을 끄면 **풀이 없이 풀이를 검증**한다 | `withTabs`는 문제 질문에만 |
| **Z3** | 편집에서 대상을 바꾸면 질문이 **눈앞에서 사라진다** | 저장 후 그 대상 탭으로 전환 |

**서버 0 · Firestore 규칙 0 · 검증 프롬프트(`lib/verify/*`) 0 · 61b/61d/61h 0 · 렌더 5사이트 0 · 폰 0.**
`ask_questions` 필드 **1개** 추가(`withTabs`) · 사용자 문서 필드 1개(`askSeedKeys`).
로직 검증 **446 → 448건**(`test:ask` 7 → 9) · 규칙 테스트 67블록 무변경.
**이름(Q8 확정)**: 이 작업 = 66b · 폴더뷰 순차 실행 → **66c** · 검증 칩 정리 → **66d**.

---

## 1. 범위 — 무엇을 안 하는가

| 항목 | 이유 |
|---|---|
| 등록 정답 대조(`{answer_line}` 치환) | Q1 — 대조는 사람 |
| 서버 `mode:'ask'` | 서버 변경. 범위·트리거를 클라에서 푼다 |
| 질문별 모델 지정 | 실측 전 추측 |
| 렌더러 인라인 코드 보호(F4 근본 처방) | 렌더 5사이트·인쇄에 번진다 |
| 기존 G1~G3 문서 자동 수정 | 씨앗은 1회 복사본 — 덕수가 앱에서 고친다(§8) |
| 폴더뷰 배치 · 칩 정리 | 66c · 66d |

---

## 2. 확정 사실 (`main f6513a0` 실측)

v2 §2의 F1~F11은 전부 사실이다. 여기에는 **착수에 필요한 요지와 v3가 새로 확인한 것**만 적는다.

| # | 사실 | 근거 |
|---|---|---|
| F1 | `ensureSeeded`는 목록이 비었을 때만 씨앗을 쓴다. 호출부는 팝오버 첫 열기 하나 | `lib/askQuestions.ts:106` · `AskListPopover.tsx:97` |
| F2 | 문답 전송은 전부 `solutionTarget: true` 고정이고 질문의 target을 보지 않는다. 범위 안내는 **참고 탭에 내용이 있을 때만** 붙는다 | `CommentPanel.tsx:891-893` · `:591` · `:601` |
| F3 | 서버는 `currentTabContent`와 `currentTabLabel`이 **둘 다 있을 때만** 풀이 절을 넣는다 → 둘을 비우면 풀이 절이 없다 | `route.ts:402` |
| F4 | 인라인 `\displaystyle` 주입이 원문 전체에 돌고 코드**펜스**만 보호한다. 씨앗 3개 모두 `수식은 $...$로 감싸.`로 끝난다 | `EditorPreview.tsx:228` · `:80` · `seed.ts:43·59·68` |
| F4′ | **D5′의 전제 확인** — 시스템 프롬프트 첫머리가 "⚠️ 최우선 출력 규칙 … 위반 시 응답이 무효 처리됩니다: **모든 수식은 반드시 `$...$`**"다. 질문 꼬리의 같은 지시는 중복이다 | `route.ts:62-68` |
| F5 | P1~P4 트리거 낱말 0 (v1·v2 실행 검사) | `route.ts:256` · `:266-267` |
| F6 | discuss는 `thinking`을 넘기지 않고 Opus 5는 사고가 기본 켜짐 → 사고 토큰이 `maxTokens`에서 깎인다(66a E2) | `route.ts:571` |
| F7 | 사용자 문서 쓰기는 기존 사용자에게 `{merge:true}` · 본인 쓰기 규칙 허용. **`getUserProfile`은 명시 매핑이라 새 필드를 못 읽는다** | `users.ts:51` · `:55-68` · `firestore.rules:27` |
| F8 | 모달 셀렉트 `해설` · [+ 새 질문]은 `target:'solution'` 하드코딩 | `AskListPopover.tsx:408` · `:377` |
| F9 | P1 "1~999 자연수"·P2 "수능 문항의 관습"은 수능형 전제 · P4는 새 문제의 성립 확인이 없다 | 문안 |
| F10 | P1·P2는 61b 문제 검증 4태그와 겹친다(대조군) · P3·P4는 보완 | `lib/verify/prompts.ts:247` |
| F11 | `listAskQuestions`는 명시 매핑 · `nextOrder`는 **전체** 최대 + 10 · 복제는 `src.order + 1`에 target 복사 | `askQuestions.ts:27-44` · `seed.ts:115` · `askQuestions.ts:78-87` |
| **F12** | **모델 기억 쓰기가 저장 키를 통째로 덮어쓴다** — `localStorage.setItem('mathory.ask.v1', JSON.stringify({ modelIds }))`, 질문 전송 때마다 호출 | `AskListPopover.tsx:45-46` · `:138` |
| **F13** | 편집 저장은 목록을 다시 읽고 모달만 닫는다 — 보이는 탭은 그대로 | `AskListPopover.tsx:150-168` |

---

## 3. 결정표 (v3)

v2의 D1~D13을 그대로 두고, **Z1~Z3의 구현 명세를 해당 결정에 붙였다**(굵게).

| # | 결정 | 근거 |
|---|---|---|
| **D1** | 카테고리 = 팝오버 맨 위 탭 `[문제] [풀이]`. 탭마다 그 target만. 마지막 탭 기억, 없으면 `문제`. [+ 새 질문]은 지금 탭의 target으로 시작(`:377`). 모달 셀렉트 `해설` → `풀이`(`:408`). **Z1: 기억은 `mathory.ask.v1` 한 키의 병합 객체 `{ modelIds, category }`로 읽고 쓴다 — 쓰는 쪽은 반드시 기존 값을 읽어 한 필드만 바꾼다.** **Z3: 편집 저장 시 대상이 지금 탭과 다르면 그 대상 탭으로 전환한다** | Q1 · F12 · F13 |
| **D2** | target별 범위. `solution` = 현행. `problem` = 문제만 지적 대상, 모든 탭 = 참고 자료 + 전용 안내. 문제 target에서는 `hasReference` 게이트 없이 **탭 내용이 하나라도 있으면 항상** 안내 | F2 · v2 S3 |
| **D3** | 질문 필드 `withTabs: boolean`(기본 `true`). `false`면 문제 탭만 보낸다(`currentTabContent`·`currentTabLabel` `undefined` · `tabSlots: []`). 씨앗 P1만 `false`. P1 문안의 "풀이는 보지 말고" 삭제. **Z2: `withTabs`는 문제 질문에서만 의미가 있다 — 모달 체크박스는 대상이 `문제`일 때만 보이고, 전송 경로는 `target === 'problem' && withTabs === false`일 때만 탭을 뺀다(풀이 질문은 저장값과 무관하게 항상 탭을 보낸다)** | Q2 · F3 |
| **D4** | 씨앗 보충 + 제안 기록. 사용자 문서 `askSeedKeys: string[]`. 열 때 `SEED − 기록`만 만들고 기록한다. **필드가 없고 목록이 비어 있지 않으면 `['G1','G2','G3']`을 제안한 것으로 본다.** 읽기는 `getDoc(doc(db,'users',uid))` 직접, 쓰기는 `batch.set(userRef, { askSeedKeys }, { merge: true })` — 질문 생성과 **한 배치**. **순수 함수 입력은 `offered: string[] \| undefined`로, `undefined`(필드 없음)와 `[]`(기록은 있으나 비었음)를 구별한다** | Q3 · F1 · F7 |
| **D5′** | 7개 문안의 마지막 "수식은 …" 줄을 **삭제**한다 → 씨앗 본문의 `$` 0개 | F4 · F4′ |
| **D6** | P1 단답 기준을 "수능형 단답이면"으로 조건부 · P2 "수능 문항의 관습" 유지 | Q5 · F9 |
| **D7** | P4 꼬리: "수치를 바꿨으면 고친 문제도 답이 하나로 정해지는지 봐 주고, 바뀐 답을 함께 적어줘." | Q6 · F9 |
| **D8** | 실험 문서 `maxTokens` 16000(콘솔). **Z4: 시간 초과를 관측 항목에 넣는다**(§9) | Q7 · F6 |
| **D9** | 신규 사용자 씨앗 order: P 10~40 · G 50~70. 덕수의 기존 G는 10~30, 보충 P는 10~40 | 표시 순서는 탭 안에서만 |
| **D10** | 전송 화면 안내 세 갈래(§5) | 보내는 것이 질문마다 다르다 |
| **D11** | `nextOrder(list, target)` = 그 target 최대 + 10. **복제는 현행 그대로**(`src.order + 1` · target 복사 — 이미 같은 target 안에 선다. v2 §6이 "변경"으로 적었으나 코드 변경 0) | F11 |
| **D12** | `withTabs`는 실험 중 질문별로 고정 | 66a D19와 같은 논리 |
| **D13** | `seedKey`는 `SEED_QUESTIONS`의 코드 전용 키 — Firestore 문서에 쓰지 않는다 | 보충 판정은 `askSeedKeys`만 본다. 읽지 않는 필드를 만들지 않는다(61f D9 교훈) |

---

## 4. 문안

**G1~G3·P2·P3는 마지막 "수식은 …" 줄 삭제 외에 무변경**(D5′).

### P1 · 문제(성립) `seedKey P1` · `order 10` · `withTabs false`
```
문제만 보고 직접 풀어서 답을 구해줘. 첫 줄에 네가 구한 답을 적어줘.
그다음 이 문제가 성립하는지 봐줘:
- 조건을 만족하는 답이 없거나 둘 이상이면, 어느 조건이 부족하거나 서로 충돌하는지 짚어줘.
- 객관식이면 네 답이 선지에 있는지, 옳은 선지가 둘 이상은 아닌지, 선지끼리 형식(기약분수·유리화·단위)이 맞는지 봐줘.
- 수능형 단답이면 네 답이 1 이상 999 이하의 자연수인지 봐줘 — 아니면 문제나 조건이 잘못된 거야.
답을 확정하지 못했으면 어디서 막혔는지 적어줘. 문제에 없는 조건을 네가 보태서 넘기지 마 — 그 자리가 곧 지적이야.
```

### P2 · 문제(표기·표현) `seedKey P2` · `order 20` · `withTabs true`
```
문제 본문의 표기와 표현만 봐줘. 답이 맞는지는 보지 마. 사소한 것이라도 빠짐없이:
- 같은 문자가 서로 다른 뜻으로, 또는 다른 문자가 같은 뜻으로 쓰인 곳
- 수학 용어나 기호를 잘못 쓰거나 어색하게 쓴 곳 — 구간·집합·함수 표기, 첨자·괄호·로그 밑, 발문의 어투처럼 수능 문항의 관습과 어긋나는 것
- 두 가지로 읽힐 수 있는 문장, 무엇을 가리키는지 분명하지 않은 지시어, 오해를 부를 만한 표현
각 지적마다 원문을 짧게 인용하고, 어떻게 잘못 읽힐 수 있는지와 고친 문안을 적어줘.
지적이 많으면 답변 길이 제한보다 빠짐없이 적는 쪽을 우선해.
```

### P3 · 문제(과조건·군더더기) `seedKey P3` · `order 30` · `withTabs true`
```
문제의 조건 하나하나가 답을 구하는 데 실제로 쓰이는지 봐줘. 풀이가 있으면 각 조건이 풀이의
어느 지점에서 쓰였는지 짚고, 없으면 네가 풀면서 어디서 쓰는지 봐.
- 그 조건을 지워도 답이 그대로 하나로 정해지면 과조건이야.
- 다른 조건에 이미 담긴 내용을 되풀이하거나 의미가 일부 겹치는 조건, 같은 말을 두 번 하는 서술은 군더더기야.
사소한 것이라도 빠짐없이 찾고, 각각 지워도 되는 근거(지운 뒤에도 답이 유일한 이유)와 삭제 문안을 적어줘.
단, 답의 존재나 유일성을 보장하는 데 필요한 조건(자연수·양수·정의역·서로 다른 점 등)은 과조건이 아니야.
지적이 많으면 답변 길이 제한보다 빠짐없이 적는 쪽을 우선해.
```

### P4 · 문제(개선 재작성) `seedKey P4` · `order 40` · `withTabs true`
```
이 문제를 더 나은 문제로 다시 써 줘. 묻는 것과 풀이의 핵심 아이디어는 그대로 두고, 세 가지를 목표로:
① 계산 부담을 줄일 것 — 풀이 과정에서 다루는 수가 1~3자리 정수, 인수분해가 필요하면 2자리 이하 정수가 되도록
   계수·조건의 수치를 조정하고, 기계적으로 반복되는 계산이 있으면 그 반복이 사라지게.
② 의미가 겹치는 조건을 하나로 합치거나 지워서 논리적으로 군더더기 없게.
③ 길고 장황한 수식·서술을 짧고 명료하게 — 읽는 시간과 부담을 줄여 수학적 본질에 집중하게.
오류나 모호한 표현이 보이면 그것도 함께 고쳐.
먼저 고쳐 쓴 문제 전문을 적고, 그 아래 바꾼 곳마다 "원문 → 수정안 → 이유"를 항목으로 정리해줘.
수치를 바꿨으면 고친 문제도 답이 하나로 정해지는지 봐 주고, 바뀐 답을 함께 적어줘.
```

### G1~G3 `seedKey G1~G3` · 신규 사용자 `order 50/60/70` · `withTabs true`
66a 씨앗 그대로, 마지막 `수식은 $...$로 감싸.` 줄만 삭제. **G1·G2는 같은 행 앞부분("지적이 많으면 … 우선해.")은 남긴다** — 그 줄의 뒤 절반만 지운다(`seed.ts:43·59`). G3는 독립 줄이라 줄째 지운다(`:68`).

---

## 5. UX

**팝오버 1단**
```
┌──────────────────────────────┐
│  [ 문제 ]   풀이              │ ← 탭. 선택은 액센트 밑줄
├──────────────────────────────┤
│ 문제(성립) r1          ⋮     │
│   문제만 보고 직접 풀어서…    │
│ …                            │
│ [+ 새 질문]                   │ ← 지금 탭의 target으로 시작
└──────────────────────────────┘
```

**편집 모달**: 대상 `문제` / `풀이` · **대상이 `문제`일 때만** 체크박스 "풀이·참고 탭 함께 보내기"(Z2) ·
저장 후 대상이 지금 탭과 다르면 **그 탭으로 전환**(Z3).

**팝오버 2단 안내(D10)**

| 경우 | 안내 |
|---|---|
| 풀이 질문 | 문제와 풀이 탭만 지적합니다. AI 풀이 등 다른 탭은 참고로만 보냅니다. |
| 문제 질문 · 탭 함께 | 문제만 지적합니다. 풀이와 다른 탭은 참고로만 보냅니다. |
| 문제 질문 · 탭 없이 | 문제만 보냅니다. 풀이 탭은 보내지 않습니다. |

셋 다 끝에 "이전 대화는 참조하지 않습니다."

**AI가 받는 모양 — 문제 질문 · 탭 함께**
```
## 문제:
(문제)

## 현재 풀이 (탭: 참고 자료):
※ 검증·지적 대상은 [문제]뿐입니다. 아래 탭은 모두 참고 자료이며, 그 서술·계산·표기는 지적하지 마세요.

### [풀이] — 참고 자료 (검증 대상 아님)
### [AI 풀이] — 참고 자료 (검증 대상 아님)
```
서버의 절 제목 "현재 풀이"는 바꿀 수 없다(서버 0). `currentTabLabel` `'참고 자료'`와 안내 문장이 그 뜻을 덮는다.

---

## 6. 구현 항목

| 파일 | 변경 |
|---|---|
| `lib/ask/seed.ts`(import 0) | `AskQuestion`·`AskQuestionInput`에 `withTabs: boolean` · 씨앗 7개(꼬리 삭제 · `seedKey`는 씨앗 항목에만) · **`planSeedTopUp({ offered: string[] \| undefined, collectionEmpty })` → `{ toCreate, nextOffered }`** · **`nextOrder(list, target)`** · **`effectiveWithTabs(q)` = `q.target === 'problem' ? q.withTabs : true`**(Z2 — 전송·안내·모달이 이 함수 하나를 본다) |
| `lib/askQuestions.ts` | 매핑에 `withTabs: data.withTabs !== false` · `createAskQuestion`이 `withTabs` 기록 · `ensureSeeded` = `getDoc(users/uid)` → `planSeedTopUp` → 질문 생성 + `askSeedKeys` merge를 **한 `writeBatch`** · `seedKey`는 문서에 쓰지 않는다 · 복제 무변경 |
| `components/comment/AskListPopover.tsx` | 탭 2개·필터 · **기억 병합 읽기·쓰기(Z1)** — `loadPrefs()`·`savePrefs(patch)`가 `{ modelIds, category }`를 합쳐 쓴다, `rememberModels` 대체 · [+ 새 질문] target(`:377`) · 모달 `풀이`(`:408`) · **체크박스는 대상 `문제`일 때만(Z2)** · **저장 후 탭 전환(Z3)** · 2단 안내 3갈래 · `onSend(message, modelIds, { target, withTabs: effectiveWithTabs(q) })` |
| `components/comment/CommentPanel.tsx` | `buildContext(opts?: { solutionTarget })` → `opts?: { ask?: { target; withTabs } }` · `withTabs:false` → 탭 루프 생략 · 둘 다 `undefined` · `tabSlots: []` · `target:'problem'` → 전 탭 참고 + 안내(게이트 없음) + 라벨 `'참고 자료'` · `'solution'` → 현행 · **`ask`가 없으면(타이핑) 현행과 같다** · `handleAskSend`(`:891`)가 세 값을 넘긴다 · `ASK_SCOPE_NOTE`는 target별 두 문장 |
| `tests/ask.test.mjs` | §7 |
| `CLAUDE.md` · `docs/roadmap.md` | 66b 절 · 66a 규약 "문답 전송의 지적 대상" 갱신 · 66c·66d 이름 · 448건 |

**규칙 0** — `ask_questions`는 필드 검증 없음(`firestore.rules:40-42`) · `users/{uid}` 본인 쓰기 허용(`:27`).

### 커밋

| S | 범위 | 완료 기준 |
|---|---|---|
| S1 | `seed.ts` + 테스트 | `test:ask` 9 · 씨앗 `$` 0개 |
| S2 | `askQuestions.ts` | 덕수 계정에 P1~P4만 생김 · G 중복 0 · 두 번 열어도 0 · 닉네임 유지 |
| S3 | CommentPanel 범위 | 문제·탭 없이 → 풀이 절 없음 · 타이핑 전송 무회귀 |
| S4 | 팝오버 | §5 · Z1~Z3 |
| S5 | 문서 | 확정본 phasedocs 등록 · 66a 확정본 §13-4 포인터 |

---

## 7. 테스트

**`tests/ask.test.mjs`** — 7 → **9건**(로직 검증 446 → **448**)

| # | 단언 |
|---|---|
| T1′ | 씨앗 7 · label·seedKey 유일 · problem 4 / solution 3 · order P 10~40 · G 50~70 · enabled · rev 1 · `withTabs`는 P1만 false |
| T2′ | G1~G3는 `오류가 없고` 포함 · P1~P4는 미포함 · G1·G2는 `빠짐없이` |
| T3 | 무변경(본문 무변경 — 이제 `$`가 없는 씨앗에도 성립) |
| T4·T5 | 무변경 |
| T6′ | `nextOrder([], 'problem') === 10` · `nextOrder([{order:10,target:'problem'},{order:30,target:'solution'}], 'problem') === 20` |
| T7′ | 씨앗 7개 트리거 0 |
| **T8** | `planSeedTopUp` ① `undefined` + 목록 있음 → **P1~P4만** · 기록 7 ② `undefined` + 비었음 → 7개 · 기록 7 ③ 기록 7 + 목록 있음 → 0 ④ 기록 7 + 비었음(전부 지움) → **0** ⑤ **`effectiveWithTabs`**: problem·false → false · solution·false → **true**(Z2) |
| **T9** | 씨앗 7개 본문 `$` 0개(D5′ 회귀) |

T8 ⑤는 T8 안에 둔다(건수를 늘리지 않는다 — 448 유지).

**실물 검수**
1. [문제] 탭에 P1~P4, [풀이] 탭에 G1~G3 · G 중복 0 · 닫았다 열어도 추가 0
2. 설정의 닉네임이 그대로(`askSeedKeys` merge 회귀)
3. P1 삭제 → 다시 열기 → 되살아나지 않음
4. P1 전송 → 바디에 `currentTabContent` 없음
5. P2 전송(문제+풀이 탭만 있는 문항) → 안내 "지적 대상은 [문제]뿐" · 풀이 절 "참고 자료"
6. G1 전송 · 타이핑 전송 → `f6513a0`과 같은 모양
7. 말풍선에 `$\displaystyle` 없음
8. [문제] 탭에서 [+ 새 질문] → 대상 `문제`로 시작 · 체크박스 보임 / 대상을 `풀이`로 바꾸면 체크박스 사라짐(Z2)
9. **[문제] 탭을 보다가 질문을 하나 보낸 뒤 팝오버를 다시 열면 여전히 [문제] 탭**(Z1)
10. **[문제] 탭의 질문을 편집해 대상을 `풀이`로 저장 → 팝오버가 [풀이] 탭으로 넘어가 그 질문이 보임**(Z3)

---

## 8. 덕수 준비물

1. **기존 G1~G3 문서의 "수식은 …" 부분 삭제** — 앱 편집 모달에서(D5′). G1·G2는 줄 끝 절반, G3는 마지막 줄. 백틱이나 "달러 기호($) 한 쌍"을 넣었다면 그것도 지운다. **저장하면 판본이 r2·r3로 오른다 — 그 뒤의 답만 비교할 것**(Z5).
2. **실험 문서 `maxTokens` 16000**(콘솔, **number 유형** — 66a E1) → 브라우저 새로고침.
3. 문제 오류가 있을 법한 문항 1~2개를 따로 골라 두면 P1 판정이 빨라진다.

---

## 9. 실험 운용

- **G1~G3는 같은 잣대의 판본 비교** — 66a §10 판정 규칙 그대로.
- **P1~P4는 서로 다른 잣대라 판본 비교가 아니다** — "어느 게 나은가"가 아니라 "각각이 쓸 만한가". 쓸 만하지 않은 것만 `enabled:false`.
- `withTabs`를 실험 변수로 쓰지 말 것(D12).
- **Z4 — 16000은 잘림을 줄이는 대신 시간 초과를 부를 수 있다.** 한 요청 예산 280초 안에 사고·출력 16000토큰과 자발적 도구 턴까지 들어가야 한다.
  **두 실패는 모양이 다르다** — 잘림은 답 끝에 잘림 문구가 붙은 채 **저장되고**, 시간 초과는 오류 버블만 뜨고 **아무것도 저장되지 않는다**(재시도하면 비용을 다시 쓴다).
  P1·P4에서 시간 초과가 두 번 이상 나오면 12000으로 내리고 기록한다.
- **Z5 — D5′로 G 판본이 바뀐다.** 꼬리 줄을 지운 뒤의 답(r2·r3 이후)과 그 전의 답을 섞어 비교하지 말 것. 대화 첫 줄의 `r{rev}`가 경계다.
- P1·P4를 따로 기록한다 — 둘이 시간·비용·잘림의 상한을 정한다.

---

## 10. 하지 말 것

- 질문 본문에 `$`를 쓰지 말 것 · 백틱으로 `$`를 감싸지 말 것(F4).
- `askSeedKeys`를 `getUserProfile`로 읽지 말 것 · merge 없이 쓰지 말 것(F7).
- **`mathory.ask.v1`에 한 필드만 담아 `setItem`하지 말 것(Z1)** — 다른 필드가 지워진다. 병합해서 쓸 것.
- **풀이 질문에서 `withTabs`를 존중하지 말 것(Z2)** — `effectiveWithTabs` 하나로만 판단한다.
- 문제 target 안내를 `hasReference`로 게이트하지 말 것 · `withTabs:false`에서 `tabSlots`를 남기지 말 것.
- 문서에 `seedKey`를 쓰지 말 것 · 라벨로 씨앗을 대조하지 말 것 · 탭을 라벨로 거르지 말 것.
- 범위 안내를 질문 문안에 넣지 말 것 · 타이핑 전송의 동작을 바꾸지 말 것.
- 66a 규약 전부 · CLAUDE.md 작업 규칙(수정 전 읽기 · 커밋까지 · roadmap · 확정본만 phasedocs).

---

## 11. 구현 기록 (2026-09-15)

### 11-1. 커밋

| 커밋 | 내용 |
|---|---|
| `c849b77` S1 | `lib/ask/seed.ts` — 씨앗 7개(꼬리 줄 삭제) · `planSeedTopUp` · `effectiveWithTabs` · `nextOrder(list, target)` · `test:ask` 7 → 9 |
| `b1c8199` S2 | `lib/askQuestions.ts` — `ensureSeeded` 보충 + `askSeedKeys` 기록(한 배치) · `withTabs` 매핑 · `toDocFields` |
| `b3f5063` S3 | `CommentPanel` — `buildContext`의 `ask: { target, withTabs }` · 문제 질문 범위 안내 · P1 문제만 |
| `14edef3` S4 | `AskListPopover` — 탭 · 기억 병합 · 문제 전용 체크박스 · 대상 탭 전환 · 안내 세 갈래 |
| S5 | 문서(이 절) |

**검증**
- 로직 하니스 18종 합계 **448**(`test:ask` 9) · `tsc --noEmit` 통과 · 규칙 변경 0이라 `test:rules` 무실행
- **S3 바이트 동일성** — 옛(`b1c8199`)·새 `buildContext`를 파일에서 뽑아 TypeScript로 변환해 같은 입력으로 실행했다.
  타이핑 대화 · 풀이 질문 · 풀이 질문 `withTabs:false`(Z2) × 탭 조합 3종(문제+풀이 · 문제+AI풀이+풀이 순서 섞음 · 문제만)
  × 긴 풀이(15,000자 자르기) = **전 조합 바이트 동일**. 문제 질문은 문제+풀이만 있는 문항에서도 안내가 붙고(S3) P1은 탭 슬롯이 비었다
- **Z1 기억 병합** — `loadPrefs`·`savePrefs`를 파일에서 뽑아 가짜 저장소로 실행. 66a 옛 값 → 탭 선택 → 질문 전송 뒤에도
  `category` 유지 · 깨진 JSON·이상한 값은 빈 기억으로 안전하게 읽힌다
- ⚠ **`npm run build`는 로컬 미실행** — dev 서버 가동 중(CLAUDE.md 규칙 5). push 후 Vercel 빌드 로그 확인

### 11-2. 계획 대비 이탈

| # | 계획 | 실제 | 이유 |
|---|---|---|---|
| **R1** | S2 = `askQuestions.ts`만 | 팝오버의 새 질문 생성 호출 두 곳(`nextOrder` 인자 · `withTabs`)을 S2에 함께 | 커밋마다 컴파일이 되게. 화면 작업은 S4 그대로 |
| **R2** | S3 = CommentPanel만 | 팝오버 `onSend` 타입·호출(`scope` 전달)을 S3에 함께 | 같은 이유 — 세 인자 함수는 두 인자 타입에 대입되지 않는다 |
| **R3** | `planSeedTopUp` → `{ toCreate, nextOffered }` | **`record`** 추가 | 기록이 이미 최신이면 사용자 문서를 쓰지 않는다(`toCreate`가 비고 `record`가 false면 배치 자체를 건너뛴다) |
| **R4** | T8 ①~⑤ | ⑤ `undefined`와 `[]` 구별 · ⑥ `effectiveWithTabs` 4경우를 T8 안에 | 건수를 늘리지 않았다(448 유지) |
| **R5** | §5 탭 모양 | 탭 줄을 **sticky**로 | 팝오버 자체가 `maxHeight 320` 스크롤러라 목록을 내리면 탭이 사라진다 |
| **R6** | — | `CommentPanel`이 `effectiveWithTabs`를 직접 import해 판단 | "판단은 함수 하나로"(Z2)를 호출부가 아니라 조립 함수 쪽에서도 지킨다 — 팝오버가 계산해 넘긴 값을 한 번 더 걸러도 결과가 같다 |

### 11-3. 남은 일

- **덕수 준비물 §8** — 기존 G1~G3의 "수식은 …" 부분 삭제(앱) · 실험 문서 `maxTokens` 16000(완료 2026-09-15) · 문항 선정
- **실물 검수 §7의 10항**
- 실험 §9 — P는 각각 쓸 만한가, G는 판본 비교. 시간 초과(Z4)와 판본 경계(Z5)를 기록

---

## 부록 A. 문서 계보

| 버전 | 작성 | 산출 |
|---|---|---|
| 질문 목록 v2 초안 | web | P1~P4 묶음 |
| 66b v1 | CLI | F1~F10 · D1~D10 · Q1~Q8 |
| 66b v2 | web | 정정 0 · S1~S6 · D5′·D11~D13 · Q1~Q8 확정 |
| **66b v3 착수판** | **CLI (2026-09-15)** | **v2 정정 0(표기 2) · Z1~Z5 · F12·F13 · 착수 가능** |

## 부록 B. v2 판정

**결정·사실 정정 0.** 전건 실물 대조 결과:

| v2 | 판정 |
|---|---|
| F1~F11 | ✓ 전부 사실 |
| S1 `getUserProfile` 명시 매핑 | ✓ `users.ts:55-68` — 새 필드를 매핑하지 않는다 |
| S2 merge 필수 | ✓ |
| S3 문제 target 안내 게이트 | ✓ `CommentPanel.tsx:591`·`:601` |
| S4·S5·S6 | ✓ |
| **D5′ 전제** "시스템 프롬프트가 이미 `$...$` 강제" | ✓ `route.ts:62-68` "⚠️ 최우선 출력 규칙 … 위반 시 응답이 무효 … 모든 수식은 반드시 `$...$`" |
| D11·D12·D13 | ✓ 수용 |

**표기 정정 2**

| # | v2 | 실측 |
|---|---|---|
| 1 | `currentTabLabel` 3갈래 `:614` | **`:615`** |
| 2 | §6 "`duplicateAskQuestion`은 같은 target 안에서 `order: src.order + 1`(D11)"을 **변경**으로 적음 | 현행이 이미 `src.order + 1`에 target을 복사한다(`askQuestions.ts:78-87`) → **코드 변경 0** |

## 부록 C. v3 보완 (Z1~Z5)

| # | 내용 |
|---|---|
| **Z1** | **탭 기억이 질문을 보낼 때마다 지워진다.** v2 D1은 카테고리를 `mathory.ask.v1`의 `category`에 두라고 했는데, 현행 `rememberModels`가 `setItem(LS_KEY, JSON.stringify({ modelIds }))`로 **키 전체를 덮어쓰고** 전송마다 호출된다(`AskListPopover.tsx:45-46` · `:138`). 그대로 두면 [문제] 탭에서 질문을 보내는 순간 탭 기억이 사라져 다음 열기에 기본값으로 돌아간다. 처방: `loadPrefs()`·`savePrefs(patch)`로 읽어서 한 필드만 바꿔 쓴다. 검수 9 |
| **Z2** | **풀이 질문에서 `withTabs:false`는 뜻이 없다.** 문제 탭만 보내면 "풀이를 검증하라"는 질문이 풀이 없이 간다. v2는 체크박스를 대상과 무관하게 두었고 전송 안내 3갈래도 이 조합을 정의하지 않았다. 처방: `effectiveWithTabs(q)` 하나 — 체크박스는 대상 `문제`일 때만 보이고, 전송은 문제 질문에서만 탭을 뺀다. 저장값은 건드리지 않는다(대상을 다시 `문제`로 바꾸면 되살아난다). T8 ⑤·검수 8 |
| **Z3** | **편집에서 대상을 바꾸면 질문이 사라진 것처럼 보인다.** 저장은 목록을 다시 읽고 모달만 닫는다(`:150-168`). [문제] 탭에서 대상을 `풀이`로 바꿔 저장하면 그 질문은 [풀이] 탭으로 옮겨 가 지금 화면에서 사라진다 — 삭제된 것으로 오해하기 쉽다. 처방: 저장 후 대상 탭으로 전환. 검수 10 |
| **Z4** | **16000의 대가는 시간 초과다.** 280초 예산 · 사고 기본 켜짐 · 자발적 도구 턴. 잘림은 문구와 함께 저장되지만 시간 초과는 아무것도 남기지 않는다. §9에 관측·되돌림 규칙 |
| **Z5** | **D5′는 G 판본을 바꾼다.** 덕수가 기존 G1~G3를 고치면 rev가 오른다 — 그 전의 답과 섞어 비교하지 말 것. 66a 첫 실험은 E1로 유효한 답이 거의 없어 손실은 작다. §8·§9 |

---

*v3 착수판 — 구현 완료(2026-09-15, §11). 서버·규칙·검증 프롬프트·61b/61d/61h 0.*
