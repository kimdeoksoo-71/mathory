# 개선묶음 M1 구현 계획서 — 기존 Phase 보강 7건 (v2 CLI 실측 교차검토판)

> 계보: v1(web, 2026-08-26) → **v2(CLI 실측 교차검토·덕수 결정 반영, 2026-08-26)** → v3(web 재검증) 예정
> 진실 원천: mathory **origin/main `32c6904`** — v2의 모든 인용은 이 해시에서 **재확인**했다
> 성격: **새 Phase가 아니다.** 7건 모두 기존 Phase의 주제를 보강하는 것이라 한 묶음(M1)으로 구현하고,
> 문서는 각 항목이 속한 Phase 문서 + roadmap 해당 절을 찾아 갱신한다(§9). roadmap에는 Phase 절 대신
> 「UI 정리 (Phase 간 소규모)」(roadmap.md:1948) 관례를 따라 M1 표 한 개만 추가한다.
> 착수 시 CLAUDE.md 규칙 1에 따라 대상 파일을 반드시 다시 읽을 것.

---

## 0. v2에서 바뀐 것

### 0-1. 덕수 결정 (2026-08-26 — 추천안 전건 채택 + 표 블록 철회)

| # | 결정 | 귀결 |
|---|---|---|
| **E-M1-1** | ① 두께 통일은 **화면만**, 인쇄 600은 유지 | D17′ |
| **E-M1-2** | `case-ref`는 `preprocessLocale` 2사본 + **자기 보호 정규식** | D19′ |
| **E-M1-3** | 새 결정 규칙 2종은 **"자동 수정 N건" 카운트에 포함** | D23 |
| **E-M1-4** | tabular 변환 위치 = **step 0 뒤**(`$$`만 벗김) | D11′·D14′ |
| **E-M1-5** | 비활성 블록에서 수식행 분할 시 **결과(들여쓰기 블록)로 활성 이동** | D3′ |
| **E-M1-6** | 경우 블록 안 `$$` 분할도 **계획서대로 `callout`(들여쓰기) 삽입** | D7 확정 |
| **E-M1-7** | `findEnclosingDisplayMath`의 인라인 `$`·코드펜스 미인식은 **이번에 손대지 않는다** | D22 |
| **E-M1-8** | 체크박스 `accentColor` 3곳은 **범위 밖**(range 4곳만 손댄다) | D8 |
| **E-M1-9** | **F(표 블록 신설)를 철회한다** — 툴바에 이미 표 삽입 다이얼로그가 있다(§1 F′) | D18 철회 |

⚠ `callout`은 **타입 id**이고 화면 라벨은 **'들여쓰기'**다(`EditorView.tsx:94`). 코칭(`coach_important`·
`coach_caution`)과는 **별개 타입**이다 — 이 문서에서 "callout"은 전부 들여쓰기 블록을 가리킨다.

### 0-2. 정정 (v1의 사실 오류 — 전부 실측)

| # | v1 | v2 |
|---|---|---|
| **C1** | §3.3 `autoFixDeterministicIssues(text, opts?): string` | 반환은 **`{ fixed, count }`**(`proofread.ts:569`). `count`는 "문법 자동 수정 N건"으로 사용자에게 나간다(`EditorView.tsx:1394`) → 새 규칙도 count에 기여해야 한다(D23) |
| **C2** | D17 "globals·PrintStyles 두 곳의 600 → inherit" | **선례를 놓쳤다.** `globals.css:437-444`가 `.preview-content .marker-gana/.marker-giyeok { font-weight: inherit }`이고 주석이 "PrintStyles에서 새어 든 600을 **화면에서만** 되돌린다 — **인쇄만 600 유지(의도된 예외)**"라고 못박았다. `.marker-circled`는 그 처방에서 **빠진 것**이다 → 화면 스코프 1줄 추가로 끝난다(D17′) |
| **C3** | D20 `chatExtract:182` 허용목록에 `case-ref` 추가 | **버그를 만든다.** `MARKER_LITERAL`은 허용목록이 아니라 **뒤 공백을 한 칸 복원하는** 목록이다(`:353-357`의 `b.marker()`) → `C1에서`가 `C1 에서`로 샌다. `case-label`도 그 목록에 없다 — 일반 span은 이미 textContent로 통과한다 → **변경 0**(D20′) |
| **C4** | G-3 "코드펜스는 `protectFences`로 보호된다" | **절반만 맞다.** `protectFences`는 `EditorPreview:78`에만 있고 인쇄 경로(`lib/preprocess.ts`→`lib/locale.ts`)에는 **없다** → 보호를 호출부에 기대면 두 경로가 갈린다. `convertCaseRefs`가 **자기 보호**를 해야 한다(D19′) |
| **C5** | D11 tabular를 step −1(맨 앞) | `normalizeDisplayMathDelimiters`는 구분자만 바꾸고 내용을 그대로 흘린다(`:495-558` 실측) → **step 0 뒤**면 `\[ \]` 분기가 사라지고 `$$`만 벗기면 된다. OCR 경로(`normalizeAndFix`)도 이미 `\[`→`$$`를 먼저 한다(D11′) |
| **C6** | B-2 "저장소에 `accent-color` CSS가 없다" | CSS 파일엔 없지만 **인라인 `accentColor` 6곳**이 있다(`EditorPreview:266`·`PdfDialog:77`·`ShareSettingsPanel:281·355·409`). 단 `type="range"`는 **정확히 4곳**이라 D8의 대상은 유효하고, 나머지는 체크박스라 범위 밖이다 |
| **C7** | 앵커 | `.marker-circled`는 globals **460-465**(v1: 459-464) · `.case-label`에 **`margin-right: 0.4em`**이 있다(v1 누락) → `case-ref`가 그 마진을 물려받으면 본문 중간에 군더더기 간격이 생긴다 |

### 0-3. 보완 (v1에 없던 것)

- **S1** `handleSplitMathLines(blockId?)`는 **활성 블록이 아닐 수 있다**(하단 툴바가 blockId를 넘긴다,
  `BlockBottomToolbar.tsx:190-193` / 배선 `EditorView.tsx:3477`) → E-M1-5로 결정.
- **S2** 새 블록에 **`isNew: true`** 를 준다(`handleSplitBlock` 선례 — 없으면 포커스·스크롤 처리가 갈린다).
- **S3** `CASE_REF_RE`를 `caseBlock.ts`에 두고 `locale.ts`가 import하는 구조는 **`LEGACY_CASE_RE`와 같은
  선례**이고(`locale.ts:15`), `test:locale`이 `--rootDir .`이라 컴파일도 이미 통과한다.
- **S4** `lib/proofread.ts`·`lib/mathSplit.ts`는 **import 0**(실측) → 단독 컴파일 테스트 하니스가 성립한다.
- **S5** D12의 roman·choices 예외는 **OCR 경로에서 무력**하다 — OCR 삽입 시점의 블록은 아직 `text`다.
  (그 블록을 나중에 roman으로 바꿔도 변환은 이미 끝나 있다.) 알고 두는 것으로 충분하다.
- **S7** **Q6 해소**: `CommentEditor` 루트(`:248-250`, `fontFamily`만)와 headerLeft 래퍼 2겹(`:253-256`)
  모두 `position`·`transform`이 없다 → **D21은 본안 그대로 되고 `position:fixed` 폴백은 불필요**하다.
- **S8** D16의 "PrintStyles `.tag` 규칙에 `.print-body` 접두 추가"는 **안전**하다 — 공개 라우트(/p·/shared)는
  PrintStyles를 부르지 않고 globals의 같은 규칙을 받는다.
- **S9** `.callout-block`은 **이 용도로 설계된 블록**이다 — globals.css:588 주석: "수식 밖 텍스트 행에
  `\tag{n}`을 달 수 있어, 수식 흐름 중 한 행만 참조번호를 매길 때 쓴다." D1·D2가 기존 설계와 맞물린다.

---

## 1. 확정 사실 (전부 `32c6904` 실측)

### A. 수식행 분할

| # | 사실 | 위치 |
|---|---|---|
| A-1 | 분할 코어 `lib/mathSplit.ts`(108줄, **import 0**). `findEnclosingDisplayMath`(:23)는 `$$` 쌍을 좌→우로 훑어 커서를 품는 범위를 돌려준다. `splitDisplayMathAtCursor`(:91)가 단일 진입점 | `lib/mathSplit.ts` |
| A-2 | `BLOCKED_ENVS`(:42)에 **`array`가 포함**돼 있다. 검사는 `$$` body 전체 대상 | :42, :97-99 |
| A-3 | aligned/align/align\*는 차단이 아니라 **태그 제거**(:57-60) 후 `\\`(옵션 `[..]` 허용)로 분할(:63), `&` 제거(`\&` 보존, :66-73), 빈 행 제거, 2행 미만이면 실패(:75, :101-103) | :57-80 |
| A-4 | 출력은 **항상 `$$`**: `rows.map(r => "$$\n"+r+"\n$$").join("\n\n")`(:83-85) | :83-106 |
| A-5 | `handleSplitMathLines`(`EditorView.tsx:1504-1521`)는 **`pushUndo()`를 부르지 않고**(`handleSplitBlock`:1715·`handleAddBlock`은 부른다) 실패 `setStatus`에 **리셋 타이머가 없다**(다른 호출부 :1360-1361 등은 2초 후 지운다) | `EditorView.tsx` |
| A-6 | 진입 2곳: 단축키 `⌘⇧L`(:2411-2414, `e.code==='KeyL'`) · 하단 툴바(`BlockBottomToolbar.tsx:190-193`, 배선 :3477 — **blockId를 넘긴다**) | 실측 |
| A-7 | "들여쓰기" 블록 = 타입 id **`callout`**(라벨만 다름, `EditorView.tsx:94`). `.callout-block`(globals.css:589-598)은 **블록 안 여러 행을 행간만으로** 그린다(`.callout-block .preview-content p { margin:0 }`, :598)고 주석이 명시하고, 용도로 **"수식 밖 텍스트 행 `\tag{n}`"** 을 못박았다(:588) | globals.css |
| A-8 | 블록 N분할 헬퍼는 없다. `handleSplitBlock`(:1707-1751)은 커서 기준 2분할이고 꼬리 블록은 `type:'text'`·`block_key: nanoid()`·`isNew:true`(원본은 키 유지) | `EditorView.tsx` |
| A-9 | 렌더 파이프라인에 `remark-breaks`가 **없다**(`EditorPreview.tsx:297` = `[remarkMath, remarkGfm]`) → 연속 행은 한 문단으로 합쳐진다. **행을 나누려면 빈 줄이 필요하다** | 실측 |
| A-10 | `preprocessMath`가 인라인 `$x$`를 `$\displaystyle x$`로 바꾼다(`EditorPreview.tsx:213-221`) → `$…$`로 넣어도 분수·√ 크기는 display와 같다. 그리고 **KaTeX는 인라인 모드에서 `\tag`를 거부한다** | 실측 + KaTeX 사양 |
| A-11 | `mathSplit` 테스트 없음 | `tests/` |

### B. 그림 조작 인터페이스

| # | 사실 | 위치 |
|---|---|---|
| B-1 | 이미지 블록 UI는 `EditorView.tsx:234-485`의 `MediaBlockContent` 안 인라인. 미리보기 쪽 리사이즈 오버레이는 `EditorPreview.tsx:225-269` | 실측 |
| B-2 | `type="range"`는 **정확히 4곳**(EditorView :313 SVG · :368 GGB · :420 이미지 / EditorPreview :264). CSS 파일에 range 관련 규칙 **0건** → 트랙·노브는 **UA 기본 파랑**이다. 인라인 `accentColor`는 미리보기 오버레이(:266)에만 있고 그 폴백 `#B8845C`는 stale | 실측 |
| B-3 | 두께·노브 크기는 어디서도 지정되지 않은 UA 기본값. `accent-color`로는 두께를 못 바꾼다 → 벤더 의사요소 필요 | 실측 |
| B-4 | 버튼은 **2단 배치**: 1행 `액자`·`컬러 유지`(:431-457, flex·center·gap 6), 2행 `그림 변경`(:459-471, 별도 div). off 배경 `--bg-hover`, on `--accent-primary`(#c96442)+흰 글자, `1px solid --border-light`, radius 6, `4px 12px`, 12px | :431-471 |
| B-5 | 로고 토큰 `--mathory-red #D97757` · `--mathory-red-dark #BC5F3F`(globals.css:97-98). 주석(:141-146): `#D97757`은 클레이 배경에서 2.73:1로 **상태 표시에는 미달**, 그런 용도엔 dark를 쓴다 | globals.css |
| B-6 | 한 단계 어두운 배경 토큰: `--bg-hover #F0EBE3` → `--bg-active #E8E2D9` | globals.css |

### C. proofread / OCR

| # | 사실 | 위치 |
|---|---|---|
| C-1 | 교정은 **결정 규칙(클라) → LLM(haiku, 마스킹 후)** 2단. `handleRunProofread`(`EditorView.tsx:1353-1397`)가 먼저 `applyDeterministicAutoFix`(:1328-1351)로 편집기에 직접 반영하고, 결과를 **"문법 자동 수정 N건"**(:1394)으로 알린다 | `EditorView.tsx` |
| C-2 | `autoFixDeterministicIssues`(`lib/proofread.ts:569-646`)는 **`{ fixed, count }`** 를 돌려준다. 순서: step 0 `normalizeDisplayMathDelimiters` → 0a `autoWrapBareNumbers` → 0b `autoWrapBareLetters` → 1 `^x`/`_x` 중괄호·수식 내 `,` → 2 수식-조사 공백 | `lib/proofread.ts` |
| C-3 | **OCR 후처리가 같은 함수를 재사용**한다: `lib/ocr.ts:79-95 normalizeAndFix`가 `\[`→`$$`, `\(`→`$` 정규화 후 `autoFixDeterministicIssues`(:93) 호출. 호출부 `EditorView.tsx:2188`·`CommentEditor.tsx:114` → 규칙을 여기에 넣으면 **교정 버튼과 OCR 삽입 둘 다** 적용된다 | `lib/ocr.ts` |
| C-4 | `(1)` 참조번호는 수식화에서 보호된다(`addAll(/\(\d+\)/g)`, :369). `\tag`/`\ref`도 보호·마스킹(:122, :368, :452) | `lib/proofread.ts` |
| C-5 | 저장소 전체에 **`(ㄱ)`(괄호 자모)를 잡는 정규식이 없다.** `GIYEOK_LITERAL_RE`(`locale.ts:59`)는 `ㄱ.`(행 시작)만, `GANA_LITERAL_RE`(:56)는 `(가)`만, `autoWrapBareLetters`의 괄호 예외(:456)는 `\([a-zA-Z]\)`만 → `(ㄱ)`은 마스킹 없이 haiku로 간다 | 실측 |
| C-6 | `\begin{tabular}` 변환은 **존재하지 않는다**(`tabular` 검색 = `tabular-nums`뿐). GFM 표는 `remarkGfm`이 렌더하고 스타일은 `EditorPreview.tsx:352-378` 인라인 · 인쇄는 `PrintStyles.css:97-98` | 실측 |
| C-7 | `autoWrapBareNumbers`·`autoWrapBareLetters`는 표 문법을 보호하지 않는다 → 표 셀의 맨 숫자 `0`은 `$0$`이 된다. **메모의 변환 목표(`| $0$ |`, `^{3}`)가 바로 이 동작**이므로 표 변환은 이 두 규칙 **앞**에 놓아야 한다 | `:325, :410` |
| C-8 | `lib/proofread.ts` 테스트 없음. **import 0**이라 단독 컴파일 하니스가 성립 | 실측 |
| C-9 | 표 생성기 `buildMarkdownTable(r, c)`(`UnifiedToolbar.tsx:650-661`) = 헤더 1행 + `:---:` 정렬행 + 본문 `r-1`행, 앞뒤 `\n\n` 보장 → `(5, 2)` = **5행 × 2열** | 실측 |

### D. `\tag{N}` 세로 위치

| # | 사실 | 위치 |
|---|---|---|
| D-1 | 두 경로: 텍스트 행 꼬리 `\tag{n}` → `<span class="tag-marker">(n)</span>`(`locale.ts:187-190`, 정규식 `/\\tag\{(\d+)\}\s*$/gm` — **수식 보호 후**라 `$…$ \tag{3}` 형태도 잡힌다) · 수식 안 `\tag{n}` → `\tag*{(n)}`(`preprocess.ts:126-128`) | 실측 |
| D-2 | `.tag-marker`(globals.css:475-481): `float:right; white-space:nowrap; margin-left:2em; font: inherit` — **세로 정렬 속성 없음** | globals.css |
| D-3 | 수식 안 태그: `.katex-display > .katex > .katex-html > .tag { top:auto; bottom:0.79em; line-height:1 }` + `.strut{display:none}`(globals.css:483-496). 주석이 "0.85em은 14px 기준 눈맞춤 값 → 0.79에서 **출발**"이라고 적어, 현재 값이 **비례 환산 출발값**임을 명시. 인쇄 사본 `PrintStyles.css:110-117`은 **접두 없음**(화면에도 걸린다) | 실측 |
| D-4 | 다행 `$$`는 `\begin{array}{l}`로 감싸고(`EditorPreview.tsx:186-199`) `\arraystretch:1.8`(:300). 환경이 이미 있으면 감싸지 않는다(:189-190) → **단일행 / array{l} / aligned 세 구조**가 같은 `bottom:0.79em`을 공유 | 실측 |

### E. 원문자 두께

| # | 사실 | 위치 |
|---|---|---|
| E-1 | 원문자는 `@font-face 'MathoryCircled'`(globals.css:37-46, `local('AppleGothic')`… `unicode-range: U+2460-2473`, `size-adjust:88%`)를 `--font-ui`/`--font-print` 스택 맨 앞에 끼우는 방식. **전용 클래스 없음** | globals.css |
| E-2 | 행머리 `①`만 `<span class="marker-circled">`로 감싸지고(`locale.ts:64`), `globals.css:460-465`가 **`font-weight: 600`**. 문장 중간 `①`은 감싸지지 않아 400 | globals.css |
| **E-3′** | **(v2 신규 — 결정적)** `(가)`·`ㄱ.` 마커는 **이미 같은 문제를 겪고 고쳐졌다**: `globals.css:437-444`가 `.preview-content .marker-gana/.marker-giyeok { font-weight: inherit }`이고 주석이 "PrintStyles에서 새어 들어와 화면에도 걸려 있던 600을 명시적으로 되돌린다. **인쇄만 600을 유지한다(의도된 예외)**". `.marker-circled`만 그 처방에서 빠졌다 | globals.css |
| E-4 | `MathoryCircled`가 잡는 로컬 폰트(AppleGothic 등)에 600 페이스가 없어 브라우저가 **가짜 굵게**를 그린다 → 체감 두께 차의 유력 원인. `::marker`·`counter(`는 저장소 0건(원문자 목록은 `<ol>`이 아니라 `<p>`+span) | 추정(스텝 0에서 확인) |

### F′. 표 블록 — **철회 (2026-08-26)**

> **되살리지 말 것.** 툴바에 **표 삽입 다이얼로그가 이미 있다**: `UnifiedToolbar.tsx:846`의 [표 삽입]
> 버튼 → `TableInsertDialog`(:540-600, 행·열 숫자 입력, 기본 3×3, 상한 50×20) →
> `insertTable`(:765-769) → `buildMarkdownTable(r, c)`(:650-661)가 **가운데 정렬 GFM 표**를 넣는다.
> v1이 표 블록을 제안한 것은 이 버튼의 존재를 잊고 표를 손으로 치고 있었기 때문이고(덕수 확인),
> 블록 타입을 새로 만들어도 **내용은 같은 마크다운 문자열**이라 얻는 것이 없다.
> 남는 것은 레지스트리 5곳·union·프리셋이라는 유지 비용뿐이다.
>
> ⚠ 표를 다루는 이 묶음의 유일한 작업은 **C의 `tabular` → md 변환**이고, 그 출력 형식은
> `buildMarkdownTable`의 관례(헤더 1행 + `:---:` 정렬행 + 앞뒤 빈 줄)를 따른다(D14′-c·d).

### G. 본문 `C1` 강조

| # | 사실 | 위치 |
|---|---|---|
| G-1 | 경우 라벨은 텍스트에서 찾지 않고 블록 타입으로 **계산**한다: `buildCaseLabels`(`lib/caseBlock.ts:67-92`) → `injectCaseLabel`(:99-114)이 제목행 앞에 `<span class="case-label">C1.</span> `를 끼운다. 렌더 5사이트는 **이미 라벨이 박힌 문자열**을 `EditorPreview`에 넘긴다 | `lib/caseBlock.ts` |
| G-2 | `.case-label`(globals.css:732-736): **`margin-right: 0.4em`** + `font-weight: var(--weight-semibold)` + `tabular-nums`. 제목행 텍스트 500(:740), 본문 400 | globals.css |
| **G-3′** | 인라인 span 주입 지점 = `preprocessLocale` **두 사본**(`lib/locale.ts:206-228` · `EditorPreview.tsx:94-174`). 수식은 `⟦MATH_n⟧`로 보호되지만 **코드펜스 보호(`protectFences`)는 EditorPreview 사본에만 있다**(:78) — 인쇄 경로(`lib/preprocess.ts:191`)에는 없다. 기존 주입 규칙은 전부 `^`/`$` 앵커라 **행 중간 매처는 이번이 첫 사례** | 실측 |
| **G-4′** | `lib/chatExtract.ts:181-183 MARKER_LITERAL`은 허용목록이 **아니라** "뒤 공백 한 칸 복원" 목록이다(`:353-357`). `case-label`도 여기 없다 → 일반 span은 textContent로 통과한다 | 실측 |

### H. 검증 확인 팝오버

| # | 사실 | 위치 |
|---|---|---|
| H-1 | 팝오버 = `VerifyChips`(`CommentPanel.tsx:1317-1404`) 내부 confirm 박스(:1363-1401): `position:absolute; bottom:calc(100% + 6px); left:0; width:250; zIndex:20` | 실측 |
| H-2 | containing block = `VerifyChips` 래퍼 `<span style={{position:'relative', display:'inline-flex'}}>`(:1336). 이 span은 헤더 flex 행(:1036-1057)에서 `AIChipBar` **뒤**에 놓여, 왼쪽 끝이 모델 칩이 끝난 지점에서 시작해 250px를 뻗는다. 패널 폭 420(`EditorView.tsx:993`) − 좌우 패딩 32 = 388px → **우측으로 넘친다** | 실측 |
| H-3 | 칩이 다음 줄로 접히면 x≈0에서 시작해 정상으로 보인다 → 모델 수·패널 폭에 따라 **간헐적**인 버그 | 실측 |
| **H-4′** | 컴포저 래퍼(:978-982)는 `position:relative`·패널 정폭. 그 아래 `CommentEditor` 루트(:248-250, `fontFamily`만)와 headerLeft 래퍼 2겹(:253-256)에는 `position`·`transform`이 **없다** → 래퍼의 `position:relative`만 걷어내면 컴포저가 곧바로 기준이 된다(**폴백 불필요**) | 실측 |

---

## 2. 결정표

### A. 수식행 분할

| # | 결정 |
|---|---|
| **D1** | **결과 = 들여쓰기(`callout`) 블록 1개, 행마다 `$…$` 한 줄, 행 사이 빈 줄.** 빈 줄이 없으면 한 문단으로 합쳐지고(A-9), 빈 줄을 두면 `.callout-block .preview-content p{margin:0}`(A-7)이 "행간만"으로 그려 준다. 하드브레이크(`  `·`\`)는 쓰지 않는다 — 트레일링 공백은 편집기·정규화에 취약하고 `\`는 LaTeX와 헷갈린다 |
| **D2** | **행 안 `\tag{n}`은 `$…$` 밖으로 뺀다**: `x=1 \tag{3}` → `$x = 1$ \tag{3}`. 인라인 모드 `\tag`는 KaTeX 오류이고(A-10), `convertTextTags`가 수식 보호 뒤에 돌아 이 형태를 정확히 잡는다(D-1). `\tag`가 없는 행은 그대로 |
| **D3′** | **블록 3분할**: 원본 X의 raw를 `before` / `$$…$$` / `after`로 나눠 ❶ `before`가 비어 있지 않으면 X는 키를 유지한 채 `before`만 남기고 새 `callout` 블록(새 `block_key`, `isNew:true`)을 X 바로 뒤에 삽입 ❷ `before`가 공백뿐이면 **X 자체를 `callout`으로 타입 변경**(키 유지)하고 raw를 행들로 교체 ❸ `after`가 비어 있지 않으면 callout 뒤에 `type:'text'` 블록(새 키, `isNew:true`)을 붙인다(`handleSplitBlock` 관례). **활성 블록은 결과 callout으로 이동한다 — 호출이 비활성 블록에서 왔더라도(E-M1-5, S1·S2)** |
| **D4** | **`array` 환경도 aligned와 같은 방식으로 분할**: `\begin{array}{<열지정>}`…`\end{array}` 태그를 제거하고 `\\`로 분할, `&` 제거. `BLOCKED_ENVS`에서 `array`를 뺀다. **단 body에 `\hline`이 있으면 표이므로 차단 유지**(전용 사유 문구). cases·matrix 계열·gathered·split은 기존 정책 유지 |
| **D5** | **`pushUndo()` 추가**(A-5) — 블록 구조를 바꾸는 작업이므로 블록 Undo 스택에 올린다. 실패 `setStatus`에도 다른 호출부와 같은 **2초 리셋**을 붙인다 |
| **D6** | `lib/mathSplit.ts`는 **import 0 순수 함수로 유지**. 새 API `splitDisplayMathToRows(content, cursor)`가 `{ ok, before, rows, after }`(rows는 D2 적용 완료 문자열)를 돌려주고 **블록 조립은 `EditorView`가** 한다. `tests/mathSplit.test.mjs` 신설 |
| **D7** | X가 `case`/`subcase`/`callout`/`gana`/`roman`/`box`일 때도 D3′를 그대로 적용한다(E-M1-6). 경우 블록 뒤에 callout이 붙으면 rail은 `case-gap`이 관통하므로 끊기지 않는다 |
| **D22** | **(v2 신규)** `findEnclosingDisplayMath`의 인라인 `$`·코드펜스 미인식은 **이번에 손대지 않는다**(E-M1-7). 별건이고 고치면 코어 재작성이다 → **현 동작을 테스트로 고정만** 한다(회귀 감지용) |

### B. 그림 조작 인터페이스

| # | 결정 |
|---|---|
| **D8** | **`.mathory-range` 클래스를 globals.css에 신설**하고 `type="range"` **4곳**(이미지·SVG·GGB 블록 + 미리보기 오버레이)에 적용한다. `appearance:none`, 트랙 높이 **2px**(`--border-light` 바탕), 진행 구간·노브 **`var(--mathory-red)`**, 노브 지름 **12px** 원(그림자 없음, 테두리 없음). 벤더 의사요소(`::-webkit-slider-runnable-track/-thumb`, `::-moz-range-track/-thumb/-progress`) 모두 작성. 기존 인라인 `accentColor`(EditorPreview:266)는 제거. ⚠ **체크박스 `accentColor` 3곳은 범위 밖**(E-M1-8) |
| **D9** | 진행 구간은 `background: linear-gradient(to right, <red> 0 var(--p), <track> var(--p))`로, `--p`는 인라인 style로 값%를 내려 준다(webkit에 `::-moz-range-progress`가 없어 이 방식이 공통). 색은 로고 레드 `#D97757` — 명시비 미달(B-5)은 **알고 쓴다**: 상태 표시가 아니라 조작 대상이고 노브가 12px 면적이다. 실물에서 흐리면 `--mathory-red-dark`로 폴백 |
| **D10** | **버튼 3개 1열**: `액자` · `컬러 유지` · `그림 변경`을 한 flex 행(`justifyContent:center; gap:6`)에. `그림 변경`은 토글이 아니므로 off 배경을 한 단계 어두운 **`var(--bg-active)`**(B-6)로 두어 구별한다. 나머지 치수는 기존과 동일. 2행 div(:459-471) 제거. SVG·GGB의 "변경" 버튼도 같은 키로 맞춘다 |

### C. proofread 규칙 2종

| # | 결정 |
|---|---|
| **D11′** | 두 규칙 모두 `autoFixDeterministicIssues`에 넣는다 → 교정 버튼·OCR 삽입 양쪽에 자동 적용(C-3). 위치는 **step 0(`normalizeDisplayMathDelimiters`) 뒤, step 0a 앞**(E-M1-4): `convertTabularToMarkdown` → `convertJamoRefs` → 기존 0a·0b·1·2. 이 자리면 `\[ \]`는 이미 `$$`가 되어 있어 **래퍼 분기가 하나로 줄고**, 표는 숫자·문자 수식화(C-7)보다 먼저 md가 된다 |
| **D12** | **`(ㄱ)`→`(1)` 규칙**: 수식·코드 밖 텍스트에서 `\((ㄱ|ㄴ|ㄷ|ㄹ|ㅁ|ㅂ|ㅅ|ㅇ|ㅈ|ㅊ)\)` → `(1)`…`(10)`. 행머리·행중 모두(참조는 "(ㄱ)에서"처럼 문장 중간에 온다). `ㄱ.`(합답형 보기)와는 문법이 달라 충돌 없다(C-5). **단 블록 타입이 `roman`·`choices`면 건너뛴다** — 그 블록에서는 `(ㄱ)`이 보기 라벨일 수 있다. 이를 위해 `autoFixDeterministicIssues(text, opts?: { skipJamoRefs?: boolean })`를 추가하고 `applyDeterministicAutoFix`가 블록 타입으로 넘긴다. ⚠ **OCR 경로(`normalizeAndFix`)는 블록 타입을 모르므로 기본(변환)으로 돈다**(S5) |
| **D13** | 변환된 `(1)`은 이미 수식화 보호 대상(C-4)이라 `$1$` 오염 없음. 마스킹(:452)에 `(ㄱ)` 분기를 **추가하지 않는다** — 결정 규칙이 먼저 돌아 haiku가 볼 때는 이미 `(1)`이다 |
| **D14′** | **`convertTabularToMarkdown(text)`**: (a) `\begin{tabular}[옵션]{열지정} … \end{tabular}`를 찾되, **바로 감싼 `$$…$$`가 있으면 함께 제거**한다(`\[ \]`는 step 0이 이미 `$$`로 바꿨다 — 분기 불필요) (b) `\hline` 제거, `\\`로 행 분할, `\&`를 보호한 채 `&`로 셀 분할, 셀 trim (c) 첫 행 = 헤더, 정렬행은 **전 열 `:---:`**, 나머지 = 본문 (d) 결과 앞뒤 빈 줄 보장(GFM, C-9와 동일) (e) `\multicolumn`·`\multirow`·`\cline`·중첩 `tabular`가 있으면 **손대지 않는다**(조용한 오변환 금지). 셀 안 `$…$`는 그대로 두고, 맨 숫자·`^3` 정리는 뒤 규칙이 한다 |
| **D15** | **`tests/proofread.test.mjs` 신설**(C-8·S4): 메모의 tabular 원문 → 목표 md를 **문자열 단위 기댓값**으로 고정한 케이스 + `(ㄱ)` 케이스(행머리·행중·수식 안 비변환·`ㄱ.` 비변환·skip 옵션) + 예외(`\multicolumn` 비변환) + **전체 파이프라인 통과 후**(`autoFixDeterministicIssues`) 결과 1건. 하니스는 61a·61c 관례 |
| **D23** | **(v2 신규)** 새 규칙 2종은 **`count`에 기여한다**(E-M1-3·C1): 표 1개 변환 = +1, `(ㄱ)` 치환 = 치환 건수만큼. "문법 자동 수정 N건"(`EditorView.tsx:1394`)이 실제 편집량을 반영해야 한다 |

### D·E. \tag 위치 · 원문자 두께

| # | 결정 |
|---|---|
| **D16** | \tag는 **"실측 → 필요 시 재보정"**. 메커니즘(`top:auto; bottom:Nem; strut 제거`)은 유지하고, 3구조(단일행 `$$` · array{l} 다행 · aligned 다행) + 텍스트 행 `.tag-marker` **4케이스**에서 "번호 글리프 박스의 세로 중심 − 마지막 행 라인박스의 세로 중심" 차를 px로 잰다(DevTools `getBoundingClientRect`). 합격 **±1px**. 한 값으로 안 맞으면 `.katex-display .tag`와 `.tag-marker`를 따로 보정한다. 인쇄 사본(D-3)은 같은 값으로 갱신하고 **이번에 `.print-body` 접두를 붙인다**(S8 — 공개 라우트는 PrintStyles를 부르지 않으므로 안전) |
| **D17′** | **원문자 두께는 화면 스코프 1줄로 끝낸다**(E-M1-1·C2·E-3′): `globals.css`에 **`.preview-content .marker-circled { font-weight: inherit; }`** 추가. 특이도 (0,2,0)이라 globals의 `.marker-circled`(0,1,0)와 PrintStyles의 미접두 사본(0,1,0)을 **로드 순서와 무관하게** 이긴다. **인쇄 600은 그대로 둔다** — `(가)`·`ㄱ.`이 이미 "화면 inherit / 인쇄 600"이고, 원문자만 인쇄에서 얇아지면 같은 문서 안에서 새로 어긋난다 |

### F. 표 블록 — 철회

| # | 결정 |
|---|---|
| ~~**D18**~~ | **철회(E-M1-9).** 툴바 [표 삽입] 다이얼로그가 이미 같은 일을 한다(§1 F′). `lib/markdownTable.ts` 추출·union 확장·레지스트리 등록·프리셋 모두 **하지 않는다** |

### G. 본문 C1 강조

| # | 결정 |
|---|---|
| **D19′** | **`CASE_REF_RE`를 `lib/caseBlock.ts`에 export**(라벨 문법의 단일 원천, G-1 옆): `/(?<![A-Za-z0-9_\\])C(\d{1,2})([a-z]?)(?![A-Za-z0-9_])/g`. 주입은 `convertCaseRefs(text)` 한 함수가 맡고 **`preprocessLocale` 두 사본의 마지막 텍스트 변환 단계**(`convertTextTags` 뒤, 수식 복원 전)에 넣는다. ⚠ **보호는 이 함수가 스스로 한다**(C4·G-3′ — 인쇄 경로에는 `protectFences`가 없다): ❶ ``` / `~~~` 펜스 ❷ 인라인 코드(백틱 런 매칭) ❸ HTML 태그 전체(`<[^>\n]+>`)와 **`<span class="case-label">…</span>` 요소 통째** ❹ 이미 `⟦MATH_n⟧`인 수식. 치환 결과는 `<span class="case-ref">C1a</span>`. `\b` 대신 lookaround인 이유는 한글 조사(`C1에서`)를 살리면서 `\mathrm{C}`·`C_1` 같은 LaTeX 잔재를 배제하기 위함 |
| **D20′** | CSS만 추가: `.case-ref { font-weight: var(--weight-semibold); font-variant-numeric: tabular-nums; }` — **`margin-right`는 주지 않는다**(C7: `.case-label`의 0.4em은 라벨 전용이다). 색·크기 변경 없음. `PrintStyles.css`에 `.print-body .case-ref` 동일값. ⚠ **`lib/chatExtract.ts`는 변경 0**(C3) — `MARKER_LITERAL`에 넣으면 `C1에서`가 `C1 에서`로 샌다 |

### H. 검증 팝오버 위치

| # | 결정 |
|---|---|
| **D21′** | `VerifyChips` 래퍼 span(:1336)에서 **`position:'relative'`를 제거**하고 팝오버를 `left:50%; transform:translateX(-50%); bottom:calc(100% + 6px)`로 바꾼다. 중간 조상에 positioned 요소가 없으므로(H-4′) 컴포저 래퍼(:978-982, 패널 정폭)가 기준이 되어 **패널 가로 중앙·컴포저 바로 위**에 뜬다. `width:250 ≤ 388`이라 클램프 불필요, zIndex 20 > 리사이즈 핸들 5. **`position:fixed` 폴백은 필요 없다**(S7). 같은 패턴의 첨부 메뉴(:1747)는 왼쪽에 붙어 있어 정상이므로 손대지 않는다 |

---

## 3. 구현 항목

### 3.1 A — `lib/mathSplit.ts` + `handleSplitMathLines`

```ts
// lib/mathSplit.ts (import 0 유지) — 신규 API
export interface RowSplit { before: string; rows: string[]; after: string }
export function splitDisplayMathToRows(content: string, cursor: number):
  ({ ok: true } & RowSplit) | { ok: false; reason: string };
//  · rows[i] = "$…$" 또는 "$…$ \tag{n}"  (D2)
//  · BLOCKED_ENVS에서 array 제거, 대신 body에 \begin{array} && \hline 이면 차단 (D4)
//  · array 태그 제거 정규식 추가: /\\begin\{array\}\{[^}]*\}/g, /\\end\{array\}/g
//  · 행별 \tag 추출: /\\tag\{(\d+)\}/ 1개 초과면 실패
//  · 기존 export(findEnclosingDisplayMath·splitDisplayMathBody·hasBlockedEnvironment)는 유지,
//    buildSplitReplacement·splitDisplayMathAtCursor는 사용처 0이 되면 제거
```

`EditorView.handleSplitMathLines`(:1504-1521) 재작성 — D3′·D5:

```
pushUndo()
r = splitDisplayMathToRows(content, cursor)
실패 → setStatus(r.reason); setTimeout(()=>setStatus(''), 2000); return
rowsRaw = r.rows.join('\n\n')                     // D1: 행 사이 빈 줄
before 공백 → X를 callout으로 타입 변경(키 유지) + raw = rowsRaw
그 외      → X.raw = before, 새 callout 블록(nanoid·isNew) 삽입
after 있음 → callout 뒤에 type:'text' 블록(nanoid·isNew)
setActiveBlockId(callout)                          // 비활성 호출이어도 이동 (E-M1-5)
```

`tests/mathSplit.test.mjs` + `package.json`의 `test:mathsplit`(단독 tsc — S4).

### 3.2 B — `.mathory-range` + 버튼 1열

`app/globals.css`에 `.mathory-range` 블록(D8·D9) · `type="range"` 4곳에
`className="mathory-range"` + `style={{ ['--p' as any]: `${pct}%` }}`(pct = (value−min)/(max−min)×100) ·
`EditorPreview:266`의 `accentColor` 제거 · `EditorView.tsx:431-471`을 단일 flex 행으로(D10).

### 3.3 C — proofread 규칙 2종 + 테스트

```ts
export function convertTabularToMarkdown(text: string): { fixed: string; count: number }   // D14′
export function convertJamoRefs(text: string): { fixed: string; count: number }            // D12
export function autoFixDeterministicIssues(
  text: string, opts?: { skipJamoRefs?: boolean },
): { fixed: string; count: number }      // ⚠ 반환 형태 불변 (C1) — count에 두 규칙 합산 (D23)
//  step 0 → [신규] convertTabularToMarkdown → [신규] convertJamoRefs(opts) → step 0a → 0b → 1 → 2
```

`EditorView.applyDeterministicAutoFix`(:1328-1351)와 `handleRetryProofreadBlock`(:1399-1423)에서
`{ skipJamoRefs: b.type === 'roman' || b.type === 'choices' }` 전달. `lib/ocr.ts:93`은 무인자 유지(S5).
`tests/proofread.test.mjs` + `test:proofread` 스크립트(D15).

### 3.4 D·E — CSS

- **D16**: 스텝 0 실측 후 `globals.css:491`(`bottom`)·필요 시 `.tag-marker` 보정,
  `PrintStyles.css:110-117` 동기화 + **`.print-body` 접두 추가**.
- **D17′**: `globals.css`에 `.preview-content .marker-circled { font-weight: inherit; }` **1줄 추가**
  (기존 `:462`의 600과 PrintStyles 미접두 사본은 **건드리지 않는다** — 인쇄 600 유지).

### 3.5 G — `case-ref`

`lib/caseBlock.ts`에 `CASE_REF_RE` + `convertCaseRefs`(자기 보호 포함, D19′) ·
`lib/locale.ts preprocessLocale`·`EditorPreview.preprocessLocale` **양쪽**에 단계 추가 ·
`globals.css`·`PrintStyles.css`에 `.case-ref`. **`chatExtract` 변경 0.**

### 3.6 H — 팝오버

`CommentPanel.tsx:1336`에서 `position:'relative'` 제거 · `:1363-1366`을 `left:'50%', transform:'translateX(-50%)'`로.

---

## 4. 작업 순서

| 스텝 | 내용 | 완료 기준 |
|---|---|---|
| 0 | **정적 실측**(코드 변경 0): (a) D16 4케이스 px 측정표 (b) E-4 확인 — `.marker-circled`를 400으로 임시 토글해 두께 차가 사라지는지 (c) Mathpix `text` 포맷이 `tabular`를 `$$`로 감싸는지 실물 1건 | 측정표를 v3 §1에 첨부. (c) 결과로 D14′(a) 유지/축소 확정 |
| 1 | **A** `mathSplit` 개편 + `tests/mathSplit.test.mjs` → `handleSplitMathLines` | T1~T5 통과 · 실물에서 aligned 3행 → 들여쓰기 블록 3행 |
| 2 | **C** 규칙 2종 + `tests/proofread.test.mjs` → 최소 배선 | 메모 tabular 원문이 목표 md와 **문자열 일치** · OCR 삽입 경로에서도 동작 · count 증가 확인 |
| 3 | **G** case-ref · **H** 팝오버 | T12~T13 |
| 4 | **B·D·E** 시각 3건을 한 번에 → **덕수 실물 검수(유일한 중간 관문)** | 슬라이더 색·두께·노브, 버튼 1열, \tag 중심 ±1px, 원문자 두께 동일 |
| 5 | 문서: §9 매핑대로 Phase 문서·roadmap 갱신, CLAUDE.md에 신규 규약 1~2줄 | 커밋(push는 덕수) |

## 5. 검증 체크리스트

- **A** T1 `aligned` 3행(`&`·`\\[4pt]` 포함) → 들여쓰기 블록 3행, `&` 제거, 빈 줄 분리 ·
  T2 `array{ll}` 2행 → 동일 분할 / `array`+`\hline` → 차단 메시지 후 2초 사라짐 ·
  T3 `cases`/`pmatrix` → 기존 차단 유지 · T4 행 안 `\tag{2}` → `$…$ \tag{2}`로 나가고 미리보기 우단에 (2) ·
  T5 `before`/`after` 4조합에서 블록 수·키·활성 블록·Undo 1회 복원 · **T5′ 비활성 블록(하단 툴바)에서 실행해도 활성이 결과로 이동**
- **B** T6 4개 슬라이더 색·트랙 2px·노브 12px 일치(Chrome·Safari) · T7 버튼 3개 1열, `그림 변경` 배경이 한 단계 어두움, 토글 on 시 불변
- **C** T8 메모 tabular → 목표 md 문자열 일치 · T8′ `(ㄱ)에서 (ㄴ)은` → `(1)에서 (2)는`, `ㄱ. 옳다` 불변, `$(ㄱ)$` 불변, roman·choices 블록 불변 · T8″ `\multicolumn` 포함 표 불변 · T8‴ 두 규칙이 count에 반영
- **D·E** T9 4케이스 \tag 중심 ±1px(화면·인쇄) · T10 행머리 ①과 문장 중 ① 두께 동일(화면), **인쇄는 600 유지 확인**
- **G** T12 본문 `C1`·`C2a`·`C1에서` 600 / `\mathrm{C}_1`·`` `C1` ``·코드펜스 안·기존 `case-label` 불변(이중 래핑 0) · 61c 직렬화로 `C1` 텍스트 보존(공백 삽입 없음)
- **H** T13 모델 칩 1개·4개, 패널 폭 320·600에서 팝오버가 항상 패널 가로 중앙

## 6. 하지 말 것 / 주의

- `EditorPreview`·`lib/locale.ts` 전처리 **두 사본을 같이 고칠 것**(CLAUDE.md). 새 정규식 상수는 `lib/caseBlock.ts`에만 두고 사본 금지.
- **보호를 호출부에 기대지 말 것** — 인쇄 경로에는 `protectFences`가 없다(C4). `convertCaseRefs`가 자기 보호를 한다.
- `$$` 앞뒤 빈 줄 정규화는 `toPersistedBlock`(`normalizeDisplayMathSpacing`) 소유 → 분할 결과에 중복 구현 금지.
- **`.marker-circled`의 인쇄 600을 건드리지 말 것**(D17′) — `(가)`·`ㄱ.`과 같은 의도된 예외다.
- **`chatExtract.MARKER_LITERAL`에 `case-ref`를 넣지 말 것**(C3) — 공백이 새어 표기가 달라진다.
- `.case-label`의 `margin-right:0.4em`을 `case-ref`가 물려받지 않게 할 것(C7).
- 700 웨이트 금지(globals.css:119-123). 다크 토큰 없음. `scrollIntoView` 금지.
- `tabular` 변환은 확신 없는 형태(`\multicolumn` 등)를 **그대로 둔다**. 절반만 변환된 표를 내보내지 말 것.
- **표 블록을 다시 제안하지 말 것**(E-M1-9) — 툴바 [표 삽입] 다이얼로그가 이미 소유한다.
- CLAUDE.md 작업 규칙: 수정 전 파일 읽기 · 커밋까지만(push는 덕수) · 완료 시 roadmap 갱신.

## 7. 이 묶음이 건드리지 않는 것

서버 라우트 · Firestore 규칙·스키마 · `/api/proofread` 프롬프트(마스킹 포함) · OCR 라우트 ·
읽기측 4개 렌더 사이트 · 공유·인쇄 레이아웃(CSS 값 동기화 제외) · 버전 canonicalize ·
체크박스 `accentColor` 3곳 · `EmptyBlockChips` · **블록 타입 union(표 블록 철회로 확장 0)**. **전부 0건.**

## 8. v3(web)에서 확인할 것

| # | 항목 |
|---|---|
| R1 | 스텝 0 (a) 측정표 — 4케이스가 한 값으로 ±1px에 들어오는지. 안 들어오면 `.tag-marker`·`.katex-display .tag` 분리 보정, 그래도 안 되면 텍스트 꼬리표 통일까지 검토 |
| R2 | 스텝 0 (b) — `font-weight:inherit` 뒤에도 두께 차가 남으면 원인은 `size-adjust`·폰트 스택이다(가짜 굵게 가설 반증) |
| R3 | 스텝 0 (c) — Mathpix가 `tabular`를 `$$`로 감싸지 않으면 D14′(a)의 래퍼 제거는 방어로만 남는다 |
| R4 | `CASE_REF_RE` 오탐 사례 수집(실제 문항 본문에서 `C1`이 경우 참조가 **아닌** 경우). 있으면 `\d{1,2}` 상한이나 문맥 조건 조정 |

## 9. 문서 갱신 매핑 (스텝 5)

| 항목 | 갱신할 Phase 문서 | roadmap 절 |
|---|---|---|
| A | `Phase57 …v4.md`(들여쓰기 블록 용도에 "분할 결과 수용" 추가) | 22-I(:411) · 57 |
| B | `Phase46 콘텐츠 이미지 배경 treatment.md` | 22-E · 46(:1215) |
| C | (phasedocs 없음 — roadmap 절에 표 추가) | 27(:583) · 28(:660) |
| D | `Phase57 …v4.md` P4 | 57(:1404) |
| E | `Phase57 …v4.md` P5 · `Phase58 ….md` P5 | 57 · 58(:1431) |
| G | `Phase59a Case 레이아웃·강조 체계 정비.md` 강조 체계 절 · `사용 가이드 — 강조와 키.md` | 59a(:1513) |
| H | `Phase61b …v4 실행판.md` 후속 절 | 61b(:1658) |
| 공통 | CLAUDE.md: `case-ref` · `.mathory-range` · proofread 옵션 인자 · **원문자/마커 두께 규약(화면 inherit·인쇄 600)** | 「UI 정리」(:1948)에 **M1 표** 1개 |

---

## 부록. 문서 계보

| 버전 | 작성 | 산출 |
|---|---|---|
| v1 | web (2026-08-26) | 초안 — 사실표 A~H · D1~D21 · 검증 요청 Q1~Q8 · 문서 매핑 |
| **v2** | **CLI (2026-08-26)** | **실측 교차검토 — 정정 7건(C1~C7)·보완 8건(S1~S5·S7~S9) · 덕수 결정 9건 반영(E-M1-1~9) · D3′/D11′/D14′/D17′/D19′/D20′/D21′ 개정 + D22·D23 신설 · D18(표 블록) 철회 · Q1~Q3·Q5~Q7 해소, 남은 것은 §8 R1~R4** |
| v3 | web | 재검증·확정 (예정) |

*v2 — 스텝 0(정적 실측)부터 착수 가능. 신규 env·비밀 0.*
