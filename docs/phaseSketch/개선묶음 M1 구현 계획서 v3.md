# 개선묶음 M1 구현 계획서 — 기존 Phase 보강 7건 (v3 web 재검증·확정판)

> 계보: v1(web) → v2(CLI 실측 교차검토) → **v3(web 재검증·확정, 2026-08-26)** → v4(CLI 실행판, 구현 기록) 예정
> 진실 원천: mathory **origin/main `32c6904`** — v3는 v2의 정정 7건·보완 8건을 이 해시에서 **다시 대조**했다(§0-1)
> 성격: 새 Phase가 아니다. 7건(A·B·C·D·E·G·H)을 한 묶음으로 구현하고 문서는 §9 매핑대로 각 Phase 문서 + roadmap 절을 갱신한다.
> roadmap에는 「UI 정리 (Phase 간 소규모)」(roadmap.md:1948) 관례로 M1 표 1개만 추가한다. **표 블록(F)은 철회 확정.**
> 착수 시 CLAUDE.md 규칙 1에 따라 대상 파일을 반드시 다시 읽을 것.

---

## 0. v3에서 한 일

### 0-1. v2 정정·보완의 재검증 (전부 `32c6904` 대조)

| v2 항목 | 판정 | 근거 |
|---|---|---|
| C1 `autoFixDeterministicIssues` 반환 `{ fixed, count }` | **승인** | `lib/proofread.ts:569` 시그니처 확인 |
| C2 `(가)`·`ㄱ.`의 "화면 inherit / 인쇄 600" 선례 | **승인** | `globals.css:437-444` 주석 원문 확인 |
| C3 `chatExtract.MARKER_LITERAL`은 공백 복원 목록 | **승인** | `lib/chatExtract.ts:181-183` · `:353-357`의 `b.marker()` 확인 → `case-ref` 추가 시 `C1에서`→`C1 에서` 오염 맞음 |
| C4 `protectFences`는 EditorPreview에만 | **승인** | `lib/`·`components/`에서 정의는 `EditorPreview.tsx`뿐(chatExtract는 주석 언급만) |
| C5 tabular 위치 = step 0 뒤 | **승인** | |
| C6 `accentColor` 인라인 6곳, range는 4곳 | **승인** | |
| C7 `.case-label { margin-right: 0.4em }` · `.marker-circled` 앵커 460 | **승인** | `globals.css:732-736`, `:460` |
| S7 팝오버 조상에 positioned 요소 없음 | **승인** | `CommentEditor.tsx:248-257`. 형제 "너무 깁니다" 경고(:1357-1361)도 인라인 span이라 `position:relative` 제거에 영향 없음 |
| S8 PrintStyles `.tag` 규칙에 `.print-body` 접두 — 안전 | **조건부 승인** | **PrintStyles.css는 `EditorView.tsx:56`이 import한다** — "인쇄 경로에만 로드"가 아니라 편집 화면에 항상 로드된다(그래서 `:139`의 미접두 `.marker-circled{600}`이 화면에 "새어 든다"). 접두를 붙이면 편집 화면에서는 globals의 같은 값만 남으므로 결과는 동일 → 안전하다는 결론은 유지. 다만 이유가 "공개 라우트가 안 부른다"가 아니라 **"값이 같다"**임을 바로잡는다 |
| S9 `.callout-block` 설계 의도 | **승인** | `globals.css:586-598` |
| D17′ 특이도 논리 | **승인 + 보강** | `.preview-content .marker-circled`(0,2,0)가 globals `:460`(0,1,0)·PrintStyles `:139`(0,1,0)를 이긴다. **인쇄 DOM은 `.preview-content`를 쓰지 않으므로**(PrintableContent는 자체 렌더, `.print-body`만) 인쇄 600은 자동으로 보존된다 — 이것이 "인쇄 600 유지"의 실제 보장 근거 |

### 0-2. v3 추가 정정·보완 (V1~V7)

| # | 내용 | 귀결 |
|---|---|---|
| **V1** | **D3′ ❷(X 자체를 callout으로 타입 변경)는 `text`·`callout`에만 적용한다.** `case`/`subcase` 이어짓기 블록(제목행 없음)처럼 `before`가 비면 X의 타입이 사라져 경우 rail·번호 계산(`buildCaseLabels`)이 바뀐다. 그 외 타입은 ❶로 간다(X는 비어도 남긴다 — 사용자가 지운다) | D3″ |
| **V2** | **D4 array 태그 제거 정규식 `\{[^}]*\}`는 `p{2cm}`·`>{$}l<{$}` 같은 중첩 열지정을 못 벗긴다.** 제거 후 `\begin{array` 잔재가 남으면 **실패로 처리**(사유: "열 지정을 해석하지 못했습니다")하고 절대 반쪽 결과를 내지 않는다 | D4′ |
| **V3** | **D14′에 셀 안 `\|` 이스케이프가 빠졌다.** GFM 표는 블록 단계에서 파이프를 자르므로 `$\|x\|$`·`\{x \mid …\}` 등 셀 안 `\|`가 표를 깨뜨린다. 셀 내용의 `\|`를 `\\|`로 이스케이프한다(remark-gfm이 셀 내용으로 넘길 때 백슬래시를 벗긴다). **`\|`(KaTeX 이중 세로선)와의 충돌 여부는 스텝 2에서 실물 확인**(R5) | D14″ |
| **V4** | **D12 전각 괄호 `（ㄱ）`도 같은 규칙으로 잡는다**(OCR·한글 문서에서 흔함). 출력은 항상 반각 `(1)` | D12′ |
| **V5** | §9의 `사용 가이드 — 강조와 키.md`는 **존재하지 않는다** — 실제 파일명은 `docs/phasedocs/사용 가이드 — 강조와 톤.md` | §9 |
| **V6** | (선택) PrintStyles `:139-140`의 미접두 `.marker-circled`·`p:has(.marker-circled)`에도 `:136`과 같이 **`.print-body` 접두를 붙이면** "새어 든 600"의 원인이 사라져 D17′의 화면 스코프 규칙이 방어가 아닌 정식이 된다. 값 변화 0. D16의 `.tag` 접두 작업과 같은 커밋에 넣는다 | D17″ |
| **V7** | D19′ 보호 ❸의 태그 정규식 `<[^>\n]+>`는 `<span class="case-label">C1.</span>` **요소 통째** 보호와 순서가 중요하다 — 요소 통째를 먼저 placeholder로 빼고, 남은 낱개 태그를 뺀다. 반대로 하면 span 내부 텍스트 `C1.`이 노출된다 | D19″ |

### 0-3. 덕수 결정 (누적, 전건 확정)

E-M1-1(원문자 화면만 inherit) · E-M1-2(case-ref 자기 보호) · E-M1-3(count 포함) · E-M1-4(tabular step 0 뒤) · E-M1-5(비활성 호출도 결과로 활성 이동) · E-M1-6(경우 블록 안 분할도 callout) · E-M1-7(`findEnclosingDisplayMath` 미개선) · E-M1-8(체크박스 범위 밖) · E-M1-9(표 블록 철회) · 표 규격 논의는 철회로 소멸 · `(ㄱ)`→`(1)`은 `roman`·`choices` 제외.

---

## 1. 확정 사실

v2 §1(A·B·C·D·E·G·H)을 **그대로 승계**한다. v3에서 바뀌거나 더해진 사실만 적는다.

| # | 사실 | 위치 |
|---|---|---|
| **B-7** | `PrintStyles.css`는 `EditorView.tsx:56`·`PrintableContent.tsx:12`·`PdfDownloadButton.tsx:6`에서 import → 편집 화면에 항상 로드 | 실측 |
| **E-5** | 인쇄 렌더(`PrintableContent`)는 `.preview-content` 클래스를 쓰지 않는다(`.print-body` 래퍼, `PrintableContent.tsx:60`). `.preview-content`는 `EditorPreview.tsx:462`의 루트 전용 | 실측 |
| **E-6** | PrintStyles의 마커 규칙 중 `.marker-gana/.marker-giyeok`(:136)는 `.print-body` 접두가 있고 `.marker-circled`(:139-140)만 미접두 | `PrintStyles.css` |
| **G-5** | `lib/locale.ts preprocessLocale` 3단계 순서(:218-224): gana → giyeok → circled → ref → **textTags** → figure → table → 4단계 `restoreMath`. `convertCaseRefs`는 `convertTableLabels` 뒤·`restoreMath` 앞이 자리 | `lib/locale.ts` |
| **H-5** | `VerifyChips` 안에서 `position`을 쓰는 것은 confirm 박스(:1363)뿐 — 래퍼 span의 `relative`를 지워도 다른 자식이 기대는 것이 없다 | `CommentPanel.tsx:1336-1401` |

---

## 2. 확정 결정표 (v3 최종 — 이 표가 실행 기준)

### A. 수식행 분할

| # | 결정 |
|---|---|
| **D1** | 결과 = 들여쓰기(`callout`) 블록 1개, 행마다 `$…$` 한 줄, 행 사이 **빈 줄**. 하드브레이크 금지 |
| **D2** | 행 안 `\tag{n}`은 `$…$` 밖으로: `$x = 1$ \tag{3}`. 한 행에 `\tag` 2개 이상이면 실패 |
| **D3″** | 블록 3분할. ❶ `before`가 비어 있지 않으면 X는 키 유지·`before`만 남기고 새 `callout`(nanoid·`isNew:true`) 삽입 ❷ **`before`가 공백이고 X.type ∈ {`text`, `callout`}일 때만** X 자체를 `callout`으로 바꾸고(키 유지) raw를 행들로 교체 — **그 외 타입은 `before`가 비어도 ❶**(V1) ❸ `after`가 있으면 callout 뒤에 `type:'text'`(nanoid·`isNew:true`). 활성 블록은 결과 callout으로 이동(비활성 호출 포함) |
| **D4′** | `array` 분할 허용: `\begin{array}{…}`/`\end{array}` 제거 후 aligned와 동일 처리. **`\hline` 포함 시 차단**(표). **태그 제거 후 `\begin{array` 잔재가 남으면 실패**(V2). cases·matrix 계열·gathered·split 차단 유지 |
| **D5** | `pushUndo()` 추가 · 실패 `setStatus` 2초 리셋 |
| **D6** | `lib/mathSplit.ts` import 0 유지. 신규 `splitDisplayMathToRows(content, cursor)` → `{ ok, before, rows, after }`. 조립은 `EditorView`. `tests/mathSplit.test.mjs` 신설 |
| **D7** | 경우 블록 안 `$$`도 D3″ 그대로(callout이 뒤에 붙음). rail은 `case-gap` 관통이라 끊기지 않음 |
| **D22** | `findEnclosingDisplayMath`의 인라인 `$`·코드펜스 미인식은 손대지 않고 **현 동작을 테스트로 고정**만 |

### B. 그림 조작 인터페이스

| # | 결정 |
|---|---|
| **D8** | `.mathory-range` 신설, `type="range"` 4곳 적용. `appearance:none`, 트랙 2px(`--border-light`), 진행·노브 `var(--mathory-red)`, 노브 12px 원. webkit·moz 의사요소 모두. `EditorPreview:266` `accentColor` 제거. 체크박스 3곳 범위 밖 |
| **D9** | 진행 구간은 `linear-gradient` + 인라인 `--p`. 로고 레드 명시비 미달은 알고 쓴다, 흐리면 `--mathory-red-dark` 폴백 |
| **D10** | 버튼 3개 1열(`액자`·`컬러 유지`·`그림 변경`), `그림 변경` off 배경 `var(--bg-active)`. 2행 div 제거. SVG·GGB "변경" 버튼 동일 키 |

### C. proofread 규칙 2종

| # | 결정 |
|---|---|
| **D11′** | 두 규칙을 `autoFixDeterministicIssues`의 **step 0 뒤·0a 앞**에: `convertTabularToMarkdown` → `convertJamoRefs` → 기존 0a·0b·1·2 |
| **D12′** | `(ㄱ)`→`(1)` … `(ㅊ)`→`(10)`. 수식·코드 밖, 행머리·행중 모두. **전각 `（ㄱ）`도 대상, 출력은 반각**(V4). `opts.skipJamoRefs`로 `roman`·`choices` 블록 제외(교정 경로). OCR 경로는 블록 타입을 몰라 기본 변환(S5, 알고 둔다) |
| **D13** | 마스킹에 `(ㄱ)` 분기 추가 안 함(결정 규칙이 먼저 돈다) |
| **D14″** | `convertTabularToMarkdown`: (a) `\begin{tabular}[opt]{spec}…\end{tabular}`, 바로 감싼 `$$…$$`가 있으면 함께 제거 (b) `\hline` 제거 → `\\` 행 분할 → `\&` 보호 후 `&` 셀 분할 → trim (c) 첫 행 헤더, 정렬행 전 열 `:---:` (d) 앞뒤 빈 줄 (e) **셀 안 `\|` → `\\|`**(V3) (f) `\multicolumn`·`\multirow`·`\cline`·중첩 tabular·열 수 불일치 행 → **손대지 않음** |
| **D15** | `tests/proofread.test.mjs`: 메모 tabular 원문 → 목표 md 문자열 일치(파이프라인 통과 후) · `(ㄱ)` 케이스(행머리·행중·전각·수식 안 비변환·`ㄱ.` 비변환·skip) · 예외(`\multicolumn`·열 수 불일치) · **셀 안 `\|` 케이스** |
| **D23** | 두 규칙 모두 `count`에 기여(표 1개 = +1, `(ㄱ)` = 치환 건수) |

### D·E. \tag 위치 · 원문자 두께

| # | 결정 |
|---|---|
| **D16** | 실측 → 재보정. 4케이스(단일행 `$$` · array{l} 다행 · aligned 다행 · 텍스트 행 `.tag-marker`) 중심차 ±1px. 인쇄 사본 동기화 + **`.print-body` 접두**(값 동일이라 편집 화면 결과 불변, B-7) |
| **D17″** | `globals.css`에 `.preview-content .marker-circled { font-weight: inherit; }` 1줄(화면만). 인쇄 600 유지는 E-5로 보장. **(선택, 같은 커밋)** PrintStyles `:139-140`에 `.print-body` 접두(V6) — 값 변화 0 |

### G. 본문 C1 강조

| # | 결정 |
|---|---|
| **D19″** | `CASE_REF_RE`(`lib/caseBlock.ts` export): `/(?<![A-Za-z0-9_\\])C(\d{1,2})([a-z]?)(?![A-Za-z0-9_])/g`. `convertCaseRefs(text)`가 `preprocessLocale` 두 사본의 `convertTableLabels` 뒤·`restoreMath` 앞(G-5)에 들어가고 **자기 보호**를 한다 — 순서: ① ```·`~~~` 펜스 ② `<span class="case-label">…</span>` **요소 통째** ③ 낱개 HTML 태그 `<[^>\n]+>` ④ 인라인 코드(백틱 런) ⑤ `⟦MATH_n⟧`은 이미 보호됨(V7 순서 준수). 치환 → `<span class="case-ref">C1a</span>` |
| **D20′** | CSS `.case-ref { font-weight: var(--weight-semibold); font-variant-numeric: tabular-nums; }` — `margin-right` 없음. `PrintStyles.css`에 `.print-body .case-ref` 동일값. **`chatExtract` 변경 0** |

### H. 검증 팝오버

| # | 결정 |
|---|---|
| **D21′** | 래퍼 span(:1336) `position:'relative'` 제거, 팝오버 `left:'50%', transform:'translateX(-50%)'`. 컴포저 래퍼(:978-982)가 기준 → 패널 가로 중앙. 폴백 불필요(S7·H-5) |

### F. 표 블록 — 철회 확정 (D18 삭제). 되살리지 말 것.

---

## 3. 구현 항목 (v2 §3 승계 + v3 수정분)

### 3.1 A — `lib/mathSplit.ts` + `handleSplitMathLines`

```ts
// lib/mathSplit.ts (import 0)
export interface RowSplit { before: string; rows: string[]; after: string }
export function splitDisplayMathToRows(content: string, cursor: number):
  ({ ok: true } & RowSplit) | { ok: false; reason: string };
//  · rows[i] = "$…$" | "$…$ \tag{n}"                        (D2)
//  · BLOCKED_ENVS에서 array 제거; body에 \begin{array} && \hline → 차단   (D4′)
//  · array 태그 제거: /\\begin\{array\}\{[^}]*\}/g, /\\end\{array\}/g
//    → 제거 후 /\\begin\{array/ 잔재면 { ok:false, reason:'array 열 지정을 해석하지 못했습니다.' }  (V2)
//  · 행별 \tag: /\\tag\{(\d+)\}/g 매치 2개 이상이면 실패
//  · 기존 export 유지; buildSplitReplacement·splitDisplayMathAtCursor는 사용처 0이 되면 제거
```

```
// EditorView.handleSplitMathLines(blockId?)  (:1504-1521 재작성)
pushUndo()
r = splitDisplayMathToRows(content, cursor)
실패 → setStatus(r.reason); setTimeout(() => setStatus(''), 2000); return
rowsRaw = r.rows.join('\n\n')
const canConvertSelf = !r.before.trim() && (X.type === 'text' || X.type === 'callout')   // V1
canConvertSelf → X.type='callout', X.raw=rowsRaw (키 유지; CM은 setContent)
else           → X.raw=r.before, callout{nanoid, isNew} 를 X 뒤에
after 있음     → text{nanoid, isNew} 를 callout 뒤에
setActiveBlockId(callout)
```

`tests/mathSplit.test.mjs` + `package.json` `test:mathsplit`. 필수 케이스: aligned 3행(`&`·`\\[4pt]`) · array{ll} · array+`\hline` 차단 · array `p{2cm}` 실패 · cases/pmatrix 차단 · `\tag` 추출 · `\tag` 2개 실패 · `\&` 보존 · before/after 4조합 · **현 `findEnclosingDisplayMath` 동작 고정**(D22).

### 3.2 B — v2 §3.2 그대로.

### 3.3 C — `lib/proofread.ts`

```ts
export function convertTabularToMarkdown(text: string): { fixed: string; count: number }   // D14″
export function convertJamoRefs(text: string): { fixed: string; count: number }            // D12′
export function autoFixDeterministicIssues(text: string, opts?: { skipJamoRefs?: boolean }): { fixed: string; count: number }
//  step 0 → convertTabularToMarkdown → convertJamoRefs(opts) → 0a → 0b → 1 → 2   (count 합산, D23)
```

`convertJamoRefs` 정규식: `/[(（]([ㄱㄴㄷㄹㅁㅂㅅㅇㅈㅊ])[)）]/g` → `(${idx+1})`. 수식·코드 보호는 `autoWrapBareNumbers`의 보호 구간 수집 방식(:365-369)을 재사용.
배선: `applyDeterministicAutoFix`·`handleRetryProofreadBlock`에 `{ skipJamoRefs: b.type === 'roman' || b.type === 'choices' }`. `lib/ocr.ts:93` 무인자 유지.

### 3.4 D·E — CSS: v2 §3.4 + V6(선택 접두).

### 3.5 G — `case-ref`: v2 §3.5 + V7 보호 순서.

### 3.6 H — v2 §3.6 그대로.

---

## 4. 작업 순서

| 스텝 | 내용 | 완료 기준 |
|---|---|---|
| 0 | 정적 실측(코드 변경 0): (a) D16 4케이스 px 측정표 (b) `.marker-circled` 400 임시 토글로 두께 차 소멸 확인 (c) Mathpix가 `tabular`를 `$$`로 감싸는지 실물 1건 | 측정표를 v4 §1에 첨부 |
| 1 | **A** `mathSplit` 개편 + 테스트 → `handleSplitMathLines` | T1~T5′ · 실물 aligned 3행 → 들여쓰기 3행 |
| 2 | **C** 규칙 2종 + 테스트 → 배선 | 메모 tabular 문자열 일치 · OCR 경로 동작 · count 증가 · **셀 `\|` 실물 확인(R5)** |
| 3 | **G** case-ref · **H** 팝오버 | T12·T13 |
| 4 | **B·D·E** 시각 3건 → **덕수 실물 검수(유일한 중간 관문)** | 슬라이더·버튼 1열·\tag ±1px·원문자 두께 |
| 5 | 문서: §9 · CLAUDE.md 규약 줄 | 커밋(push는 덕수) |

## 5. 검증 체크리스트

v2 §5를 승계하고 다음을 더한다.
- **A** T2′ `array` 열지정 `p{2cm}` → 실패 메시지(반쪽 결과 0) · T5″ `subcase` 이어짓기 블록(제목 없음)에서 분할 → X 타입 유지 + callout 뒤 삽입(V1)
- **C** T8⁗ 셀 안 `$\|x\|$` 포함 tabular → 표가 깨지지 않고 절댓값 렌더 · T8⁗′ 전각 `（ㄱ）` → `(1)`
- **E** T10′ (V6 적용 시) 편집 화면에서 `.marker-circled` 계산값이 600→400으로 바뀌는 곳이 원문자 마커뿐인지 DevTools로 확인

## 6. 하지 말 것 / 주의

v2 §6 전부 + 다음.
- D3″ ❷(자기 타입 변경)를 `text`·`callout` 밖으로 넓히지 말 것 — 경우 구조가 무너진다.
- `array` 태그 제거가 실패했을 때 **분할을 강행하지 말 것**(V2).
- tabular 셀의 파이프를 이스케이프하지 않은 채 내보내지 말 것(V3).
- "PrintStyles는 인쇄 전용"이라고 가정하지 말 것 — `EditorView.tsx:56`이 항상 로드한다(B-7). 미접두 규칙은 화면 규칙이다.

## 7. 이 묶음이 건드리지 않는 것

v2 §7과 동일. 블록 타입 union 확장 0 · `chatExtract` 0 · 서버·Firestore·프롬프트 0.

## 8. v4(CLI 실행판)에서 답할 것

| # | 항목 |
|---|---|
| R1 | D16 측정표 — 한 값으로 ±1px에 드는지, 아니면 `.tag-marker`/`.katex-display .tag` 분리 보정 |
| R2 | 스텝 0(b) — inherit 뒤에도 두께 차가 남으면 `size-adjust`·폰트 스택 재진단 |
| R3 | 스텝 0(c) — Mathpix `$$` 래핑 여부(D14″-a는 방어로 유지) |
| R4 | `CASE_REF_RE` 오탐 사례 수집 |
| **R5** | 셀 안 `\\|` 이스케이프가 remark-gfm → remark-math 경로에서 `\|`(KaTeX ‖)로 오독되지 않는지 실물 확인(V3). 오독되면 대안: 셀 안 수식의 `|`를 `\mid`/`\lvert…\rvert`로 치환 |
| **R6** | V6(선택 접두) 적용 후 편집 화면 회귀 0 확인 — 특히 `p:has(.marker-circled)` 들여쓰기가 globals `:466-470` 규칙만으로 유지되는지 |

## 9. 문서 갱신 매핑 (스텝 5)

| 항목 | 갱신할 Phase 문서 | roadmap 절 |
|---|---|---|
| A | `Phase57 리스트 여백·목록 블록·강조문 블록 v4.md`(들여쓰기 블록 용도에 "분할 결과 수용") | 22-I(:411) · 57 |
| B | `Phase46 콘텐츠 이미지 배경 treatment.md` | 22-E · 46(:1215) |
| C | (phasedocs 없음) | 27(:583) · 28(:660) |
| D | `Phase57 …v4.md` P4 | 57(:1404) |
| E | `Phase57 …v4.md` P5 · `Phase58 제목 블록·key sentence 톤·수식 크기.md` P5 | 57 · 58(:1431) |
| G | `Phase59a Case 레이아웃·강조 체계 정비.md` 강조 체계 절 · **`사용 가이드 — 강조와 톤.md`**(V5) | 59a(:1513) |
| H | `docs/phaseSketch/Phase61b 정밀 검증 구현 계획서 v4 실행판.md` 후속 절 | 61b(:1658) |
| 공통 | CLAUDE.md: `case-ref` · `.mathory-range` · proofread 옵션 인자 · 마커 두께 규약(화면 inherit·인쇄 600) · **"PrintStyles는 편집 화면에도 로드된다"** | 「UI 정리」(:1948)에 M1 표 1개 |

---

## 부록. 문서 계보

| 버전 | 작성 | 산출 |
|---|---|---|
| v1 | web (2026-08-26) | 초안 — 사실표 A~H · D1~D21 · Q1~Q8 |
| v2 | CLI (2026-08-26) | 실측 교차검토 — 정정 C1~C7 · 보완 S1~S9 · 덕수 결정 E-M1-1~9 · D18 철회 |
| **v3** | **web (2026-08-26)** | **v2 재검증(C1~C7·S7~S9 전건 승인, S8 근거 정정) · 추가 정정 V1~V7 · D3″/D4′/D12′/D14″/D17″/D19″ 확정 · R5·R6 신설** |
| v4 | CLI | 실행판 — 구현 기록·측정표·R1~R6 답 (예정) |

*v3 — 확정. 스텝 0부터 착수 가능. 신규 env·비밀 0.*
