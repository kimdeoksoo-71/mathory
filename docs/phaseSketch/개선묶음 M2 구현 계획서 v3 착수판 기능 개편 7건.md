# 개선묶음 M2 구현 계획서 — 기능 개편 7건 (v3 web 재검증·착수판)

> 계보: 스케치 메모(2026-08-28) → v1(web) → v2(CLI 실측 교차검토, v2.1·v2.2 포함) → **v3(web 재검증·착수판, 2026-08-28)** → v4(CLI 실행판, 구현 기록) 예정
> 진실 원천: mathory **origin/main `af918fa`** — v3는 v2의 정정 8건·실측 확정 D39′~D48′·잔여 질문 Q-B~Q-H를 이 해시에서 **다시 대조**했다(§0-1). v3 추가 정정·보강은 W1~W11(§0-2).
> **승계 원칙: 본문에 다시 적지 않은 v2의 사실표(§2)·결정표(D1′~D48′)·구현 항목(§4)·검수(T1~T31)·하지 말 것(§8)·문서 매핑(§9)은 전부 그대로 유효하다.** v3는 바뀐 것과 더해진 것만 적는다.
> 착수 시 CLAUDE.md 규칙 1에 따라 대상 파일을 반드시 다시 읽을 것. push는 덕수. 다크 모드 없음 — 새 색 토큰에 다크 정의 금지.

실행 순서(변경 없음): **스텝 1** B+E+G → **스텝 2** A → **스텝 3** C → **스텝 4** D+F(동일 스텝 필수, D33′) → **스텝 5** 문서.

---

## 0. v3에서 한 일

### 0-1. v2 재검증 (전부 `af918fa` 재대조)

| v2 항목 | 판정 | 근거 |
|---|---|---|
| 정정1 (57곳 = prompt 6·confirm 17·alert 34, AppShell 19) | **승인** | 전수 재카운트 일치. §4-A 파일별 합계 56도 재검산 일치 |
| 정정2 (sync 핸들러 2곳뿐) | **승인** | `EditorView.tsx:2557` `handleDeleteTab`(호출자 `:3393`, `stopPropagation`은 동기 완료) · `AppShell.tsx:661` 인라인 `onSheetImport` 확인 |
| 정정3 (A-4 = 문항의 폴더 이동, 순환 방지 불요) | **승인** | `:563`은 `handleProblemAction` case `'move'` |
| 정정4 (스타일 원본 = SheetImportModal, radius 10·테두리 없음) | **승인** | `SheetImportModal.tsx:76-108` 확인 |
| 정정5 (z-index 실측, `Z_DIALOG=10500` 유효) | **승인** | `Sidebar.tsx:444·509` = 10000, `SvgViewer.tsx:375` = 10000, `ContextMenu.tsx:74` = 1000, `UnifiedToolbar:572`·`BlockBottomToolbar:164` = 3000 전부 확인 |
| 정정6 (`[ㄱ-ㅊ]` 금지 → 명시 10자) | **승인** | `GIYEOK_LITERAL_RE`(`locale.ts:59`)가 명시 10자인 이유와 동일. 유니코드 사실 |
| 정정7 (요소 통째 보호는 `.case-label` 하나뿐) | **승인 + 보강** | `caseBlock.ts:54-58` 확인. 단 목록에 **누락 1건 추가 발견 → W3** |
| 정정8 (본문 최소폭 736, `LABEL_GAP = 2.8em`) | **승인 (탭 본문에 한해)** | `TabBody.tsx:36` 확인. 단 **제목행은 다른 값 → W1** |
| A-9·A-10 (죽은 파일·layout.tsx 서버 컴포넌트) | **승인** | `PdfDownloadButton` 실참조 0(docs 주석뿐) · `app/layout.tsx` 'use client' 없음, `<body>{children}</body>` |
| C-10 (test:locale 하니스) | **승인** | `package.json:11` — `tests/locale.test.mjs` 이름까지 일치 |
| C-11~C-16, B-7, D-16~D-19, F-6 (실측 계열) | **승인** | 실측 재현은 생략하되 전제(코드 구조·정규식·순서)를 전건 재대조 — 모순 없음. `preprocess.ts:128·131`의 `\tag*{(n)}`·`\text{(n)}`, `protectMath`(`locale.ts:80-92`), 3단계 순서(`:236-244`), `marker-case-sub` 실재(`locale.ts:168`) 확인 |
| D45′ (`max-content`) · D46′ (sticky는 D23′ 의존) · D47′ (자모 정규식) · D48′ (보호 목록 확장) | **승인** | D47′의 잔여 오탐 수용 논거(무음+무표시)도 타당 |
| G-6′·G-7′ (`coach_caution` 상수 5곳+`solutionOutline.ts:74` · `normalizeBlockType` 호출 `:1184`·`:2710`) | **승인** | 전건 라인 일치 |
| D37′ ("렌더 사이트가 `key={block.id}`") | **승인** | `TabBody.tsx:151` 등 확인 |
| **D-18 · D40′ (제목 좌단 179px, 카드 좌단 203px 정렬)** | **수치 정정** | 제목행의 gap은 2.8em이 아니라 **고정 28px** → **W1** |
| **D15′ (공백 제거 텍스트 일치)** | **조건부 승인** | giyeok·case 정의부는 꼬리 `.`가 붙어 그대로는 불일치 → **W2** |
| **D19′⑥ (멱등성 테스트)** | **조건부 승인** | 현 보호 목록으로는 멱등성이 깨진다 → **W3** |

### 0-2. v3 추가 정정·보강 (W1~W11)

| # | 내용 | 귀결 |
|---|---|---|
| **W1** | **제목행의 라벨↔본문 gap은 `LABEL_GAP = 28`(고정 px, `ProblemView.tsx:514`)이다** — Phase 59a가 TabBody만 `2.8em`으로 비례화하고 제목행은 남겨 두었다. 즉 fs15에서 제목 좌단 = 32+105+28 = **165px**, 탭 본문 좌단 = 179px로 **이미 14px 어긋나 있다**(fs24에서는 39px). D40′ 실행 시 **제목행 gap도 `2.8 × contentFontSize`로 통일**하고 카드 좌패딩 40px 들여쓰기를 더해 좌단 203px에 맞춘다 — 잠복 비정합 해소를 겸한다 | D40″ |
| **W2** | **D15′의 "공백 제거 텍스트 일치"는 2개 유형에서 그대로는 실패한다**: giyeok 정의부는 `ㄱ.`(마침표 포함, `locale.ts:183`)인데 참조는 대개 `ㄱ`, case 정의부 `.case-label`은 `C1.`(`injectCaseLabel`, `caseBlock.ts:145`)인데 `.case-ref`는 `C1`. 비교 정규화 규칙을 명시한다: **trim → 공백 제거 → 꼬리 `.` 1개 제거** 후 비교. tag `(3)`↔`(3)` · circled `①`↔`①` · gana `(가)`↔`(가)`는 원래 일치 | D15″ |
| **W3** | **D12′ 확장 보호 목록에 `ref-marker` 자신이 빠졌다** — 멱등성(D19′⑥)이 깨진다: 2회째 실행에서 이전 실행이 만든 `<span class="ref-marker" …>①</span>`의 여닫는 태그가 낱개 태그 보호로 각각 빠지면 내부 `①`이 노출돼 재감쌈된다. 보호 목록에 **`ref-marker` 요소 통째**를 추가 (최종 목록: 코드펜스 → `ref-marker`·`marker-gana`·`marker-giyeok`·`marker-circled`·`marker-case-sub`·`tag-marker`·`case-label` **요소 통째** → 낱개 태그 → 인라인 코드) | D12″ |
| **W4** | **Q-G 해소**: `lib/pdfPrint.tsx`의 alert(`:64`)는 `printProblemPdf` **진입부 검증**이고, print DOM(`tempDiv`)은 그 **뒤**에 붙는다 → 다이얼로그가 인쇄 스냅샷에 섞일 수 없다. 함수도 이미 `async`라 `await alertDialog(...)` 치환이 자연스럽다 | D5′에 흡수 |
| **W5** | **슬림 바와 절대배치 컨트롤이 충돌한다**: 글자크기 조절(`ProblemView.tsx:716`, `top:16`)·우측 단 토글(`:768`, `top:52`)은 **루트 기준** absolute다. 제목행이 98→36으로 접히면 `top:52` 토글이 콘텐츠 카드 위로 겹친다 → 두 컨트롤의 `top`을 제목행 접힘 상태에 연동한다(접힘 시 글자크기 `top:5`(슬림 바 36px 세로 중앙), 토글은 그 아래 유지하되 실측 조정). 좌표는 상수화 | D49 |
| **W6** | **Q-B 확정 = height 애니메이션**: 제목행은 스크롤 컨테이너의 **형제**(위)라서 높이 전환이 sticky `top:0` 계산에 영향을 주지 않는다(sticky 기준은 스크롤 컨테이너 내부). transform은 시각만 밀고 레이아웃 공간을 회수하지 못해 목적(공간 회수, D-16)에 반한다. `height 98↔36 + overflow:hidden + transition 0.2s ease`. 재계산 비용 쟁점은 소멸 | D50 |
| **W7** | **Q-D 확정 = CSS 변수 오프셋**: 풀이 라벨 sticky를 `top:0`으로 두면 sticky 문제 행(z 상위)에 **상시 가려진다** — Q10의 1차 단순화는 폐기. 컨테이너에 `--m2-q-sticky-h`(sticky 문제 행의 **접힘 상태 실측 높이**)를 세우고 라벨은 `top: var(--m2-q-sticky-h)`. 갱신은 토글·글꼴·폭 변경 시에만 `ref.offsetHeight` 1회 측정(ResizeObserver 불요 — Q10이 우려한 상시 관측 비용 없음). 문제 카드를 **수동으로 펼친 채** 스크롤하면 라벨이 카드 뒤로 숨는 것은 수용(그 상태에서는 문제 카드가 화면 대부분을 차지한다) | D51 |
| **W8** | **Q-H 해소 = 수용**: FolderView 카드는 `height 320 + overflow:hidden + 하단 페이드`가 **모든 블록을 같은 문법으로 자르는** 구조다. 코칭 항상 펼침(D38′)의 잘림도 그 문법 안에 있다. 코드 대응 없음, 검수 실물 확인만 | T10′에 흡수 |
| **W9** | **Q-C 초기값 확정**: `T1 = 80` · `T2 = 8` · 시차 `300ms` · 쿨다운 `400ms`(접힘·펼침 각각). 전부 상수화하고 **구현 중 튜닝 허용 범위**를 함께 적는다 — T1 60~120 · 시차 200~500 · 쿨다운 300~600. 트랙패드 관성 스크롤 진동은 구현 기록(R1)으로 이월 | D52 |
| **W10** | 제목행 `minHeight 98`의 주석("EditorView 제목 57+탭 41과 가로 경계선 Y 일치")이 뜻하는 정렬은 **슬림 바 상태에서 의도적으로 깨진다** — EditorView↔ProblemView 전환 시 경계선 Y가 다를 수 있음을 E-M2-2(배경 차이 수용)와 같은 수용 범위로 기록. 펼침 상태의 98은 유지 | 기록 |
| **W11** | **제목 `<h1>`이 `...mainColStyle`을 spread한다**(`ProblemView.tsx:607`) — D24′로 본문 폭이 `widthEm`을 따르게 되면 **제목 폭도 같은 `widthEm` 값을 따라야** 제목·카드 우단이 함께 움직인다. `mainColStyle`을 `widthEm` 기반으로 바꾸면 자동 충족되지만, 제목행과 TabBody가 **각자 mainColStyle 사본**을 갖고 있으므로 둘 다 갱신할 것 | D24′에 보강 |

### 0-3. 덕수 결정 (누적, 전건 확정)

E-M2-1~4(방향) · Q1~Q14(v2 확정) · S1·S3~S6(v1 소결정, S2→Q2) · **v3: Q-B(→D50)·Q-C(→D52)·Q-D(→D51)·Q-G(해소)·Q-H(수용) 전건 처리 — 잔여 질문 0.**

---

## 1. 확정 사실 (v2 §2 승계 + v3 추가분)

| # | 사실 | 위치 |
|---|---|---|
| **D-20** | 제목행 gap = `LABEL_GAP = 28`(고정 px). 제목행 내부 래퍼 `padding '22px 32px 0'`, 라벨 박스 `7 × fs`, `<h1>`은 `...mainColStyle`(35em) spread — 제목 좌단 165px(fs15)로 탭 본문(179px)과 현행도 불일치 | `ProblemView.tsx:514-519·539-541·604-612` |
| **D-21** | 글자크기 조절 = 루트 기준 `absolute, top:16`(`:716`) · 우측 단 토글 = `top:52`(`:768`) · 루트가 `position:relative`(`:523-529`) — 제목행 접힘과 충돌(W5) | `ProblemView.tsx` |
| **A-11** | `printProblemPdf`의 alert는 진입부(탭 0개 검증), print DOM 부착 전. 함수는 `async` | `lib/pdfPrint.tsx:63-72` |
| **C-17** | 정의부 span 내용 실측: `marker-gana` = `(가)` · `marker-giyeok` = **`ㄱ.`** · `marker-circled` = `①` · `tag-marker` = `(n)` · `case-label` = **`C1.`** — 꼬리 `.` 2종이 W2의 근거 | `locale.ts:178·183·192·205`, `caseBlock.ts:145` |
| **C-18** | `convertRefReferences`(`\ref{n}` → 평문)는 3단계 중반(`:239`)에 이미 실행된다 — D11′①은 이 함수를 span 산출로 바꾸는 것이므로, **새 스캐너(마커 뒤·`convertCaseRefs` 직전)가 돌 때 ref-marker span은 이미 본문에 존재한다** → W3의 보호가 1회 실행에서도 안전망이 된다 | `locale.ts:236-244` |
| **G-11** | 인쇄 선택지에는 "넘치면 수동 2행 구성(JS 측 class 전환)" 경로가 있다(`.print-choices-row` 주석) — B의 center 전환은 행 단위 적용이라 2행 구성에서도 각 행 안에서 성립 | `PrintStyles.css:246` |

---

## 2. 확정 결정표 (v3 최종 — v2 결정 중 아래만 개정·신설, 나머지 D1′~D48′ 전부 유효)

| # | 결정 |
|---|---|
| **D12″** | (C) D12′의 요소 통째 보호 목록에 **`ref-marker` 추가**(W3). 최종: 코드펜스 → `ref-marker`·`marker-gana`·`marker-giyeok`·`marker-circled`·`marker-case-sub`·`tag-marker`·`case-label` 요소 통째 → 낱개 태그 → 인라인 코드. D19′⑥(멱등성)은 이 목록이 전제다 |
| **D15″** | (C) D15′의 DOM 매칭에 **비교 정규화** 명시(W2): 정의부·참조부 텍스트 모두 `trim → 공백 제거 → 꼬리 '.' 1개 제거` 후 비교. `normalizeRefText()` 한 함수로 두고 탐색 경로 전체가 공유(D41′의 "경로 하나 유지"와 정합) |
| **D24″** | (D) D24′에 보강(W11): `widthEm`은 **제목행 `mainColStyle`과 TabBody `mainColStyle` 두 사본 모두**에 적용한다 — 제목·카드 폭이 함께 움직여야 D40″ 정렬이 전 폭에서 성립 |
| **D40″** | (D) D40′ 개정(W1): 제목행 gap을 `28` → **`2.8 × contentFontSize`** 로 통일(TabBody `LABEL_GAP_EM` 상수를 export해 공유, 사본 금지). 제목 좌단 = 카드 본문 좌단 = **16 + 7em + 2.8em + 40px** — 슬림 바·펼침 모두 동일 기준, 접힘/펼침에서 제목이 가로로 움직이지 않는다. (현행 165px 대비 우측 이동 — 잠복 비정합 해소를 겸하며, 검수 T26에 "제목·카드 좌단 일치"로 반영) |
| **D49** | (D) 절대배치 컨트롤의 제목행 연동(W5): 글자크기 조절·우측 단 토글의 `top`을 제목행 상태에 연동 — 펼침 `16/52`(현행), 접힘 `5/실측값`. 좌표는 상수 객체로 묶고 transition은 제목행 height와 같은 0.2s. 구현 후 실측 보정(R2) |
| **D50** | (D) 제목행 접힘 = **height 애니메이션 확정**(W6): `height: 98 ↔ 36` + `overflow:hidden` + `transition: height 0.2s ease`. transform 금지(공간 미회수). sticky top 재계산 없음(제목행은 스크롤 컨테이너 밖) — Q-B 소멸 |
| **D51** | (D) 풀이 라벨 sticky = **`top: var(--m2-q-sticky-h)`**(W7): 변수 값 = sticky 문제 행의 접힘 상태 실측 높이. 갱신은 문제 카드 토글·`contentFontSize`·`widthEm` 변경 시 1회 `offsetHeight` 측정. 수동 펼침 중 라벨 가림은 수용. Q10·Q-D 소멸 |
| **D52** | (D) 자동접힘 상수(W9): `M2_COLLAPSE = { T1: 80, T2: 8, DELAY_MS: 300, COOLDOWN_MS: 400 }`. 튜닝 허용 범위 T1 60~120 · DELAY 200~500 · COOLDOWN 300~600 — 범위 밖 조정은 문서 갱신 동반. 트랙패드 진동은 R1 |

---

## 3. 구현 항목 (v2 §4 승계 + v3 수정분)

- **4-C 보강**: `lib/locale.ts`에 `normalizeRefText()` export(D15″) · 보호 목록에 ref-marker(D12″) · `tests/locale.test.mjs`의 멱등성 케이스는 "2회 실행 후 ref-marker 중첩 0"으로 구체화.
- **4-D 보강**: `TabBody.tsx`의 `LABEL_GAP_EM` export → `ProblemView.tsx` 제목행이 import(D40″) · `mainColStyle` 두 사본 모두 `widthEm` 반영(D24″) · 절대배치 컨트롤 top 상수화(D49) · 제목행 height 전환(D50) · `--m2-q-sticky-h` 세팅 함수(D51) · `M2_COLLAPSE` 상수(D52).
- 그 외 파일 목록·치환 전수(56곳/14파일)·스텝 구성은 v2 그대로.

---

## 4. 검증 체크리스트 (v2 §7 승계 + 추가·개정)

- **T19+** (C) 멱등성: `preprocessLocale` 2회 실행 후 `ref-marker` 중첩 0(D12″) · giyeok 참조 `ㄱ`(마침표 없음)이 정의부 `ㄱ.`와 매칭됨 · `C1` 참조가 `.case-label`의 `C1.`와 매칭됨(D15″)
- **T26″** (D) 개정: 접힘/펼침에서 제목 좌단 불변 **+ 제목 좌단 = 카드 본문 좌단**(D40″, 11·15·24px 3조건) · 슬림 바 상태에서 글자크기 조절·우측 단 토글이 콘텐츠와 안 겹침(D49)
- **T28″** (D) 개정: 풀이 라벨이 sticky 문제 행 **아래**에 고정되고 가려지지 않음(D51) — 문제 카드 수동 펼침 상태의 가림은 수용 확인만
- **T32** (D) EditorView↔ProblemView 전환: 제목행 펼침 상태에서 경계선 Y 일치 유지(98 유지, W10) — 슬림 바 상태의 불일치는 수용
- **T33** (B) 인쇄 수동 2행 구성 경로에서도 행별 center 정렬 성립(G-11)

## 5. 하지 말 것 (v2 §8 승계 + 추가)

- (C) 보호 목록에서 **`ref-marker`를 빼지 말 것**(W3) — 멱등성이 깨진다. 비교 정규화 없이 raw `textContent` 일치로 매칭하지 말 것(W2).
- (D) 제목행 접힘을 **transform으로 구현하지 말 것**(D50) — 공간이 회수되지 않는다. 풀이 라벨 sticky를 `top:0`으로 두지 말 것(D51). 제목행 gap에 **고정 px을 되살리지 말 것**(D40″ — TabBody `LABEL_GAP_EM` 주석의 em/px 혼합 함정과 같은 이유). `--m2-q-sticky-h` 갱신에 ResizeObserver를 넣지 말 것(토글·글꼴·폭 변경 시 1회 측정으로 충분).

## 6. 이 묶음이 건드리지 않는 것

v2와 동일: 신규 env·비밀 0 · Firestore 규칙 0 · 서버 0 · 배치 마이그레이션 0 · 블록 타입 union 0 · 기존 모달 8종 재작성 0 · `chatExtract` 0 · EditorView U-프레임 0 · 모바일 0.

## 7. v4(실행판)에서 답할 것

| # | 항목 |
|---|---|
| **R1** | 트랙패드 관성 스크롤에서 D52 초기값의 진동 여부 — 필요 시 허용 범위 안 튜닝, 범위 밖이면 방향 감지(delta 부호) 보조 도입 검토 |
| **R2** | D49 접힘 상태 컨트롤 top 실측값 · 패널 열림 애니메이션(0.18s) 중 sticky 폭 추종 |
| **R3** | D23′/D45′ 전환 후 우측 단·댓글 패널·점프 오프셋(D29′) 어긋남 실측 보정 |
| **R4** | D47′ 자모 오탐 실사용 사례 수집(`ㄱ자` 외 추가 유형) |
| **R5** | D40″로 제목 좌단이 이동한 실물 인상 — 위화감 시 제목행만 들여쓰기 조정 여지(카드 좌단 기준은 유지) |

## 8. 문서 갱신 매핑

v2 §9 승계. 추가: (D·F) CLAUDE.md에 `M2_COLLAPSE` 상수·`--m2-q-sticky-h` 규약·"제목행 gap = TabBody `LABEL_GAP_EM` 공유(고정 px 금지)" 1줄.

---

## 부록. 문서 계보

| 버전 | 작성 | 산출 |
|---|---|---|
| v1 | web (2026-08-28) | 초안 — 사실표 A~G · D1~D33 · E-M2-1~4 · S1~S6 · Q1~Q10 |
| v2 | CLI (2026-08-28) | 실측 교차검토 — 정정 8건 · Q1~Q14 · D1′~D48′ · v2.1/v2.2 실측(B-7·C-11~16·D-16~19·F-6) |
| **v3** | **web (2026-08-28)** | **v2 재검증(정정 8건·실측 계열 전건 승인, D-18/D15′/D19′⑥ 3건 조건부→개정) · 추가 정정 W1~W11 · D12″/D15″/D24″/D40″ 개정 · D49~D52 신설 · Q-B~Q-H 전건 해소(잔여 질문 0) · R1~R5 신설** |
| v4 | CLI | 실행판 — 구현 기록·R1~R5 답 (예정) |

*v3 — 확정. 스텝 1부터 착수 가능. 잔여 질문 0.*
