# Phase 60 — T4 인쇄 기준선 관측 (`phase60 before.pdf`, 2026-08-18)

원문: `phase60 before.md` · 기준 커밋 `aff3bd2` (변경 전)

## 기준선이 담고 있는 것

| 블록 | 원문 | 랩 | 마커 뒤 공백 |
|---|---|---|---|
| (가)(나) 상자 | `(a) 서로 다른…` | **2줄** | 있음 |
| 〃 | `(b)두 직선…` | **2줄** | **없음** |
| ㄱ,ㄴ 상자 | `(i) 함수f(t)=t…` | **3줄** | 있음 |
| 〃 | `(ii) 점 P는…` | 1줄 | 있음 |
| 〃 | `(iii)함수 f(x)는…` | **2줄** | **없음** |
| 목록 | `①②③` | 1줄 | — |
| 경우(p.2) | C1·C2·C2a·C2b·C3 + rail | — | — |
| 그 밖 | 제목·display 수식·`\tag{1}\tag{2}`·`\ref`·SVG 그래프 | | |

## 변경 후 **같아야** 하는 것 (T4 — 다르면 §4.2(b) 롤백)

1. **`(가)`와 `(나)`의 본문 시작 x좌표가 서로 같다** — 원문 공백 유무가 달라도. 현행은 `min-width:2.5em`이 간격을 공급해 동일하다
2. **첫 줄 본문 시작점 = 랩 라인 시작점** (내어쓰기 작동). `(가)`·`(나)`·`ㄱ.`·`ㄷ.` 넷 다
3. **`ㄱ.` 계열의 마커 뒤 간격이 `(가)` 계열보다 넓다** — 인쇄 giyeok도 2.5em이라 좁은 글리프만큼 여백이 남는다
4. **마커 굵기가 본문보다 굵다** (600 유지 — 인쇄만의 의도적 예외)
5. ①②③ 밭 · 경우 rail · 수식 · `\tag` 위치 전부 불변

## 변경 후 **달라져야** 하는 것

없음. 이 문항의 마커는 전부 레거시 `(a)`/`(i)`라 인쇄 산출물은 **완전 동일**해야 한다.
(리터럴 `(가)` 뭉침 해소 = T14는 이 PDF에 없다 → 화면에서 별도 확인)
