# 개선묶음 M2 구현 계획서 — 기능 개편 7건 (v1 web 초안)

> 계보: 스케치 메모 「M2 기능 개편 모음」(2026-08-28) → **v1(web, 2026-08-28)** → v2(CLI 실측 교차검토) → v3(확정) 예정
> 진실 원천: mathory **origin/main `af918fa`** (2026-08-27) — 본문 모든 파일:라인은 이 해시에서 실측
> 성격: M1과 같은 묶음 관례. 7건(A~G)을 한 묶음으로 계획하되, **구현·검수는 §5의 4개 스텝 그룹으로 분할**한다(D가 단독으로 M1 전체 규모).
> 착수 시 CLAUDE.md 규칙 1에 따라 대상 파일을 반드시 다시 읽을 것. push는 덕수. 다크 모드 없음 — 새 색 토큰에 다크 정의 금지.

| 항목 | 내용 | 난이도 |
|---|---|---|
| **A** | 팝업창 일원화 (네이티브 alert/confirm/prompt 57곳 → 자체 다이얼로그) | 중 (호출부 수가 비용) |
| **B** | 선택지 블록 세로 정렬 (baseline → center) | 하 |
| **C** | 참조 인용 hover 말풍선 | 중 |
| **D** | ProblemView 보기 개편 (탭별 카드·폭 조절·스크롤 자동접힘) | 상 |
| **E** | 폴더뷰 카드보기 열수 제한 해제 | 하 |
| **F** | 왼쪽 사이드바 자동접힘 제거 | 하 (보완 포함 시 중) |
| **G** | 코칭블록 단일화(Tip) + 접힘/펼침 | 하~중 |

---

## 0. 덕수 결정 (E-M2-1 ~ E-M2-4, 2026-08-28 확정)

| # | 결정 |
|---|---|
| **E-M2-1** | (C) 참조 인식 범위 = **텍스트 참조 자동 인식**. `\ref{n}`·`C1` 명시 표기 + 본문 중간의 `①`·`(가)`·`ㄱ` 자동 인식까지. **수식 내부(`$…$` 안) 참조는 제외.** 오탐은 lookaround로 최소화하되 일부 감수 |
| **E-M2-2** | (D) 개편 범위 = **ProblemView만, 모바일 제외**. EditorView는 현행 U-프레임 유지(화면 전환 시 배경 차이 감수). 모바일 대응은 별도 Phase |
| **E-M2-3** | (F) 자동접힘 **완전 제거 + 가로 스크롤 보완**. ProblemView의 `overflowX:'hidden'`을 스크롤 가능 구조로 전환(구현은 D의 컨테이너 개편에 편승, §3-F). 문항 이동 시 강제 펼침 3곳도 함께 제거해 수동 접힘 상태 존중 |
| **E-M2-4** | (G) 하위호환 = **타입 id 유지, 표시만 통일**. `coach_important`·`coach_caution` 둘 다 'Tip' 라벨 + Important 아이콘 + 단일 색으로 렌더. 마이그레이션 0 |

### 0-1. v1 소결정 (덕수 승인 하에 확정 — 이견 시 v2에서 뒤집기 가능)

| # | 내용 |
|---|---|
| S1 | (A) 다이얼로그는 Confirm·Prompt·Alert 3종 + Promise API(`await confirmDialog(…)`). 전역 토스트 신설은 범위 외 |
| S2 | (A) `AppShell.tsx:563` "폴더 이동" prompt는 단순 치환 불가 → **폴더 선택 다이얼로그(폴더 트리 재사용)**로 별도 소항목 A-4 |
| S3 | (B) 화면(`ChoicesBlock`)·인쇄(`PrintStyles`) 동시 수정, 11·15·24px 3조건 실측 검수 |
| S4 | (E) 카드보기일 때만 콘텐츠 래퍼 `maxWidth` 해제. 제목바 래퍼·리스트보기는 현행 유지. `justifyContent:'center'` 유지(Phase 62 D10 존중) |
| S5 | (G) 접힘 상태는 **비영속**(문서에 저장 안 함). 인쇄·편집 미리보기는 항상 펼침 |
| S6 | (G) 단일 색 = 기존 `--coach-important` 보라 `#6639ba` 유지 — `#E8DFCE` 배경 4.5:1 검증이 이미 끝난 값(Phase 59a). 새 색 계산 불필요 |

---

## 1. 확정 사실 (전부 `af918fa` 실측)

### A. 팝업

| # | 사실 | 위치 |
|---|---|---|
| A-1 | 네이티브 팝업 총 **57곳/15파일**: prompt 6 · confirm 14 · alert 37. `window.` 접두 0건 | 전수 목록 §3-A |
| A-2 | prompt 6곳은 전부 "이름 입력"(폴더 생성·개명, 문항 개명, 폴더 이동, 시트 가져오기 내 폴더 생성) | `AppShell.tsx:381·393·420·494·563`, `SheetImportModal.tsx:171` |
| A-3 | Tailwind 없음. 모달은 인라인 style + `globals.css` CSS 변수. 기존 모달 8종의 공통 관례: 오버레이 `fixed inset 0` + `rgba(0,0,0,0.35~0.4)` + flex center, 본체 `var(--bg-card)` 계열 · radius 10~12 · 오버레이 클릭 닫기 + `stopPropagation` | `RestoreConfirm.tsx:26-73` 외 |
| A-4 | 입력 모달의 완성 원형 = `NicknameSetupModal`(input + Enter 확정 + `isComposing` IME 가드 + 에러 슬롯). 확인 모달 원형 = `RestoreConfirm`(header/body/footer 3단 + 위험 버튼) | `NicknameSetupModal.tsx:46-99`, `RestoreConfirm.tsx` |
| A-5 | `BatchVerifyDialog.tsx:26-27` 주석: SheetImportModal 규격의 **사본**("상수가 export돼 있지 않아 사본") — 스타일 상수 승격의 자연스러운 출발점 | `BatchVerifyDialog.tsx:28-59` |
| A-6 | z-index 무질서: 오버레이 계층만 900/1000/1400/2000/9000/9998/9999 혼재, 최고 10000(사이드바 컨텍스트 메뉴). 토큰 없음 | 전역 |
| A-7 | `confirm()` 동기 호출 15곳 중 대부분은 이미 `async` 핸들러 안. `switch/case` 안 호출( `AppShell.tsx:417-458·491-588`)과 다행 메시지(`\n` join) 4곳 주의 | `AppShell.tsx:438-441·596` 등 |
| A-8 | 공용 UI 컴포넌트 선례 = `components/ui/ContextMenu.tsx` (화면 밖 보정·외부 클릭 닫기) | — |

### B. 선택지 정렬

| # | 사실 | 위치 |
|---|---|---|
| B-1 | 화면 렌더는 `ChoicesBlock.tsx` 단일 구현, **6개 사이트 공유**(EditorView·TabBody·FolderView·ProblemTabContent·SheetImportModal). 인쇄만 사본(`PrintChoicesBlock`) | `ChoicesBlock.tsx`, `PrintableContent.tsx:159-185` |
| B-2 | 그리드 `repeat(3|5, 1fr)`, 파서가 5개에서 끊으므로 화면은 항상 1행. 셀 높이는 grid 기본 `stretch`로 이미 동일 | `ChoicesBlock.tsx:27-34` |
| B-3 | **어긋남의 원인**: 셀 내부 flex `alignItems:'baseline'` — 큰 수식(`.base:has(.mfrac,.sqrt,.op-symbol.large-op)`의 `padding .4em` + KaTeX vlist)이 든 셀은 첫 라인박스 베이스라인이 아래로 내려가 원문자 y가 셀마다 달라짐 | `ChoicesBlock.tsx:36`, `globals.css:548-552` |
| B-4 | 큰 수식 패딩이 상하 **대칭 0.4em** → center 정렬 시 수식도 시각 중앙에 가까움. `.choices-grid .preview-content p{margin:0}`이 이미 있어 중앙 계산 오염 없음 | `globals.css:548-557` |
| B-5 | 인쇄 사본도 같은 baseline 구조(`.print-choice-item{align-items:baseline}`) | `PrintStyles.css:247-263` |
| B-6 | `PrintStyles.css`는 편집 화면에도 항상 로드(M1 B-7) — 새 규칙엔 `.print-body` 접두 필수 | M1 v3 §1 |

### C. 참조 인용

| # | 사실 | 위치 |
|---|---|---|
| C-1 | 전처리는 사본 2벌: `lib/locale.ts`(공용·인쇄) ↔ `EditorPreview.tsx:95-181` 인라인. 정규식 상수만 공유, 순서는 손 동기화(기존 규약) | — |
| C-2 | **정의부(원본)는 이미 전부 span/요소로 래핑**: `\tag{n}` 수식 → KaTeX `.tag` / 텍스트 행 → `.tag-marker` / 행 시작 `①` → `.marker-circled` / `(가)` → `.marker-gana` / `ㄱ.` → `.marker-giyeok` / 경우 제목 → `.case-label` | `locale.ts:176-207`, `caseBlock.ts:140-155` |
| C-3 | **재인용부는 `.case-ref`(C1)만 마크업 존재.** `\ref{n}`은 평문 `(n)` 치환(`locale.ts:196-198`), 본문 중간 `①`·`(가)`·`ㄱ`은 무변환 평문 | `locale.ts`, `EditorPreview.tsx:167` |
| C-4 | 자기 보호 치환의 완성 전례 = `convertCaseRefs`(펜스·기존 span·낱개 태그·인라인 코드 보호 + lookaround `(?<![A-Za-z0-9_\\])`). M1-G(D19″)에서 보호 순서까지 확정됨 | `caseBlock.ts:42-61` |
| C-5 | 인덱스→원본 매핑 자료구조는 경우(C1)만 존재(`buildCaseLabels`). 나머지는 없음 — DOM 탐색으로 대체 가능(정의부 마커 클래스가 전부 있으므로) | `caseBlock.ts:108-133` |
| C-6 | 떠 있는 팝업 배치 전례 = `SelectionInsertPopup`(rect 기반 + `innerWidth` 클램프 + 스크롤 시 접기). 전용 tooltip 컴포넌트는 없음 | `SelectionInsertPopup.tsx` |
| C-7 | ⚠ `globals.css:857-858` 주석: 수식 영역 hover를 **JS state로 하지 말 것** — 리렌더가 MathText innerHTML을 다시 써서 드래그 선택이 죽음 → hover 처리는 이벤트 위임 + 포탈로, 프리뷰 리렌더 유발 금지 | — |
| C-8 | `EditorPreview:393-399`가 모든 `.katex`에 `data-math-id` 부여(인스턴스별 0부터 — Phase 59 E7 규약) | — |

### D. ProblemView

| # | 사실 | 위치 |
|---|---|---|
| D-1 | 골격: 루트(아이보리 `--bg-functional`) → 제목행(minHeight 98, flexShrink 0) → 콘텐츠 행 → **클레이 U-프레임 = 스크롤 컨테이너**(`contentScrollRef`, `overflowY:auto·overflowX:hidden`, `--bg-content` 배경, 상·좌·우 0.5px 테두리, `justifyContent: panelMode ? 'unsafe flex-end' : 'center'`) → 가운데 영역 **`display:'table'`** + 하단 스페이서(`height:70vh, width:0`) | `ProblemView.tsx:524-706` |
| D-2 | **탭은 이미 세로 배열.** 탭 1개 = [라벨 열 7em | 본문 열] 가로 2단 행, 라벨 클릭 = 접기/펼치기(`openTabs` 상태, 기본 전부 펼침, 비영속) | `TabBody.tsx:179-228`, `ProblemView.tsx:115·249-251` |
| D-3 | 본문 폭 = **`35 × contentFontSize` 고정**(maxWidth 아님, `flexShrink:0`) | `TabBody.tsx:64-66` |
| D-4 | `question` 탭에만 이미 카드형 분기 존재(배경 클레이색·padding 20/24·radius 8·`marginLeft:-24`) — 배경이 클레이와 같아 사실상 안 보임 | `TabBody.tsx:230-239` |
| D-5 | sticky 전례는 저장소 전체에서 `ListView.tsx:107` 한 곳(제목행 sticky + "래퍼가 아이보리를 칠해 위 8px·라운드 바깥을 덮는다" 함정 주석 포함) | `ListView.tsx:100-122` |
| D-6 | 접기 시 시야 튐 보정 전례 = `useOutlineState`의 `keepAnchor` + `useLayoutEffect` rect 델타 보정 | `useOutlineState.ts:47-63` |
| D-7 | 폴더뷰 카드 규격: `--card-surface(=--bg-content 클레이)` · `0.5px solid --border-content` · radius 12 · hover `--block-bg-active #E8DFCE` + shadow | `FolderView.tsx:546-566`, `globals.css:752-755` |
| D-8 | 경우 블록 rail·dot·chevron은 본문 좌측 **바깥 −1.8em** — 카드에 `overflow:hidden`이면 잘림(Phase 59a Q5의 FolderView 예외가 그 사례). 좌측만 넓힌 해법 전례 = `PublicViewerShell` ContentCard(`padding 32 36 32 40` + `maxWidth calc(35em+76px)`) | `globals.css:167-178·738-759`, `PublicViewerShell.tsx:129-142` |
| D-9 | `--case-dot-fill` 기본값이 클레이(`--bg-content`) — 카드 배경을 바꾸면 컨테이너에서 재정의 필요(전례 `ProblemTabContent.tsx:126`) | `globals.css:157-161` |
| D-10 | `handleJumpToBlock`이 `contentScrollRef` 기준 좌표로 점프 — sticky 헤더 도입 시 오프셋 보정 필요 | `ProblemView.tsx:298-345` |
| D-11 | U-프레임은 EditorView와 공유 자산(CLAUDE.md: "둘을 항상 함께 손댈 것") — **E-M2-2로 이 규약을 의도적으로 깨는 결정**이며, CLAUDE.md에 예외를 기록해야 함 | CLAUDE.md |
| D-12 | 반응형 media query 전무(공개 뷰어 셸 880px 분기 하나뿐). `.case-block` 상하 padding 금지(마진 collapse, Phase 59 G7) · `paddingBottom:100vh` 금지(스페이서 div 사용) · `[data-noscroll]` 규약 | CLAUDE.md |

### E. 폴더뷰 열수

| # | 사실 | 위치 |
|---|---|---|
| E-1 | grid는 `repeat(auto-fill, 520px)` + `justifyContent:center` + gap 20. media query·minmax 없음 | `FolderView.tsx:529-538` |
| E-2 | **2열 제한의 진범 = 콘텐츠 래퍼 `maxWidth:1200`**: 가용 1136px에 3열 필요폭 1600px → 항상 최대 2열 | `FolderView.tsx:503` |
| E-3 | 제목바+하위폴더 칩은 별도 래퍼(`:379`, 같은 maxWidth 1200). Phase 62 D8의 정렬 기준선은 **리스트보기** 행↔제목 정렬(`ListView.tsx:97-98`) — 카드보기는 grid가 center라 지금도 제목과 좌단이 안 맞음 | `FolderView.tsx:379` |
| E-4 | 조건부 style 함정: longhand는 키 제거 금지, 항상 값을 줄 것(`maxWidth: card ? 'none' : 1200`) | CLAUDE.md:112-113 |
| E-5 | FolderView는 2곳 마운트(폴더 화면 `AppShell:711` · 공유 스코프 `:758`) — 래퍼가 FolderView 내부라 자동 동반 | Phase 62 C-8 |

### F. 사이드바

| # | 사실 | 위치 |
|---|---|---|
| F-1 | 자동 접힘 = `useEffect`(view.type problem/editor → `setCollapsed(true)`) + 명령형 3곳(`:349·370·371`). 강제 펼침 4곳(`:368·376·616·829`) | `AppShell.tsx:620-625` 외 |
| F-2 | 사이드바 폭 260/56, 리사이즈 200~min(480, 33%), 폭 비영속 | `Sidebar.tsx:629-630`, Phase 62 D13 |
| F-3 | **Phase 62 K1**: ProblemView 본문은 고정폭 + `justifyContent:center` + `overflowX:hidden` → 가용폭 부족 시 **좌측이 잘린 채 스크롤로 닿을 수 없음**. fs15 본문만 722px, 우측 패널 포함 1146px(사이드바 56 기준). 사이드바 260 유지 시 +204px | Phase 62 v6 `:42-55`, `ProblemView.tsx:666-678` |
| F-4 | ⚠ 단순히 `overflowX:'auto'`로만 바꾸면 **flex `justifyContent:center` 특성상 좌측 넘침에는 여전히 닿을 수 없다** — 컨테이너를 block + 자식 `margin:'0 auto'` 구조로 바꿔야 스크롤이 전 범위에 닿음. D의 `display:'table'` → 카드 열 전환과 같은 지점이므로 **D에 편승** | CSS 사양 |
| F-5 | EditorView 쪽은 `content-frame`이 이미 `overflowX:'auto'`(잘림 없음, 최소폭 ≈1054px@15는 내부 가로 스크롤) | `EditorView.tsx:3482-3489` |

### G. 코칭블록

| # | 사실 | 위치 |
|---|---|---|
| G-1 | 종류 2종: `COACH_LABELS = { coach_important:'Important', coach_caution:'Caution' }` + `isCoachBlock` + `coachClassName`. 라벨은 렌더 시 부착, `raw_text`는 본문만, Firestore에 라벨 없음 | `lib/coachBlock.ts:17-31` |
| G-2 | 메모의 "네모 말풍선 안 느낌표" = **`IconCoachImportant`가 정확히 그것**(둥근 사각 말풍선 + 꼬리 + 느낌표). 외부 아이콘 패키지 금지 규약 | `Icons.tsx:500-518` |
| G-3 | 렌더 5사이트 모두 `CoachLabel` 공용(색은 CSS `--coach-accent`가 소유 — 인라인 색 금지, 인쇄가 `#000` 덮어쓰기) | `CoachLabel.tsx:6-22`, `PrintStyles.css:166-182` |
| G-4 | 5사이트: `EditorView:3692-3703` · `TabBody:148-156` · `FolderView:340-348` · `ProblemTabContent:89-90` · `PrintableContent:107-108` — 분기 순서는 사이트마다 다름, 공통 앵커는 `callout` 바로 앞 | CLAUDE.md:190-198 |
| G-5 | `.coach-block .coach-label`은 특이도 (0,2,0) 필수(`.solution-tone` 동률 회피) | `globals.css:636-638` |
| G-6 | `EditorView`의 `<select>`에 svg/ggb "현재값 보존 option 폴백" 전례 있음(`:900-906`) — caution 숨김 시 재사용 | — |
| G-7 | 접힘 상태 키는 `blockKeyOf(b) = block_key || id`(doc id는 저장마다 재발급) · Toggler(aria-expanded) · `keepAnchor` 전례 | `caseBlock.ts:75-77`, `OutlineSections.tsx:36-70` |
| G-8 | `lib/coachBlock.ts:15-16` 주석 "라벨 어절은 영문 그대로(GitHub alert 시각 문법, v4 Q2)" — **이번에 'Tip'으로 뒤집는 결정.** 근거: 종류가 1개가 되면 GitHub 어휘 체계(Important/Caution 대비)가 소멸하므로 원문 유지의 이유가 사라짐 | — |

---

## 2. 결정표 (v1 — 교차검토 대상)

### A. 팝업창 일원화

| # | 결정 |
|---|---|
| **D1** | 신규 `components/ui/dialogStyles.ts` — BatchVerifyDialog의 사본 상수를 승격해 단일 스타일 원천으로: 오버레이 `rgba(0,0,0,0.4)` flex center · 본체 `var(--bg-card)` · `0.5px solid var(--border-light)` · radius 12 · shadow `0 12px 40px rgba(0,0,0,0.25)` · `fontFamily var(--font-ui)` · 위험 버튼 `var(--accent-danger)` |
| **D2** | z-index 토큰 신설(`lib/constants.ts` 또는 dialogStyles): `Z_DIALOG = 10500` — 현행 최고 10000(사이드바 메뉴)·9998/9999(전체화면 뷰어)·9000(시트 모달) 전부 위. 기존 z-index 재배치는 범위 외(값 조사표만 CLAUDE.md에 기록) |
| **D3** | 신규 `components/ui/DialogHost.tsx` + `lib/dialogs.ts`: 모듈 싱글턴 + Promise API. `alertDialog(msg)` → `Promise<void>` · `confirmDialog({title?, message, danger?, confirmLabel?})` → `Promise<boolean>` · `promptDialog({title, message?, defaultValue?, placeholder?, validate?})` → `Promise<string|null>`. `message: string | string[]`(다행은 배열, `white-space:pre-line` 불요). Host는 `app/layout.tsx`에 마운트(admin 라우트 포함 전 화면 커버) |
| **D4** | PromptDialog는 NicknameSetupModal 원형 승계: Enter 확정 + `isComposing` IME 가드 + Escape 취소 + 열릴 때 autoFocus·전체 선택. ConfirmDialog는 RestoreConfirm 3단 레이아웃 승계. Escape/오버레이 클릭 = 취소(resolve false/null) |
| **D5** | 치환: confirm 14곳 → `await confirmDialog`, prompt 5곳(폴더 이동 제외) → `await promptDialog`, alert 37곳 → `await alertDialog`. 호출부는 이미 대부분 async — 예외(`ShareSettingsPanel.tsx:154·195`, `ListView.tsx:75`)는 핸들러를 async로 전환. `switch/case` 안 `await`는 `break` 위치 재확인 |
| **D6** | **A-4 폴더 이동**: `AppShell.tsx:563`의 "이름 타이핑 매칭" prompt를 폐기하고 신규 `FolderPickerDialog`(트리 = `lib/folder-tree.ts`의 `buildFolderTree`/`flattenVisible` 재사용, 현재 폴더·자기 자신·자기 후손 비활성)로 교체. 순환 이동 금지 검사(`:406` alert)는 픽커에서 원천 차단 |
| **D7** | 네이티브 팝업 사용 금지 규약을 CLAUDE.md에 추가("alert/confirm/prompt 직접 호출 금지 — `lib/dialogs.ts` 경유"). 기존 모달 8종의 다이얼로그화(재작성)는 범위 외 — 신규 3종+픽커만 |

### B. 선택지 정렬

| # | 결정 |
|---|---|
| **D8** | `ChoicesBlock.tsx:36` `alignItems: 'baseline'` → `'center'`. 라벨 `lineHeight:1` 유지(실측 후 어긋나면 v2에서 `inherit` 재검토) |
| **D9** | 인쇄 동기화: `PrintStyles.css` `.print-choice-item`의 `align-items: baseline` → `center`(규칙에 `.print-body` 접두 확인, 미접두면 붙여서) |
| **D10** | 내용이 2줄로 wrap된 선택지에서 라벨이 콘텐츠 전체 높이의 중앙에 오는 것은 **의도된 동작으로 수용**(원문자 정렬 일관성이 우선). 검수에서 실물 확인, 부자연스러우면 v2에서 라벨만 첫 줄 높이에 center하는 절충 재검토 |

### C. 참조 인용 hover

| # | 결정 |
|---|---|
| **D11** | 재인용 마크업 신설: `<span class="ref-marker" data-reftype="tag\|circled\|gana\|giyeok\|case" data-ref="…">…</span>`. 인식 규칙(E-M2-1): ① `\ref{n}` → 기존 평문 치환(`convertRefReferences`)을 span 치환으로 변경(reftype=tag, ref=n) ② 본문 중간(행 시작 아님)의 원문자 `[①-⑮]` → reftype=circled ③ 본문 중간 `(가)~(하)` → reftype=gana ④ 본문 중간 낱자 `[ㄱ-ㅊ]`(앞뒤가 한글 음절·자모가 아닐 것, 뒤따르는 `.` 선택 포함) → reftype=giyeok ⑤ `C1`은 기존 `.case-ref` 재사용(reftype=case). **평문 `(3)` 숫자는 자동 인식하지 않음** — 수식 번호 재인용은 `\ref{n}` 규약 유지 |
| **D12** | 삽입 위치: `preprocessLocale` 사본 2벌의 마커 변환들 **뒤**·`convertCaseRefs` 인접(M1 G-5 순서 준수), 자기 보호는 D19″(M1) 순서 그대로(펜스 → span 요소 통째 → 낱개 태그 → 인라인 코드 → `⟦MATH_n⟧`은 기보호). 정규식 상수는 `lib/locale.ts` export, 사본에 재작성 금지 |
| **D13** | hover 동작(메모 사양): `.ref-marker { cursor: help }`(= 물음표 커서, "아이콘이 ?로 바뀜"의 구현) → **1000ms 지속 후** 말풍선 표시. 마우스가 마커 또는 말풍선 위에 있는 동안 유지, 벗어나면 닫힘. 스크롤 시 즉시 닫힘(C-6 전례) |
| **D14** | 말풍선 구현: 신규 `components/ui/RefTooltip.tsx` — **document.body 포탈 1개 + 스크롤 컨테이너에 이벤트 위임**(mouseover/out). EditorPreview·TabBody에 React state 추가 금지(C-7 규약). 위치는 `getBoundingClientRect` + 화면 클램프(SelectionInsertPopup 전례). z-index는 D2 토큰 계층(`Z_DIALOG - 100`) |
| **D15** | 원본 조회는 **DOM 탐색**(자료구조 신설 없음): 같은 스크롤 컨테이너에서 reftype별 정의부 선택자(`.tag-marker`/KaTeX `.tag`/`.marker-circled`/`.marker-gana`/`.marker-giyeok`/`.case-label`)를 훑어 텍스트가 일치하는 마커를 찾고, 그 마커가 속한 문단(가장 가까운 `p`·수식행·경우 제목행)을 `cloneNode(true)`로 복제해 말풍선에 주입. **원문 인덱스 포함은 자동 충족**(마커가 문단 안에 있으므로). 미발견 시 말풍선 미표시(무음) |
| **D16** | 적용 화면: ProblemView(`TabBody`) + 공개 뷰어(`ProblemTabContent`). 편집 미리보기·인쇄·폴더뷰 카드는 제외(편집 방해·지면 무의미·카드 축소판) |
| **D17** | 말풍선 스타일: `var(--bg-card)` + `0.5px solid var(--border-light)` + radius 8 + shadow, `maxWidth: min(35em, 80vw)` · `maxHeight 40vh` · `overflow:auto`. 수식은 이미 KaTeX 렌더 완료본을 복제하므로 재렌더 없음 |

### D. ProblemView 보기 개편

| # | 결정 |
|---|---|
| **D18** | **배경 전환**: 클레이 U-프레임(테두리 포함) 제거, 스크롤 컨테이너 배경을 아이보리(`--bg-functional`)로. 탭 본문 열을 카드로: `--card-surface(클레이)` · `0.5px solid --border-content` · radius 12(폴더뷰 카드와 동일 문법, D-7). 카드 컨테이너에 `--case-dot-fill: var(--card-surface)` 재정의(D-9). EditorView는 무변경(E-M2-2) — CLAUDE.md의 "U-프레임 2곳 공유" 규약에 예외 기록 |
| **D19** | **카드 기하**: 본문 폭 `W = widthEm × contentFontSize`(기본 35em) 유지, 카드 = 본문 + `padding '20px 36px 20px 40px'`(좌 40px ≥ 2.2em@15 — 경우 거터 잘림 방지, D-8 전례). 카드에 `overflow:hidden` **금지**. 탭 라벨 열은 카드 밖 좌측(현행 위치 유지 = 메모의 "탭 레이블은 클레이 영역 밖으로" 충족). 카드 간 세로 여백 `2em`(접힘 시 `1em`) |
| **D20** | **컨테이너 개편**: 가운데 영역 `display:'table'` → 일반 block 열 + `margin:'0 auto'`, 스크롤 컨테이너는 `overflowX:'auto'`(F-4 — 좁을 때 가로 스크롤로 전 범위 도달). `justifyContent` 분기(패널 열림 시 flex-end)는 `margin-left:auto` 계열로 등가 이전. 하단 스페이서 div(70vh) 유지(`paddingBottom:100vh` 금지 규약) |
| **D21** | **가로폭 조절**: 상태 `widthEm`(기본 35, min 35, max 50, 1 단위). UI는 우측 상단 글자크기 조절 옆에 −/+ 스테퍼. `localStorage 'mathory.problemWidthEm'` 전역 영속(문항별 아님). `TabBody` `mainColStyle`의 `35`를 prop으로 |
| **D22** | **문제 카드**: 첫 카드, 스크롤 컨테이너 안 `position:sticky; top:0`(ListView D-5 전례 — 래퍼가 아이보리를 칠해 통과 비침 차단). 라벨('문제') hover 후 **클릭**으로 접기/펼치기 토글(`openTabs.question` 재사용) — wheel 이벤트로는 토글 안 됨(메모 사양). 접힘 시: 본문 첫 줄만(`maxHeight` 1행 + overflow hidden + 하단 페이드) + 카드 배경 한 단 진하게(`--block-bg-active #E8DFCE` — hover 색과 동일, 명암비 기검증) |
| **D23** | **풀이·풀이2 카드**: sticky 없음, 접기/펼치기 없음(접힘은 문제 카드 전용 — 기존 `openTabs`의 풀이 탭 토글은 라벨 클릭에서 제거). 스크롤하면 문제 카드 아래로 밀려 들어가며 사라짐(sticky 문제 카드가 자연히 연출). **라벨 열은 `position:sticky; top: 문제카드 접힘높이 + 여백`** — 카드 본문이 스크롤돼도 좌측 라벨은 고정(메모 사양) |
| **D24** | **스크롤 자동접힘**(문제 탭만): `contentScrollRef`의 `onScroll` 1곳에서 처리. 순방향: `scrollTop > T1`(≈80px)이면 ① 제목행(아이보리 영역, `ProblemView:531-654`)을 높이 애니메이션으로 접고 ② **300ms 시차** 후 문제 카드 접힘(0.2~0.5s 사양 내). 역방향: `scrollTop < T2`(≈8px, 최상단)이면 ① 문제 펼침 ② 이어서 제목행 복원. **히스테리시스**: T1 ≠ T2 + 접힘·펼침 각각 쿨다운, 접힘으로 인한 높이 급변은 `keepAnchor`(D-6) 방식으로 scrollTop 보정 — 진동(flapping) 방지. 자동접힘은 **사용자 수동 토글을 덮어쓰지 않음**: 수동 조작 후에는 방향 전환(최상단 복귀)까지 자동접힘 보류 |
| **D25** | `handleJumpToBlock`(D-10)에 sticky 문제 카드 높이만큼 오프셋 보정 추가(점프 대상이 헤더 뒤로 숨지 않게) |
| **D26** | 우측 단(220px)·댓글 패널·글자크기 조절의 절대배치 좌표는 현행 유지, 개편 후 실측으로 어긋남만 보정. 모바일 무대응(E-M2-2) — 단 자동접힘 임계값·애니메이션은 추후 모바일에서 재사용 가능하게 상수화 |

### E. 폴더뷰 열수

| # | 결정 |
|---|---|
| **D27** | `FolderView.tsx:503` 래퍼: `maxWidth: effectiveViewMode === 'card' ? 'none' : 1200`(longhand 항상 값 지정, E-4). 제목바 래퍼(`:379`)·리스트보기·grid 정의(520px 고정 트랙·gap 20·`justifyContent:center`)·카드 규격은 전부 무변경. 결과: 1920 창(사이드바 접힘)에서 3열, 2560에서 4열 |

### F. 사이드바 자동접힘 제거

| # | 결정 |
|---|---|
| **D28** | `AppShell.tsx:620-625` useEffect 삭제 + 명령형 `setCollapsed(true)` 3곳(`:349·370·371`) 삭제 + 강제 펼침 `setCollapsed(false)` 4곳(`:368·376·616·829`) 삭제 — 접힘 상태는 사용자 토글만이 바꾼다. `collapsed` 상태·토글 버튼·Sidebar 접힘 분기 20곳은 전부 유지 |
| **D29** | 좁은 창 잘림 보완은 **D20이 담당**(block + margin auto + `overflowX:'auto'`). 따라서 F는 D와 같은 스텝에 묶는다 — F만 먼저 나가면 K1 잘림이 실결함으로 승격되므로 단독 선행 금지 |

### G. 코칭블록

| # | 결정 |
|---|---|
| **D30** | 표시 단일화(E-M2-4): `COACH_LABELS` 두 키 모두 `'Tip'` · `CoachLabel`은 타입 무관 `IconCoachImportant` · `coachClassName`은 두 타입 모두 `'coach-block coach-important'` 반환(색 = 보라 `#6639ba` 단일, S6). `globals.css`의 `.coach-caution` 규칙은 죽은 규칙으로 삭제. `IconCoachCaution`은 미사용화(파일에는 보존, v2에서 삭제 판단). G-8의 어절 결정 번복 사유를 coachBlock.ts 주석·CLAUDE.md에 기록 |
| **D31** | 에디터: `BLOCK_TYPES`에서 `coach_caution` 제거, `BLOCK_TYPE_LABELS.coach_important = '코칭 (Tip)'`. 기존 caution 블록 선택 시 `<select>` 현재값 보존 폴백(G-6 전례) — 표시는 '코칭 (Tip)'. 신규 생성은 항상 `coach_important`. 타입 union·Firestore·`normalizeBlockType`·`exportMd` 무변경 |
| **D32** | 접힘/펼침: 신규 `components/ui/CoachBlock.tsx` — 기존 5사이트 공통 패턴(`div.coach-block > CoachLabel + EditorPreview`)을 흡수하고 `collapsible` prop 추가. 접힘 = **아이콘만**(라벨 텍스트·본문 숨김), 펼침 = 아이콘+라벨+본문. 아이콘 클릭 토글(`role="button"` + `aria-expanded` + Enter/Space, Toggler 전례). **기본 접힘.** 상태는 컴포넌트 로컬 `useState`(비영속, S5) — 키 개념 불필요(인스턴스 생명주기와 일치) |
| **D33** | 사이트별 적용: TabBody·ProblemTabContent·FolderView = `collapsible`(기본 접힘) / EditorView 미리보기·PrintableContent = 항상 펼침 + 토글 UI 없음(인쇄에 버튼 금지). 접힘 아이콘의 색·크기는 기존 `.coach-label` 토큰 그대로, 클릭 영역만 확대(패딩). 특이도 (0,2,0) 유지(G-5) |

---

## 3. 구현 항목 (스케치)

### 3-A. 팝업 (신규 4파일 + 호출부 15파일)

```
components/ui/dialogStyles.ts   // D1 — BatchVerifyDialog S 상수 승격 + Z_DIALOG(D2)
lib/dialogs.ts                  // D3 — 모듈 싱글턴: subscribe/open, alertDialog·confirmDialog·promptDialog
components/ui/DialogHost.tsx    // D3 — app/layout.tsx에 마운트. 큐 처리(중첩 호출은 순차)
components/ui/FolderPickerDialog.tsx  // D6 — buildFolderTree/flattenVisible 재사용
```

호출부 치환 대상(전수): `AppShell.tsx`(prompt 4·confirm 4·alert 10 + 폴더 이동 1) · `EditorView.tsx`(confirm 1·alert 4) · `CommentPanel.tsx`(confirm 2·alert 5) · `CommentEditor.tsx`(alert 4) · `ImageUploadButton.tsx`(alert 3) · `SheetImportModal.tsx`(prompt 1) · `ListView.tsx`(confirm 1·alert 2) · `BazaarView.tsx`(confirm 1) · `ShareTargetModal.tsx`(confirm 1) · `ShareSettingsPanel.tsx`(confirm 3) · `PublishList.tsx`(confirm 3·alert 3) · `ProblemView.tsx`(alert 1) · `PdfDownloadButton.tsx`(alert 1) · `lib/pdfPrint.tsx`(alert 1) · `app/admin/migrate/page.tsx`(confirm 1)

### 3-B. 선택지 (2줄)

`ChoicesBlock.tsx:36` `'baseline'`→`'center'` · `PrintStyles.css` `.print-choice-item` 동기(+접두 확인).

### 3-C. 참조 hover

```
lib/locale.ts        // REF_* 정규식 export + convertRefMarkers(text) — D11·D12
EditorPreview.tsx    // 인라인 사본에 같은 단계 삽입(순서 동기)
components/ui/RefTooltip.tsx  // D13~D15·D17 — 포탈 1개 + 위임 리스너 attach(containerRef)
TabBody.tsx / ProblemTabContent.tsx  // 스크롤 컨테이너에 RefTooltip 장착 — D16
globals.css          // .ref-marker { cursor:help; ... } — 밑줄 점선 등 시각 표시는 v2 실측 후
```

### 3-D·F. ProblemView (동일 스텝)

```
ProblemView.tsx   // D18 프레임 제거·배경 전환 / D20 컨테이너 block+margin auto+overflowX auto
                  // D21 widthEm 상태+스테퍼+localStorage / D24 onScroll 자동접힘(히스테리시스+keepAnchor)
                  // D25 점프 오프셋 / D28은 AppShell
TabBody.tsx       // D19 카드화(본문 열 래핑)·라벨 열 sticky(D23)·widthEm prop
AppShell.tsx      // D28 — 자동접힘·강제 펼침 7곳 삭제
globals.css       // 카드·접힘 페이드·제목행 접힘 트랜지션 토큰
```

### 3-E. 폴더뷰 (1줄)

`FolderView.tsx:503` `maxWidth: effectiveViewMode === 'card' ? 'none' : 1200`.

### 3-G. 코칭블록

```
lib/coachBlock.ts        // D30 — 라벨 'Tip'·className 단일화·번복 사유 주석
components/ui/CoachBlock.tsx  // D32 — 공통 패턴 흡수 + collapsible
5개 렌더 사이트          // D33 — 패턴 치환(분기 순서는 사이트별 현행 유지)
EditorView.tsx           // D31 — BLOCK_TYPES·라벨·select 폴백
globals.css              // .coach-caution 삭제·접힘 상태 스타일
```

---

## 4. 폭 예산표 (E·F 상호작용 — 검수 기준)

E(열수 해제)와 F(사이드바 펼침 유지)는 같은 가용폭을 두고 경쟁한다. 사이드바 260 기준:

| 화면 | 필요 창폭 (사이드바 260) | 비고 |
|---|---|---|
| 폴더뷰 3열 | ≈ 1924 (1600+64+260) | 사이드바 접으면 1720 |
| ProblemView 본문 fs15 | 982 | D20 이후엔 부족 시 가로 스크롤 |
| ProblemView fs15 + 댓글 패널 | 1350 | 〃 |
| EditorView | 1314 | 초과분은 기존 내부 가로 스크롤 |

---

## 5. 작업 순서 (스텝 그룹 — 그룹 단위로 커밋·검수)

| 스텝 | 항목 | 완료 기준 |
|---|---|---|
| **1** | **B + E + G** (소형 3건) | T1~T7. 그룹 끝에 덕수 실물 검수 ① |
| **2** | **A** (팝업 4파일 + 치환 57곳 + 픽커) | T8~T12. 검수 ② — 특히 폴더 이동 픽커·시트 모달 위 프롬프트 |
| **3** | **C** (참조 hover) | T13~T16. 검수 ③ |
| **4** | **D + F** (ProblemView 개편 — 반드시 동일 스텝, D29) | T17~T24. 검수 ④ (최대 관문) |
| 5 | 문서: §8 매핑 + CLAUDE.md 규약 갱신 | 커밋(push는 덕수) |

---

## 6. 검증 체크리스트

- **B**: T1 로그·적분·번분수·짧은 수식 혼재 5지에서 원문자 y 일치(11·15·24px 실측) · T2 인쇄 미리보기 동일 · T3 2줄 wrap 선택지 실물 확인(D10)
- **E**: T4 1920/2560 창에서 3/4열 · T5 리스트보기 폭 불변(1136) · T6 공유 스코프 화면(`AppShell:758`)도 동일 · T7 hover·페이드·rail 미표시 예외(CLAUDE.md:206) 회귀 0
- **G**: T8′ 기존 caution 문서 → Tip 라벨·보라·Important 아이콘 렌더 + 에디터 select 폴백 · T9′ 기본 접힘(아이콘만) → 클릭 펼침, 키보드 토글 · T10′ 인쇄·편집 미리보기 항상 펼침 + 인쇄에 버튼 없음 · T11′ 폴더뷰 카드에서 아이콘만 표시가 자연스러운지
- **A**: T8 confirm 14곳 각 취소/확정 양 경로 · T9 prompt IME(한글 조합 중 Enter) · T10 시트 모달(z 9000) 위에서 다이얼로그가 위에 뜸 · T11 다행 메시지(폴더 삭제·휴지통) 줄바꿈 · T12 폴더 이동 픽커 — 동명 폴더 구분·자기 후손 비활성·순환 금지
- **C**: T13 4종 reftype 각각 hover → 1초 후 말풍선 + 원문 인덱스 포함 · T14 오탐 점검(선택지 블록·경우 라벨·`\mathrm` 잔재에 ref-marker 미부착) · T15 말풍선 위 수식 렌더·드래그 선택 생존(C-7) · T16 스크롤 시 닫힘·화면 가장자리 클램프
- **D·F**: T17 카드 색·테두리·radius가 폴더뷰 카드와 동일 문법 · T18 경우 블록 rail·dot이 카드 좌측 패딩 안에서 안 잘림 + dot-fill 관통 없음 · T19 문제 카드 sticky + 라벨 클릭 토글(wheel로는 토글 안 됨) + 접힘 첫 줄·톤 진해짐 · T20 순방향: 제목행 접힘 → 300ms 후 문제 접힘, 역방향 최상단: 역순 복원, **경계 scrollTop에서 진동 0** · T21 풀이 카드 라벨 sticky · T22 폭 스테퍼 35→50em·최소 35 미만 불가·localStorage 복원 · T23 좁은 창(1200px)에서 가로 스크롤로 본문 좌단 도달 가능(K1 해소) · T24 검증 리포트 점프가 sticky 뒤에 안 숨음 · T25 사이드바: 문항 진입·이탈에 접힘 상태 불변, 수동 토글만 동작
- **공통**: 글꼴 11·15·24px 3조건, EditorView↔ProblemView 전환 시 기능 회귀 0(배경 차이는 수용, E-M2-2)

## 7. 하지 말 것 / 주의

- (A) 기존 모달 8종을 이번에 재작성하지 말 것 — 신규 3종+픽커만(D7). z-index 전면 재배치도 금지(토큰+조사표만).
- (B) 그리드 셀 래퍼 구조(`display:contents`·서브그리드) 변경 금지 — stretch가 깨진다.
- (C) hover를 React state로 만들지 말 것(C-7) — 위임+포탈. 평문 `(3)` 자동 인식 금지(D11). 사본 2벌 중 한쪽만 고치지 말 것.
- (D) 카드에 `overflow:hidden` 금지(D-8) · `.case-block`에 상하 padding 금지 · `paddingBottom:100vh` 금지 · `justifyContent:center`+overflow 조합 재도입 금지(F-4).
- (F) **D 없이 F만 선행 배포 금지**(D29) — K1 잘림이 실결함이 된다.
- (G) `CoachLabel`에 인라인 색 금지(인쇄 `#000` 덮어쓰기가 못 이김, G-3). 타입 id·union·exportMd 건드리지 말 것(E-M2-4).
- 공통: `PrintStyles.css`는 화면에도 로드 — 새 규칙 `.print-body` 접두. 조건부 style은 longhand 항상 값 지정. 새 색은 `#E8DFCE` 기준 4.5:1(`((c+0.055)/1.055)^2.4`).

## 8. 문서 갱신 매핑 (스텝 5)

| 항목 | 갱신 |
|---|---|
| A | CLAUDE.md: 네이티브 팝업 금지 규약 + `lib/dialogs.ts` 사용법 + `Z_DIALOG` 토큰 + z-index 조사표 |
| B | Phase61a 문서의 ChoicesBlock 절에 정렬 규약 1줄 |
| C | CLAUDE.md: `ref-marker` 규약(전처리 순서·자기 보호·평문 숫자 비인식) · `사용 가이드 — 강조와 톤.md`에 hover 안내 |
| D·F | CLAUDE.md: **U-프레임 공유 규약에 예외 기록**(ProblemView는 카드 구조, EditorView만 U-프레임) + 자동접힘 제거 + `mathory.problemWidthEm` 키 · Phase62 문서 K1·T8″에 "M2에서 해소" 후기 |
| E | Phase62 문서 D8·D10 절에 카드보기 maxWidth 해제 후기 |
| G | CLAUDE.md 블록 타입 절(코칭 = Tip 단일 표시, id 2종 유지) · `사용 가이드 — 강조와 톤.md` 코칭 절 개정 · coachBlock.ts G-8 번복 주석 |
| 공통 | roadmap.md에 M2 표 1개(M1 관례) · 확정본은 `docs/phasedocs/` 승격 |

## 9. 교차검토(v2)에서 답할 것

| # | 질문 |
|---|---|
| **Q1** | (C) 원문자·`(가)`·낱자 `ㄱ` 자동 인식 정규식의 오탐 실측 — 특히 낱자 `ㄱ`의 경계 조건(조사 앞 "ㄱ은"·나열 "ㄱ, ㄴ")과 `roman`·`choices` 블록 내부 제외 여부 |
| **Q2** | (C) KaTeX `.tag` 정의부의 DOM 텍스트 매칭이 안정적인지(`\tag*{(3)}` 렌더 결과 실측) — 불안정하면 `.tag-marker`(텍스트 행)만 1차 지원 |
| **Q3** | (D) `display:'table'` 제거 시 폭 계산(fit-content·스페이서 width:0)의 실제 회귀 — 우측 단·댓글 패널 열림 애니메이션 중 sticky 폭 추종 포함 |
| **Q4** | (D) 자동접힘 임계값 T1/T2·쿨다운 실측치와 트랙패드 관성 스크롤에서의 진동 여부 |
| **Q5** | (D) 제목행 접힘 애니메이션 방식(height vs transform) — 접힘 중 sticky 문제 카드의 top 재계산 비용 |
| **Q6** | (A) DialogHost를 `app/layout.tsx`에 둘 때 서버 컴포넌트 경계 처리('use client' 래퍼)와 admin 라우트 동작 확인 |
| **Q7** | (A) `lib/pdfPrint.tsx:64`의 alert는 React 밖 유틸 — 모듈 싱글턴 API로 호출 가능한지(가능해야 D3 설계 성립) |
| **Q8** | (G) FolderView 카드(높이 320 고정)에서 기본 접힘 코칭이 카드 미리보기 줄 수 계산에 주는 영향 |
| **Q9** | (E·F) 폭 예산표(§4) 실측 — 사이드바 펼침 상태에서의 실사용 열수·D20 가로 스크롤 UX |
| **Q10** | (D) 폭 스테퍼 max 50em의 타당성(우측 단 220 + 댓글 패널과의 동시 공존 폭) |

---

## 부록. 문서 계보

| 버전 | 작성 | 산출 |
|---|---|---|
| **v1** | **web (2026-08-28)** | **초안 — 사실표 A~G(`af918fa` 실측) · 결정 D1~D33 · 덕수 결정 E-M2-1~4 · 소결정 S1~S6 · Q1~Q10** |
| v2 | CLI | 실측 교차검토 (예정) |
| v3 | web | 확정판 (예정) |

*v1 — 교차검토 대기. 신규 env·비밀 0 · Firestore 규칙 0 · 서버 0 · 마이그레이션 0.*
